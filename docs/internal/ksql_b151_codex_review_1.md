# B151 仕様 R1 レビュー（Claude・実測付き）

- 実施: 2026-08-07
- 対象: [仕様 R1](ksql_b151_join_number_pushdown_spec_r1.md)（codex 作・「そのまま押し下げ」前提）
- 結論: **指摘なしで採用。仕様が「未確認」とした最大のリスク 2 件を実装前に実測で解消した。**

## 1. 実測（v3.59.0・APP4228・検証レコードは投入→確認→削除）

**§4.4 の空セル固定表のうち未実測だった `!=` / `NOT IN` を確認した。**

```
個数 != 10      押し下げ経路・FULL_SCAN 強制経路とも {空セル, 9} を返し {10} を除外 → 一致
個数 NOT IN (10) 同上 → 一致
```

仕様 §4.4 の表（空セルは `!=` / `NOT IN` で true）と**完全一致**。
`<` / `<=` / `>=` / 境界すれすれ / 3 経路一致は起票 §2.5〜2.6 で実測済み。

## 2. 静的主張の抜き取り（全一致）

- `src/types/ast.ts:652-668`＝`raw` 保持・`numberLiteralText()` の plain decimal 無損失正規化 ✓
- `src/core/scalarCompare.ts:159-174`＝6 演算子が同一 `cmp` から判定 ✓
- `src/core/scalarCompare.ts` band 構造・`canProveTotalWindowOrder` 等は本日別件で確認済み ✓

## 3. 仕様側からの訂正を受け入れた（歴史の記述）

**codex §2.4 が起票 §2.6 の推定を訂正**＝10 進の型付き比較契約は **v3.0.0** から存在し、
B76（基準 v3.25.0）より**前**。「B76 後に導入されて静かに無効化された」のではなく、
**B76 の IEEE-754 判断が成立時点から v3.0.0 公開契約と不整合だった疑い**が正しい
（厳密な実装導入日は git 履歴なしでは未特定）。起票 §2.6・台帳・memory の該当記述を訂正した。

## 4. 残る実測（実装後・§13 のとおり）

- kintone query の literal 受理範囲（30 桁・小数 10 桁付近・`1e30` 等）→ 拒否されるなら許可範囲を狭める
- `numberPrecision` 変更アプリ（30/10・10/2 UP/DOWN 等）での境界・丸め・全演算子
- `IN` の同値表記要素（`1` / `1.0` / `1e0`）・重複・大リスト
