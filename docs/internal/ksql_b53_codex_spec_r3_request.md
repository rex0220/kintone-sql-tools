# B53 仕様 R3 リフレッシュ依頼（codex）——B160 同梱・調査報告反映・設計 3 決定済み

**[仕様 R2](ksql_b53_recursive_cte_cycle_phase1_spec.md) を R3 へ改訂する。実装はしない。仕様文書の改訂のみ。**
方向はオーナー判断（2026-08-09）＝B160 同梱・下記 3 決定。

対象リポジトリ: `C:\Users\rex02\Projects\kintone-sql-tools`（`main`・v3.65.0）

前提資料（すべて読むこと）:
- [仕様 R2](ksql_b53_recursive_cte_cycle_phase1_spec.md)（改訂のベース。公開意味論の 2 核心＝path スコープ／3 絶対上限常時 fail-closed は**変更しない**）
- [事前調査報告](ksql_b53_b160_codex_investigation_report.md)（R2 と v3.65.0 の食い違い 11 項目・file:line 付き。R3 の主材料）
- [B160 起票](ksql_b160_window_warning_generated_column_issue.md)／[B140 起票](ksql_b140_cte_groupby_total_order_issue.md)／[B140 spec R2](ksql_b140_cte_groupby_total_order_spec_r2.md)（免除文言の条件定義・`$id` 関数従属の切り分け）
- [B149 起票](ksql_b149_generate_series_issue.md)（v3.59.0 の生成列免除の範囲）

## 0. 禁止事項

**静的作業のみ。** コード変更・ファイル書き込み・`git`・kSQL MCP・ビルド・テスト実行を禁止
（read-only sandbox。R3 本文は最終メッセージとして出力し、保存は Claude が行う）。

## 1. 決定事項（変更しない・R3 本文へ反映する）

1. **キーワード＝§7.3 のソフトキーワード方式を維持**。直近 `CROSS`（ハード予約・調査報告 A-3）には
   倣わない（既存フィールド名 `RECURSIVE`/`CYCLE`/`TO`/`DEFAULT` を壊さない安全側。
   `TO`/`DEFAULT` を句区切りに使う既存文脈は 0 件＝調査報告 A-2 を根拠として §7.3 に追記）
2. **境界エラー＝専用エラークラス＋構造化プロパティ**。
   `kind: "DEPTH" | "ROWS" | "EXPANSIONS"`・実効上限・検出値・CTE 名を readonly プロパティで持つ。
   **`KsqlEngineError` の公開 code union（`src/engine-library/errors.ts:8-14`）へは追加しない**＝
   `normalizeEngineError()` では当面 `EXECUTION_ERROR` へ写像（将来 Pro の実需が出たら code 昇格）。
   固定識別子（R2 §5.1 の `RECURSIVE_CTE_MAX_*_EXCEEDED`）は**メッセージ文頭に置かず**クラス側の
   識別に移す（B100 の内部識別子露出を避ける）。メッセージは日本語・実効上限・検出値・CTE 名入り。
   source 取得超過は既存 `FetchAllLimitError` のまま（変更なし）。§5.1 をこの形へ書き換える
3. **型整合＝planning 時証明を維持**（R2 §3.4 の意味論は不変）。ただし調査報告 B-3 の事実
   （既存の列メタ推論は実行結果の後にしか走らない・EXPLAIN 静的処理は列名のみ）を踏まえ、
   **seed/再帰項の射影に対する静的型推論器の新設**を実装作業として §4・§11 に明記する。
   Phase1 の射影文法が狭い（§1.1）ことを推論器のスコープ根拠として書く。
   B143（EXPLAIN warnings）への相乗り可能性は備考 1 行で触れる（依存にはしない）

## 2. R3 で反映するもの

1. **調査報告「仕様 R2 と現行実装の食い違い」11 項目の全数**を該当節へ反映する。特に:
   - CTE 列名リスト非対応・自己参照の登録順（調査 A-4）＝§7 に「現行 parser からの変更点」として明記
   - AST 現状（`GenerateSeriesStatement` 追加済み・`recursive`/`recursiveSpec` なし）＝§7.2 を rebase
   - UNION 位置対応が二系統ある事実（調査 B-4）＝再帰実装がどちらへ接続するか（または統一するか）を規定
   - 実体化・押し下げの接続点（調査 B-2）＝§4.2 は B155 共有 leaf policy 上での実装として書き直す
   - 設定配管は `maxRecords` の実例（調査 C-2）を配管対象の全数リストとして §5.2 へ
   - EXPLAIN/dry-run の 8 経路（調査 D-1）を §9.4 の受入条件へ全数列挙
   - ガードのエラー形式は決定 2 の形へ（B158/B159 の `ArgumentError` 流儀には合わせない旨を明記）
2. **B160 同梱節を新設**: 全順序警告の免除文言の一般化＝
   **B140（集約キー）・B160（生成列×JOIN）・B53（再帰 CTE 出力）の 3 形を単一の条件文言で判定できる**
   免除条件を設計する。制約:
   - 「JOIN で同一キーに複数行が出る形では同順が実際に生じる」ため無条件の免除は書けない
     （B160 起票 §4 案 A の但し書き）
   - 案 B（機構による抑止拡大）は採らない。判定に必要な情報（JOIN 由来 provenance・パーティション内
     一意性）が警告判定点に存在しないことは調査報告 E-3 で確定済み
   - 文言変更が触れる逐語テスト 8 assertion site（調査報告 E-2）を受入条件として列挙する
   - 再帰 CTE 出力（`深さ` 等）は厳密単調ではなく同順が普通に生じるため、
     「生成列だから免除」とは書けない。3 形それぞれで読み手が判定できる文になっているかを自己検証する
3. **文書同期の出荷チェックリスト**を新節に: 調査報告 F-2 の全数
   （言語リファレンス 2 箇所・plugin spec・内部比較資料 2 件・B149 仕様 2 件・MCP instructions・
   tool schema・statement syntax catalog）＋境界既定値表の B136 型パリティテスト 1 本（調査 F-3 の判定）
4. **§11 工数見積りの更新**: 調査報告末尾の増減事実を反映（静的型推論器の新設は増・
   GENERATE_SERIES/B155/警告伝播の資産化は減）。総レンジが 18〜29 人日から動くなら根拠つきで更新

## 3. 変更しないもの

- 公開意味論の 2 核心（path スコープ循環判定／3 絶対上限の常時 fail-closed）
- 戦略 B 固定・Phase1 の対象/対象外（§1）・境界の既定値（depth=100・rows=10,000・expansions=100,000）
- §12 の決着表（R3 で変えた項目があれば行を追記し、既存行は書き換えない）

## 4. 報告形式

最終メッセージ＝**R3 仕様の全文（markdown）のみ**。前置き・後書き・要約を付けない。
先頭のステータス行に「仕様 R3（2026-08-09）・B160 同梱・設計 3 決定反映・Claude レビュー待ち」と
決定 3 点の 1 行要約を記載する。R2 からの変更点一覧は文書末尾に §14 として付ける
（レビューが差分で読めるように・変更箇所→調査報告の該当項目の対応つき）。
