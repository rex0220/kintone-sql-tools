# kSQL 作成ルール（Claude に前置きとして渡す試験用・R1・2026-09-16）

<!-- 2026-09-16 追記: 公開用の全文（v3.78.0 版＝10・14・15 を修正後の書き方に、4 に「分からなければ」を追加）は 09_AIに書かせてレビューする.md §6 に置いた。本ファイルは 3 巡目の測定に使った R1 原文として残す。要点 8 行は v3.78.0 の MCP instructions（B183）に入れたが、前置きなしの 3 回測定では作法（RANK・除数ガード・依頼された列だけ）を変えなかった（番外編 §4） -->

<!-- 用途: 第 3 回の依頼文の前に貼り、番外編 1〜2 巡目と同じ依頼で結果が変わるかを測る（同一依頼の before/after）。効いた項目だけを第 9 回の共通プロンプトか MCP の instructions に昇格させる。根拠は第 0〜9 回と番外編の実測 -->

```text
このチャットでは kSQL MCP で kintone 用の SQL を書いてもらいます。kSQL は標準 SQL と違う点があるので、次のルールで書いてください。

【調べてから書く】
1. フィールドは推測せず ksql_describe_app で確認する。ラベルではなくフィールドコードを使う。ルックアップ・コピー元・重複禁止の印も見る
2. 構文は ksql_docs で該当の節を読んでから使う。ksql_validate に試し打ちして通る形を探さない

【JOIN】
3. ON はフィールド同士の等式 1 本だけ。結合キーは「コピー元: YES」のフィールドと相手のレコード番号（例: 案件.顧客No_ = 顧客.顧客No）
4. INNER JOIN では、WHERE で絞れる側・件数の少ない側を FROM に書く（結合キーの絞り込みは FROM 側から JOIN 先にしか効かない）
5. LEFT / RIGHT JOIN は押し下げが効かず両方全件になる。「無い側も出す」と依頼されたときだけ使い、その場合も絞る側は先に一時テーブルへ実体化する

【WHERE と日付】
6. 選択系（ドロップダウン・ラジオ・チェックボックス）の条件は IN で書く
7. 日付の条件は WHERE に相対日付関数（THIS_MONTH() など）かリテラルの半開区間で書く。WHERE で DATE_FORMAT / YEAR などで列を包まない（全件取得になる）
8. 相対日付関数は WHERE 専用。SELECT で今日が要るときは CURRENT_DATE()、整形は DATE_FORMAT

【空セルと型】
9. kintone に NULL は無く、未設定は空文字 ''。除外は != ''、判定は = ''
10. 0 埋めは CASE WHEN x = '' THEN 0 ELSE x END で書く。COALESCE / ISNULL で集計値を包まない（結果が型を失い、並べ替え・順位・累計が文字列順になる。算術で包むと 0 になる）
11. 数値の比較は 10 進厳密、算術と SUM / AVG は倍精度。100.0 は慣習で、整数除算は無い

【集計とウィンドウ】
12. 集計とウィンドウ関数は同じ SELECT に書けない。ウィンドウの結果は同じ SELECT の式でも使えない。集計する段 → ウィンドウを列に出す段 → その列を使う段、と CTE か一時テーブルで分ける
13. 累計は ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW を明示し、ORDER BY は集約キーまでのタイブレークを付ける
14. 総計で割るときは CASE WHEN 総計 = 0 で 0 除算を先に受ける（kSQL のゼロ除算は NaN）

【名前と出力】
15. 列の別名に英字を使わない。別名の英字は小文字に正規化され、次の段から元の名前で参照できない。物理フィールドと同じ名前の別名も付けない
16. 依頼された列だけ出す。依頼に無い列・条件・ORDER BY・LIMIT を足さない。順位は指示が無ければ RANK()（同額は同順位）、割合は小数第 1 位

【取得量】
17. GROUP BY・集計・DISTINCT・ローカル ORDER BY は完全入力が必要で、上限に達すると部分結果ではなくエラーになる。件数の当たりは COUNT(*) で先に付ける

【書き込み】
18. 更新は、対象を一時テーブルに確保 → ASSERT で件数を制限 → UPDATE … FROM か UPSERT … KEY の順。まず VALIDATE ONLY INTO #err を付けた形で検証する。UPDATE の CHECK は更新前の値を見る
19. ksql_mutate は呼ばない。本実行は人間が行う

【検証と説明】
20. 書いたら ksql_validate → ksql_explain の順に通し、fetch summary と理由行を添える。依頼があれば ksql_query で件数だけ確かめる
21. 完成した SQL は SQL だけのコードブロックで示し、各句の説明を 1 行ずつ付ける。根拠は ksql_docs の節名か EXPLAIN の行で示し、推測は推測と書く。できないことはできないと言ってから代替案を出す
```

## 結果（R1・2026-09-16）

- 改訂前の依頼文 + 上のルールを Claude Desktop に渡した（user）。Claude は `ksql_describe_app` で両アプリを確認し、`ksql_docs` の R15・§10.1・§7・§13 を読んでから回答
- 返った SQL を Claude Code が `ksql_query` で実行 → **10 行・順位（9 位が 2 社）・構成比・累積構成比・区分がすべて第 3 回の表と一致**。別名は日本語のみ、`COALESCE` 無し、`RANK`、`ROUND(…, 1)`、`CASE WHEN 総計 = 0`、依頼に無い列無し。番外編 1 巡目（別名）・2 巡目（`COALESCE`）の誤りは依頼文を変えずに消えた
- 差異: 列順が `会社名, 売上合計, 順位`。FROM が顧客側（ルール 4 の「件数の少ない側」は実行禁止で分からず、Claude は「推測。逆なら入れ替えても結果は同じ」と明示＝ルール 21 が効いた）。EXPLAIN は顧客 ALL・案件 `PREFILTERED (未確定)`。結果は同じ
- 判定: ルール 10・15・16・21 は効いた。ルール 4 は件数が分からない状況では判断できないので、「分からなければ WHERE で絞れる側、無ければどちらでも可と書く」に文言を直す候補
- 次: ルール全文を第 9 回に掲載（番外編は第 9 回参照）。効いた項目を第 9 回の共通プロンプトへ、残りを MCP instructions への追加候補として起票するかは user 判断

## 測り方

1. Claude Desktop で新しいチャットを開き、上のルールを貼る
2. 続けて第 3 回の依頼文（番外編 1 巡目と同じ・改訂前）を貼る
3. 返ってきた SQL を `ksql_query` で実行し、第 3 回の表（10 行・順位 9 が 2 社・区分 A4/B2/C4）と突き合わせる
4. 番外編の 1 巡目（別名で実行エラー）・2 巡目（`COALESCE` で文字列順）と同じ誤りが出るかを見る。出なければルール 10・15 が効いた証拠、出れば文言を直す
5. 効いた項目だけを第 9 回の共通プロンプトに足す。それでも足りないものは MCP の instructions（構文カタログの隣）へ入れる候補

## 根拠（実測した回）

| ルール | 出典 |
| :-- | :-- |
| 3・4・5・17 | 第 1 回（FROM の順で 10 件上限を通るか 100 件で止まるか） |
| 6 | 第 1 回（本シリーズの方針） |
| 7・8 | 第 0 回 例 3、第 2 回（`TODAY()` は SELECT で ParseError） |
| 9・10 | 第 2 回、番外編 2 巡目（`COALESCE(SUM, 0)` で文字列順・算術で 0＝B182） |
| 11・14 | 第 3 回 |
| 12・13 | 第 2 回・第 3 回 |
| 15 | 番外編 1 巡目（B181） |
| 16 | 第 9 回 共通プロンプト 5 番、番外編（`顧客No` 列の追加、`ROW_NUMBER`） |
| 18・19 | 第 5 回、第 9 回 |
| 20・21 | 第 9 回 |
