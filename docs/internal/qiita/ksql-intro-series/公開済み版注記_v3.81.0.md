# 公開済み記事への版注記（B184・B190 / v3.81.0・v3.83.0）— Qiita 側で user が反映する文案

<!-- 2026-09-16 作成。第 0・2・3 回は v3.77.0 で実測して公開済み。反映したらこのファイルの各項目にチェックを付ける。
     注記の裏取り（dev・v3.82.0 MCP）で B190 を発見: CTE／一時テーブル source の CASE 条件左辺のウィンドウが落ちる。v3.83.0 で修正。注記は v3.83.0 リリース後（npm 公開済み）に載せる -->

## 方針（2026-09-16 に変更）

- **「書けない」「できない」「不可」と断定している文は、その文自体を書き換える。** 文の後ろに注記を足すだけだと、太字の断定が目に入って古い情報に見える（user 指摘）。
- 書き換えた文には版を入れる: 「〜できる（v3.81.0〜）。公開時の v3.77.0 では〜」の形。公開時点の実測（表・EXPLAIN・エラー文）は事実として残す。
- NG 例のエラー文は「v3.77.0 時点の出力」と分かるように前置きを直し、削らない。
- 補足が長くなる箇所だけ `> **注記（v3.81.0 以降）**` の引用ブロックを足す。

## 第 0 回（https://qiita.com/rex0220/items/d7db0da75da8c734b46d）

- [ ] 差分早見表の行「集計とウィンドウ | 同じ SELECT に書ける | **同じ SELECT に書けない**。CTE で段を分ける」を書き換え:

| 集計とウィンドウ | 同じ SELECT に書ける | 同じ SELECT に書ける（v3.81.0〜）。ウィンドウの結果を関数の引数・算術・`CASE` の中でも使える。`OVER` で参照できるのはグループキー・集計の別名・集計式・`GROUPING()`。公開時の v3.77.0 では書けず、CTE で段を分けていた |

## 第 2 回（https://qiita.com/rex0220/items/8261e9aaab9eef3a0fb5）

- [ ] 冒頭の「違いは … **`LAG` の結果を同じ SELECT の式に使えない**の 3 点」→「違いは … **`LAG` の結果を同じ SELECT の式に使えない（v3.77.0 時点。v3.81.0 以降は使える）**の 3 点」

- [ ] 段階 3 の見出し「前月差 — ウィンドウ関数の結果は次の段で使う」はそのまま。NG 例の前置き「-- NG: LAG の結果を同じ SELECT の式で使う」→「-- v3.77.0 時点は NG（v3.81.0 以降は通る）: LAG の結果を同じ SELECT の式で使う」。ParseError の出力は残し、その直後に:

> **注記（v3.81.0 以降）**: この形は通るようになりました。`件数 - LAG(件数) OVER (ORDER BY 年月) AS 前月差` を同じ SELECT に書けます（先頭行の `LAG` は空文字で、算術では 0 として扱われます。空文字のまま返したいときは `CASE WHEN LAG(...) = '' THEN ''`。この `CASE` 条件に置く形は v3.83.0 以降）。本文の 5 段版は、段ごとに値を確かめたいときの書き方として残します。

<!-- 実測（dev・B190 修正後）: 埋め済み CTE に対して 件数 - LAG(件数) OVER (ORDER BY 年月) と CASE WHEN LAG(...) = '' THEN '' ELSE LAG(...) END を同じ SELECT に書き、5 段版と同じ 9 行（2025-11 の前月差 1・前月件数 空）。CASE 条件に LAG を置く形は v3.81.0〜v3.82.0 では `unknown field code(s): __ksql_window_0 (埋め済み)` で落ち、v3.83.0 で修正（B190） -->

- [ ] 落とし穴「**ウィンドウ関数の結果は同じ SELECT の式で使えない。** 次の段へ。集計と同じ SELECT にも書けない」を書き換え:

- **ウィンドウ関数の結果は同じ SELECT の式で使える（v3.81.0〜）。集計と同じ SELECT にも書ける。** 公開時の v3.77.0 では使えず、次の段へ渡していた。段を分ける書き方は今も有効

## 第 3 回（https://qiita.com/rex0220/items/d417ba52766f73cb965d）

- [ ] 「kSQL では、この『ウィンドウ関数を式の中で使う』ができません」を書き換え:

kSQL でも、この形はそのまま書けます（v3.81.0〜）。違いは `ROWS UNBOUNDED PRECEDING` の省略形が使えず `ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW` と書く点だけです。公開時の v3.77.0 では「ウィンドウ関数を式の中で使う」ができず、次の 3 段の CTE に分けていました。3 段版は、段ごとに値を確かめたいときの書き方として今も有効です。

```sql
WITH base AS (
  SELECT a.会社名, SUM(b.売上) AS 売上合計
  FROM APP4149 AS b JOIN APP4148 AS a ON b.顧客No_ = a.顧客No
  GROUP BY a.会社名
)
SELECT 会社名, 売上合計,
       RANK() OVER (ORDER BY 売上合計 DESC) AS 順位,
       ROUND(売上合計 * 100.0 / SUM(売上合計) OVER (), 1) AS 構成比,
       ROUND(SUM(売上合計) OVER (ORDER BY 売上合計 DESC, 会社名
                               ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)
             * 100.0 / SUM(売上合計) OVER (), 1) AS 累積構成比
FROM base
ORDER BY 売上合計 DESC, 会社名
```

<!-- 実測（dev・v3.83.0 MCP）: 3 段版と同じ 10 行・警告なし。集計を base に分けて次の SELECT の CASE 条件に `SUM(売上合計) OVER () = 0` を書く形は v3.81.0〜v3.82.0 で `unknown field code(s): __ksql_window_0` で落ち、v3.83.0 で修正（B190）。上の SQL は CASE 条件にウィンドウを置いていないので v3.81.0 から通る -->

- [ ] 見出し「kSQL ではこう書く — 3 段の CTE」→「kSQL で段ごとに確かめる — 3 段の CTE」（本文と表・EXPLAIN はそのまま）

- [ ] 「段の役割」表の「kSQL の制約」列を書き換え（列見出しを「公開時（v3.77.0）の制約」に）:

| 段 | すること | 公開時（v3.77.0）の制約 |
| :--- | :--- | :--- |
| `base` | JOIN して集計し、粒度を会社別に決める | 集計とウィンドウは同じ SELECT に書けなかった（v3.81.0〜は書ける） |
| `ranked` | **ウィンドウを列として出すだけ**。式で包まない | ウィンドウ結果は同じ SELECT の式に使えなかった（v3.81.0〜は使える） |
| 最終 SELECT | 出た列を使って割る・丸める・区分する | ここではウィンドウ関数ではなく、実体化した列に対する通常の式として書ける |

- [ ] 「この分け方自体が制約への対処です。第 2 回の `LAG` で 5 段になったのも同じ理由でした」→「この分け方は公開時の制約への対処でしたが、段ごとに値を確かめられる利点は v3.81.0 以降も変わりません。第 2 回の `LAG` で 5 段になったのも同じ理由でした」

- [ ] 見出し「差分 — 何が同じ SELECT に書けないか」→「差分 — 公開時（v3.77.0）は何が同じ SELECT に書けなかったか」。NG 例の前置き「-- NG: GROUP BY と同じ SELECT にウィンドウを書く」→「-- v3.77.0 時点は NG（v3.81.0 以降は通る）: GROUP BY と同じ SELECT にウィンドウを書く」。ParseError は残し、続く文を書き換え:

`OVER` の中で集計関数を参照する形は、v3.77.0 では解析できませんでした。v3.81.0 以降はこの例がそのまま通り、`OVER` の中でグループキー・集計の別名・集計式・`GROUPING()` を参照できます。集計を `base` に切り出す書き方も引き続き使えます。

- [ ] 「ウィンドウ結果と式」の段落を書き換え:

第 2 回で見たとおり、`ROUND(SUM(x) OVER (), 1)` や `件数 - LAG(件数) OVER (...)` は v3.77.0 では ParseError でした。v3.81.0 以降は通ります（`CASE` の条件の中に直接書く形は、元が CTE のとき v3.83.0 以降）。
`ranked` に `総計` と `累計` を列として出しておけば、最終段では `累計 * 100.0 / 総計` を `CASE` の条件の中にも書けます。段を分けるときは今もこの形です。

- [ ] 「差分」の「集計とウィンドウ」の説明文の後に、集計から ABC 区分までを 1 つの SELECT に書いた kSQL（v3.81.0〜）と、`base` CTE + 式内ウィンドウ + `CASE` 条件の kSQL（v3.83.0〜）を追加（草稿 03 の該当節をそのまま）

- [ ] 「発展 — ROLLUP」の制約行「…**ウィンドウ関数との併用は不可**。ABC の表と総計行は別のペイン（別の SQL）に分けるのが素直です」を書き換え、続けて `PARTITION BY GROUPING(会社名)` で順位と構成比を付ける kSQL と結果表を追加（草稿 03 の該当節をそのまま）:

- 制約: grouping の項目は物理フィールドのみ（式・別名・CTE 列は不可）。ウィンドウ関数との併用は v3.81.0 以降は可。公開時の v3.77.0 では併用不可で、ABC の表と総計行を別のペイン（別の SQL）に分けていました

<!-- 実測（dev・2026-09-16）: ROLLUP(会社名) + RANK() OVER (PARTITION BY GROUPING(会社名) ORDER BY SUM(売上) DESC) は v3.83.0 で通る（明細 10 行 + 合計 1 行）。
     構成比 = ROUND(SUM(売上) * 100.0 / SUM(SUM(売上)) OVER (PARTITION BY GROUPING(会社名)), 1) は v3.83.0 で内部エラー（B191）。v3.84.0 で修正（25.3 … 0 / 合計 100）。**構成比を含む形は v3.84.0 の npm 公開後に載せる** -->

- [ ] 「落とし穴のまとめ」の 3 項目を書き換え（他の項目はそのまま）:

- **集計とウィンドウは同じ SELECT に書ける（v3.81.0〜）。** 公開時の v3.77.0 では書けず、集計を CTE に切り出していた。段ごとに確かめたいときは今も切り出す
- **ウィンドウ結果は同じ SELECT の式に使える（v3.81.0〜。`CASE` 条件の中は元が CTE なら v3.83.0〜）。** 公開時は列として出し、次の段で割る・丸める・`CASE` に使っていた
- **`ROLLUP` とウィンドウの併用は可（v3.81.0〜）。項目は物理フィールドのみ**

- [ ] まとめの直後に 1 行:

> 上の 3 項目は v3.81.0（B184）と v3.83.0（B190）で kSQL 側が対応したものです。残りの項目（`ROWS` の明示とタイブレーク、`OVER (...)` と `AS 別名`、完全入力、`WHERE rn = 1`、倍精度、ゼロ除算）は v3.83.0 でも同じです。

- 変えないもの: タイトル、3 段版の SQL と結果表、EXPLAIN の節、RANGE の落とし穴、「`WHERE rn = 1` は同じ SELECT では効かない」、`ROLLUP` の項目が物理フィールドのみという制約

参考: 集計から区分までを 1 つの SELECT に書いた形（v3.81.0 で実測。草稿 03 の「差分 — 集計とウィンドウ」に掲載済み）:

```sql
SELECT RANK() OVER (ORDER BY SUM(b.売上) DESC) AS 順位,
       a.会社名, a.顧客ランク, COUNT(*) AS 案件数, SUM(b.売上) AS 売上合計,
       CASE WHEN SUM(SUM(b.売上)) OVER () = 0 THEN 0
            ELSE ROUND(SUM(b.売上) * 100.0 / SUM(SUM(b.売上)) OVER (), 1) END AS 構成比,
       CASE WHEN SUM(SUM(b.売上)) OVER () = 0 THEN 0
            ELSE ROUND(SUM(SUM(b.売上)) OVER (ORDER BY SUM(b.売上) DESC, a.会社名
                                              ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)
                       * 100.0 / SUM(SUM(b.売上)) OVER (), 1) END AS 累積構成比,
       CASE WHEN SUM(SUM(b.売上)) OVER () = 0 THEN 'C'
            WHEN SUM(SUM(b.売上)) OVER (ORDER BY SUM(b.売上) DESC, a.会社名
                                        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)
                 * 100.0 / SUM(SUM(b.売上)) OVER () <= 80 THEN 'A'
            WHEN SUM(SUM(b.売上)) OVER (ORDER BY SUM(b.売上) DESC, a.会社名
                                        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)
                 * 100.0 / SUM(SUM(b.売上)) OVER () <= 95 THEN 'B'
            ELSE 'C' END AS 区分
FROM APP4149 AS b
INNER JOIN APP4148 AS a ON b.顧客No_ = a.顧客No
GROUP BY a.会社名, a.顧客ランク
ORDER BY 売上合計 DESC, a.会社名
```

## 参照

- CHANGELOG v3.81.0 / v3.83.0、言語リファレンス §10.1・§10、レシピ R15 / R16（1 段版と 3 段版の併記）
- 第 9 回のルール 12 と MCP Writing rules 5 行目は同じ内容に更新済み
