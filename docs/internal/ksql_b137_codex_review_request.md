# B137 列数の違う `UNION` をエラーにする 仕様 R1 codex レビュー依頼

**レビュー依頼であり実装依頼ではない。コードは 1 行も変更しないこと。**
git 操作をしないこと。kSQL MCP を叩かないこと（headless で無言停止する）。`npm test` は不要。

## 依頼

`UNION` / `UNION ALL` の左右で列数が違うときエラーにする仕様 R1 のレビュー。

対象リポジトリ: `C:\Users\rex02\Projects\kintone-sql-tools`（v3.49.0）

| ファイル | 役割 |
|---|---|
| `docs/internal/ksql_b137_union_column_count_spec.md` | **レビュー対象の R1** |
| `docs/internal/ksql_b137_union_column_count_issue.md` | 起票（方向確定の根拠） |
| `src/execute.ts:4880-4930` | 通常経路の `executeUnion` |
| `src/execute.ts:5008-5040` | CTE 経路の `UNION` |
| `src/parser/parser.ts:1260-1275` | `tryParseUnionChain`（連鎖の木の形） |
| `src/engine/process.ts:1304-1332` | wildcard 展開（列がいつ確定するか） |

**背景**: 直近 7 件（B123/B124/B125、B126/B127、B133、B130）でも同じ形のレビューをしてもらい、
**いずれも R1 の中核前提が覆った**（合計 65 件を全件反映）。**同じ系統の穴が無いかを見てほしい。**

**とくに直前の B130 では、こちらの受入条件の書き方が誤っていて
「既存挙動が変わらないことを確認する」つもりの文が「検出せよ」という要求になり、
実装者がエンジンを変更してしまった**（差し戻して本件を起票した経緯）。
**同じ曖昧さが R1 に残っていないかを見てほしい。**

## 特に見てほしい点（コードで真偽が決まるもの）

### 1. 【最優先】判定を入れる場所は 2 か所で本当に足りるか

R1 §2.2。`execute.ts:4908` と `:5022` の 2 か所を挙げている。

- **ほかに `UNION` の結果を組み立てている経路が無いか**
  （engine-library の `runQuery` / `explainQuery`、バッチ、`CREATE TEMP TABLE AS <UNION>`、
  `INSERT ... SELECT` のソースが `UNION` の場合など）
- **`mergeUnionColumnMeta`** は列数の違いをどう扱っているか。矛盾しないか
- 2 か所を**共通ヘルパーに寄せる**とき、**引数に何を渡すべきか**
  （`SelectResult` 全体か、`columns` だけか）

### 2. 3 段以上の連鎖の挙動（R1 §2.3）

R1 は「パーサが左結合の木を作るので、最左の列数が基準になる」としている。

- **本当に左結合か**（`tryParseUnionChain` を確認してほしい）
- **`WITH` の中や括弧付きで右結合になる形は無いか**
- **落ちる段が R1 の記述どおりか**

### 3. 実行タイミング（R1 §0.1・§3.3）

R1 は「両辺を実行してからのエラーになる。静的には弾けない」としている。

- **本当に静的に弾けないか。** 一部でも静的に判定できる形はあるか
  （両辺とも列を明示した `SELECT` で、CTE も `*` も無い場合など）
- 弾けるなら**そこだけ先に弾く価値があるか**（API を消費せずに済む）
- **`Promise.all` で両辺を並行実行している**現行の形を変えずに済むか

### 4. `EXPLAIN` はどうなるか

R1 が触れていない。**`EXPLAIN` は行を取らずに計画だけ返す**はず。

- **`EXPLAIN <列数の違う UNION>`** は今どうなっているか
- エラーにすべきか、計画だけ返すべきか。**R1 はこれを決めていない**

### 5. 受入条件の穴（R1 §3）

**B130 と同じ失敗を繰り返さないため**、次を見てほしい。

- **「〜が意図どおり検出される」のような、要求とも確認ともとれる文が残っていないか**
- §3.2（回帰）で**固定すべきなのに書かれていない形**が無いか
- **`deduplicateRows`** は `leftCols` だけを見ているが、列数チェック後なら問題ないか

### 6. 移行案内（R1 §5）

- 示している直し方（`SELECT c, '' FROM y`）は**実際にパースできるか**
  （**過去に、実行していないサンプルを 4 回書いている**）
- **ほかに移行が必要な形**が無いか

### 7. 影響範囲（R1 §4）

- `b130DescribeFlags.test.ts` の該当テスト以外に、**現状挙動に依存しているテストが無いか**
- **文書（言語リファレンス・レシピ）に列数の違う `UNION` の例が無いか**

## 出したい成果物

`docs/internal/ksql_b137_codex_review_1.md` に。

- 結論（実装着手可能 / 要修正・件数）
- 指摘（重要度 高/中/低・該当 §/file:line・内容・**コード引用による根拠**・提案）
- 上の 7 点への回答（コード引用つき）
- 仕様が正しかった点（R2 で消さないため）

重要度: 高 = そのまま実装すると誤る/既存を壊す、中 = 実装が詰まる/受入の穴、低 = 表現。
**根拠のないコメントは書かないでほしい。** 確認できなかった項目は「未確認」と明記のこと。
