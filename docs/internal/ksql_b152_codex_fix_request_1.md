# B152 修正依頼 1（codex・最終チェック指摘 Medium 2 件）

**[最終チェック報告](ksql_b152_codex_final_check_report.md)の指摘 2 件を修正する。**
禁止事項は実装依頼と同じ（git・version・CHANGELOG・release/・台帳・ビルド・MCP・MEMORY.md）。

## 1. Medium 1 — 4 桁年の canonical 判定を `setUTCFullYear` 方式へ

`joinDateTimeLiteralPolicy.ts` の `Date.UTC()` は年 0〜99 を 1900 年代へ解釈する
（**B149 の `generateSeries.ts` で潰したのと同じ罠**。同じ方式＝`setUTCFullYear()` で実在日判定に揃える）。

- **年の受理範囲は 0001〜9999 に明示**する。**実測済み（そのまま使え）**＝kintone query は
  `日付 >= '0001-01-01'`・`'0100-01-01'`・`'9999-12-31'` をすべて受理（実 APP4228・エラーなし）
- DATE / DATETIME 双方に境界テストを追加: `0001`・`0099`・`0100`・`0999`・`1000`・`9999`
  （修正前の実装で `0001`〜`0099` が unsafe に落ちることを確認してから直す）

## 2. Medium 2 — 空セル期待表を独立した期待値として固定

受入テストの 3 経路相互一致に加えて、**演算子ごとの期待 `$id` 集合を仕様 §6 の固定表どおり
直接 assert** する（両側が同方向に誤ると通過する穴を塞ぐ）。

- DATE 系: `=` `!=` `<` `>` `<=` `>=` それぞれの空セル包含/除外
- TEXT / LINK: `=` `!=` `IN` `NOT IN` の空セル包含/除外

## 3. 実行と報告

`npm test` 全体（認証環境変数はプロセス内除外。**あなたの環境で Jest cache の EPERM が出る場合は
その旨だけ報告し、全体ゲートは Claude 側で回す**）。最終メッセージ＝修正報告のみ。
