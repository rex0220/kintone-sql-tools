# B149 実装依頼（codex）

**[仕様 R2](ksql_b149_generate_series_spec_r2.md) を実装する。R2 が正本。
R1（`ksql_b149_generate_series_spec_r1.md`）は破棄済み——参照しないこと。**

対象リポジトリ: `C:\Users\rex02\Projects\kintone-sql-tools`（v3.58.0）

## 0. 禁止事項

- **git 操作を一切しない**（`git status` も含む）。コミットは Claude 側
- **`package.json` の version・`CHANGELOG.md`・README を変更しない**（リリース時に Claude が行う）
- **esbuild / ビルド・`npm publish` をしない**（ビルドは Claude 側）
- kSQL MCP を叩かない。自分の MEMORY.md を読まない
- **エラー・警告の人間向け本文に内部語（AST 名・実行モード名・内部 reason・関数名）を出さない**
- 公開型へプロパティを足す場合は**必ず optional（`?`）**にする（公開型は利用者が構築できる。
  必須追加は破壊的変更）

## 1. 実装範囲

R2 の §2〜§11 の全部と、§12 受入条件のテスト化。文書は次を同期する:

- `docs/ksql_language_reference.md`（R2 §14 の「言語リファレンスに明記する」項目）
- `ksql_docs` のセクション（言語リファレンスから機械生成なら追随を確認）
- MCP / CLI の schema・description（`GENERATE_SERIES` に触れる箇所）

**CHANGELOG・README・version・リリース作業は範囲外**（Claude が行う）。

## 2. テストの要件

- **R2 §12 の各受入条件を、公開結果で観測するテスト**にする（結果オブジェクト・送出される例外の
  種別と本文・`warnings`・`getSelectColumnMeta`・mock client の API 呼び出し回数）。
  **内部関数名・AST フィールド名を assert しない**
- **受入条件 ↔ テストの対応表**を報告に含める（§12.2 A1 → どのテスト、の形で全件）
- **警告抑止（§6.6）のテストは両方向**＝M4/M5/M6 の `warnings` 空配列と、
  M7/M8 の**既存警告文の全文一致**（文言は R2 に記載。実測照合済みなので変えないこと）
- 新規テストが**新経路を実際に drive している**ことを、対応表で説明できること

### 既存テストを書き換えるとき（毎回のルール）

- **止めて報告するのは「期待している挙動が変わる」書き換え**
  （エラーが警告になる／拒否が成功になる／値が変わる）。
  **既存テストの書き換えは、既存の決定を覆している合図。**
- **`toEqual` の網羅検証へ、新しく足したフィールドを機械的に加えるのは可**
  （既存の検証の意味が変わらないため）。**ただし報告に「どのテストへ何を足したか」を列挙する。**
- **「既存テスト」は main にコミット済みのものだけ。**

## 3. 進め方

1. 実装順は **コード → テスト → 文書**。途中で作業ツリーが壊れた状態を長く残さない
2. 実装後に **`npm test` を全体で回し、結果（suites / tests の数と pass / fail）を報告に含める**。
   落ちたまま終えない。直せない場合は理由を報告に書く
3. 仕様どおりに実装できない箇所を見つけたら、**仕様を黙って曲げず、報告に「仕様との差分」として明記**する
4. **自分が書いた仕様を自分で実装している。甘く見ないこと。**
   仕様の受入 SQL が 1 つでも実装で通らないなら、それは実装か仕様のどちらかの欠陥であり、報告する

## 4. 報告（最終メッセージ）

最終メッセージは**実装報告だけ**を出力する:

1. 変更・追加ファイル一覧（1 行ずつ・役割付き）
2. 受入条件 ↔ テストの対応表（§12 全件）
3. `npm test` の結果
4. 既存テストへ加えた変更の列挙（機械的追記を含む全部）
5. 仕様との差分・実装中に見つけた問題
6. R2 §15 のうち、テストで担保できず **Claude の実測が必要なもの**
