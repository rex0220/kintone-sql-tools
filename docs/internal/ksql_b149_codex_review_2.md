# B149 仕様 R2 検証（Claude・実測付き）

- 実施: 2026-08-07
- 対象: [仕様 R2](ksql_b149_generate_series_spec_r2.md)（codex 作）
- 結論: **R1 レビューの指摘 6 件と案 a がすべて反映されている。R2 を正本として確定し、実装待ちへ。**

## 検証内容

1. **R1-1（DECLARE 例）**: 全例が `DECLARE @start = 1;` 形。`DEFAULT` の出現は
   「受け付けない」と書いた §1.1 の事実表 1 か所のみ（正しい使い方）。日本語変数名の例は残っていない。
   §2.4 に変数名規則（英字/`_` 開始）を明記、§15.1 / §15.8 に実測項目あり
2. **案 a（ウィンドウ警告抑止・§6.6）**: 抑止条件は既存 `canProveTotalWindowOrder` と同じ
   保守的な直接証明に限定（JOIN・UNION・サブテーブル・再実体化・式経由は対象外＝警告維持）。
   受入は両方向（M4/M5/M6 = `warnings` 空配列、M7/M8 = 既存警告文の全文維持）。
   B140 の推論との区別も §6.6 と §13.2 に明記
3. **M8 の期待警告文の実測照合 ○**: 手書き CTE + `SUM OVER (ORDER BY)` を v3.58.0 で実行し、
   実際の既定 `RANGE` 警告文と **R2 が固定した期待文が完全一致**することを確認した
   （M7 の `LAG` 警告文も R1 レビュー時の実測と一致）
4. **新規静的主張の抜き取り ○**: `src/execute.ts:2564-2577` の `canProveTotalWindowOrder` は
   記載どおり（DIRECT・JOIN なし・CTE なし・サブテーブルなしで `$id`/レコード番号のみ証明）
5. **R1-3〜R1-6**: `ksql_validate` 二段階契約（§10.12 の表＋§12.12＋§15.9）、
   `TODAY()` 変数の許可（§2.4＋受入 D7 固定時計付き＋`NOW()`→DATETIME 拒否も §15.8）、
   保存クエリ read-only 分類（§9.4＋§12.13）、`+` 符号の許可（§3.3＋A7/D6＋`+0` は step 0）
   ——いずれも反映済み

## 状態

- **R2 が正本。R1 は破棄**（下敷きとしての履歴のみ。実装依頼は R2 だけを参照させる）
- 次: **実装依頼を codex へ**（実装後、§15 の未確認事項を Claude が実測）
