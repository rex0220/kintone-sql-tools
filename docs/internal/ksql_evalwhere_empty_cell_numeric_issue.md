# 課題: FULL_SCAN の数値比較が空セルを 0 として扱い、SIMPLE/kintone と乖離する

- 作成日: 2026-07-15
- ステータス: **実機確認済＋極端閾値も MCP/APP4221 で独立検証済（−∞ モデル確定）・修正方針を値ベース −∞ 準拠へ確定（案a 棄却）。空セル意味論の修正と数値プレフィルタ全体の安全性は分離する**
- 更新履歴:
  - 2026-07-15 R1: 起票
  - 2026-07-15 R2: codex レビュー反映（実機確認を4レコード×4演算子マトリクスへ拡張・EXPLAIN 事前確認／ASSERT の `Number("")` 重複＝共通化／サブテーブル UPDATE・DELETE・REORDER への波及と回帰／案a の正確な意味論＝空左辺は文字列フォールバックせず即 false・右辺空/文字列範囲の扱いを明示）
  - 2026-07-15 R3: **実機結果を反映（重要）**。kintone は空の数値セルを **−∞（あらゆる数より小さい）として扱う**（`>`/`>=` で除外・`<`/`<=` で含む）。**案a（空→常に偽）は当初判定として棄却**（`<=`/`<` で SIMPLE と再乖離）。修正方針を「JS も kintone の −∞ 準拠に揃える」へ確定。
  - 2026-07-15 R4: codex レビュー反映（[P1] 「修正後は全範囲演算子で押し下げ安全」を撤回＝**空セル包含性は解決するが、数値プレフィルタ全体の安全性（IEEE-754 精度・数値表現）は③で別途検証するまで未確定**／[P1] 「数値文脈限定」を**左辺型ではなく値ベースの規則**へ確定（`leftStr===""` かつ `rightStr!==""` かつ数値化可能）／[P2] R2 由来の「空値を除外」を −∞ 規則へ訂正・ASSERT ドリフトを明記／[P2] −∞ は「確認した範囲内で一致」と表現・追加閾値を推奨／[P2] 右辺空・文字列比較の現状維持テストを追加）。
  - 2026-07-15 R5: codex レビュー反映（[Medium] 値ベース規則の数値化可能を **`Number.isFinite` に限定**＝`"Infinity"`/`"-Infinity"` は特殊規則に入れず従来経路へ（`-∞ < -∞` 等の誤りを回避）／[Medium] 共通比較器の単体テストに**明示的な期待値**（`"" < "-1000000"`→true など6ケース）＋LEFT JOIN 欠損側×数値右辺の波及ケースを追加／[Low] MCP 旧ビルド注記を「ディスク最新・常駐プロセスが旧コード保持＝リロードで解消」に更新）。**MCP/APP4221 検証と実装前ゲート通過済み → 実装着手可**。
- 発見経緯: [ksql_like_predicate_pushdown_spec.md](ksql_like_predicate_pushdown_spec.md) R2 レビュー（[P1]）。述語分割で数値範囲を押し下げるための前提調査中に、`evalWhere` の空セル数値化が独立した挙動問題であると判明。
- 分担: Claude=起票/観点、Codex=検証/実装/テスト
- 位置づけ: v2.2.0 バンドルの構成要素②（①第0段 `5c987e0` 済 → ②本課題 → ③型メタ付き数値プレフィルタ）。本課題の決着が③（数値段）の前提。

## 事象（コード裏取り済み）

`evalWhere` の比較は、範囲演算子 `> < >= <=` を**数値化して比較**する（[src/engine/evalWhere.ts:118-133](../../src/engine/evalWhere.ts#L118)）:

```ts
const leftNum  = Number(leftStr);
const rightNum = Number(rightStr);
const numeric  = !Number.isNaN(leftNum) && !Number.isNaN(rightNum);
switch (op) {
  case ">":  return numeric ? leftNum > rightNum  : leftStr > rightStr;
  case "<":  return numeric ? leftNum < rightNum  : leftStr < rightStr;
  case ">=": return numeric ? leftNum >= rightNum : leftStr >= rightStr;
  case "<=": return numeric ? leftNum <= rightNum : leftStr <= rightStr;
}
```

**`Number("") === 0`（空文字は NaN ではなく 0）** のため、空セルの数値フィールドに対して JS 側で次が**真**になる:

```sql
数値フィールド >= 0     -- 0 >= 0 → true（空セルも一致）
数値フィールド <= 0     -- 0 <= 0 → true
数値フィールド > -1     -- 0 > -1 → true
```

kintone の SIMPLE モード（REST API クエリ）が空セルを数値範囲条件から除外するなら、**同じ SQL が実行モード（SIMPLE / FULL_SCAN）で異なる結果**を返す（過去に修正した WHERE/LIKE のモード乖離と同種）。

- 純粋な `WHERE 数値 >= 0` は SIMPLE（kintone へ押し下げ）なので、この乖離は **FULL_SCAN を誘発する要素（LIKE・関数・サブクエリ等）と AND したとき**に顕在化する。例: `WHERE 数値 >= 0 AND 備考 LIKE '%x%'` は全件 JS 評価となり、空セル行が混じる。

## 影響・波及先（[P1-3] コード裏取り済み）

`evalWhere` は SELECT の WHERE だけでなく、**書き込み・並べ替えの対象選定**にも直接使われる。範囲比較の空セル挙動が変わると**更新/削除対象が変わる**ため SELECT 以上に重要:

- **サブテーブル UPDATE の対象選定**（[execute.ts:2405](../../src/execute.ts#L2405) `expanded.filter((r) => evalWhere(stmt.where, r.flat))`）
- **サブテーブル DELETE の対象選定**（[execute.ts:2471](../../src/execute.ts#L2471)）
- **REORDER の対象選定**（[execute.ts:2678](../../src/execute.ts#L2678)）
- FULL_SCAN の WHERE / **JOIN 後の WHERE**（LEFT/RIGHT JOIN の欠損側も空文字になる）
- **HAVING**（[process.ts の applyHaving 経由](../../src/engine/process.ts#L364)）
- **CASE WHEN の条件**
- **モード依存の結果差**（FULL_SCAN で空セルが数値範囲に一致 / SIMPLE は kintone 依存）。件数・集計がモードでぶれる。
- **述語分割（数値段）のブロッカー**: kintone が空を範囲条件から除外するなら、数値範囲を押し下げると JS で真の行を取りこぼす＝超集合性が壊れる。

> `=` / `!=` は文字列比較（`leftStr === rightStr`）なので `数値 = 0` は空セルで偽。**範囲演算子 `> < >= <=` のみ**が対象。

## ASSERT / BETWEEN は別実装だが同じ問題を複製（[P1-2]）

ASSERT は `evalWhere` を通らないが、[`compareAssertValues`（execute.ts:872-884）](../../src/execute.ts#L872) が**同じ `Number("")` 判定を複製**している（コメントにも「型規則は evalWhere の evalOp と同一」）。`evalWhere` だけ直すと次のドリフトが残る:

- WHERE / HAVING / サブテーブル DML（`evalWhere`）: **−∞ 規則**（空左辺は `>`/`>=` で除外・`<`/`<=` で含む）
- ASSERT / BETWEEN（`compareAssertValues`）: **空を 0 として比較**（未修正だとドリフト）

→ **修正は比較処理を共通化して揃える**（推奨）。共通化しないなら ASSERT を対象外にする理由を明記する。

## 実機確認（[P1-1] 4レコード × 4演算子マトリクス）

**1組の `>= 0` だけでは全範囲演算子の超集合性を確定できない。** 空・`0`・`-1`・`1` の 4 レコードを用意し、各演算子で SIMPLE と FULL_SCAN を比較する（`ORDER BY $id` で突き合わせやすく）:

```sql
-- 事前に EXPLAIN で確認:
--   SIMPLE 版  = kintone query に金額条件がある
--   FULL_SCAN 版 = LIKE 起因の FULL_SCAN で、金額は押し下げられていない

-- 各演算子について SIMPLE / FULL_SCAN を比較（金額が空のレコードが含まれるか）
SELECT $id, 金額 FROM APP100 WHERE 金額 >= 0  ORDER BY $id;   -- SIMPLE
SELECT $id, 金額 FROM APP100 WHERE 金額 >= 0  AND $id LIKE '%' ORDER BY $id;  -- FULL_SCAN
-- 同様に:  金額 <= 0 / 金額 > -1 / 金額 < 1  を SIMPLE と (… AND $id LIKE '%') で
```

- `$id LIKE '%'` は FULL_SCAN を誘発するためだけの常真条件（`$id` は常に値ありで全件が通過）。
- **判定**（実施済み・R3 で −∞ モデルへ）: SIMPLE は `>=0`/`>-1` で除外・`<=0`/`<1` で含む＝**空は −∞**。当初の「除外なら案a」という見立ては**R3 で棄却**。
- **注意**: 全ケース一致しても「**空セル問題をクローズできる**」だけ。**数値プレフィルタ全体の安全性（型判定・数値表現の書式差）は③で別途検証**が必要。
- 公式仕様（[フィールド形式](https://cybozu.dev/ja/kintone/docs/overview/field-types/) / [クエリ](https://cybozu.dev/ja/kintone/docs/overview/query/)）では空値が空文字で返ることと範囲演算子の存在は確認できるが、**空値が範囲条件に含まれるかは明記なし** → 実機確認が必要（こちらからは取得不可）。

## 実機結果（v2.1.2・確定）

数値フィールド `金額` に 空 / 0 / -1 / 1 の 4 レコードで確認:

| 演算子 | SIMPLE（kintone） | FULL_SCAN（JS 現状） |
|---|---|---|
| `金額 >= 0` | **空を除外** | 空を含む |
| `金額 <= 0` | **空を含む** | 空を含む |
| `金額 > -1` | **空を除外** | 空を含む |
| `金額 < 1` | **空を含む** | 空を含む |

**モデル**: kintone は空の数値セルを **−∞（あらゆる数より小さい値）** として扱う（4点すべてが「空 < 任意の数」で説明可能）。すなわち **`>` / `>=` で除外・`<` / `<=` で含む**。現状 JS は `Number("")===0` で常に 0 扱いのため、`>=`/`>` で乖離する。

### 極端閾値の確認（ksql MCP・APP4221 で独立検証済み・2026-07-15）

APP4221（`金額` NUMBER。$id=1 が空・2=0・3=-1・4=1）に対し、Claude が ksql MCP で SIMPLE と FULL_SCAN を直接比較（FULL_SCAN は EXPLAIN で `mode: FULL_SCAN` / `kintone query: (全件取得)` ＝金額非押し下げを確認）:

| クエリ | SIMPLE（kintone＝−∞） | FULL_SCAN（JS＝0 扱い） |
|---|---|---|
| `金額 <= -1000000` | `$id=1`（空）の 1 件 | 0 件 |
| `金額 >= -1000000` | `$id=2,3,4` の 3 件（空を除外） | `$id=1,2,3,4` の 4 件（空を含む） |

→ 極端閾値でも **空 = −∞**（`<=` で含む・`>=` で除外）で崩れない。**数学的な −∞ の証明ではない**が、結論は「**kintone で確認した数値範囲（±1,000,000）内では、空値を −∞ として扱うモデルと一致する**」。`BETWEEN`（例 `金額 BETWEEN 0 AND 100` ＝ `>=0 AND <=100`）は下限 `>=` 側で空を除外する想定。

> 補足（別件・解決済み）: 検証中、FULL_SCAN の 0 件 SELECT が `columns: []` を返し、`$id LIKE` が kintone に押し下がりエラーになる旧挙動が出た。原因は **`.mcp.json` が起動する `dist-mcp/ksql-mcp.js` は最新（ディスク上）だが、常駐 MCP プロセスが起動時の旧コードをメモリに保持していた**こと。**VSCode ウィンドウ再読み込みでプロセスを再起動し解消**（再読み込み後は 0 件 SELECT が列を返し、`$id LIKE` は JS 評価、stage-0 の `$id` プレフィルタも有効）。ソース更新時は「`npm run build:mcp` → ウィンドウ再読み込み」。

## 論点（要決定）

1. **kintone 実挙動**: 上記で確定＝**空は −∞**（`>`/`>=` 除外・`<`/`<=` 含む）。
2. **修正方針（案a は棄却）＝値ベースの −∞ 規則（[P1-2]・コードレベル確定）**: `evalWhere`（[:118](../../src/engine/evalWhere.ts#L118)）は**左辺のフィールド型を知らない**（`leftStr`/`rightStr` のみ）。よって「数値フィールドだけ −∞」は判定不能。**左辺型ではなく解決値で**定義する:

   > **左辺解決値が空文字（`leftStr === ""`）で、右辺解決値が空でなく有限数（`rightStr !== "" && Number.isFinite(Number(rightStr))`）な範囲比較** のとき、左辺を −∞ として扱う。

   ```ts
   const rightNum = Number(rightStr);
   const emptyLeftFiniteNumericContext =
     leftStr === "" && rightStr !== "" && Number.isFinite(rightNum);
   if (emptyLeftFiniteNumericContext) {
     // ">" → false / ">=" → false / "<" → true / "<=" → true
   }
   ```
   - **有限数に限定する（[Medium・R5]）**: `Number.isNaN` だけだと `"Infinity"`/`"-Infinity"` も数値文脈に入る（右辺の算術式がゼロ除算になる経路等で到達し得る）。右辺が `-Infinity` のとき、固定分岐は `-∞ < -∞`→true・`-∞ >= -∞`→false と**数学的に誤る**。今回確認したのは**有限の閾値**なので、`Number.isFinite` に限定し、**非有限値は従来の比較経路へ流す**（今回のスコープ外・挙動不変）。
   - この規則は**空のテキストフィールドでも右辺が有限数なら −∞ 扱い**になる（現行の動的数値判定に沿う。「フィールド型として数値限定」ではない点を明記）。
   - **右辺空は特別処理に入れず現状維持**（`rightStr !== ""` で除外）。`field > <リテラル>` が主ユースケースで、右辺空を巻き込まない。
   - **案a（`空 → 一律 false`）は不可**: `<=`/`<` で kintone は空を**含む**のに JS が除外 → SIMPLE/FULL_SCAN が再び乖離する。
   - **文字列比較文脈**（右辺が空・非数値・非有限）は**不変**。−∞ 扱いは上記の値ベース条件に限定する。
3. **ASSERT の共通化（[P1-2]）**: `compareAssertValues` も同じ −∞ 規則に揃える（共通化）。`BETWEEN` は下限 `>=`・上限 `<=` に分解されるため、`>=` 側で空が除外される。
4. **波及の回帰**: サブテーブル UPDATE/DELETE・REORDER の**確認ダイアログ件数と実際の対象**を固定するテスト。

## 推奨方針（−∞ 準拠・確定）

- **範囲比較で `leftStr==="" && rightStr!=="" && Number.isFinite(Number(rightStr))` のとき左辺を −∞ 扱い**（`>`/`>=`→false、`<`/`<=`→true）。文字列フォールバックはさせない。右辺空・非数値・**非有限（Infinity 等）**は不変。
- **`evalWhere` と `compareAssertValues` を共通化**して同一規則にする。
- **サブテーブル DML・REORDER を回帰テストに含める**（対象件数・確認ダイアログ件数を固定）。
- **非対象（今回変更しない・別問題）**: 算術関数内の `Number("")`、数値ソート、集計（`SUM`/`AVG` 等）。

## 本課題のスコープと ③ との分離（[P1-1]・重要）

**本課題で解決するのは「空セルの包含性（SIMPLE/FULL_SCAN 一致）」のみ。** 「修正後は数値プレフィルタが全範囲演算子で安全」は**未確定**として切り離す:

- JS の `Number` は **IEEE-754**。桁の大きい整数（例 レコード値 `9007199254740993` と右辺 `9007199254740992`）は同一の浮動小数へ丸められ、JS では `<=` が真になり得るが、kintone が 10 進で比較すれば偽。**kintone 押し下げが JS で真の行を除外＝超集合性が壊れる**可能性が残る（公式仕様でも JS 安全整数域への限定は確認できない）。
- したがって **③（数値プレフィルタ）では別途、次のいずれかが必要**:
  - 正確な 10 進比較を実装し、数値リテラルも字句を失わず保持する、または
  - 安全に保証できる数値ドメイン（例 安全整数域）を定義して押し下げ対象を限定する。
- **型メタデータだけでは解決しない**（精度・表現の問題は型情報とは独立）。

## 受入テスト観点（修正時・修正前 fail → 修正後 pass）

- 空セルの数値フィールドで、**SIMPLE と FULL_SCAN の結果が一致**する:
  - `金額 >= 0` → 空を**除外**、`金額 > -1` → 空を**除外**
  - `金額 <= 0` → 空を**含む**、`金額 < 1` → 空を**含む**
- `=` / `!=` は不変（`数値 = 0` は空セルで偽のまま）。
- 文字列フィールドの範囲比較（`field > ''` 等・非数値文脈）が**不変**。
- HAVING / CASE WHEN の範囲比較でも同じ −∞ 挙動（`>=` で空除外・`<=` で空含む）。
- **サブテーブル UPDATE/DELETE・REORDER**: 空数値セルを含む範囲条件で対象件数・確認ダイアログ件数が修正前後で正しく変わる（`>=`/`>` は対象から外れ、`<=`/`<` は含む）。
- ASSERT / `BETWEEN` の範囲比較が `evalWhere` と一致（共通化後）。`金額 BETWEEN 0 AND 100` は空を**除外**（下限 `>=0`）。
- **共通比較器の単体テスト（明示的な期待値・[Medium R5]）**: 値ベース規則の意図的な波及と現状維持を、次を**明示値付き**で固定する:
  | 左 | op | 右 | 期待 | 意味 |
  |---|---|---|---|---|
  | `""` | `<` | `"-1000000"` | **true** | 空左辺・有限数右辺＝ −∞ 規則 |
  | `""` | `>=` | `"-1000000"` | **false** | 同上 |
  | `""` | `>=` | `"0"` | **false** | −∞ 規則 |
  | `""` | `<=` | `"0"` | **true** | −∞ 規則 |
  | `"-1"` | `>` | `""` | **false** | 左辺非空・右辺空＝現状維持（右辺を −∞ に巻き込まない） |
  | `"-1"` | `<` | `""` | **true** | 現状維持 |
  | `""` | `>=` | `""` | **true** | 左右空＝現状維持 |
  | `"A"` | `>` | `""` | **true** | 文字列比較＝現状維持 |
- **LEFT JOIN 欠損側の波及（1 ケース固定）**: LEFT/RIGHT JOIN の欠損側フィールドも空文字になるため、**欠損側フィールド（空）× 数値右辺の範囲比較**を1ケース固定し、値ベース規則が JOIN 経由でも意図どおり（`>=` で除外・`<=` で含む）に波及することを監視する。
- （非有限の回帰・任意）右辺が `Infinity`/`-Infinity` に解決される経路（例 ゼロ除算算術式）で、空左辺が −∞ 特殊規則に**入らず従来経路**になること。

## 位置づけ

- **述語分割 第0段（`$id` のみ）のブロッカーではない**（`5c987e0` は独立に安全）。
- 本課題は**数値段（③）の前提**であり、かつ**押し下げと独立に「FULL_SCAN の数値範囲が空セルを拾う」挙動の是非**でもある。

## 次

実機確認済＋極端閾値も MCP/APP4221 で独立検証済（−∞ モデル確定・実装前ゲート通過）。修正方針は **値ベースの −∞ 準拠**で確定（案a 棄却）。**本課題は「空セル包含性の解決（SIMPLE/FULL_SCAN 一致）」に限定**し、数値プレフィルタ全体の安全性（IEEE-754 精度・数値表現）は③で別途扱う。次は **codex に本 R4 をレビュー → 仕様案（値ベース −∞ 実装・`evalWhere`/`compareAssertValues` 共通化・波及回帰）→ 実装（分担どおり）**。
