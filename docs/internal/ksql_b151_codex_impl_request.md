# B151 実装依頼（codex）

**[仕様 R1](ksql_b151_join_number_pushdown_spec_r1.md) を実装する。R1 が正本。**
レビューは[指摘なしで通過](ksql_b151_codex_review_1.md)し、§4.4 の空セル
`!=` / `NOT IN` は実装前に実測済み（両経路一致）。

対象リポジトリ: `C:\Users\rex02\Projects\kintone-sql-tools`（v3.59.0）

## 0. 禁止事項（B149 実装依頼と同じ）

- **git 操作を一切しない**（`git status` も含む）。コミットは Claude 側
- **`package.json` の version・`CHANGELOG.md`・README・`release/`・台帳を変更しない**
  （仕様 §9.7 の該当項目はリリース時に Claude が行う）
- **esbuild / ビルド・`npm publish` をしない**
- kSQL MCP を叩かない。自分の MEMORY.md を読まない
- エラー・警告の人間向け本文に内部語を出さない
- 公開型へのプロパティ追加は必ず optional（`?`）

## 1. 実装範囲

仕様 §14 の **Step 1〜4**（literal policy → JOIN classifier → runtime/EXPLAIN → B84 表・言語リファレンス同期）と、
§11 の受入条件のうち **mock client / オフラインで検証できる全項目**のテスト化。

**Step 5（実機・`numberPrecision` 変更アプリ・全 surface smoke）は範囲外**＝実装後に Claude が実測する。

## 2. テストの要件

- §11 の受入 SQL を**そのまま**使い、公開結果（rows・`$id` 集合・EXPLAIN の行・
  mock client の records API query 文字列と呼び出し回数）で観測する
- **受入条件 ↔ テストの対応表**を報告に含める（§11.x → テスト名で全件。
  実機必須で残る項目は「実機（Claude）」と明記）
- **§11.20 の B84 パリティ**＝分類器を 1 セル壊すと落ちる・文書を 1 セル戻すと落ちる、を実際に確認
- 新規テストが**修正前の実装で落ちる**ことを確認してから直す（対応表に修正前 fail の別を記す）

### 既存テストを書き換えるとき（毎回のルール）

- **止めて報告するのは「期待している挙動が変わる」書き換え**。
  ただし本件は仕様が明示的に変える挙動がある＝**NUMBER `=` の `relation: superset` → `exact`・
  strict の安全整数制限解除を固定している既存テストは、仕様 §4.3 に従って書き換えてよい**。
  報告に「どのテストをどう変えたか」を全件列挙する
- `toEqual` 網羅への機械的追記は可（報告に列挙）
- 「既存テスト」は main にコミット済みのものだけ

## 3. 進め方と報告

1. 実装順はコード → テスト → 文書（B84 表・言語リファレンス・B76/B84 歴史注記）
2. 実装後に **`npm test` を全体で回す**（ホストの `KSQL_USERNAME` / `KSQL_PASSWORD` は
   テストプロセス内だけ除外してよい）。結果を報告に含める。落ちたまま終えない
3. 仕様どおりにできない箇所は黙って曲げず「仕様との差分」として報告
4. **自分が書いた仕様の実装である。甘く見ないこと**
5. 最終メッセージ＝実装報告のみ: 変更ファイル一覧／受入 ↔ テスト対応表／`npm test` 結果／
   既存テスト変更の列挙／仕様との差分／Claude が実測すべき残項目（§11・§13 の実機分）
