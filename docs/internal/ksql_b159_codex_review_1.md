# B159 仕様 R1 レビュー（Claude）

- 実施: 2026-08-08
- 対象: [仕様 R1](ksql_b159_generate_series_month_step_spec_r1.md)（codex 作）
- 結論: **採用。ブロッカーなし・実装依頼への注記 2 件。**

## 確認内容

1. **確定事項への適合**＝month/year step（係数付き・day 実装と同一規則）・月初/年初アンカー限定・
   **累積加算の明示的禁止**（§5.1＝アンカー＋行番号からの直接算出。丸めが系列に入り込む余地を
   構造で消す）・DATE 出力・`LAST_DAY`/`DATE_FORMAT` 併記・B149 契約不変・10,000 ガード合流
2. **境界の詰めが正確**:
   - stop 非アンカーの方向別正規化（§5.4）＝負方向でも stop を下回らない
   - 向き逆でもアンカー違反はエラー優先（§4.4＝引数不正は 0 行に隠さない）
   - `12 months` と `1 year` のアンカー契約差の明文化（§5.3）
   - §13.12 の上限境界例は検算一致（1000-01→1833-04 の月数＝9999＋1＝ちょうど 10,000）
   - 西暦 1〜9999 年で `Date.UTC` の 0-99 年補正へ非依存（B149/B152 で 2 度出た罠の先回り）
3. **実需の核心が受入に固定**＝§12 の月次 0 埋め＋`LAG` で「空月直後の前月値が 0」
   （2 か月前を見ない）を逐語データ付きで要求
4. **B158 同時開発の分析**（§14）＝parser/AST の機能依存なし・同一ファイル編集競合は
   **B158 先行統合 → B159 追随**で解決・ガードのカウンタは別のまま・同梱受入 SQL あり

## 実装依頼への注記（2 件・仕様本文の変更は不要）

1. **§16 の文書同期の分担**＝codex は言語リファレンス・MCP schema/説明・syntax catalog・
   smoke assertion まで。**README・CHANGELOG・台帳・リリース履歴・version metadata は
   Claude 工程**（従来の禁止事項が優先）
2. **既存テストの変更は §15.3 の 1 件のみ**（B149 の month 拒否テスト→成功テストへ置換）を
   明示列挙。それ以外の既存テスト変更は「仕様との差分」として報告

## 実装順序（§14.2 どおり）

**B158 実装を先に main へ統合し、B159 は追随**。本レビュー時点で B158 実装を投入する。
