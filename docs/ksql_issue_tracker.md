# kSQL 課題・改善案・Issue 一括管理

- 最終更新: 2026-08-06
- 現在の最新リリース: **v3.56.3**（2026-08-06・publish 済み・実機確認済み）。→ [リリース履歴](ksql_release_history.md)
- 次回リリース計画: **未定**。要対応は **B140 の案 A**（CTE の `GROUP BY` キーを全順序として証明する。**仕様 R1 はレビューで中核が崩れており R2 から**）。**B134 は実需待ち**（レシピ R17 で当面しのぐ）。他は優先 低 か実需待ち（§1）。クローズ済みは §3。
- 目的: 課題・改善案・Issue の**進捗 / 効果 / リリースバージョン**を1か所で俯瞰する。個別の詳細は各文書へリンク。

## 運用ルール

- **本書は一覧（インデックス）**。仕様・診断・受入条件などの本文は各文書に置く。
- 各文書の先頭 `- ステータス:` 行が正（single source of truth）。本書はそれを集約する。
- **複数の課題にまたがる意味論は「横断仕様」を正とし、個別文書はそれを参照する**（事実を書き写さない）。現在: [文字列の扱い](internal/ksql_string_semantics.md)。実測で覆ったら**まず横断仕様を直し**、個別文書はそれに従う。
- 状態が変わったら **本書・各文書のステータス行・`CHANGELOG.md`・auto-memory** を揃えて更新する。
- リリース時は [`ksql_release_history.md`](ksql_release_history.md) へ版数・効果を1行追記し、「§1 バックログ」から該当行を落とす（本書 §2 の直近5版も更新する）。
- **本書は前向きの文書。**「現在の最新リリース」「次回リリース計画」「§1 バックログ」に**リリース済みの経緯を書かない**。済んだ決定は履歴側へ移す（過去にヘッダ1行が 978 字まで肥大した）。

## 凡例（状態区分）

| 記号 | 意味 |
|---|---|
| ✅ | リリース済み |
| 🚧 | 実装済み・リリース/実機確認待ち |
| 📋 | 仕様確定・実装待ち |
| 📝 | ドラフト / 評価 / 提案（仕様前 or 判断待ち） |
| ⏸ | 保留（対象外化の判断済み） |
| ❌ | クローズ（実装しない。実需が出たら再起票） |
| 🐞 | 残課題（未着手のバグ） |

効果の種別: **正しさ**（結果の整合・バグ修正） / **性能**（API 消費・速度） / **機能**（新機能） / **安全性**（誤操作・データ破損防止）

---

## 1. バックログ（未リリース・要対応）

進捗が動くのはここ。**優先度は「正しさ/安全性 > 機能 > 性能改善の上積み」**で暫定。**リリース済みの経緯は書かない**（版ごとの内容と決定理由は [リリース履歴](ksql_release_history.md)）。状態が変わった行はここから落とし、履歴へ1行追記する。

**リリース済みの 12 件は [課題台帳アーカイブ](ksql_issue_archive.md) へ移した**（2026-07-29）。
内容は削っていない。版ごとの決定理由は [リリース履歴](ksql_release_history.md) を参照。

| # | 課題 / 改善案 | 種別 | 状態 | 効果 | 優先 | 文書 |
|---|---|---|---|---|---|---|
| B148 | `GROUP BY` で集計もキーもされていない列が、黙って先頭行の値を返す | 課題 | 📝 **R1 は codex レビューで破棄・R2 から**（2026-08-06）→ [B148](internal/ksql_b148_bare_column_group_by_issue.md) / [R1](internal/ksql_b148_bare_column_group_by_spec.md) / [レビュー 1](internal/ksql_b148_codex_review_1.md)。**【最重要】位置づけが変わった＝同じ規則が既に kSQL にあり、拡張 grouping の経路にだけ効いている**〔`SELECT 製品名, 個数, SUM(個数) FROM APP4228 GROUP BY ROLLUP(製品名)` → `ArgumentError: B65 non-aggregate field 個数 in SELECT is not a grouping item (reason=B65_NON_GROUPED_DEPENDENCY)`。**B148 は「標準 SQL の規則を新しく入れる」話ではなく「規則はあるのに片方の経路にしか効いていない」話**。**[B145 の v3.56.1](internal/ksql_b145_describe_subtable_field_issue.md) と正確に鏡像**＝あちらは plain `GROUP BY` がエラーで拡張 grouping が素通りしていた。**同じ「経路ごとに検査が揃っていない」形が向きを変えて 2 度目**。**帰結＝新しい判定器ではなく [`groupingValidation.ts`](../src/core/groupingValidation.ts) の既存検査を ordinary `GROUP BY` へ広げるのが Phase 1 の中心。reason code も既にある。ただし現行のエラー文は内部語で移行方法を示しておらず、揃えるなら拡張 grouping 側の文面も改善される**〕。**codex レビュー＝Critical 3 / High 5 / Medium 1**〔**Critical 1（`ksql_validate` は schema を読まないので「同じエラー」は実現不能）＝実測で成立**（違反クエリに `ok:true`）→ **static と schema-aware の二段階契約へ**／**Critical 2（`plainGroupByPlan` はそのままでは使えない。`EXPRESSION` は式を保持しない）**／**Critical 3（`HAVING` のみ集計）＝実測で前提が崩れた**（`GROUP BY` 無しの `HAVING` は 2 形とも `ParseError`。**codex は「次の有効形」と書いたがその形は有効ではない**。集計開始条件と実行パイプラインの変更は不要）／High 1 式の正規形が未定義（**不確実ならエラー側へ倒す**）／High 2 CTE・一時テーブル・UNION・サブクエリ・`INSERT SELECT` への適用単位を明記せよ（**`UPDATE FROM` は AST に `SelectStatement` を持たないので対象外と書き分ける**）／High 3 拡張 grouping の既存検査と混ぜるな（→ 上記の最重要発見につながった）／High 4 ウィンドウは集計開始条件に含めない（**実測で確認＝`SUM(x) OVER ()` は 3 行返る。Phase 1 で壊してはいけない回帰**）／High 5 adversarial case とエラー契約が不足／Medium 1 プラグイン bundle の受入が不足〕。**CTE 経由も素通りすることを実測**（ordinary `GROUP BY` はどこでも検査されていない）。**【R2 のスコープが決まった】既存検査（B65）の到達範囲を実機で全数確認**〔**拡張 grouping 経路では `SELECT`／`HAVING`／`ORDER BY` の非集計列・`SELECT *`・JOIN の修飾名（`t.個数` と名指し）・CTE の中がすべてエラーになり、キーへの式（`個数 + 1`）は許可されて値も正しい**（キー 646 → 647）。**ordinary `GROUP BY` はそのすべてが素通り**〕。**R1 が設計しようとしていたもの（句の走査・JOIN 修飾・CTE 適用・wildcard・キーへの式）はほぼ全部すでにある**。**足りないのは 1 点だけ＝grouping item を物理フィールドしか受け付けない**（`GROUP BY ROLLUP(年月)` は `B65 field 年月 does not exist in a physical APP source.`）。**ordinary `GROUP BY` は別名と式を許す（§8 の契約・主用途）ので、そこを教えれば残りは付いてくる**。**R2 の中心＝既存検査の grouping item identity を別名（`ALIAS_SAFE`）と式（`EXPRESSION`）へ広げ、そのうえで ordinary `GROUP BY` と `GROUP BY` 無し集計で検査を有効にする**（codex の Critical 2 推奨案 B とちょうど噛み合う）。**副産物＝エラー文が内部語（`B65 ... (reason=...)`）で、`B65 wildcard projection is not supported in Phase1.` は開発時の Phase ラベルが利用者に漏れている。ordinary へ広げるとき R1 §4.3 の要件へ揃えれば拡張 grouping 側も同時に良くなる**。**codex 傾向の 5 回目の観測＝静的な主張は当たり、動的な主張は外れることがある**（今回外れた Critical 3 は「この SQL は有効形である」という実行しないと分からない主張。**codex 自身が未確認と明記していた点は評価できる**）。**方針＝標準 SQL に合わせる（オーナー判断）＝集計を含むクエリでは、列参照は `GROUP BY` に現れるか集計関数の引数の中にあるかのどちらかでなければエラー**。**3 か所すべてで通ることを実測**〔①`GROUP BY` あり②**`GROUP BY` 無しの集計＝`SELECT 製品名, 個数, SUM(個数), COUNT(*) FROM APP4228` が 1 行（食パン / 646 / 85438 / 1000）を返す。**1000 件が 1 行に畳まれ、製品名と個数は先頭レコード。`GROUP BY` を書いていないので集計したつもりが無い人ほど踏み、見た目が明細行と区別が付かない**③`HAVING 個数 > 0` も素通り〕。**エラーも警告も文書の規定も無い**。**実測（APP4228）**＝`SELECT 製品名, 個数, 日付, SUM(個数) AS 合計, COUNT(*) AS 件数 FROM APP4228 GROUP BY 製品名` が `食パン / 個数 646 / 日付 2025-08-05 / 合計 23429 / **件数 220**` を返す。**`個数` と `日付` は集計もキーもされていないのに値が返り、その値は素の `SELECT ... LIMIT 3` の 1 行目と完全に一致する＝グループの先頭レコードの値**。**220 件のうち 1 件の `個数` が「食パンの個数」として出ている**。**式でも同じ**（`個数 * 2 AS 倍` は先頭行の 2 倍 = 1292）。**重い理由＝踏む形が普通に書かれる**＝`SELECT 製品名, 個数, SUM(個数) AS 合計 ... GROUP BY 製品名` は「製品・数量・合計」と読め、`個数` を意味のある値として受け取るのが自然。**集計し忘れは最も普通の書き間違いで、標準 SQL も MySQL の既定（`ONLY_FULL_GROUP_BY`）もこれをエラーにするのはまさにそのため**。**[B147](internal/ksql_b147_aggregate_alias_shadows_key_input_issue.md) より踏みやすい**（B147 は別名の衝突という珍しい条件が要るが、**本件は「集計を書き忘れる」だけ**）。**文書・実装・テストの状態**＝言語リファレンス §8 は **`GROUP BY` の別名解決順を細かく書いているのに、非集計・非キー列については何も書いていない**／実装にガード無し／**意図として固定した既存テストは見つからなかった**＝**「意図した仕様」か「穴」かが文書から決められない**（MySQL の loose mode と同じ挙動なので互換として意図した可能性はあるが、それなら書かれているはず）。**案＝A エラーにする（標準 SQL・MySQL 既定と同じ・破壊的だが落ちる側の結果は元々誤っている）／B 警告（値は変えない。[B145](internal/ksql_b145_describe_subtable_field_issue.md) 案 B と同じ形で既存 SQL を壊さない）／C 文書化のみ（安いが静かに間違うのは止まらない）／D 何もしない**。**【オーナー指摘で見立てを改めた】＝案 A（エラー）を推す**。起票時は「案 B → 様子を見て案 A」と書いたが弱腰だった。**理由1＝警告では届かない**（B145 の `HAVING` は警告が出ていたのに 0 行を見た人に届かず、B140 は警告疲れそのもの。**警告疲れを起こしている系に、意味のない値を守るための警告をもう 1 本足すのは筋が通らない**）。**理由2＝返る値に正しい読みが 1 つも無い**（220 件のうち任意の 1 件で、近似でも代表値でもない。**v3.56.1 で拡張 grouping をエラーにしたのと同じ基準＝静かに返る値が無意味なら破壊的でもエラーにする**）。**理由3＝正当な用途（関数従属）には既に正しい綴りがある**＝`SELECT 製品名, MIN(仕入先) AS 仕入先, SUM(個数) ... GROUP BY 製品名`。**定数列に対する `MIN` はその定数**で、**MySQL が `ANY_VALUE()` を足したのと同じ役割を `MIN`/`MAX` が既に果たす**（`ANY_VALUE` 新設は不要）。**したがって案 A の代償は「機能が失われる」ではなく「書き方が変わる」だけ**。**エラーの位置＝レコード取得前**（§8 が既に持つ「レコード取得前にエラー」の一覧に並べる。取りに行ってから落ちるのは無駄な API 消費）。**出荷前に測ること**＝掲載 SQL（レシピ・言語リファレンス）は**測って該当なし**（2026-08-06。`GROUP BY` の射影は全てキーか集計）／既存テスト 233 suites は実装すれば落ちて分かる／**依頼元の資産は該当なし（2026-08-06・オーナーが把握しており照会不要。この形は使われていない）**／**残るのは保存クエリとプラグイン利用者で、こちらは測る手立てが無い**。リリースノートとエラー文で案内する。**帰結＝該当クエリを持っている人は、次に実行して落ちたときに初めて知る。ゆえにエラー文が唯一の案内になる**。**受入条件に含める**＝①**どの列が問題かを名指しする**②**「存在しない」と読めないこと**（その列は存在する。v3.56.1 で `unknown field code(s)` が「そんな項目は無い」と読まれ誤った結論へ誘導した）③**書き換え後の形をそのまま示す**（`MIN(<列>)` へ／`GROUP BY` に足す、の 2 択を併記＝意味が変わるので利用者が選ぶ）④**示した形が実際に動くこと**（B140・B145 で「従うと壊れる助言」を 3 回出しているため実測してから載せる）。**「なぜ決まらないか」を書く**のは、v3.56.2 で**症状だけ書くと別の入口から来た人に通じない**と学んだため。**次に測ること＝①`ORDER BY`/`HAVING`/ウィンドウの引数でも先頭行が使われるか②JOIN 込み③CTE・一時テーブル経由④レシピや文書の掲載 SQL にこの形が無いか（あれば [B141](internal/ksql_b141_doc_sql_unverified_issue.md)）⑤**「先頭行」が安定しているか**——取得順・並列取得・押し下げで変わるなら**同じクエリが日によって違う値を返す**ことになり、さらに重い**。 | 正しさ | 高 | [B148](internal/ksql_b148_bare_column_group_by_issue.md) |
| B147 | 集計の別名がキー式の入力フィールドを上書きし、キー列が静かに空になる | 課題 | 🐞 **残課題（実測済み・未着手）**（2026-08-06 起票）→ [B147](internal/ksql_b147_aggregate_alias_shadows_key_input_issue.md)。**エラーも警告も出ず、値が空になる**。**実測（APP4228）＝集計の別名を変えただけで壊れる**〔`SELECT DATE_FORMAT(日付,'%Y-%m') AS 年月, SUM(個数) AS 合計 ... GROUP BY 年月` は `年月="2025-08"` で正しいが、**`SUM(個数) AS 日付` にすると `年月=""` になる**。**集計値は両方とも `6685` で正しく、壊れるのはキー列だけ**〕。**CTE は無関係で素の `SELECT` でも起きる**。**出力名は衝突していない**（`年月` と `日付` は別名）ので、[B145](internal/ksql_b145_describe_subtable_field_issue.md) の重複出力名のように列が潰れる形とは違い、**列は 2 本とも出て片方の中身だけが空になる＝手がかりが何も無い**。**原因（codex のコード読解・未検証）＝集計値はグループ行へ別名で書き込まれ（`src/engine/process.ts:416-431`）、その後の投影（同 2021-2028）でキー式を再評価するため、`DATE_FORMAT(日付,…)` が既に数値になった `日付` に対して評価される**。**グループ化そのものは正しい**（集計値が合っているのが証拠）。**壊れるのは「グループ化した後にキー式をもう一度評価している」ところ**。**重い理由＝`SELECT ..., SUM(x) AS <元の列名>` は自然に書かれる形**で、**大半は壊れない**（同じ SELECT の別の列が上書きされたフィールドを式で参照しているときだけ）。**滅多に出ないが、出たときに気づけない＝B119〜B122 と同じ形**。**発見の経緯＝[B140](internal/ksql_b140_cte_groupby_total_order_issue.md) の対応案を codex に出させ、その中の「post-group alias 衝突」を候補キーの危険として挙げてきたのを実測したら、候補キー以前に現に誤った値を返していた**。**次に測ること＝①影響範囲（`DATE_FORMAT` 以外。**数値どうしの上書きなら空にならず違う値になる可能性があり、そのほうが悪い**）②キー列以外③`HAVING`/`ORDER BY`/ウィンドウ④ウィンドウ関数の別名⑤DML の `SET` 側**。**案（未検討）＝A 投影でキー式を再評価しない／B 衝突をエラーに（破壊的・大半は無害なのに落ちる）／C 書き込み先を分ける／D 警告のみ（値は誤ったまま）**。**B140 とは独立**＝B140 Phase 1 では「候補キーを作らない条件」として除外が要るが、**本件は B140 を進めるかに関わらず単独で扱う**。 | 正しさ | 高 | [B147](internal/ksql_b147_aggregate_alias_shadows_key_input_issue.md) |
| B146 | 版を確かめる費用が高い（版 1 行のために索引 6.8KB が返る） | 改善 | 📝 **起票のみ。案 A で確定だが着手は合図待ち。一度実装したが戻した**（2026-08-06・[依頼元 §3](../../ksql-analytics/docs/internal/kSQLエンジンへの返信-20260806-v3563.md)）→ [B146](internal/ksql_b146_docs_version_lookup_cost_issue.md)。**[B101](internal/ksql_b101_mcp_stale_process_version_issue.md) で「探しに行ける場所」を作ったが、そこへ行く費用を測っていなかった**。**実測＝索引 6,821 バイト / 178 行に対し、欲しい版の行は 144 バイト（47 倍）**。依頼元の見積り（約 3.5KB）より重い。**依頼元の規約は「目次を丸ごと読み込まない」ので、版の確認だけが規約の例外**になっていた＝**引き出しを習慣にしてほしいのに、引く費用が相手の規約違反を強いていた**。**索引の先頭に置く判断そのものには依頼元も賛成**（探しに行った人が最初に当たる場所）。**あわせて判明＝`instructions` の陳腐化は想定より重い**〔依頼元のセッションで `ksql_docs`=3.56.3 / CLI=3.56.3 に対し **`instructions`=3.46.0＝10 版ぶん古い**。その間に MCP は少なくとも 10 回差し替わり、**同じセッションの中で v3.47.0〜v3.56.3 の挙動を実測してきた**という。**B101 §7 の「会話の開始時に 1 回」は正しいが、どれくらい古くなるかを書いていなかった**→**「接続時のみ」ではなく「セッション中の再読み込みは反映されない。長いセッションほど確実に古い」**へ改める。**こちらのセッションでも同日に再現**＝`instructions`=3.56.2 / `ksql_docs`=3.56.3 で**新機能が最初に自分自身を捕まえた**〕。**案 A＝`ksql_docs {"section":"version"}` で版の行だけ返す**（依頼元の提案・索引の先頭行と同じ文字列を使うので二重管理にならない）／案 B＝索引を軽くする（**章を探す人の役に立たなくなる**ので不採用）／案 C＝何もしない（**1 回なら許容範囲だが習慣にはできない**）。**採用＝案 A。索引の先頭にも残す。****鍵一覧・不正キーの案内・索引の案内の 3 か所から見つけられるようにした**＝**引き出しは、探しに行った人が見つけられて初めて成立する**。**「版数は文書ではない」という B101 §4 の反対はここでさらに弱くなる**＝**`ksql_docs` は「この MCP について尋ねる口」として使われているのが実態**。**着手しない理由＝47 倍は比であって実数は 6.8KB。1 回なら何でもない。****「習慣にすると積み上がる」はもっともらしい議論であって観測ではなく、依頼元も「1 回なら許容範囲」「急ぎではありません」と書いている。****B101 が解いた問題（版を確かめられない）は解けており、本件はそれを安くする別の話。****反対材料のほうが具体的だった**＝**固定テストを 2 本壊した**（節キー 46→47・`expectedText` が `version` を知らない）。**あれは手を止めさせるための装置**で実際に止まったのに、**止まった理由を「通すために直す」で処理しかけた**。しかも**同日に 5 版出しており、publish は毎回オーナーの操作（2FA）。この 5 本目がいちばん証拠が薄い**。**一度実装したが、リリースせずに戻した（再開は 15 分程度）。****着手の合図**＝①依頼元が「費用のせいで版の確認を省いた」と報告②同じ取り違えがもう一度起きた③他の理由で MCP を触る版があり、ついでに入れられる。**【オーナー指摘】口を増やすこと自体の費用**＝バイト数では案 A は小さいが、効くのは別の軸。**tool を 1 本増やすと全セッションにスキーマが載る**（案 C を落とした理由）が、**節キー 1 本にも量ではない形で同じ費用がある**＝**`version` は文書ではないので、`ksql_docs` の契約が「文書 + その他」になる**。**次に「安く置く場所が欲しい」ものが出たとき、また同じ理由でここへ来る**。**B101 §4 の「版数は文書ではない」はこれを言っていたのに、2 回とも「もう越えた」で片づけた**。**越えたのは索引の先頭 1 行**で、あれは「このサーバーは何か」を尋ねる呼び出しの答えとして筋が通る。**節キーを増やすのは、そこから一段進んで契約を変える＝同じ判断ではない**。**したがって再開しても案 A をそのまま採らず、「文書ではないものを `ksql_docs` に載せてよいか」を先に決める**（決め方次第では既存 tool の返り値に混ぜるか、B101 案 B の自己検出になる）。**「ついでに入れられる」は契約を変えてよい理由にならない。****手順の反省＝測ってから実装し、実装してから起票した。起票が最後になると、案の比較が事後の正当化になりやすい。実際、起票を求められて初めて「必要か」を考え直し、結論が逆になった。** | 改善 | 中 | [B146](internal/ksql_b146_docs_version_lookup_cost_issue.md) |
| B143 | `EXPLAIN` が `warnings` を返さない | 改善 | 📝 **起票のみ。文書化は v3.54.4 で実施、実装は未着手**（2026-08-06・[依頼元 §6.1](../../ksql-analytics/docs/internal/kSQLエンジンへの返信-20260806-v3543.md)）→ [B143](internal/ksql_b143_explain_warnings_issue.md)。**実測**＝CTE 上の `LAG` も既定フレームのウィンドウも、`ksql_explain` は計画の行（`window ...:` / `frame: RANGE ...`）には出すが **`warnings` は常に空**で、実行すると警告が出る。**依頼元の運用は「実行前に `ksql_explain` まで通す」**ため**「explain を通したから警告は無い」と読み違える**（**素の状態で回した別セッションが「詰まった点」の 1 番目に挙げた**）。**警告疲れ（B140）とは逆向き**＝警告が出ないことで出るはずのものを見落とす。**実装の見通し**＝生成器 `collectDefaultRangeWindowWarnings(stmt, resolveField, context)` は**書いた SQL だけで決まる**ので計画段階で判定できるはずで、**`EXPLAIN` は既にフォーム定義を読んでいる**。ただし**EXPLAIN は実行とは別経路**で、resolver がその経路にあるかは未確認。**着手前に確認＝EXPLAIN 経路に `WhereFieldSemanticsResolver` 相当があるか**（無ければ metadata の追加取得が要らないかを見る。「レコード API を呼ばない」契約は metadata だけなら保てる）。**案 A＝`EXPLAIN` でも返す**（計画と実行で同じ警告文になる利点）／**案 B＝文書化のみ（v3.54.4 で実施済み）**／案 C＝何もしない。**見立て＝案 B は入れた。案 A は resolver が既にあれば安い、無ければ見送り。****順序としては [B140](internal/ksql_b140_cte_groupby_total_order_issue.md) 案 A が先**（入れば CTE 経路の全順序警告は出なくなり、本件の価値も下がる）。 | 改善 | 低 | [B143](internal/ksql_b143_explain_warnings_issue.md) |
| B141 | 文書に載せた SQL が実行されないまま公開される（説明を直しても例が残る） | 課題 | 📝 **個別の 5 件は修正済み。「値の表」の機械照合は v3.54.3 で構造導出へ強化、散文は未着手（下流複製は刻印で対処）**（2026-08-06 起票）→ [B141](internal/ksql_b141_doc_sql_unverified_issue.md)。**同じ形で 5 回起きた＝説明文や挙動は正しく直っているのに、それを写した例や表が古いまま残る。読み手はそちらを写すので直した側が届かない。****4 回目は同じリリースサイクル内で起き、同一文書の中が矛盾した**＝v3.54.0 で `MIN`/`MAX` の空集合を `0` から空文字へ変えたとき、**除数ガードの表だけが `0` のまま残り、同じ文書の集計関数の節と食い違った**。**しかもその表は 1 つ前のリリースで自分が書いたもので、依頼元の指摘で気づいた**。**本件の具体＝言語リファレンスの `NULLIF` の注記**が「空文字が NULL 相当のため `Number('') = 0` となり結局ゼロ除算で `NaN` になる」と原因を正しく説明したうえで、**その 3 行下の ○ が、まさにその理由で NaN を出していた**（`CASE WHEN 金額 = 0 THEN '' ELSE 1000 / 金額 END`＝**空文字は算術では 0 になるが比較では 0 と等しくない**ため空セルの行が `ELSE` へ落ちる）。**実測**＝`LEFT JOIN` で一致しない行（空セル）で旧ガードは `NaN`・新ガード `= '' OR = 0` は空、本物の 0 は新旧とも捕まえ、正常値は不変。**発見の経緯＝依頼元が R16 の `LAG` 先頭行で同じ罠を報告し、他に無いか探して当たった**（依頼元は R16 しか見ていない）。**機械で防げていない理由**＝B136 は列名という照合可能な対象だったので[同期テスト](../src/__tests__/b136DocsColumnParity.test.ts)で塞げたが、**例の SQL が期待どおりの値を返すかは実行しないと分からない**。`docs:check` はリンクと台帳構造しか見ず SQL は素通りする。**案 A＝文書の SQL を実行するテスト**（ネットワークが要り既存 228 suites のオフライン単体テストには入らない）／**案 B＝実行可能な例を機械抽出し固定スタブアプリに対して実行**（ネットワーク不要にできるがスタブ整備が本体）／案 C＝運用ルールのみ（**現に 3 回破れている**）／案 D＝レビュー観点の明文化（費用ゼロだが B136 でも同じことを言って再発）。**案 E＝新規に追加・変更した例だけ実行必須**（依頼元の提案）。**案 B は降ろした**＝`APP100` のような架空アプリは**読みやすさのために選ばれており、実在アプリへ置き換えると読み手の環境で再現しない例になる**（費用が大きいだけでなく文書として悪くなる）。**しかし案 E では 4 回目を止められなかった**＝案 E は「追加・変更した例を実行する」だが、**壊れたのは変更していない別の節**で実行対象に入らない。**学び＝「実行してから載せる」は書いた瞬間の正しさしか守らない。挙動を変えたときに古くなる文書は実行では見つからない。機械照合が要るのは例の SQL ではなく、挙動を写した表のほう**だった。**そこで v3.54.1 で機械化**＝[空集合の戻り値のパリティテスト](../src/__tests__/b141EmptyAggregateDocsParity.test.ts)。**しかしその機械化が 5 回目を出した**＝マーカー 1 つ分の表しか見ておらず、**同じ §8 にあるもう 1 つの表**（「0 件時の挙動」）を素通りさせた。**機械化そのものが、防ごうとした穴と同じ形をしていた**（1 か所を直して同じことを書いた別の場所が残る）。**v3.54.3 で構造導出へ強化**＝「値が無いとき」「0 件時の値」という列見出しを持つ表を**文書全体から自動で見つけて全部照合する**（表を新しく足しても自動で対象になる）。**強化版が直す前の文書で落ちることを確認済み**。**学び＝「機械照合を入れた」は「その照合が網羅している」を意味しない。網の張り方が対象を列挙する形（マーカー・許可リスト）だと、同じ取り残しがメタレベルで再発する。網は構造から導出する。****下流複製には案 F（刻印）＝挙動を写した表に `（実測・vX.Y.Z）` を刻む**（依頼元の提案・v3.54.2 で採用。**複製先が自分で staleness を検出でき、こちらが複製先を把握する必要がない**）。**残る穴＝散文で書かれた挙動の説明**は依然として守れない。 | 正しさ | 中 | [B141](internal/ksql_b141_doc_sql_unverified_issue.md) |
| B134 | 「取引の無い日を 0 として並べる」ことができない（連番・日付系列の生成） | 改善 | 📝 **方向確定＝案 A（日付系列の生成）だが着手は実需が出てから。レシピ R17 は v3.52.0 でリリース済み**（2026-08-06）→ [B134](internal/ksql_b134_series_generation_issue.md)。**【判断を 2 つ改めた】①「B53 との関係整理が先」を撤回**＝B53 は**見積り 18〜29 人日で実需待ちの棚上げ中**であり、**日付系列のために払う額ではない**。「B53 を待つ」は実質「無期限に何もしない」なので、**B134 の判断を B53 に紐づけない**。**②「データ源の無い行を作るのは kSQL の性格が変わる」という案 A への反対理由も弱まった**＝`SELECT '2026-08-01' AS 日付, 0 AS 件数 UNION ALL ...` が**現に動く**（実測）。レシピ R7 が同じ形で固定リストを実体化しており、**kSQL は既に無から行を作れる**。`CALENDAR()` は既にある性質の一般化。**案 C（カレンダーアプリ＋LEFT JOIN）を推奨としては出さない**＝**用意した範囲を外れると同じ静かな失敗が再発する**うえ、全利用者に準備を強い、読み取り専用プロジェクトでは投入手段も別になる。**今すぐ着手しない理由＝実需が今は無い**（依頼元は B128 が `LAG` 先行になり月次では歯抜けが起きない。履歴 13 か月すべてに行があることを実測済み。短期は固定窓で足りている）。**当面の手当て＝レシピ R17 を追加**（マスタ起点の `LEFT JOIN` で 0 を出す形。**掲載 SQL は 6 行 vs 8 行の両方を実機で実行して確認**）。**条件を `WHERE` に書くと `LEFT JOIN` の後に効いて元の問題へ戻る**ことと、**日付には起点になるマスタが無いので同じ手が使えない**ことを明記した。**着手条件**＝依頼元が日次の 0 埋めを実際に必要とする分析を始めた／カレンダーアプリで回避した利用者が境界で踏んだ／`SERIES` や `CALENDAR` を最初に書いた形が観測された。（起票時の分析は以下）**出典＝[B129 の照会に対する `ksql-analytics` の回答 §3](../../ksql-analytics/docs/internal/kSQLエンジンへの返信-B129-20260805.md)**（Q3「3 段 CTE で書けない形はあるか」への回答で**ウィンドウとは別種の書けない形**として挙がった）。`GROUP BY 日付` は**データのある日しか行を作らない**が、**kintone に日付ディメンションが無く kSQL にも連番生成（`generate_series` 相当）が無い**ため 0 埋めした日次系列を作れない。**依頼元は同じ問題を製品側では回避できていた**（マスタ起点の `LEFT JOIN`＋`CASE`。直近 90 日に出庫の無い 2 製品が消え 8 行のはずが 6 行だった）が、**日付には起点になるマスタが無いため同じ手が使えない**。**B119〜B122 と同じ「静かに間違う」系列**＝エラーは出ず、**行が無いことが「0」ではなく「無かったこと」になる**。**「動きが止まった製品」が表から消え、検出したいのはまさにその製品**。**B128 が成立しない理由の 1 つでもある**（日次集約しても取引のあった日の行しかできず、7 行が 15〜29 日に化ける）。**案 A＝日付系列の生成（`CALENDAR(from,to)` 等）／案 B＝汎用の連番／案 C＝利用側でカレンダーアプリを作り `LEFT JOIN`（今日でもできる）／案 D＝文書化のみ**。**見立て＝[B53](internal/ksql_b53_recursive_cte_cycle_phase1_spec.md)（再帰 CTE）との関係を先に整理すべき**＝標準 SQL では `WITH RECURSIVE` で日付系列を作るため、**B53 が入れば案 A/B は不要になる可能性がある**。**再帰 CTE の実需として「日付系列の生成」が挙がったのは初めて**で、B53 の再評価材料になる。 | 正しさ | 中 | [B134](internal/ksql_b134_series_generation_issue.md) |
| B140 | CTE の `GROUP BY` キーを全順序として証明できず、主用途で毎回警告が出る | 課題 | 📝 **案 C（文言の経路分け）は v3.52.0 でリリース済み。案 A（`GROUP BY` キーを全順序の根拠にする）は仕様 R1・要修正のまま**（2026-08-06）→ [仕様 R1](internal/ksql_b140_cte_groupby_total_order_spec.md) / [レビュー](internal/ksql_b140_codex_review_1.md)（高 3・中 4・低 1）。**放置した場合の問題を実測して、想定より悪いことが分かった**＝**警告が指示する対処が CTE 経路では実行できない**〔「ORDER BY にレコード番号などのタイブレークキーを足してください」に従うと `unknown field code(s): レコード番号 (月次)` で落ちる。CTE には `レコード番号` が無いため〕。**「読み飛ばされる」より悪く、従うと壊れる助言**だった。**そこで案 C を先に出す**＝経路に応じて助言を変える（CTE / 一時テーブルでは「その表の中で一意になる列（元の集約のキーなど）を ORDER BY に含めてください」）。**信号は `stmt.from.cteName` ではなく `context`**（実体化の過程で `cteName` がクリアされるため実測で判明。JOIN は `DIRECT` のままなので従来の案内が維持される）。**B127 と B128 の両方に効く**（判定器と警告生成が共通）。**実機で 4 形を確認**（CTE の LAG / CTE の既定 RANGE / 直接 APP の両方）。**案 A（`GROUP BY` キーを全順序の根拠にする）はレビューで中核が崩れた**〔①**出力列名の一致はグループキーとの同一性を証明しない**＝現行は同名の物理列があれば alias より先に物理列で解決するため `SELECT UPPER(k) AS k ... GROUP BY k` では出力 `k` が非単射になり**偽陰性**②**CTE キャッシュに定義 AST が残らない**（行・列・表示/型メタだけ）③**複合 `GROUP BY` は列単位フラグで表せない**ので**関係レベルの候補キー**と伝播規則（投影・rename・filter・DISTINCT・UNION・JOIN・集約・重複名・CTE 連鎖・一時テーブル）が要る〕。**案 A は別途 R2 から。****[B128](internal/ksql_b128_lag_lead_spec.md) の実機検算中に発見。** 依頼元の実需そのもの（月次集約 CTE → `LAG`）で**毎回「ORDER BY は全順序でないため…」の警告が出る**が、**`年月` は CTE の `GROUP BY` キーなので構造上一意**＝**偽陽性**。**判定器自体は正しく動いている**（direct APP で `レコード番号` を含む形は実測で抑止された）。問題は **CTE 由来の列を証明できない**ことだけで、`canProveTotalWindowOrder` は物理フィールドの `$id` / `RECORD_NUMBER` しか根拠にできない。**重要な理由＝警告疲れ**。B127（ウィンドウ既定フレーム）は「**実害があるときだけ警告する**」設計で全順序と判定できれば出さないが、同じ判定器を共有した結果**B128 の主用途が毎回警告される**。**主用途で毎回出る警告は読み飛ばされ、そうなると B127 の警告まで効かなくなる**。**正しさには影響しない**（値は正しい）。**案 A（見立て）＝CTE / 一時テーブルの `GROUP BY` キー（と `DISTINCT` 列）を全順序の根拠として証明できるようにする**（列メタの拡張が要るので独立した仕様とレビューを経る）／案 B＝CTE 経由では警告を出さない（**本物の非全順序も見逃す**ので B127 の設計思想に反する）／案 C＝文言を「判定できませんでした」に変える（**案 A までのつなぎとして併用可**）／案 D＝現状のまま。**要確認＝B127 側にも同じ偽陽性があるか**（集計ウィンドウを CTE の上で使う形）。**あるなら本件は B127 の穴でもある**。 | 正しさ | 中 | [B140](internal/ksql_b140_cte_groupby_total_order_issue.md) |
| B131 | SQL 側での `maxRecords` 要求値の宣言 / 超過の事前検知 | 改善 | 📝 **評価（優先 低）**（2026-08-05 起票・依頼 ⑥）。既定 500 件を超えるアプリで集計が `FetchAllLimitError` になる。**B123 で集計クエリの `EXPLAIN` が通るようになり `fetch: ALL` / `complete input: required` は事前に読めるようになった**（この改善は効いている）が、**`EXPLAIN` はレコード件数を知らないため超過するかは判断できない**。**案 A＝SQL ファイルに `-- ksql: maxRecords=100000` を宣言**（呼び出し側の上限を超えない範囲で採用すれば安全側を壊さない）／**案 B＝`EXPLAIN` の opt-in で件数を取り事前警告**（既定では取らない＝「レコード API を呼ばない」契約を維持）。依頼元は SQL ヘッダの人間向けコメント＋カタログの件数で回避済み。（→ [評価](internal/ksql_analytics_request_20260805_evaluation.md)） | 改善 | 低 | [評価](internal/ksql_analytics_request_20260805_evaluation.md) |
| B100 | 完全入力エラーの語順 — 対処が最後の 5 文目にあり、小さい表示領域で読めない | 改善 | 📝 **評価（優先 低）**（2026-07-29）＝[Pro の報告 v3.34.0 §3](../../ksql-dashboard-pro/docs/internal/kSQLエンジンへの報告-v3340.md)。**Pro は「現状のままで結構です」と明記**しているが、**測ると射程が B98 より広かった**ため起票。**【実測】完全入力を理由に止まる 9 つの形すべてが 5 文で、対処は 5 文目**（`AGGREGATE` 137 字・対処 101 字目 〜 `OUTER_JOIN_NON_PRESERVED` 163 字・対処 127 字目）。**Pro のペインは高さ 3 行程度で先頭 1〜2 文しか見えず、その範囲に対処が入らない**。**内部識別子（`complete input reason: AGGREGATE` 等）の露出も 9 形すべて**で、**B98 で始まったものではない**。**B98 が最長なのは `OUTER_JOIN_NON_PRESERVED` が最長の識別子でアプリ番号も付くため**。文面は 3 箇所（`completeInputErrorPrefix`／`throwCompleteInputError`／`FetchAllLimitError` 本文）の連結で、**語順を変えると `message` の先頭が全形で変わる**。**案 A 語順入れ替え（④⑤①②③）／案 B 識別子だけ末尾（効果が小さい）／案 C 何もしない／案 D `FetchAllLimitError` へ `reasons`・`maxRecords`・`appIds` を任意プロパティで純加法追加（[B95](internal/ksql_b95_truncation_visibility_issue.md) と同じ形）**。**見立て＝案 D + 案 A が素直だが優先度は低い**〔誤った結果を返す問題ではない（止まるべきときに止まっている）／Pro は対応不要と明言／`FetchAllLimitError` という型で判定でき表示は利用者側で吸収できる〕。**次に開くとき必要なもの＝`message` の先頭を照合しているテスト・smoke の実数**（案 A の費用がこれで決まる）。（2026-07-29・Pro の報告から起票） | 改善 | 低 | [B100](internal/ksql_b100_failclosed_message_order_issue.md) |
| B61 | AI 行動検証シナリオセットの運用化（B60 継続） | 改善 | 📝 **評価・継続運用**。機械 guard は「カタログが正しい」まで、「AI が正しく読めるか」は行動検証でしか分からない非対称への対策（文型×依頼のシナリオ台帳）。**残＝スクリプト半自動化・Desktop 面・失敗観測→台帳追加ループの運用化**（小粒・継続タスク） | 機能 | 中 | [B61 issue](internal/ksql_b61_ai_behavior_scenario_set_issue.md) |
| B53 | `WITH RECURSIVE` / `CYCLE` 句（再帰 CTE） | 改善 | 📝 **方向確定（2026-07-23）＝B40 と二者択一で B53 採用**（BOM 多段展開が B53 Phase1 で完結・B40 は可変長 Phase2 が別途必要）。SQL:1999 `WITH RECURSIVE`＋SQL:2016 `CYCLE`。単一再帰 CTE＋必須境界＋CYCLE 最小形・戦略 B（アプリ1回実体化＋メモリ反復）。**仕様 R2・Claude レビュー済＝実装着手可能水準で凍結・実需待ちで棚上げ**（R1→codex→レビュー→R2 で指摘4件反映済み・§13・§5.3 規模目安あり）。**実装着手は BOM/循環の具体ユースケース確認後**（見積り 18〜29 人日の大型投資・B40 と同じく資産化） | 機能 | 中 | [B53 spec R2](internal/ksql_b53_recursive_cte_cycle_phase1_spec.md) / [eval](internal/ksql_b53_recursive_cte_cycle_evaluation.md) |
| B54 | User API（ユーザー・組織・グループ情報）対応 | 改善 | 📝 **評価**。cybozu.com 共通 [User API](https://cybozu.dev/ja/common/docs/user-api/)（`/v1/`・users/orgs/groups）を `__USERS__`/`__ORGS__`/`__GROUPS__` の read-only 仮想テーブルで SELECT/JOIN 可能に。組織階層（`parentCode`）は B53 と相乗。論点＝別ベースパス `/v1/`・権限・ページング・結合キー（code）。**次＝実需確認・権限/面の実機確認→方向確定なら Phase1 仕様 R1** | 機能 | 中 | [B54 eval](internal/ksql_b54_user_api_directory_evaluation.md) |
| B63 | プラグイン面での SQL 構文・関数の説明表示 | 改善 | 📝 **評価・方向判断**。プラグインの SQL 入力画面から構文骨格・関数・方言差をその場参照（B55/B60 の人手書き面版）。既存の機械同期済みカタログ（B60 骨格＋B55 関数＋言語リファレンス）を再利用し二重管理しない。論点＝表示範囲/供給方式/UI 形態。**次＝実需・UI 方針確認→方向確定なら Phase1 仕様 R1** | 機能 | 中 | [B63 eval](internal/ksql_b63_plugin_syntax_help_evaluation.md) |

---

## 2. リリース済み履歴（版数・効果）

**全 65 版の履歴は [`ksql_release_history.md`](ksql_release_history.md) へ分割した**（2026-07-27）。
本書が 102KB まで肥大し、進行中の課題を見るのに履歴を毎回読む状態だったため。

直近5版のみ再掲する。**経緯・撤回した案・失敗の記録は履歴側にある。**

| バージョン | 内容 |
|---|---|
| **v3.56.3** | B101 再開＝**常駐 MCP の版を `ksql_docs`（引数なし）の索引先頭で名乗る**。v3.34.1 の `instructions` 版だけでは**版を確かめようとした人が CLI の `--version`（別プロセス）を見て取り違えた**。**押し付けは引き出しを代替しない。** |
| **v3.56.2** | B145 **警告文が症状と合っていなかった**のを修正（見えるのは 0 行なのに「全行が空になります」と言っていた。**`HAVING` で 0 行を見た人が自分の話だと思えない**）。**原因と代表的な現れ方を並べる形へ**。**`HAVING` は黙って 0 行**になることを §19 に明記。**挙動は不変。** |
| **v3.56.1** | B145 追補。**拡張 grouping（`ROLLUP` / `CUBE` / `GROUPING SETS` / `GROUPING()`）に明細項目を書くとエラーに**（従来は**全レコードが空キーの 1 グループへ畳まれた表**が黙って返っていた・**挙動が変わる**）。`GROUP BY` の案内を「存在しない」から「別の表にある」へ（**別アプリ JOIN で `FROM` 側を誤案内していたのも修正**）。**句ごとの挙動を §19 に表と原理で文書化**（値として出す位置なら空 + 警告、キーとして使う位置ならエラー）。 |
| **v3.54.3** | B141 §8「0 件時の挙動」の表が v3.54.0 の変更に追随していなかったのを修正（**同じ節に空集合の表が 2 つあり、片方だけ古かった**）。**パリティテストが「もう 1 つの表」を素通りさせていた**のを修正＝マーカー 1 つ分ではなく**「値が無いとき」列を持つ表を文書全体から自動検出して全部照合する**形へ。**v3.25.0 の移行注記を削除**（29 版前・現行仕様だけ版数の枠を外して残した）。**エンジンの変更なし。** |
| **v3.53.0** | B140-C2 「従っても消せない」警告に限界を併記（CTE の `GROUP BY` キーで並べていても警告が出る形があり、**助言に従う手段が無かった**。「集約結果の列は一意でも証明できないため、すでに一意な場合もこの警告が出ます」を追記）。B141 文書の SQL の取り残しを 2 件修正（レシピ R16 の注記が CTE 経路なのに `レコード番号` を名指ししていた／**言語リファレンスのゼロ除算ガードの ○ が、その注記自身が説明した理由で `NaN` を出していた**＝`= 0` は空セルを捕まえない）。**エンジンの警告文以外は文書のみ。警告を出す条件と値は不変。** |
| **v3.49.0** | B130 `DESCRIBE` に「値の由来」4 列（`ルックアップ` / `コピー元` / `重複禁止` / `計算式`・該当時 `YES`・**`SELECT *` は 3→7 列**）。B129 ウィンドウ診断に一般形＋○×の例とレシピ R15。B136 の一部＝言語リファレンスの `SHOW APPS` / `DESCRIBE` の例が実行できなかったのを訂正。B135 `mcp:smoke` の期待値ずれ。 |

リリース時は**履歴側へ1行追記**し、本書 §1 から該当行を落とす。ここの直近5版も合わせて更新する。

## 3. 保留・対象外

| 項目 | 状態 | 理由 | 文書 |
|---|---|---|---|
| UPSERT 変更行スキップ `ONLY CHANGED` | ⏸ 保留 | 差分型では効果がリラン時の監査情報保護に限定・実装コスト重（既存値読み取り戦略/型別正規化/64 文字キー問題）。エラー行隔離（B12）の検証エンジンが先・需要立証後に再評価 | [spec](internal/ksql_only_changed_upsert_spec.md) |
| 実行ログ自動記録 / 更新前スナップショット退避 / チャンク実行・レジューム | ⏸ 保留 | ログは `@batch_id`＋現行 INSERT で運用可・スナップショットは `#before` レシピで代替・チャンクは適用限界の外（数十万件級は連携方式見直しが先）。バッチ強化 [roadmap](internal/ksql_batch_processing_roadmap.md) | [roadmap](internal/ksql_batch_processing_roadmap.md) |
| 複数 SQL の並列実行 | ⏸ 対象外 | 順次バッチのみ採用。並列は評価時に対象外化 | [eval](internal/multi-statement-temp-table-evaluation.md) |
| `bulkRequest`（M5） | ⏸ 保留 | v1.4.0 では見送り。実機スパイクとセットで判断 | [temp-table](internal/ksql_batch_temp_table_spec.md) |
| B4 保存クエリのパラメータ化 `:name` | ❌ クローズ（実装しない・2026-07-29） | 中核価値「外部から動的値を安全注入」は `@var`（DECLARE 外部注入）が既に提供済みで、**Pro が実際に採用し「変数コントロールバーの課題はエンジンへの依頼が不要になった」と報告**（2026-07-29）。残る固有価値は「カタログ永続化＋保存クエリでの利用」だけで、それも「保存クエリの単文制約を緩めて DECLARE+SELECT バッチ＋既存 @var」の軽量路線の方が安い。仕様 R2 は流用可能な水準だが、**判断材料は出そろっており寝かせても情報は増えない**ため閉じる。実需が出たら再起票する。**【2026-08-05 追記】ここで挙げた「単文制約を緩める軽量路線」に実需が出たため [B133](internal/ksql_b133_saved_query_batch_issue.md) として起票した**（B129 の検討で一時テーブルによる多段が保存できないことが判明）。 | [eval](internal/ksql_saved_query_params_evaluation.md) / [draft](internal/ksql_saved_query_params_spec.md) |
| B91 MCP のプラグイン互換モード（環境変数） | ❌ クローズ（Pro が取り下げ・2026-07-29） | **Pro が SQL 資産 83 本を機械的に監査し、該当 0 本**〔`IMPORT` 0／DML の `VALIDATE ONLY` 0／`EXPLAIN <DML>` 0／書き込む DML 0〕。**構造的に踏みにくい**＝5 形はすべて「書き込みを伴う作業の下準備」で、**ダッシュボードのペインは画面に表示するためのもの**（Pro 談）。データ品質ペインも**先頭 `VALIDATE APPn`**（両方が受理）で書けている。**決め手は「互換モードが純加法にならない」**＝MCP の `VALIDATE ONLY` を取り上げることになる。Pro は「純粋な絞り込みだと思っていたが、MCP の中核機能を削ることだった」と。**再提起の条件**＝AI が書いた SQL がペインで動かなかった実例が出たら。**そのときはモードではなく `ksql_validate` のフラグ**（Pro 談）。Pro は監査を機械的に回せる形にし、SQL 資産が増えたら定期的に測り直すとのこと。**【申し送り】案 D を実装する場合の穴**（Pro §2.1・要望ではなく参考）＝**`ksql_validate` を呼ばずに `ksql_query` を直接叩く経路が残る**。「渡し忘れ」が「呼び忘れ」に形を変えるだけなので、**同じフィールドを `ksql_query` / `ksql_explain` の応答にも載せる**必要がある。 | [B91 issue](internal/ksql_b91_mcp_plugin_compat_mode_issue.md) / [確認文](internal/ksql_pro_inquiry_b91_20260729.md) |
| B73 エンジンエラーの構造化情報公開 / 多言語対応 | ❌ クローズ（Pro が対応不要と回答・2026-07-29 オーナー判断） | **要望元の Pro が対応不要と回答**（2026-07-27）＝エンジンのメッセージを**翻訳せず `[CODE] メッセージ` でそのまま表示**する方針で、**日英混在は仕様として受容**。**位置・トークンも `messageKey`＋`params` も多言語化も不要**。**他所からの実需も出ていない**ため、**保留のまま置かずクローズする**。**【クローズしても残る契約】エラー `code` の値と意味を変えないこと**＝**Pro はエラー種別をメッセージ本文ではなく `code` で判定している**〔`PARSE_ERROR`／`READ_ONLY_VIOLATION`→構文エラー表示、`SEARCH_ABORTED`／`FETCH_LIMIT_EXCEEDED`→取得上限表示、`CLIENT_ERROR`／`EXECUTION_ERROR`→メッセージで細分化〕。**v3.27.0 の B80 で `code` を `PARSE_ERROR` のまま保った配慮が実際に有効だった**と Pro が明記しており、**B68 でも `code` 維持を受入条件に入れて実装済み**。**再起票する場合も、既存 `code` の値と意味は変えないこと。** | [eval](internal/ksql_b73_error_structured_i18n_evaluation.md) |
| B104 言語リファレンスの構文ブロックと文型テンプレートの乖離が検知されない | ❌ クローズ（何もしない・2026-07-30 オーナー判断） | **測った結果、検査が守る対象が小さかった**。**実在した乖離は SELECT 基本構文の `KORDER BY` 1 件だけ**（修正済み・commit `5b3b9c0`）で、**それも人が読んで見つかった**。**テンプレートの節キーワードをリファレンスへ機械照合すると 7 件が「無い」と出るが、すべて誤検知**〔`[JOIN句]` のような**意図的な略記**／`CHECK WHEN` 5 箇所・`ON DUPLICATE` 16 箇所・`APPLY` 11 箇所・`VALIDATE ONLY` 23 箇所は**専用節に存在**〕。**使える検査にするには略記の許可リストとリファレンス側マーカーの設計・保守が要る**。**[B84] の押し下げ表は「実装から導ける事実」なので生成が効いたが、構文の要約は「何を強調するか」の判断を含むため同じ手が効きにくい**。**再起票の条件＝2 件目の乖離の実例**。そのときは §4 の案 A（基本構文ブロックを指定して照合）の費用を払う根拠になる。 | [B104](internal/ksql_b104_syntax_block_drift_issue.md) |
| B106 テストスイートの監査（無駄・重複・意味のないテストの整理） | ❌ クローズ（整理しない・2026-07-31 オーナー判断） | **測った結果、消してよいものが実質ゼロ**。**【内訳】jest の 5,052 件のうち書かれた `test()` は 2,376 個**で、**差分約 2,600 件は `test.each` のパラメータ行の展開**（132 ファイル）＝**数字自体が実態より大きく見えている**。**【定番の無駄】`skip`/`only`/dead 0・同一ファイル内の同名重複 0・跨ぎ同名 4（層が違う別契約）**。**【規模】テスト 56,097 行 : 実装 47,963 行 = 1.17 倍・増加は 5 リリースで +64 記述・B 番号付き 66/201 ファイルは決定の記録**。**【実行時間】`npm test` 壁時計 約 50 秒＋e2e 19 秒。重い suite は①`evalFunc.surrogate`（総当たり性質検査・2 位の約 2 倍）②MCP 統合系（実サーバー接続）③エンジン統合系**で、**無駄に重いものは無い**（順位は 3 回の計測で安定・絶対値は計測汚染あり）。**【判断】整理で得るのは数字だけで、失うのは決定の記録**（この repo ではテストが仕様書を兼ねる）。**実行時間も削除では縮まず**、surrogate の分割は「網羅が常時走る」保証を失うので勧めない。**再起票の条件＝①`npm test` が数分に達したとき②本物の重複の実例が出たとき③mutation 監査に費用を払う判断をしたとき**。 | [B106](internal/ksql_b106_test_suite_audit_issue.md) |
| B113 `RELATIVE_DATE` 変数への範囲トークン（任意区間の単一変数対応） | ❌ クローズ（見送り・2026-08-02 オーナー判断） | **測って前提が 1 つ覆った**＝**kSQL に展開機構は無い**（`= THIS_MONTH()` は kintone へ素通しで、範囲として解釈するのは kintone 側）。kintone に範囲関数が無いため `DATE_RANGE` は**エンジンが述語の形そのものを書き換える**（`= @p` → `>= a AND < b'`）ことになり、**kSQL 側で初めての述語書き換え**＝B111（値ノードの差し替え）と同じ機構には乗らない。加えて**変数解決はメタデータ解決より前**（実行計画の `variables/subqueries resolved -> metadata resolved`）なので、依頼の狙い（型に応じた境界）にはフェーズ順序の変更が要る。**決め手＝correctness は engine 変更なしで消える**＝範囲形の上端を「終了日の翌日」にする**半開区間**なら DATE / DATETIME を同じ書き方で扱え、**TZ を知らなくても正しい**（実測: `<= '2026-07-14'` 併用は 0 件・`< '2026-07-15'` なら 2 件）。依頼の動機のうち「RFC3339 の使い分けが復活する」はこれで解消し、残るのは「書き方が 2 系統」という説明上の価値のみ。Pro も急ぎでない・代替手段ありと明記。**再起票の条件＝①2 系統が実際の混乱として報告されたとき②任意区間を単一変数で扱う別の実需**。**副産物**＝言語リファレンスの日時境界の案内を RFC3339 から半開区間へ改める価値がある（次リリース候補）。 | [B113](internal/ksql_b113_relative_date_range_token_issue.md) |
| B115 CLI テストが開発者のシェル環境変数に依存して緑赤する | ✅ クローズ（運用で回す・記述で対応・2026-08-04 オーナー判断） | **しくみは変えず、運用として `README.md` の「ローカル開発」に記述した**（テスト実行時は `KSQL_*` を外す・落ちたらまず `env | grep KSQL_` を見る）。**隔離機構を足さない理由**＝認証の優先順位（CLI 引数 → 環境変数 → 設定ファイル）は**仕様どおり**で、テストは実際の CLI をそのまま駆動している。隔離は「正しい挙動をテストのために覆う」形になり、テスト群への適用と保守も要る。**実測で対象が広がった**＝実装が読む env は **`KSQL_*` が 32 個**あり認証系は一部（`KSQL_CONFIG` や `KSQL_PROFILE` が残れば**別の設定を読んで**通ったり落ちたりする）。**`KINTONE_*` は kSQL が読んでいない**。**危険の本体は「赤が常態化して無視されるようになる」こと**で、今回も B114 のレビューで「B114 が壊した」と誤認しかけた——**その危険は原因と確認手順が書かれていれば大きく下がる**（時間を食ったのは切り分けであって修正ではない）。**再検討の条件＝①同じ誤認の再発②開発者が増えたとき**（そのときは `npm test` 側で落とす中央 1 箇所を検討する。テスト群への個別適用はしない）。**未確認の別論点**＝設定に `tokenMap` があるとき env のパスワード認証が黙って優先されるのが期待に合うか。 | [B115](internal/ksql_b115_env_dependent_test_issue.md) |
| B116 未リリースの文書変更を次の機能リリースに同梱する | ✅ クローズ（v3.42.0 で消費・2026-08-04） | 文書 4 件（①日時境界の**半開区間**②README の `KSQL_*` 運用③未知の `fetch` 値を軽い側へ寄せない④押し下がる形への**書き換え表**）を v3.42.0 の CHANGELOG に 1 節としてまとめ、消費した。**単独の文書リリースを出さず次の機能リリースに載せる**という運用は機能した（B117 が来たため待ち時間も短かった）。**同じ状況が再発したら同じ形で起票する。** | [B116](internal/ksql_b116_pending_doc_changes_issue.md) |
| B40 グラフデータモデル（SQL/PGQ・プロパティグラフ／`MATCH`） | ⏸ 保留（B53 と二者択一で B53 採用・2026-07-23） | **B53（再帰 CTE）と同用途（到達可能性・循環検出・可変長経路）の別アプローチで、両方は過剰＝方向判断で B53 を採用**。決め手＝本命の BOM 多段展開が **B53 Phase1 で完結**するのに対し、B40 は **Phase1 が固定長のみで BOM 展開は可変長 Phase2 が別途必要**（19〜31 人日の Phase1 に上積み）・`CREATE PROPERTY GRAPH` 定義の前準備も要る。B40 が B53 を上回るのは「複数エッジ種別を跨ぐパターン照会・途中ノード条件付き経路検索」まで欲しいときだが、実需は探索的で未確認。**BOM 超のグラフ照会需要が具体化したら再評価**（Phase1 仕様 R1・codex レビュー済の資産は流用可） | [B40 eval](internal/ksql_property_graph_evaluation.md) / [Phase1 spec](internal/ksql_property_graph_phase1_spec.md) |
| B65-P2 B65 Phase2 残（#6 CTE/temp 実体化列・#3 式 grouping item・#7 window 併用・#4 GROUPING_ID） | ⏸ 保留（代替策あり・実需待ち・2026-07-23） | コア4件（#1 CUBE / #2 HAVING GROUPING / #5 SELECT DISTINCT / #8 static validate）は v3.18.0 で出荷済み。残4件は**新しい集計能力でなく書き方の直接性のみ**で、いずれも代替策がある＝**#4 は Phase1 の単一引数 `GROUPING()` を複数並べれば level 判別可・#7 の順位は「B65 body を CTE に materialize → 外側で既存 `RANK()`」の2段形で等価（累計 `SUM() OVER()` は B65 でなく window 側の別テーマ）・#3 は #6 の下位互換**。唯一純 kSQL で書けない**#6（正規化/計算済み列を集計軸に）も kintone 計算フィールド追加で迂回可**。実需が出るなら **#6 が起点**。着手前に @既存代替との差分で再評価 | [B65 Phase2 eval](internal/ksql_b65_phase2_evaluation.md) |
| B6 KLIKE 外部結合 非 nullable 側の押し下げ解禁 | ⏸ 却下（代替策あり・2026-07-21） | **回避策で用途を安全にカバーできるため却下**。外部結合の非 nullable 側で KLIKE を高速化したい場合は「**KLIKE で一時テーブル/CTE を作ってから JOIN**」で等価に実現できる（KLIKE は JOIN なしの単純 SELECT で押し下げ・実体化集合を LEFT JOIN・v3.11.0 の B51 修正で CTE 版も可・実データ確認済み）。専用実装（非 nullable 側判定に結合順/来歴解析が必要・誤ると P0 誤結果再導入）はリスクに見合わず、実需も未確認。詳細と回避策は v2 spec §2.6 | [v2 spec §2.6](internal/ksql_klike_pushdown_v2_spec.md) |

---

## 関連文書

- **横断仕様（正・single source of truth）**
  - **文字列の扱い**（文字数の定義・サロゲートペア・比較順序・ホスト依存）: [`ksql_string_semantics.md`](internal/ksql_string_semantics.md) — **個別文書は事実を書き写さず本書を参照する**
- **リリース済み履歴（全版・経緯つき）**: [`ksql_release_history.md`](ksql_release_history.md)
- リリース履歴の一次情報: [`CHANGELOG.md`](../CHANGELOG.md)
- 主要 RDB（MySQL/Oracle/SQL Server）との機能比較・欠落機能の効果評価: [`ksql_sql_feature_comparison_evaluation.md`](internal/ksql_sql_feature_comparison_evaluation.md)
- 言語仕様: [`ksql_language_reference.md`](ksql_language_reference.md)
- バッチ実務レシピ: [`ksql_batch_recipes.md`](ksql_batch_recipes.md)
- リリース手順: auto-memory `release-procedure.md`
