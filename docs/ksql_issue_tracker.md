# kSQL 課題・改善案・Issue 一括管理

- 最終更新: 2026-07-28
- 現在の最新リリース: **v3.30.0（2026-07-28・npm latest 3.30.0）＝B86 実体化ソースの不存在列を fail-closed 化（破壊的）／B85／B84／B83＝リリース完全完了**。**B86 は破壊的変更**（誤った結果を返していたもの・v3.25.0/v3.27.0 と同じ判断基準）。**デプロイ済み MCP で実機確認済み**。版ごとの内容と決定理由は [リリース履歴](ksql_release_history.md)。
- 次回リリース計画: **v3.31.0＝B87 / B88 / B89 / B90 の4件が実装済み（未リリース）＝リリース判断待ち**。**B89 で B68 の穴を2つ塞ぎ**（静的検証エラーの診断情報／`EXPLAIN <DML>` の受理）、**B90 で silent wrong result を1つ潰した**（非数値変数の `NaN`）。**Pro への連絡に含めるべき訂正・注意が4件**＝①メタデータ取得増（Pro 側のメモ化で吸収と回答済み）②**「エラー時の文の特定は手当て済み」は静的検証エラーでは正しくなかった**（B89 で修正）③**`EXPLAIN <DML>` の拒否は純加法ではない**④**非数値変数の fail-closed は依頼の範囲を超える**（`ROUND()` 経路の挙動も変わる）。**B91（依頼③）は B89 の後に再評価**（許可集合は `runBatch` 相当で確定済み）。残りは実需待ち（B53 / B54 / B63）・継続運用（B61）・保留（B73）。
- **Pro（ksql-dashboard-pro）**: v3.30.0 連絡 → **返信受領（2026-07-29）＝報告3件・依頼3件**。**①B86 の影響なし**＝出荷・公開 SQL **53 本を機械的に監査**し裸の識別子は 0 件（**テンプレート 12 種は文字列リテラルを含まない**構造・LIKE/KLIKE は SQL 資産で未使用・出荷サンプル 9 ペインは全件 explain 成功）。**②確認依頼③は Pro の誤診と確定**＝自前クライアントに制約を足したら **MCP と完全一致（10 行 / errorCount 12）**。`createReadonlyKintoneClient` が実行時に制約を返すこともバンドル内で確認され、**Pro のペインは以前から全種類を検証できていた**。当初 8 件だったのは Pro のクライアントが**サブテーブル分岐だけ制約を落としていた**ため。**`constraintMetadata` を警告でなく開示にした判断も「正しいと思う」と同意**（Pro 環境でもアプリごとに持つ制約がまちまちだった）。**③`runBatch` の採用を決定**（2026-07-29）＝前回「急ぎでない」から反転。**`SET`/`DECLARE` の検討漏れ**が理由で、**`runBatch` でしか書けない有用な形が4つ**あった。**`DECLARE` + `variables` は押し下げも効く**ことを Pro が実測し、**変数コントロールバーの課題はエンジンへの依頼が不要になった**（注入 UI は Pro 側の仕事と理解）。**依頼＝①explainQuery のバッチ対応（高・代替なし）②変数の直接算術（中）③MCP 互換モード（低）**→ **B89 / B90 / B91 として起票し、①②を v3.31.0 に含めるとオーナー決定**。**返信文を用意（送付可）**＝[2026-07-29 返信](internal/ksql_pro_reply_20260729.md)。**返信の柱は3点**＝①**v3.30.0 で「メタデータの取得量は増えません」と断言した点の訂正**（B87 で 1 実行×1 アプリ 1 回増・並行実行ぶんも増・10 ペイン同時なら最大 20 回。**Pro は常駐プロセスなので必ず数字で伝える**）②**「配線するだけ」の明確な否定**（否定しないと複文対応だけ入った版で「まだ弾かれる」となる）③**②は (a) で受ける**と明言。**質問は2件だけ・どちらもブロックしない形**＝戻り値の形（案 A＝lines に文番号付き連結・公開型を変えない）と、依頼③の許可集合が `runQuery` 相当か `runBatch` 相当か。**B88 は Pro に心当たりがある可能性が高い**ため確認を促す（日付で絞るペインは 0 件の日が普通に発生する）。**取り込み＝tgz 3.30.0 を `file:` 参照で固定・199 テスト通過・desktop.js 1,304KB / config.js 1,426KB（+3〜4KB）**。**`VALIDATE` は採用の方向**で `present`/`absent` をペイン注記に出す設計。**押し下げ表は「正本はエンジンの言語リファレンス」と明記**しレシピ集は早見版へ降格。**Pro が「依頼しない」と判断した5件**（`>=`/`<=` の JOIN 押し下げ・選択系 `=`・`SET` 右辺の別変数参照・一時テーブル JOIN の server-only 関数・`guard` の export）は**再度持ち出さない**と明記。**B73 は `code` 維持を了承済み**。**【2026-07-29 返信を送付 → Pro から回答受領】質問2件とも回答あり・新規依頼なし。****①戻り値は案 A で確定**（`lines` への文番号付き連結）＝Pro が求めるのは「構文が通るか」と「通らなければどこが悪いか」の2点だけで、**計画の中身は表示に使っていない**。**②依頼③の許可集合は `runBatch` 相当が正しい**＝「こちらの書き間違いでした」と Pro が訂正（`runQuery` に揃えると **互換モードのほうが本番より狭くなる逆転**が起きる）。**③「配線するだけ」の指摘も受け入れ**＝「受理集合が違うという指摘はこちらの見落としでした」。**単文 `VALIDATE APPn` が通るのは「嬉しい副産物」**（品質チェックのペインを設定画面で検証できる）。**【重要】B87 のメタデータ増は Pro 側で吸収される見込み**＝Pro は engine へ渡す client を `SharedRecordCache` で包み **getFields/getApps/getNumberPrecision/getProcessStatuses をアプリ ID 単位でメモ化**（9 ペインが同一インスタンスを共有）。**実測＝エンジンからの呼び出し 3 回に対し実 GET は 2 回**。「10 ペイン×2 アプリで最大 20 回」は**自前キャッシュを持たない利用者の数字**で Pro には該当しない。→ **TTL は追加しない**ことに Pro も同意（「先回りの TTL 追加はしないでください」）。**再集計・自動更新のたびにキャッシュを破棄**するので更新周期ごとに 1 アプリ 1 回は発生するが従来と同じで、**「設定変更が次の更新から反映される」ほうが利用者の期待に合う**との評価。**B88 は現状 実害なしだが採用直前だった**＝テンプレート・レシピ・出荷サンプルは**すべて明示列**で `SELECT *` 未使用のため現行ペインは無傷。ただし **K-33 で採用を決めた `CREATE TEMP TABLE #当月 AS SELECT * … THIS_MONTH(); … JOIN` がまさに該当**し、Pro から**「採用直前に直していただけたことになります」**。**constraintMetadata のばらつきも確認**＝同じ SFA パック内でも present/absent が異なり（顧客管理は range なし・案件管理は required と range なし）、**警告にしていたら毎回 誤警告だった**と Pro も同意。**v3.31.0 の取り込み時に4点を確認予定**＝explainQuery のバッチと VALIDATE／変数の直接算術／**メタデータ取得の実回数**／0 行 `SELECT *` の列。
- 目的: 課題・改善案・Issue の**進捗 / 効果 / リリースバージョン**を1か所で俯瞰する。個別の詳細は各文書へリンク。

## 運用ルール

- **本書は一覧（インデックス）**。仕様・診断・受入条件などの本文は各文書に置く。
- 各文書の先頭 `- ステータス:` 行が正（single source of truth）。本書はそれを集約する。
- **複数の課題にまたがる意味論は「横断仕様」を正とし、個別文書はそれを参照する**（事実を書き写さない）。現在: [文字列の扱い](internal/ksql_string_semantics.md)。実測で覆ったら**まず横断仕様を直し**、個別文書はそれに従う。
- 状態が変わったら **本書・各文書のステータス行・`CHANGELOG.md`・auto-memory** を揃えて更新する。
- リリース時は [`ksql_release_history.md`](ksql_release_history.md) へ版数・効果を1行追記し、「§1 バックログ」から該当行を落とす（本書 §2 の直近5版も更新する）。
- **本書は前向きの文書。**「現在の最新リリース」「次回リリース計画」「§1 バックログ」に**リリース済みの経緯を書かない**。済んだ決定は履歴側へ移す（過去にヘッダ1行が 978 字まで肥大した）。

## 凡例（状態区分）

| 記号 | 意味 |
|---|---|
| ✅ | リリース済み |
| 🚧 | 実装済み・リリース/実機確認待ち |
| 📋 | 仕様確定・実装待ち |
| 📝 | ドラフト / 評価 / 提案（仕様前 or 判断待ち） |
| ⏸ | 保留（対象外化の判断済み） |
| 🐞 | 残課題（未着手のバグ） |

効果の種別: **正しさ**（結果の整合・バグ修正） / **性能**（API 消費・速度） / **機能**（新機能） / **安全性**（誤操作・データ破損防止）

---

## 1. バックログ（未リリース・要対応）

進捗が動くのはここ。**優先度は「正しさ/安全性 > 機能 > 性能改善の上積み」**で暫定。**リリース済みの経緯は書かない**（版ごとの内容と決定理由は [リリース履歴](ksql_release_history.md)）。状態が変わった行はここから落とし、履歴へ1行追記する。

| # | 課題 / 改善案 | 種別 | 状態 | 効果 | 優先 | 文書 |
|---|---|---|---|---|---|---|
| B61 | AI 行動検証シナリオセットの運用化（B60 継続） | 改善 | 📝 **評価・継続運用**。機械 guard は「カタログが正しい」まで、「AI が正しく読めるか」は行動検証でしか分からない非対称への対策（文型×依頼のシナリオ台帳）。**残＝スクリプト半自動化・Desktop 面・失敗観測→台帳追加ループの運用化**（小粒・継続タスク） | 機能 | 中 | [B61 issue](internal/ksql_b61_ai_behavior_scenario_set_issue.md) |
| B53 | `WITH RECURSIVE` / `CYCLE` 句（再帰 CTE） | 改善 | 📝 **方向確定（2026-07-23）＝B40 と二者択一で B53 採用**（BOM 多段展開が B53 Phase1 で完結・B40 は可変長 Phase2 が別途必要）。SQL:1999 `WITH RECURSIVE`＋SQL:2016 `CYCLE`。単一再帰 CTE＋必須境界＋CYCLE 最小形・戦略 B（アプリ1回実体化＋メモリ反復）。**仕様 R2・Claude レビュー済＝実装着手可能水準で凍結・実需待ちで棚上げ**（R1→codex→レビュー→R2 で指摘4件反映済み・§13・§5.3 規模目安あり）。**実装着手は BOM/循環の具体ユースケース確認後**（見積り 18〜29 人日の大型投資・B40 と同じく資産化） | 機能 | 中 | [B53 spec R2](internal/ksql_b53_recursive_cte_cycle_phase1_spec.md) / [eval](internal/ksql_b53_recursive_cte_cycle_evaluation.md) |
| B54 | User API（ユーザー・組織・グループ情報）対応 | 改善 | 📝 **評価**。cybozu.com 共通 [User API](https://cybozu.dev/ja/common/docs/user-api/)（`/v1/`・users/orgs/groups）を `__USERS__`/`__ORGS__`/`__GROUPS__` の read-only 仮想テーブルで SELECT/JOIN 可能に。組織階層（`parentCode`）は B53 と相乗。論点＝別ベースパス `/v1/`・権限・ページング・結合キー（code）。**次＝実需確認・権限/面の実機確認→方向確定なら Phase1 仕様 R1** | 機能 | 中 | [B54 eval](internal/ksql_b54_user_api_directory_evaluation.md) |
| B63 | プラグイン面での SQL 構文・関数の説明表示 | 改善 | 📝 **評価・方向判断**。プラグインの SQL 入力画面から構文骨格・関数・方言差をその場参照（B55/B60 の人手書き面版）。既存の機械同期済みカタログ（B60 骨格＋B55 関数＋言語リファレンス）を再利用し二重管理しない。論点＝表示範囲/供給方式/UI 形態。**次＝実需・UI 方針確認→方向確定なら Phase1 仕様 R1** | 機能 | 中 | [B63 eval](internal/ksql_b63_plugin_syntax_help_evaluation.md) |
| B68 | kSQL read-only ライブラリの read-only 機能拡張（VALIDATE・一時テーブルバッチ） | 改善 | ✅ **リリース済み（v3.29.0・2026-07-28）**（2026-07-27・Step 1〜5 完了）。**Pro のダッシュボードが engine library を使っており、生成 AI が MCP で作った SQL を library が実行するため、両面の許可構文の差は「AI が検証して通した SQL が本番で落ちる」事故になっていた**。**実装＝①判定を `isReadOnlyStatement()` へ一本化**（独自 allowlist を廃止）**②単文 `VALIDATE` を解禁**し `QueryResult.validateStats?` を公開（純加法・非破壊）**③`runBatch()` を新設**（文単位で read-only 強制・`results[]` は `QueryResult` 再利用）**④一時テーブル上限を公開契約化**（既定 10,000 行・同時 16 表・truncate 指定でも error・利用者アプリのプロセス内実体化）＋`variables`（`DECLARE` のみ・`SET` 注入は拒否）**⑤失敗時は throw**（案 A・オーナー決定）〔レビューで**失敗バッチが `ok:false` のまま部分結果を返す**ことを実測し、B79 の「部分結果が黙ってアプリロジックへ流れ込むほうが危険」と衝突すると判明。`ok` を削除し `statementIndex`/`statementType` だけをエラーに載せる〕**⑥parity をテストで固定**〔型レベル網羅・例外3件（`IMPORT`/`APPLY`/DML `VALIDATE ONLY`＝いずれも書き込み前提）・**面の固定**〕。**レビューで契約の穴を3件・codex が実装前に3回停止**〔16表は同時数／例外リストと非スコープの矛盾／受入条件の訂正漏れ〕。**parity は破壊テストで空回りしていないことを確認**（`runQuery` からのみ外す破壊で面固定テストだけが落ちる）。npm test 186 suites/4,813 green・docs/pack/declaration/bundle-guard/mcp:verify green・**B82 guard も green**。SemVer=**minor（純加法）**。 | 機能 | 中 | [B68 計画](internal/ksql_b68_engine_library_readonly_impl_plan.md) / [B68 eval](internal/ksql_b68_engine_library_readonly_extensions_evaluation.md) |
| B73 | エンジンエラーの構造化情報公開 / 多言語対応 | 改善 | ⏸ **保留（Pro 都合ではクローズ可・2026-07-27 Pro 返信）**。**Pro は対応不要と回答**＝エンジンのメッセージを**翻訳せず `[CODE] メッセージ` の形式でそのまま表示**する方針で、日英混在は仕様として受容。**位置・トークンも `messageKey`＋`params` も不要**、多言語化の要望も無し。**ただし1点だけ契約要求あり＝エラー `code` の値と意味を変えないこと**〔Pro はエラー種別を**メッセージ本文ではなく `code` で判定**している。`PARSE_ERROR`/`READ_ONLY_VIOLATION`→構文エラー表示、`SEARCH_ABORTED`/`FETCH_LIMIT_EXCEEDED`→取得上限表示、`CLIENT_ERROR`/`EXECUTION_ERROR`→メッセージで細分化〕。**v3.27.0 の B80 で `code` を `PARSE_ERROR` のまま保った配慮がまさにこの点で有効だった**と明記されている。→ **今後 B73 を実装する場合も既存 `code` の値と意味を変えない**こと。**B68 でも `code` 維持を受入条件に入れて実装済み**。実需が他所から出るまで着手しない。 | 機能 | 中 | [B73 eval](internal/ksql_b73_error_structured_i18n_evaluation.md) |
| B81 | MCP instructions の語数予算が構造的に枯渇 | 改善 | ✅ **リリース済み（v3.29.0・2026-07-28）**（2026-07-27）。**語数予算を散文とカタログ列挙で分けて計上**するようにした（案 D）。実測 **total 548 = catalog 258 + prose 290**。起票時に「散文は P1〜P3 の 116 語」と見積もったのは誤りで、**P4/P5 の説明文も散文**のため実際は 290 語だった。閾値は **prose ≤ 320 / catalog ≤ 420 / total ≤ 700**、**exact 固定 `toEqual({total,catalog,prose})` は維持**（意図しない増減を fail-loud に）。`src/mcp/instructionsBudget.ts` を新設し、**カタログ由来の語数は文面ではなく元データ**（`KSQL_FUNCTION_CATALOG` / `STATEMENT_SYNTAX_CATALOG` / CHECKS / CONTROL）**から数える**。**破壊テストで肝心の性質を確認**＝関数を5個足すと catalog 258→263・**prose は 290 のまま**（旧ルールでは total 553 で上限 550 を突破していた）＝**機能追加が散文の予算を食わなくなった**。**【重要】上限値に外部根拠は無い（§7・実装後の調査）**＝MCP 仕様は instructions を "Optional instructions for the client" と書くだけで**サイズ規定なし**、SDK も `z.string().optional()` で**長さ制約なし**、**B60 の 500 も根拠が記録されていない**。実コストは毎セッション**約 1,000 トークン**（上限まで使っても増分 300〜400）で**無視できる水準**。**測る単位も目的に合っていない**〔文脈コストが関心事ならトークンで測るべき／指示追従の劣化が関心事なら 550 語に閾値がある根拠はない〕。→ **guard の位置づけを「予算」から「変化の検知」へ**＝**exact 固定を本体**とし（B62 502→525・B67 529→541・B76 552 の単調増加を実際に捕捉してきた実績がある）、**上限は「超えたら妥当性を再検討するトリガー」**として扱う（超過時に機械的に圧縮せず上限自体の妥当性を問う）。**トークン計測への置換は実需が出てから**（数字を作り直しても根拠の無さは変わらないため先回りしない）。 | 改善 | 中 | [B81 issue](internal/ksql_b81_mcp_instructions_word_budget_issue.md) |
| B82 | リリース時に公開文書の未リリース表記を検出 | 改善 | ✅ **リリース済み（v3.29.0・2026-07-28）**（2026-07-27）。**B70 §7 の残作業**。v3.25.0 で**リリース済みの B75/B77/B78 が言語リファレンスでは「Unreleased の破壊的変更」のまま出荷**された（2箇所）。version-sync-guard は**版数の一致**を見るもので**語の残存は守備範囲外**だった。**同種の事故は再発**〔v3.28.0 でも `release/README.txt` と `CHANGELOG.md` に「次回リリースの B76 Phase B で」という前方参照が残り、リリース作業中に人手で発見・修正。**「次回リリース」はそのリリースが出た瞬間に誤りになる**〕。**実装＝`version-sync-guard.mjs` に `--release` モード**〔対象 `docs/ksql_language_reference.md` / `release/README.txt`、検出語 `Unreleased`/`未リリース`/`次回リリース`、**`CHANGELOG.md` は開発中に見出しを持つのが正常なので対象外**、失敗時はファイル名・行番号・該当行を出力〕。**`npm test` は従来どおり**（未リリース機能の記述は正常なので開発を妨げない）、**`prepack` からのみ有効**。v3.25.0 と同じ取り残しを再現して**リリースモード exit 1 / 通常モード exit 0** を実測。**残る限界＝語の一致で見るため別の言い回しは捕まらない**ので、公開文書は版に依存しない表現で書く運用が本質。 | 改善 | 中 | [B82 issue](internal/ksql_b82_release_stale_marker_guard_issue.md) |
| B83 | MCP instructions の VALIDATE 診断列数が実態と違う | 改善 | ✅ **リリース済み（v3.30.0・2026-07-28）**（2026-07-28）。**起票時の診断を訂正＝「5 は古い数値」ではなく「2形の取り違え」**〔`VALIDATE ... INTO #err` は **9 列**、**`VALIDATE ... SUMMARY INTO #err` は 5 列**で、`src/core/batch.ts` が `stmt.summary` で切り替える。**実機で両方確認**〕。**tool description が SUMMARY 形の列数を唯一の形のように書いていた**（カタログのテンプレートには `[SUMMARY]` があり例も SUMMARY を使うため、AI が既定形を書くと食い違う）。**言語リファレンスは元から正しかった**（「詳細出力は固定9列」）＝**ずれは MCP 面に限定**で、起票時に「docs が実装と drift」と一般化したのは過大だった。**対応＝数を消さず両方書く**（`nine columns, or five with SUMMARY`）。AI には具体的な数のほうが有用で、2形あると分かれば取り違えない。**`UPSERT_SELECT` のカタログ例も追加**し、B68 parity の「カタログ例の穴」を閉じた（`missingFromCatalog` の期待値を `["UPSERT_SELECT"]`→`[]` へ＝**カタログが全 AST 文型を網羅**するより強い不変条件になった。**穴を塞いだら追跡テストが落ちる**という正しい働きも確認）。該当文は instructions ではなく tool description にあり **B81 の語数予算に影響なし**。npm test 186 suites/4,813 green。 | 改善 | 中 | [B83 issue](internal/ksql_b83_mcp_validate_columns_docs_issue.md) |
| B84 | どの述語が押し下がるかが公開ドキュメントに無い | 改善 | ✅ **リリース済み（v3.30.0・2026-07-28）**（2026-07-28）。**Pro からの質問2件がどちらも「押し下がるかどうかが公開文書に無い」ことに起因**した。**①JOIN 内の数値が押し下がらない**＝実機で切り分けた結果**演算子の違い**で意図した仕様〔`案件No >= 5` は JOIN で全件取得／`> 5`・`= 5`・`$id <= 3000` は押し下がる。根拠は Phase A spec §5.2「**IEEE-754 境界のため inclusive は不可**」で非スコープとしても列挙済み。**Pro の「v3.25 以前と入れ替わった」は誤解**で、旧抽出器 `isNumericCandidate` を確認したところ**Phase A 以前から一般 NUMBER は厳密のみ**、`$id` だけが5演算子〕。**②選択系の `=` が押し下がらない**＝**kintone のクエリ文法が `in`/`not in` のみ**なので `=` は表現できず押し下げようがない（公式表で確認済み）。**Pro が明示的に「言語リファレンスに明記いただけると助かります」と依頼**。**原因＝結果は常に正しいので気づく契機が無く、遅いだけで動いてしまう**〔Pro 実測で取得件数 3.6 倍差〕。**Pro は自前でレシピ集に実測表を作った＝利用者に実測させている状態**で、エンジン側が正を出すべき情報。**対応＝言語リファレンスに押し下げの節を新設**（現状は相対日付・KLIKE・JOIN の各節に分散し横断的に読めない）。**手書きすると B74/B83 と同じ drift を起こすため、Phase A matrix と `classifyJoinPushdownLeaf` から生成する案を優先検討**（B60 が statement templates と function catalog で採っている方式）。手書き 0.5 人日／生成 1〜1.5 人日。公開挙動の変更なし。**B68 Step 5 で言語リファレンスに触れるので同時なら追加コスト小**。**【2026-07-28 設計確定】方針＝案 B（テストで直積を分類器へ流し公開文書の表と照合）**。**着手前に実証済み**＝`classifyJoinPushdownLeaf` へ **20 型 × 8 演算子 = 160 通り**を流して **34ms で表が生成**でき、**生成結果が実挙動と 5/5 一致**（実 kintone・v3.29.0。`superset`/`exact` は押し下がり `unsafe` は全件取得）＝**表は嘘にならない**ことを確認。**案 A（分類器をデータ表へリファクタ）は却下**＝本番ロジックに触る必要があり、押し下げの安全性は過去に何度も事故った領域（v2.0.0 の `LIKE` 全廃）で docs のために触る理由がない。**最大の注意点＝表が説明するのは「JOIN・field vs literal」の1軸だけ**〔単一表は WHERE 全体の直列化で**型を問わず通る**別機構／**`$id` は型ではなく名前で判定される別経路**で全比較演算子が可（probe では `RECORD_NUMBER` 型が全て unsafe になるため、**表に混ぜると逆の結論を読ませる**）／server-only 関数と KLIKE も別軸〕。**Pro が「単一表では押し下がるのに JOIN では」と混乱したのはこの2機構の差**なので、**表の前にどの軸の話かを明示**する。**公開文書は 2 値（押し下がる/押し下がらない）で書く**〔`exact`/`superset` は内部の安全性区分で、**元の WHERE を client で再評価するため結果は同じ**。3値のまま出すと「superset だと結果が違うのか」という疑問を生む〕。**やらないこと＝`>=`/`<=` の押し下げ拡張**（B76 Phase A で IEEE-754 境界のため不可と判断済み・**本課題は可視化であって拡張ではない**）。**【実装】型集合は TypeScript AST で分類器2ファイルを走査**して導出（grep より堅い方式を codex が採用）＝`KSQL_` 派生型と擬似型を除外して **25 型**。言語リファレンスに **§WHERE の REST 押し下げ**を新設し、**軸を先に明示**（alias 付き物理 APP だけの INNER JOIN・field vs literal の単一条件）した上で **2 値**の表を掲載。**`$id` と `RECORD_NUMBER` の違いを理由つきで明記**（`$id` は正準なレコード ID と証明できるが JOIN 中の `RECORD_NUMBER` は別アプリ由来かもしれない＝**書かないと表が逆の結論を読ませる**）。**破壊テストで両方向を確認**〔分類器へ新しい型を足す→落ちる（表に無い行を差分表示）／公開文書の表を1セル書き換える→落ちる〕＝**受入 #7 が実際に働く**ことを実測。**本番コードは無変更**（分類器に一切触れていない）。**実装前に codex が1回停止**＝仕様が「実装から導け」と要求しながら **probe が手書き 19 型**で自己矛盾していた（実装は `LINK`/`RICH_TEXT`/`FILE`/`STATUS_ASSIGNEE`/`CATEGORY` も扱う）→ 型集合の規則を §3.1 として確定。npm test 187 suites/4,814 green・docs-smoke・mcp:verify green。 | 改善 | 中 | [B84 計画](internal/ksql_b84_pushdown_visibility_spec.md) / [B84 issue](internal/ksql_b84_pushdown_visibility_docs_issue.md) |
| B85 | ライブラリの VALIDATE が制約を検証できず黙って 0 件を返す | 正しさ | ✅ **リリース済み（v3.30.0・2026-07-28）**（2026-07-28・Pro 返信の確認依頼③）。**同じ `VALIDATE` 文が MCP は 10 行/errorCount 12、ライブラリは 0 行/0**（選択肢違反だけ一致）。**エラーも警告も無く「エラーなし」と読める**＝fail-closed の思想と一貫しない（Pro 指摘）。**原因は Pro の推測（契約上の限界）とは違った**＝**実測でパイプラインは制約を素通ししている**〔`getFields` が `minLength/required` を返せば 2 件検出、返さなければ 0 件〕。**真因＝型が宣言していないこと**。**TypeScript は余剰プロパティを拒否する**ので自前クライアントの利用者は**渡そうとしても止められる**。**`createReadonlyKintoneClient` は実行時には制約を返している**（型だけ狭められている）＝**同関数の利用者は既に完全な検証ができる**。**メタデータ取得量も増えない**。**案 A（実装済み）＝`ReadonlyFieldInfo` へ `required/minValue/maxValue/minLength/maxLength` を宣言**（`hasAuditableConstraint` が見る集合と完全一致・型は `string`）。公開 snapshot が全 12 プロパティを照合するので**型が痩せたら落ちる**。**案 C（実装済み）＝内訳は `COUNT(*)` ではなく `SUM($err_count)`**（サブテーブル違反が1行に複数エラーとしてまとまるため 10 行 vs errorCount 12）。**【重要・案 B の設計を変更】当初の「制約情報が皆無なら警告」は実測で破綻**〔**APP4147 は 21 項目すべて制約なし**＝制約が1つも無いアプリは実在し、警告すると誤警告になる。**無視される警告は無いのと同じ**。さらに**ライブラリは「アプリに制約が無い」と「クライアントが落としている」を原理的に区別できない**〕。→ **推測をやめ「検証したもの」を開示する**設計へ変更（§8）＝**渡されたメタデータに含まれていた制約種別を列挙**する。観測可能な事実のみで推測を含まない。置き場所は `validateStats`（`warnings` より KPI 提示時に必ず目に入る）。**「0 件でした」より「選択肢だけ検証して 0 件でした」のほうが誤った安心を与えない**。**Pro の質問への回答＝parity テストは受理可否の担保であって結果の一致は対象外**（本課題はその隙間で、parity の定義を見直す契機）。**B78（黙って 0 件）・B79（黙って誤った値）と同系列**。案 B 残 0.5〜1.0 人日・SemVer=minor。**【案 B 実装】`validateStats.constraintMetadata { present, absent }`**（4種＝必須/文字数/上下限/選択肢）。**実際の VALIDATE 対象フィールドだけから算出**し、**`hasAuditableConstraint` と判定ロジックを共有**（二重管理を作らない）。**独立検証で4形すべて期待どおり・警告 0**〔全種別/選択肢だけ/文字数だけ/**制約なし**〕＝**開示は入力の写しで推測が入っていない**ことを実測。 | 正しさ | 高 | [B85 issue](internal/ksql_b85_library_validate_constraints_issue.md) |
| B86 | 実体化ソースで存在しない列参照が検証されず LIKE で全件が返る | 正しさ | ✅ **リリース済み（v3.30.0・2026-07-28）**（2026-07-28）。CTE／一時テーブル／SHOW APPS／DESCRIBE の authoritative `columns` と物理 APP の defs を同じ source-aware preflight で検証し、LIKE・`=`・全列参照位置・subquery・UNION 全枝・混在 JOIN・SELECT-based DML を records GET／confirm／POST／PUT 前に拒否。`defs=[]` は materialized source の抜け道にせず、`rows=[] && columns=[]` は JOIN なし0行読出しだけ既存挙動、JOIN は schema-unavailable error。`INSERT` / `UPSERT` の通常・VALIDATE ONLY・ON ERROR SKIP で GET／confirm／POST／PUT 0回を mock 固定。**実装時判明:** 静的監査の「既存テスト影響0件」と異なり、空 temp の `SELECT *, extra` を誤成功として固定した既存テスト1件があり、B86 契約どおり error 期待へ更新。移行例は `LIKE 顧客` → `LIKE '顧客'`。**【実装レビュー】独立検証で全形が期待どおり**〔`LIKE`/`=` とも `unknown field code(s): 存在しない (#t)` で拒否・**INSERT は POST 0 回**・混在 JOIN は物理側も `(APP200)` で拒否・正しい形は回帰なし〕。**エラーが発生源を名指しする**のでどのソースの列が無いかがすぐ分かる。**仕様の「既存テスト影響 0 件」は実際には 1 件あった**＝空 temp の `SELECT *, extra` を成功として固定していたテストで、**誤った挙動を正しいものとして固定していた**例（静的監査では見つからず実行して判明）。error 期待へ更新済み。移行案内は CHANGELOG・言語リファレンス・`release/README.txt` の3箇所。npm test 188 suites/4,853 green。 | 正しさ | 高 | [B86 仕様](internal/ksql_b86_materialized_unknown_column_spec.md) / [B86 issue](internal/ksql_b86_materialized_unknown_column_issue.md) |
| B87 | アプリ定義キャッシュが無効化されず追加した項目が拒否される | 正しさ | ✅ **実装済み（未リリース）**（2026-07-28）。**kintone に項目を追加してデプロイしても ksql は「存在しない」と拒否する**〔`SELECT レコード番号, B2確認C FROM APP423` → `ArgumentError: unknown field code(s): B2確認C (APP423)`。**同一プロセス・同一アプリで `SELECT *` は同じ項目が見えている**＝内部矛盾〕。**原因＝[execute.ts:5034-5040](../src/execute.ts#L5034) の6キャッシュがモジュール変数で TTL も無効化も無い**。`cacheContext` は [buildCacheContext](../src/node/appProfiles.ts#L395) が作る**内容由来の文字列**でプロセスごとに変わらず、**同じアプリを参照する限り最初に取得した定義を使い続ける**。**レコードは常に最新**（getRecords は非キャッシュ）でこの非対称が矛盾を生む。**影響は MCP と engine ライブラリ**（常駐）で、**CLI・プラグインは短命なので実害なし**。**古くなるのは項目追加だけではない**＝選択肢の増減（IN 押し下げの optionOrder 実在検証）・プロセス管理の有効化（STATUS 押し下げ）・数値精度（VALIDATE の整数桁）・**必須/文字数/上下限（v3.30.0 の B85 で公開したばかりの制約検証が、管理者が制約を厳しくしても古い条件で「エラーなし」を返す）**。**実測（実 kintone APP423・検証後に復元済み）**＝追加前 31 項目 → 追加後 raw GET 33 / describe 31（古いまま）/ `SELECT *` 27 列（新項目あり）。**表面化しなかった理由**＝CLI とプラグインでは起きず、MCP も再読み込みで直るため原因に到達しにくい。**fail-closed 側の誤検知**なので silent wrong result ではない。**ただし B86（v3.30.0）で不存在列の検証を強化した結果、この誤検知が届く範囲は広がっている**。**対応案＝A リビジョン照合（正確・要 API）／B TTL（単純・遅延は残る）／C 明示無効化（原因到達が前提で単独では弱い）／D 不存在列エラー直前に1回だけ再取得（誤検知だけ狙える・押し下げ側は直らない）**。**A＋D の併用が本命**。**B2 の前提条件**＝B2 は getFields から 0 行の列を作るため、**古い定義のままだと B2 が避けようとしている「空の日だけ列数が違う」を B2 自身が作り出す**。**B2 の列順の懸念①は解決済み**＝実 kintone 5 アプリで getFields の順序と `SELECT *` の列順が完全一致。**後から追加した項目は末尾ではなく途中に入る**（APP423 では 6・8 番目）が、**その予測不能な位置まで両者は一致する**。**【2026-07-28 仕様確定】起票時の推奨（案 A＋D）は実コード確認で撤回**＝**`KintoneClient` にリビジョン取得の口が無く**、追加すれば **B85 で判明した Pro の自前クライアントが壊れる**（必須メソッド追加は破壊的変更）。しかも**リビジョン取得自体が 1 API** なので案 E とコストが同じ＝**より複雑で壊れやすく同じコスト**。**採用＝案 E（実行単位スコープ）**＝内容由来の cacheContext（プロファイル分離＝正しさ）は残し**実行ごとに一意な識別子を連結して終了時に破棄**。**案 D は不要**（陳腐化が消えるので誤検知が起きない）。**コスト実測**＝増分は「1 実行 × 1 アプリあたり 1 回」だけで、**1 実行内の重複排除（2 アプリ JOIN=2 回・バッチ4文=2 回）は不変**＝キャッシュ本来の価値は維持。getRecords より小さい。**入口は3つ**（execute / executeBatch / buildBatchExplainPlans＝**既定値がプロファイルを区別しない固定文字列**という別の危険も同時に消える）。**Pro へ訂正が要る**＝v3.30.0 連絡で「メタデータの取得量は増えません」と伝えたが、1 実行×1 アプリあたり 1 回増える。**【実装 2026-07-28】案 E を実装**＝スコープキーを `\0inv:N` 連結（NUL 区切りで内容由来キーと衝突しない）にし、**3 入口すべてに `finally` で解放**（`execute` / `executeBatch` / `buildBatchExplainPlans`）。6 キャッシュすべてを削除する。**独立検証＝修正前 3/4 fail → 修正後 4/4 pass**（テストフック非依存の挙動テストだけを抜き出して HEAD の execute.ts で実行）。**唯一 修正前も pass したのは「1 実行内の 2 アプリ JOIN は 2 回」＝不変を固定するテスト**なので正しい。**副作用＝並行実行どうしの重複排除は失われる**〔従来は in-flight Promise を共有して 1 回。既存テスト2件がこの変化を示し、`status.json` 1→2 回・settings 1→2 回へ更新。**バッチ内・1 実行内は不変**〕。**公開面は不変**（declaration-smoke の B66 snapshot が 6 values / 26 types / 12 properties のまま・内部テストフックは engine-library へ漏れていない）。**B65 行番号の行ずれ 2 件は内容を確認**（2615=`if (stmt.groupBy.length === 0)`／2654=`const groupBy = stmt.groupBy;` でどちらも B71 plain GROUP BY の AST 読み取り＝意図した対象のまま）。**codex が実装前に1回停止**＝Claude のプロンプトが `npm run docs-smoke` と書いたが実在するのは `engine:docs-smoke` だった（**6 回連続で停止の原因は Claude 側**）。**ゲートは Claude が自分で測定**＝npm test 191 suites/4,888 green・snapshot 22 不変・CLI e2e 26 green・engine docs/declaration/bundle-guard・mcp:verify・version:check すべて green。**Pro へ訂正が要る**＝v3.30.0 連絡の「メタデータの取得量は増えません」に加え、**並行実行ぶんも増える**ことを明示する。 | 正しさ | 中 | [B87 仕様](internal/ksql_b87_metadata_cache_spec.md) / [B87 issue](internal/ksql_b87_field_metadata_cache_staleness_issue.md) |
| B88 | 0 行の SELECT * が列を失い空の日だけバッチが落ちる（B2 残スコープ） | 正しさ | ✅ **実装済み（未リリース）**（2026-07-28）。**B86 §8.7 の残る限界と v2.11.0 仕様書 §3.6 に散在していた残スコープを正式起票**〔「着手可能な候補」として参照され続けていたが**台帳に行が無く見つからない状態**だった〕。**事象＝0 行なのは正しいが列名まで消える**（`CREATE TEMP TABLE #e AS SELECT * FROM APP300; SELECT * FROM #e` → columns=[]）。**実害2形**＝`INSERT ... SELECT` が「列数（0）と INSERT のフィールド数が一致しません」／JOIN は B86 の schema-unavailable error。**「差分バッチの空日だけ落ちる」**＝データがある日は動き 0 件の日だけ止まる、最も気づきにくい壊れ方。**原因は起点1箇所**＝**0 行の `SELECT * FROM APP` で `getFields` が一度も呼ばれていない**（実測）＝できないのではなく、していない**。**v2.11.0 のパイプライン伝播は正しく動いており**（明示列で実体化すれば 0 行でも列が残る）**起点に列が無いので運ぶものが無い**だけ。**v2.11.0 §3.6 が案ア（対象外）を選んだ理由2つは B86 で両方とも条件が変わった**〔①「実需が薄い」→ B86 が JOIN 利用を error 化して明示的に止まるようになった ②「`getFields` を撃つ費用対効果」→ **B86 が既に実行前に取得済み**で、追加コストは WHERE も無い純粋な `SELECT *` だけ〕。**【実測 2026-07-28】最大の懸念だった列順は解消**＝実 kintone **5 アプリで getFields の順序と `SELECT *` の列順が完全一致**〔APP4147 21/18・APP452 30/27（テーブル・プロセス有）・APP424 24/16（テーブル2本）・APP423 31/25・**APP423 に項目追加後 33/27**〕。**後から追加した項目は末尾に付かない**（APP423 では 6・8 番目に挿入）**が、その予測不能な途中位置まで両者は一致する**＝順序は作成順でもレイアウト順でもなく `fields.json` と `records.json` が同じ内部順序を共有していると考えるのが妥当。**検証は項目を追加・デプロイ→確認→削除・デプロイで元の定義へ復元済み**。**除外規則も確定**＝CATEGORY／REFERENCE_TABLE／サブテーブルの子（**SUBTABLE 本体は列として残る**）を除外し、**STATUS / STATUS_ASSIGNEE は型では決まらず `status.json` の enable に依存**（APP452 true＝含む／APP4147・APP424 false＝含まない）＝**追加取得が必要**。**残る決めごと＝`$id`/`$revision`** は getFields に無く位置を導出できない（**$id は 4/4 で末尾だが $revision は規則が見つからない**）。**実害は位置ではなく列数**なので**末尾に2列足す案を推奨**（位置ずれは書き込み用途に影響せず、列数が合うほうが重要）。**対象外**＝JOIN を伴う 0 行 `SELECT *`・`_p.*`・混在 `SELECT *, extra` の `*` 分（いずれも列合成順が行依存で 0 行では確定不能）。**B87 が前提条件**＝古い getFields で 0 行スキーマを作ると**本課題が避けようとしている「空の日だけ違う」を本課題の修正が作り出す**。**規模 0.75〜1.5 人日**（`status.json` の追加取得ぶん上限寄り）。SemVer=**minor（0 行時に列が増える方向のみ・1 行以上は不変）**。**B87 と合わせて 1.75〜3.5 人日で 0 行に起因する不具合が完全に消える**。**【実装 2026-07-28】codex が実装前に1回停止＝仕様の穴2件**（**7 回連続で原因は Claude 側**）。**①サブテーブル仮想テーブル**＝`APP100$明細` も AST 上は `cteName === null` なので条件をそのまま書くと**親アプリの列を復元してしまう**（§7 対象外にも書いていない完全な見落とし）。→ **対象外にせず別規則で対応**＝実測（実 kintone 2/2 一致）で `_pid, _rid, _idx` ＋当該サブテーブルの子（getFields 順）と判明し導出が容易だったため。`$id`/`$revision` は付かない。**②「0 行」の意味**＝FULL_SCAN はローカル WHERE・LIMIT・DISTINCT を経て 0 行になり得る。→ **最終結果で判定**（利用者が体験するのは「結果が空」）。**ただし codex が想定した再実行は不要**＝`sourceColumns` が [process.ts:1161](../src/engine/process.ts#L1161) の空列フォールバックでしか使われないことをコードで確認し、**post-hoc fill** へ設計変更（`runFullScan` を 2 回実行しないことを受入 11 で固定）。**Claude が自分で塞いだ穴**＝**実体化して保存する場合は保存より前に埋める**（`MaterializedTable.columns` が空のまま保存されると連鎖が直らない）。受入条件 10→12 件。**実装＝新規 `src/core/emptyWildcardSchema.ts`**（28 型の判定表を `satisfies` で固定し暗黙の追加を許さない）＋ execute.ts の 3 箇所で post-hoc fill。**仕様に無い判断が1件＝未知型は fail-closed で throw**（Claude は網羅確認までしか指定していなかった）。**影響は 0 行復元の経路のみで、その経路は現状すでに壊れている**（columns=[]）ので、黙って誤った列集合を作るより落ちるほうが B78/B79/B86 の系譜に一致＝**妥当と判断し CHANGELOG に明記**。**独立検証＝修正前 11/14 fail → 修正後 14/14 pass**（codex 報告の 4 件より広く落ちた）。**修正前も pass した 3 件は「適用条件外は API を追加しない」＝不変の固定と、新モジュール単体の 2 件**で正しい内訳。**B86 のテスト2件の変更を精査＝保証は弱まらず強まっている**〔①空テーブルへの不存在列参照が**素通し→拒否**（列が復元され検証できるようになった）②JOIN 入力の schema-unavailable error が**解消**〕。**`INSERT/UPSERT ... SELECT *` の文言も変わる**＝「0 行のため列を特定できませんでした」→ 実際の列数不一致（列が特定できるようになったため事実でなくなった）。**B65 行番号の1行ずれ2件は内容を確認**（2616/2655 とも B71 plain GROUP BY の AST 読み取りのまま）。**ゲートは Claude が自分で測定**＝192 suites/4,902 green（B87 時点 191/4,888 から **+1 suite/+14 tests＝新テストちょうど**）・snapshot 22 不変・CLI e2e 26 green・engine docs/declaration/bundle-guard（**公開面不変＝6 values/26 types/12 properties**）・mcp:verify・version:check すべて green。 | 正しさ | 中 | [B88 仕様](internal/ksql_b88_empty_wildcard_schema_restore_spec.md) / [B88 issue](internal/ksql_b88_empty_wildcard_schema_restore_issue.md) |
| B89 | engine ライブラリの explainQuery がバッチを受けず Pro の構文チェックが効かない | 機能 | ✅ **実装済み（未リリース）**（2026-07-29・Pro 依頼①）。**Pro の設定画面は SQL 入力を 0.6 秒デバウンスで `explainQuery` に通し「✓ 構文 OK」を出している**が、複文は `PARSE_ERROR: This API accepts one statement` になる＝**正しい SQL でも「構文エラー」と表示される**。**Pro の3依頼で唯一 代替手段が無い**（暫定は「複文なら検証スキップ」だが構文エラーの位置が分からなくなる）。**経緯＝B68（v3.29.0）で出した `runBatch` を Pro が採用した結果、その検証面の欠落が顕在化した**〔Pro は前回「急ぎでない」としていたが **`SET`/`DECLARE` の検討漏れ**と判明し採用決定。**`runBatch` でしか書けない有用な形が4つ**＝構成比（従来「SQL では出せない」と文書化）／Top N + その他／VALIDATE の内訳グラフ／外部パラメータ注入〕。**【重要】Pro の見立て「配線するだけ」は不正確**＝プランナの有無ではなく**受理集合が論点**。`runBatch` は `assertRunBatchStatement`（read-only 全般・CREATE TEMP TABLE/SET/DECLARE/VALIDATE を含む）だが、**`explainQuery` は `isExplainableReadOnlyStatement`＝SELECT/WITH/UNION のみ**。**複文対応だけ足しても Pro は救われない**（バッチには CREATE TEMP TABLE 等が入る）。→ **本当の依頼は「`explainQuery` が `runBatch` と同じ集合を受けること」**。**Pro 自身が「runBatch は単文も流せるので実行経路を一本化する」と書いており、検証経路も同じ集合でなければ必ずずれる**。単文側も広がるが**従来拒否していたものを通す方向のみ＝純加法**。**プランナは既存**＝[buildBatchExplainPlans](../src/execute.ts#L9595) が metadata API 以外を呼ばず計画を返す（**B87 で実行単位スコープ化した3つ目の入口**）。**戻り値は案 A（`lines` に文番号付きで連結）を推奨**＝公開型を変えずに済む（配列を足すと B66 declaration snapshot が動く）。**エラー時の文の特定は B68 の `statementIndex` で手当て済み**。1.0〜1.25 人日・SemVer=minor（純加法）。**【実装 2026-07-29】codex が実装前に5回停止＝仕様の誤り／既存実装の穴**（**12 回連続で停止の指摘はすべて正しい**）。**①複文内 `EXPLAIN` は受理**（R1 の「拒否を推奨」は誤り＝`assertRunBatchStatement` が既に展開して受理し parity テストも固定済み。拒否は受入4違反かつ破壊的変更）。**②振り分けは文型でも判定**（R2 の「単文は従来経路」は誤り＝`parseExplain` が許すのは SELECT/WITH/INSERT/UPSERT/UPDATE/DELETE/REORDER/VALIDATE/IMPORT だけで、`EXPLAIN SHOW APPS` は parse error）。→ **文数1かつ対象が SELECT/WITH/UNION のときだけ従来経路**とし、**「今日すでに通っている形」に限定することで受入2（既存出力の不変）が構造的に保証される**。接頭辞は文数2以上のときだけ。**③単文の SET/DECLARE/CREATE/DROP は拒否したまま**（R3 の受入3b は誤り＝`analyzeBatch` が意図的に拒否しており、explain だけ緩めると **explainQuery が runBatch の上位集合**になり「構文 OK と出たのに実行できない」という**本課題と同じ形の不整合**を作る）。**④静的検証エラーに文の情報を載せる**（R4 の「B68 で手当て済み」は誤り＝`withStatementDiagnostic` は **executeBatch が文別結果を返した後の実行時失敗にしか届かず**、`BatchAnalysisError` は `statementType` を持たず `normalizeEngineError` で `statementIndex` も落ちていた）。**これは B68 の既存の穴で runBatch でも同じ**だったため**両経路に入れた**。**Pro が最も必要とする場面**（入力中の構文エラー）に直結。**⑤`assertRunBatchStatement` のバグを塞いだ**＝`EXPLAIN` を展開した target を **IMPORT の判定にしか使っておらず**、read-only/APPLY/DML は外側で判定していた。`isReadOnlyType("EXPLAIN")`=true・`isDmlType("EXPLAIN")`=false のため **runBatch が `EXPLAIN UPDATE` を受理**していた（explainQuery は元から拒否＝既存テスト3件が意図された姿を示していた）。**純加法ではなくなった**（受理範囲が1形だけ狭まる）が、**read-only ライブラリで DML の計画を出せること自体が意図外**・**EXPLAIN は書き込まないので誤った結果を得ていた利用者がいない**・**B68 parity が固定しているのは `EXPLAIN SELECT`** の3点で許容と判断。**APPLY は元から塞がっていた**（`statementHasApplyBlocks` が内部で EXPLAIN を展開）＝Claude の R6 の推測は外れ。契約として明示的に固定した。**死んだコードを1つ削除**＝`isExplainableReadOnlyStatement` が本番・テストとも参照ゼロになり、doc コメントが「explainQuery の契約を独立させる」という**B89 で無くなった契約**を説明したまま残っていた（B74/B83 と同じ drift 源）。**独立検証＝修正前 19/24 fail → 修正後 24/24 pass**（codex 報告の 15/16 より広く落ちた）。**既存テストの変更は 0 件**で、**`statementGuard.test.ts` の既存3件（`EXPLAIN UPDATE`/`EXPLAIN IMPORT`/`DELETE` を READ_ONLY_VIOLATION で拒否）がそのまま通る**＝⑤の判断の正しさを独立に裏づけた。**受入4の網羅は20文型・例外なし**（単文で無意味な4型はバッチ文脈の受理と単文の拒否を別々に固定）。**ゲートは Claude が自分で測定**＝193 suites/4,926 green（B88 時点 192/4,902 から **+1 suite/+24 tests**）・snapshot 22 不変・**公開面不変（6 values/26 types/12 properties）**・engine docs/bundle-guard・mcp:verify・version:check すべて green。 | 機能 | 高 | [B89 仕様](internal/ksql_b89_library_explain_batch_spec.md) / [B89 issue](internal/ksql_b89_library_explain_batch_issue.md) |
| B90 | 変数を算術式に直接書けない（関数で包めば動く） | 改善 | 📝 **評価・起票（優先 中／回避策あり）**（2026-07-29・Pro 依頼②）。**実機で再現**＝`SELECT (顧客No * 100) / @total` は ParseError だが `ROUND(顧客No * 100 / @total, 1)` は動く（5.9/23.5/17.6/35.3/17.6）。**原因＝式文法が2系統**〔関数引数・SET 右辺の `SCALAR_*` は `@var` を受けるが、**SELECT 列の算術 `parseArithPrimary` に `TokenKind.VARIABLE` の分岐が無い**（受けるのは括弧・単項符号・文字列/数値関数・NUMBER・IDENT/BIDENT のみ）〕。**Pro が困る理由＝構成比（全体比 %）はダッシュボードで最も要望の多い列の1つ**で、従来「SQL では出せない」と文書化していたものが `runBatch` 採用でようやく書けるようになった。説明が「`ROUND()` で包んでください。理由は文法上の都合です」となり納得しづらい。**【重要】案 (a) は現実的**＝**変数は実行前に解決され評価器や押し下げ分類器へ届かない**。[resolveBatchVariableReferences](../src/execute.ts#L1975) が**木全体を汎用に走査**して `{type:"VARIABLE"}` をリテラルへ置換するため、**算術木の中に置いても同じように解決される＝押し下げの安全性に触れずに済む**。配列変数の誤用も既に塞がれている。**コスト＝`ArithNode` 型の波及**（TypeScript が漏れを検出するので安全側だが作業量はある）。**本課題の中核は「解決前に走る経路が無い」ことの機械的確認**（`buildBatchExplainPlans` は計画前に解決を呼んでいるが、同種の経路が他に無いか）。**単文で `@x` を算術に書いた場合は従来どおり明確なエラー**にする。案 (b)＝言語リファレンス §25 に「関数で包む」と明記するだけなら 0.25 人日だが、**(a) を採るなら (b) は不要**。1.0〜1.5 人日・SemVer=minor（純加法＝従来 ParseError が通る）。**【実装 2026-07-29】仕様を書く前に既存契約をコードで確認**（B89 で5回停止した反省）＝**①変数の解決は実行ディスパッチより前**（execute.ts:1697）で評価器・押し下げ変換器には解決済みリテラルしか届かない**②`collectVariableRefs` と `resolveBatchVariableReferences` はどちらも型ではなく構造で探す深さ優先走査**なので算術木でも追加実装なしで効く**③単文の既存エラーもそのまま出る**（実測）。**それでも codex が3回停止＝いずれも Claude 側**〔**(a) §3.2 と §7 の直接矛盾**（全消費側でエラーにせよ と 同じファイルを触るな を併記）**(b) 消費側の数え漏れ**＝「17 箇所・7 ファイル」は grep の手集計で `arithColDefaultKey`（`n.type === "FIELD_REF"` でパターンから漏れた）を取りこぼし、実際は 18 箇所・8 ファイル**(c) 単文エラー文言の文書間の食い違い**（codex は停止理由にせず正しい方を採ると判断）〕。**→ 数を仕様から外し「網羅は TypeScript のビルドに列挙させる」へ変更**（**B84 で押し下げ表を実装から生成させたのと同じ考え方**を、自分の仕様執筆に適用できていなかった）。**押し下げ変換器はガードのみ追加可**とした＝`collectArithNode`/`walkArith` は**未知のノードを黙って読み飛ばすフィールド収集器**で、**VARIABLE を黙殺すると参照フィールドの取りこぼし**→ B86 の検証漏れや取得フィールド欠落という silent wrong result になり得る。**到達性を確認**＝解決前に走る `analyzeBatch` は `resolveSelectMode` に届くが**列の型と WHERE の形しか見ず算術オペランドを歩かない**ため、ガードは正常系に影響しない。**実装＝パーサに VARIABLE 分岐＋`ArithNode` に `VariableRef`。6 ファイル 11 箇所すべてで未解決到達時に InternalError**。**codex が回帰を1件自力で検出**＝`UPDATE SET n = -@x` が一度受理されたため **SELECT 算術限定のパーサコンテキストへ狭めて解消**。**独立検証＝修正前 7 件 fail → 修正後 pass**（codex 報告の6件より1件多い）。**既存テストの変更は追加のみで書き換えゼロ**。**特に良いテスト2つ**＝①非数値変数のエラーが **`client.getCalls`=0**（取得前に停止）②**WHERE 変数の解決後 pushdown が同値の直接リテラルと一致**（押し下げに変数が届いていないことを結果で証明）。**送付済みの Pro 連絡文への追記は Claude が差し戻した**（送った文書は書き換えられない。内容は v3.31.0 の連絡へ）。**言語リファレンスへ A14（SELECT 列の算術オペランド）と非数値変数のエラーを追記**、`cli-code-guide` の `ArithNode` を 5 種類へ更新。**ゲートは Claude が自分で測定**＝193 suites/4,934 green（B89 時点 193/4,926 から **+8 tests**）・snapshot 22 不変・**公開面不変（6 values/26 types/12 properties）**・engine docs/bundle-guard・mcp:verify・version:check すべて green。 | 改善 | 中 | [B90 仕様](internal/ksql_b90_variable_arithmetic_spec.md) / [B90 issue](internal/ksql_b90_variable_arithmetic_issue.md) |
| B91 | MCP にプラグイン互換モードを追加する（環境変数） | 改善 | 📝 **評価・起票（優先 低／Pro 側に代替あり・B89 の後）**（2026-07-29・Pro 依頼③＝継続）。環境変数（例 `KSQL_PLUGIN_MODE=1`）で ksql_validate / ksql_explain / ksql_query に runQuery と同じ許可リストを適用してほしい。**ツール引数ではなく環境変数を希望する理由＝引数だと AI が渡し忘れる**（`.mcp.json` に探索用と互換モード用の2エントリを登録して併存できる）。**Pro が優先度を下げた**＝**設定画面の構文チェックが `explainQuery`（プラグインの guard 経路）を通すため、CLI/MCP 専用構文はペインを開いた時点で既に弾かれる**。本件が埋めるのは**「AI が MCP で書いた SQL が設定画面を開くまで誤りと分からない」時間差だけ**で、誤った結果が出る類ではない。**B89 が実現すると本件の価値も上がる**（バッチが検証できれば互換モードでも同じ判定が返せる・Pro 談）ため**先に B89**。**検討事項3点**＝①環境変数という形（面ごとの許可集合を環境変数で切り替える前例が無い。プロファイルは既にツール引数）②**Pro が欲しいのが `runQuery` 相当か `runBatch` 相当か**（`runQuery` は行を返す文に限られる。**Pro は `runBatch` を採用済み**なので後者の可能性が高い）③MCP の許可集合が2通りになると **B68 の parity テストの対象も2通り**になる保守コスト。**未見積もり＝B89 完了後に再評価**。 | 改善 | 低 | [B91 issue](internal/ksql_b91_mcp_plugin_compat_mode_issue.md) |

---

## 2. リリース済み履歴（版数・効果）

**全 63 版の履歴は [`ksql_release_history.md`](ksql_release_history.md) へ分割した**（2026-07-27）。
本書が 102KB まで肥大し、進行中の課題を見るのに履歴を毎回読む状態だったため。

直近5版のみ再掲する。**経緯・撤回した案・失敗の記録は履歴側にある。**

| バージョン | 内容 |
|---|---|
| **v3.30.0** | B86 実体化ソースの不存在列を fail-closed 化（**破壊的**）／B85 VALIDATE の検証範囲開示／B84 押し下げ可否の公開／B83 |
| **v3.29.0** | B68 engine ライブラリの read-only 拡張（`runBatch` / 単文 `VALIDATE` / parity 固定）／B81／B82 |
| **v3.28.0** | B76 Phase B — JOIN で server-only 15 関数（第5-W / 第5-L） |
| **v3.27.0** | B79 外部結合の検索打ち切りを fail-closed ／ B80 engine ライブラリの reason 保持 |
| **v3.26.0** | B76 Phase A — JOIN 述語の APP 別 prefilter ／ B70 版数同期ガード |

リリース時は**履歴側へ1行追記**し、本書 §1 から該当行を落とす。ここの直近5版も合わせて更新する。

## 3. 保留・対象外

| 項目 | 状態 | 理由 | 文書 |
|---|---|---|---|
| UPSERT 変更行スキップ `ONLY CHANGED` | ⏸ 保留 | 差分型では効果がリラン時の監査情報保護に限定・実装コスト重（既存値読み取り戦略/型別正規化/64 文字キー問題）。エラー行隔離（B12）の検証エンジンが先・需要立証後に再評価 | [spec](internal/ksql_only_changed_upsert_spec.md) |
| 実行ログ自動記録 / 更新前スナップショット退避 / チャンク実行・レジューム | ⏸ 保留 | ログは `@batch_id`＋現行 INSERT で運用可・スナップショットは `#before` レシピで代替・チャンクは適用限界の外（数十万件級は連携方式見直しが先）。バッチ強化 [roadmap](internal/ksql_batch_processing_roadmap.md) | [roadmap](internal/ksql_batch_processing_roadmap.md) |
| 複数 SQL の並列実行 | ⏸ 対象外 | 順次バッチのみ採用。並列は評価時に対象外化 | [eval](internal/multi-statement-temp-table-evaluation.md) |
| `bulkRequest`（M5） | ⏸ 保留 | v1.4.0 では見送り。実機スパイクとセットで判断 | [temp-table](internal/ksql_batch_temp_table_spec.md) |
| B4 保存クエリのパラメータ化 `:name` | ⏸ 保留（候補外・2026-07-21） | 中核価値「外部から動的値を安全注入」は `@var`（DECLARE 外部注入）が既に提供済み。残る固有価値は「カタログ永続化＋保存クエリでの利用」だけで、それも「保存クエリの単文制約を緩めて DECLARE+SELECT バッチ＋既存 @var」の軽量路線の方が安い。仕様 R2 は流用可能水準だが実需未確認。着手前に @var との差分で再評価（実需が出たら候補へ戻す） | [eval](internal/ksql_saved_query_params_evaluation.md) / [draft](internal/ksql_saved_query_params_spec.md) |
| B40 グラフデータモデル（SQL/PGQ・プロパティグラフ／`MATCH`） | ⏸ 保留（B53 と二者択一で B53 採用・2026-07-23） | **B53（再帰 CTE）と同用途（到達可能性・循環検出・可変長経路）の別アプローチで、両方は過剰＝方向判断で B53 を採用**。決め手＝本命の BOM 多段展開が **B53 Phase1 で完結**するのに対し、B40 は **Phase1 が固定長のみで BOM 展開は可変長 Phase2 が別途必要**（19〜31 人日の Phase1 に上積み）・`CREATE PROPERTY GRAPH` 定義の前準備も要る。B40 が B53 を上回るのは「複数エッジ種別を跨ぐパターン照会・途中ノード条件付き経路検索」まで欲しいときだが、実需は探索的で未確認。**BOM 超のグラフ照会需要が具体化したら再評価**（Phase1 仕様 R1・codex レビュー済の資産は流用可） | [B40 eval](internal/ksql_property_graph_evaluation.md) / [Phase1 spec](internal/ksql_property_graph_phase1_spec.md) |
| B65-P2 B65 Phase2 残（#6 CTE/temp 実体化列・#3 式 grouping item・#7 window 併用・#4 GROUPING_ID） | ⏸ 保留（代替策あり・実需待ち・2026-07-23） | コア4件（#1 CUBE / #2 HAVING GROUPING / #5 SELECT DISTINCT / #8 static validate）は v3.18.0 で出荷済み。残4件は**新しい集計能力でなく書き方の直接性のみ**で、いずれも代替策がある＝**#4 は Phase1 の単一引数 `GROUPING()` を複数並べれば level 判別可・#7 の順位は「B65 body を CTE に materialize → 外側で既存 `RANK()`」の2段形で等価（累計 `SUM() OVER()` は B65 でなく window 側の別テーマ）・#3 は #6 の下位互換**。唯一純 kSQL で書けない**#6（正規化/計算済み列を集計軸に）も kintone 計算フィールド追加で迂回可**。実需が出るなら **#6 が起点**。着手前に @既存代替との差分で再評価 | [B65 Phase2 eval](internal/ksql_b65_phase2_evaluation.md) |
| B6 KLIKE 外部結合 非 nullable 側の押し下げ解禁 | ⏸ 却下（代替策あり・2026-07-21） | **回避策で用途を安全にカバーできるため却下**。外部結合の非 nullable 側で KLIKE を高速化したい場合は「**KLIKE で一時テーブル/CTE を作ってから JOIN**」で等価に実現できる（KLIKE は JOIN なしの単純 SELECT で押し下げ・実体化集合を LEFT JOIN・v3.11.0 の B51 修正で CTE 版も可・実データ確認済み）。専用実装（非 nullable 側判定に結合順/来歴解析が必要・誤ると P0 誤結果再導入）はリスクに見合わず、実需も未確認。詳細と回避策は v2 spec §2.6 | [v2 spec §2.6](internal/ksql_klike_pushdown_v2_spec.md) |

---

## 関連文書

- **横断仕様（正・single source of truth）**
  - **文字列の扱い**（文字数の定義・サロゲートペア・比較順序・ホスト依存）: [`ksql_string_semantics.md`](internal/ksql_string_semantics.md) — **個別文書は事実を書き写さず本書を参照する**
- **リリース済み履歴（全版・経緯つき）**: [`ksql_release_history.md`](ksql_release_history.md)
- リリース履歴の一次情報: [`CHANGELOG.md`](../CHANGELOG.md)
- 主要 RDB（MySQL/Oracle/SQL Server）との機能比較・欠落機能の効果評価: [`ksql_sql_feature_comparison_evaluation.md`](internal/ksql_sql_feature_comparison_evaluation.md)
- 言語仕様: [`ksql_language_reference.md`](ksql_language_reference.md)
- バッチ実務レシピ: [`ksql_batch_recipes.md`](ksql_batch_recipes.md)
- リリース手順: auto-memory `release-procedure.md`
