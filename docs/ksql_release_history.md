# kSQL リリース済み履歴（版数・効果）

- 最終更新: 2026-08-07
- 目的: 出荷済みの各版が**何を変え・なぜそう決めたか**を1か所に残す。進行中の課題は [課題台帳](ksql_issue_tracker.md) を参照。
- 本書は [`ksql_issue_tracker.md`](ksql_issue_tracker.md) の §2 を分割したもの（2026-07-27）。台帳が 102KB まで肥大し、進行中の課題を見るのに履歴を毎回読む状態だったため。

## 書き方の約束

- **新しい順**に1版1行。各機能の詳細は [`CHANGELOG.md`](../CHANGELOG.md) と各文書を参照する。
- **「何をしたか」ではなく「なぜそう決めたか・何を間違えたか」を残す。**撤回した案とその理由も書く。
  同種の失敗を繰り返さないための記録なので、成功だけを書かない。
- 効果種別 = 正しさ / 性能 / 機能 / 安全性。
- リリース時はここへ1行追記し、台帳 §1 バックログから該当行を落とす。

## 履歴

| バージョン | 内容 | 効果 | 文書 |
|---|---|---|---|
| **v3.59.0** | **B149 `GENERATE_SERIES` — 整数・日付系列の生成（2026-08-07・minor・新機能）**。**B134（0 埋め日次系列が作れない・「動きが止まった製品」が表から消える）の具体形**で、B134 はこの版で解決しクローズ。**オーナー起票（PG 仕様の参照資料付き）から同日で起票→仕様 R1→レビュー→R2→実装→最終チェック→リリースまで到達**。**仕様・実装は codex、レビュー・実測・リリースは Claude**（B148 で確立した分担の 3 件目。今回も機能した）。**形＝文 CTE**（`WITH s AS (GENERATE_SERIES(...))`・`DESCRIBE` の前例に乗る。`FROM` 直置きは Phase 1 対象外）・**PG 準拠境界**（stop を超えない・向きが逆なら 0 行・step 0 エラー・負 step 可）・日付 step は `day` のみ・**上限 10,000 行**（`WITH` 文内合計・事前算出）・型メタ伝播。**R1 レビュー（Claude・実測付き）の要点 2 つ**＝①**仕様の `DECLARE` 例が `DEFAULT`・日本語変数名の二重で壊れていた**（B141 の「従うと壊れる例」を仕様段階で捕捉。実構文は `DECLARE @start = 1`・変数名は英字/`_` 開始のみ）②**生成系列＋`LAG` で「全順序でない」警告が発火することを実測で発見**→**案 a 採用＝構成保証（厳密単調）による抑止**。抑止条件は既存 `$id` 証明と同形の保守的直接証明に限定し、JOIN・`UNION`・一時テーブル経由は警告維持（**B140 で見送った「推論による証明」とは別物**という区別を仕様に明記）。**codex の実装後セルフチェックが出荷前に 2 件捕捉**（数値リテラルの丸め後判定＝`1.0000000000000000001` を `1` で誤受理／`TIME` の誤診断）＝**B148 に続き 2 度目の実績。この工程は残す価値がある**。修正は `parseExactDecimal` による丸め前判定を実行時と AST-only 検証の両経路へ適用（回帰テストは修正前 fail を確認してから追加）。**実機確認（ビルド済み CLI・APP4228）**＝0 埋め（取引の無い日が 0 で並ぶ・手書き系列の実測値と一致）・警告抑止・数値順ソート・`EXPLAIN`（`records API: none`）・step 0 エラー。**実機確認中に B150 を発見**＝CTE→APP JOIN 押し下げが DATE キーで kintone 生エラー（`GAIA_IQ03`）。**v3.58.0 の手書き系列でも再現＝既存経路で B149 の回帰ではない**が、日付キー JOIN が「最初に書かれる形」になり露出が増えるため起票。**CLI は純粋系列でも `--app` が必要**（接続先解決の都合）も観測。 | 機能 | [B149](internal/ksql_b149_generate_series_issue.md) / [R2](internal/ksql_b149_generate_series_spec_r2.md) / [レビュー1](internal/ksql_b149_codex_review_1.md) / [実装報告](internal/ksql_b149_codex_impl_report.md) |
| **v3.58.0** | **B147 集計・ウィンドウの別名が入力フィールドを上書きしていた／B140 無視してよい条件を書く（2026-08-07・minor・挙動が変わる）**。**B148 の実測中に codex が「post-group alias 衝突」を候補キーの危険として挙げ、実測したら現に誤った値を返していた**のが起点。**症状は式の種類で変わる**〔`DATE_FORMAT` は**空**／算術は **2584（＝2 × 合計。正しくは 1292）**／連結は **`23429-x`（合計を連結。正しくは `食パン-x`）**／**`CASE` は分類が反転（「小」を「大」と言う）**〕。**`DATE_FORMAT` の「空になる」がいちばん良い方だった**＝空なら気づくが、**もっともらしい数値や反転した分類は気づけない**。**ウィンドウ関数の別名でも起き、集計も `GROUP BY` も無い素の `SELECT` で踏む**（`個数 * 2 AS 倍, ROW_NUMBER() OVER (…) AS 個数` → 倍 = 2, 4, 6）＝**起票時の「集計の別名」という括りより広かった**。**グループが 1 件だと合計とキーが一致するため、小さなデータでは正しく見える**＝**集約が実際に集約したときだけ壊れる**。**原則は [B148 R3 §3.1](internal/ksql_b148_bare_column_group_by_spec_r3.md) と同じ**＝**標準 SQL では `SELECT` 句の別名は同じ句の他の式からは見えない**。**採用＝案 C（`SELECT` 出力と入力フィールドを別の名前空間で保持）**。**案 B（衝突をエラー）を採らなかったのが要点**＝**`SELECT 商品, SUM(数量) AS 数量` は自然で無害**であり、落とすと実害だけが残る。**いちばん難しいのは「`SELECT` 式からは見えなくしつつ `HAVING` / `ORDER BY` からは見えたままにする」**（どちらも既存の公開契約）。**仕様も実装も codex・レビューは Claude**（[分担変更](internal/ksql_b148_bare_column_group_by_issue.md)の 2 件目）＝**仕様は指摘 0 件で通り、実測で範囲が 1 つ広がった**（ウィンドウ別名。**仕様は §8.1 で先回りできていた**）。**Claude の実測が受入の質を決めた**＝**「グループが 1 件だと偶然合う」を渡さなければ、修正前も pass するテストが書かれていた**。**実機で 5 件の修正と 2 件の回帰を確認**（`HAVING 個数 >= 20000 ORDER BY 個数 DESC` が牛乳 29351 / 食パン 23429 で不変）。**あわせて B140**＝依頼元が「**困っていたのは警告が出ることではなく、消し方が分からないこと**」と回答し、**実装（候補キー）は不要**と判断。**代わりに「元の集約のキーをすべて `ORDER BY` に含めているなら、この警告は無視して構いません」を追記**。**成り立つことを両側から実測**〔engine は**表記違い（`'1'` と `'01'`、`'1'` と `'1.0'`）を同順にする**が、**kintone の `NUMBER` は保存時に正規化する**（`01` → `1`、`1e2` → `100`。検証用レコードを入れて読み戻し、削除済み）／**空セルと `0` は同順にならない**（空は最小値。依頼元の実データと mock の両方）〕＝**engine 単体では危ないが、kintone が表記違いを保持できないので実際には起きない**。 | 正しさ | [B147](internal/ksql_b147_aggregate_alias_shadows_key_input_issue.md) / [R1](internal/ksql_b147_aggregate_alias_shadows_key_input_spec.md) / [B140](internal/ksql_b140_cte_groupby_total_order_issue.md) |
| **v3.57.0** | **B148 集計されていない列参照を標準 SQL に合わせてエラーにする（2026-08-07・minor・挙動が変わる）**。**発見の連鎖が 3 段**＝[B140](internal/ksql_b140_cte_groupby_total_order_issue.md) の対応案を codex に出させ→その中の「post-group alias 衝突」を実測したら[B147](internal/ksql_b147_aggregate_alias_shadows_key_input_issue.md)（**現に誤った値を返していた**）→**B147 の影響範囲を測る途中で本件が出た**。**実測（APP4228・1000 レコード）**＝`SELECT 製品名, 個数, 日付, SUM(個数), COUNT(*) FROM APP4228 GROUP BY 製品名` が `食パン / 646 / 2025-08-05 / 23429 / **件数 220**` を返す＝**220 件のうち 1 件の値が「食パンの個数」として出ていた**（素の `SELECT ... LIMIT 3` の 1 行目と完全一致）。**`GROUP BY` 無しがいちばん危ない**＝`SELECT 製品名, 個数, SUM(個数), COUNT(*) FROM APP4228` が **1000 件を 1 行に畳んで返し、見た目が明細行と区別が付かない**。**エラーも警告も文書の規定も無し**。**踏む形が普通に書かれる**（`SELECT 製品名, 個数, SUM(個数) AS 合計` は「製品・数量・合計」と読める。**集計し忘れは最も普通の書き間違いで、標準 SQL も MySQL の既定もこれをエラーにするのはまさにそのため**）。**【最重要】これは「新しい規則を入れた」話ではない**＝**同じ規則は既に `groupingValidation.ts` にあり、拡張 grouping の経路にだけ効いていた**（`GROUP BY ROLLUP(製品名)` は `B65_NON_GROUPED_DEPENDENCY` でエラー）。**[v3.56.1 の B145](internal/ksql_b145_describe_subtable_field_issue.md) と正確に鏡像**（あちらは plain `GROUP BY` がエラーで拡張 grouping が素通り）＝**同じ「経路ごとに検査が揃っていない」形が向きを変えて 2 度目**。**仕様は 3 版かかった**〔**R1（Claude）＝「名前一致で対応づける」で崩れた**（実データで反例 2 件＝物理列が別名より優先されるため `DATE_FORMAT(日付,…) AS 日付 … GROUP BY 日付` は日単位でグループ化され出力に重複が出る／出力名の重複が通り集計値がキーを上書きする）**R2（Claude）＝「identity の差し替えだけで済む」で崩れた**（`validateGroupingPlanning()` は `GROUPING_SETS` でなければ即 `null` を返し、物理前提が構造に染み込んでいた。**共通層／ordinary policy／B65 policy の 3 層分離が必要**）**R3（codex 作成・Claude レビュー）＝指摘 2 件で通過**〕。**役割を入れ替えたのが効いた**＝**codex はコードを広く読んで書く仕様が正確、Claude は実測と「助言が実際に使えるか」の判断が効く**。**実装も codex**（3 層分離・`aggregateDependencyValidation.ts` 新設）。**Claude のレビューで直したもの**＝**エラー文が式を「式」としか呼ばない**（別名があるのに使わず、**算術式は別名・関数式は「式」と割れていた**。列の多い SELECT でどれを直せばよいか分からない）→**呼び名の優先順を「列名→別名→関数名→式」に統一**（素の列で別名より列名を優先するのは、**具体的な書き換え例が「呼び名＝依存名」のときだけ出る**ため）。**codex の自己申告を支持した判断**＝**複合式・`HAVING`・JOIN・CTE では完成 SQL を組み立てず方針だけ示す**（**汎用 AST→SQL serializer が無く、推測で組み立てると `GROUP BY CASE`（parser が受理しない）や alias 衝突を再導入する＝まさに「従うと壊れる助言」の作り方**）→**R3 §8 を実装に合わせて改訂**。**既存テスト 8 本を書き換え**（すべて B148 が禁止する形を成功として固定していたもの。1 本だけ性質が違い、`b67RelativeDateSurfaces` は**B148 と衝突しない題材へ移して元のテストの目的を保った**）。**実機で確認**＝**通り続けるべき 10 形すべて値も不変**（主用途・`CASE` 別名・`YEAR(日付)+1 … GROUP BY YEAR(日付)` の semantic leaf・ウィンドウ専用・scalar subquery grouping ほか）／**エラーになるべき 8 形すべて発火**／`EXPLAIN` でも同じ診断／`CREATE TEMP TABLE` の source も拒否し後続は fail-fast。**依頼元の資産・掲載 SQL に該当なし**（オーナーが把握・照会不要）。**事前照会をしないので、エラー文が保存クエリ・プラグイン利用者にとって唯一の案内**。**リリース後の実測で 2 件が片付き、B148 はクローズした（2026-08-07）**〔**プラグインのブラウザ実測＝全項目 OK**（エラー本文が最後まで読める／レコード API より前に止まる／示された移行 SQL をコピーしてそのまま実行できる／サブテーブルの案内が `APPn$表`／通り続けるもの 7 形も通る）＝**engine・CLI・MCP・プラグインの 4 経路で同じ診断**／**表記ゆれによる偽陽性＝8 形すべて通り 0 件**（`1`／`1.0`・修飾／非修飾の両方向・余分な括弧・`SUBSTR`／`SUBSTRING`・連結・リテラル別名・空白）。**「厳しすぎて既存クエリを落とす」懸念は実測では出なかった**〕。**残件 3 件は保留**＝①拡張 grouping の wildcard 診断が共通層を通らない②`runBatch`／`runQuery` の first error 差（**MCP 経路では再現せず**）③`ksql_validate` の未確定マーカー。**3 件とも値は正しく、診断の質の話**なので、**B148 が止めたかった「静かに間違う」はリリースで解けている**。 | 正しさ | [B148](internal/ksql_b148_bare_column_group_by_issue.md) / [R3](internal/ksql_b148_bare_column_group_by_spec_r3.md) / [実装報告](internal/ksql_b148_codex_impl_report.md) |
| **v3.56.3** | **B101 再開＝常駐 MCP の版を `ksql_docs` で確かめられるようにする（2026-08-06・patch）**。**v3.34.1 の案 A'（`instructions` の 1 行目に版数）が 1 週間で破れた**＝依頼元が `ksql.js --version` を常駐 MCP の版だと思って測り、**CLI は 3.56.2・常駐は 3.56.1** で取り違えた。**§7.4 の「失敗の向きは安全側（誤って新しいと思い込むことはない）」が破れた**＝**別の神託（CLI）に聞くと逆向きに倒れる**。**なぜ案 A' で届かなかったか**＝`instructions` は**押し付け**で、会話の開始時に 1 回配られるだけ。**版を確かめようと思った人はその時点で探しに行く**ので、探しに行ける場所に無ければ手近な `--version` に手が伸びる。**この限界は §4 の案 A' の欄に「接続時に 1 回配られるだけ」と書いてあった**＝**限界として書いてあったものが 1 週間で実害になった**。**§4 で取り下げていた案 A（`ksql_docs` に版数）を採り直した**＝取り下げ理由 4 件のうち **3 件が崩れ**（`_meta` は §2ter で実クライアント 2 つが読めないと判明／標準の議論も同時に弱化／「テスト 5・smoke 4 を揺らす」は**実装して 39 tests 全通・exact 固定は無く `includes` だけ**だったので揺れなかった）、**残る 1 件「版数は文書ではない」は §2 自身が引数なしの `ksql_docs` を「このサーバーは何か」を尋ねる呼び出しと書いており文書内で矛盾していた**。**索引の先頭行**＝`kSQL MCP server version 3.56.3 — the resident process that answered this call. A CLI --version reports a different process and can disagree.`。**版数の出所は `src/mcp/serverVersion.ts` の 1 つ**にまとめた（`instructions` と索引が別々に書くと B141 で 5 回やった「片方だけ古くなる」を招く）。**案 A' は残す**（押し付けと引き出しは別の役割）。**案 B（陳腐化の自己検出）は保留のまま**＝**次に同じ取り違えが起きたら、押し付け・引き出しの両方が効かない証拠になるので案 B へ進む**。**学び＝疑わない人向けの押し付けは、疑った人向けの引き出しを代替しない。** | 改善 | [B101](internal/ksql_b101_mcp_stale_process_version_issue.md) |
| **v3.56.2** | **B145 警告文が症状と合っていなかった／`HAVING` は黙って 0 行（2026-08-06・patch・挙動は不変）**。**依頼元が「`HAVING` がいちばん怖い形」と指摘**＝`GROUP BY` 系は止まり、射影は空の列が見えるが、**`HAVING` は「行が無い」だけで、分析としては正当な「該当なし」と区別が付かない**。**警告を読み飛ばすとそのままレポートに載る**。**裏を取ったら文面と症状が食い違っていた**＝見えるのは **0 行**なのに、警告は「**全行が空になります**」と言っていた。**「行が空」と「行が無い」は別物**なので、**0 行を見た人がこの警告を自分の話だと思えない**。**「従うと壊れる助言」ではないが「読んでも自分の話だと分からない助言」**だった。**原因を書き、代表的な現れ方を並べる形へ**＝「APP100 からは値が取れず、エラーにならないまま常に空になります（列は空、集計は空、`HAVING` などの条件は空との比較になります）」。射影・集計・`HAVING` の 3 形で実測確認。**§19 にも「黙って 0 行」と、0 行が返ったときは警告を必ず確認することを明記**（依頼元の分析＝`GROUP BY` 系は止まる／射影は目視できる／`HAVING` だけ区別が付かない、をそのまま採用）。**挙動は変えていない**（値も警告を出す条件も同じ）。**依頼元の資産に `ROLLUP` × 明細項目は無く、v3.56.1 の破壊的変更に実害が無いことも確定**（`ROLLUP` は 2 箇所ともサブテーブルの無いアプリ）。 | 正しさ | [B145](internal/ksql_b145_describe_subtable_field_issue.md) |
| **v3.56.1** | **B145 追補＝拡張 grouping の静かな誤り・案内文の誤り・句ごとの挙動の文書化（2026-08-06・patch・挙動が変わる）**。**依頼元の訂正から始まった**＝依頼元は「`SELECT <子の列> FROM APP<親>` が落ちる」と報告し、こちらは「落ちない・全行が空」と実測した。**どちらも正しく、分かれ目はクエリの形**だった（素の射影＝空 / `GROUP BY`＝エラー）。**こちらが「実際は落ちない」と一般化したのが誤り**。**そこから 3 件出た**〔①**`GROUP BY` の文面が誤り**＝`unknown field code(s)` だが**その項目は存在する**。「そんな項目は無い」と読まれると**静かに空になるのと同じ誤った結論**（データが無い／項目名が違う）へ誘導する→別表を案内する文面へ②**拡張 grouping（`ROLLUP`/`CUBE`/`GROUPING SETS`/`GROUPING()`）は素通り**＝plain `GROUP BY` はエラーになるのに、**エラーにならず「全レコードが空キーの 1 グループへ畳まれた表」が返っていた**（実測 14 レコード→2 行。**分布を見たつもりが単一バケット**）。原因は grouping resolver の `physicalMatch` が明細項目も「存在する」と扱っていたこと→plain と同じ案内でエラーへ③**句ごとの挙動が 3 通りに分かれる**ことが未文書→§19 に表と原理を追加〕。**原理＝値として出す位置なら「空 + 警告」、キーとして使う位置なら「エラー」**（射影・集計・`ORDER BY`・`HAVING` は値なので空を出せば処理が成立し、エラーにすると既存の SQL が壊れる。`GROUP BY` 系は行の同一性に使うので、常に空だと結果そのものが無意味になる）。**`WHERE` だけは kSQL が判定せず kintone の `GAIA_IQ07`**（依頼元も「これで十分」と回答）。**codex レビューを 2 回**〔**仕様レビュー**＝S1（句別に予測できない）→**揃えず文書化**を選択・S2（射影は警告のままで妥当）→**破壊的なので次の major か strict mode 向きという評価に同意**・S3（`WHERE` を kintone 任せ）→記録のみ・S4（同じ文面を 2 か所にベタ書き＝**B141 の形**）→**共通 formatter へ集約**・**I1（高）＝別アプリ JOIN で `FROM` 側の APP を誤って案内**→**実測で再現**（`APP4229$テーブル` と案内するが APP4229 にサブテーブルは無い。**従うと落ちる＝3 度目**）→`Map<コード, {appId, owner}[]>` にして**候補が 1 つに定まるときだけ名指し**・I2（既定引数）→削除・I3（拡張 grouping に新文面が出ない）→**実測すると「出ない」どころか「エラーにすらならない」**で②の発見につながった。**コードレビュー**＝R1（`_p.` が別サブテーブルの同名項目を拾う）→**再現せず**（`_p.` は grouping resolver では別名として扱われ `physicalMatch` に到達しない。**変更前後で同じ**＝元からの制限）・**R2（中）＝別名で修飾すると古い文面に戻る**→**実測で再現**し両経路を揃えた・R3（重複排除）→問題なし・R4→S4 と同じ・R5（破壊的変更）→**リリースノートに明記**〕。**codex は 2 回とも「動的な主張は実測しないと分からない」傾向**（B144 では高 2 件が再現せず、B145 では高が当たり、今回は I1/R2 が当たり R1 が外れた）。 | 正しさ | [B145](internal/ksql_b145_describe_subtable_field_issue.md) |
| **v3.56.0** | **B145 `DESCRIBE` に `サブテーブル` 列を追加／親から明細項目を選んだら警告（2026-08-06・minor・挙動が変わる）**。**依頼元の返信 §3 を裏取りしたら報告より悪かった**＝依頼元は「`SELECT <子の列> FROM APP<親>` が落ちる」と報告してきたが、**実際は落ちず全行が空になる**（`SELECT $id, 数値_0, 文字列__1行__2 FROM APP4233` は 5 行すべて空。同じ項目を `FROM APP4233$テーブル` から選ぶと 5/3・3/2・2/5 と値がある）。**エラーも警告も出ない**。**逃げ道も無かった**＝`DESCRIBE APP4233$テーブル` は `ParseError` で、**「どの項目がサブテーブルの中か」を問い合わせる手段が無い**。**B130 とまったく同じ形**（型だけでは判別できず、推測がしばしば当たるので外れたときだけ静かに間違う）で、**エージェントが `DESCRIBE` を読んで SQL を書くという主用途に直撃する**。**案 A + 案 B を採用**＝①`DESCRIBE` に `サブテーブル` 列を `タイプ` の直後へ（**その項目がどの表にあるかは型の次に読む情報**）。**`SELECT *` が 7→8 列の破壊的変更**で、移行案内は B130（3→7）と同じ形②親テーブルの `SELECT` が明細項目を参照したら警告（**値は変えない**＝既存の SQL を壊さない。`validateSelectFieldCodes` が `string[]` を返し `mergeSelectWarnings` で合流）。**案 A だけでは「読んだのに気づかなかった」を防げず、案 B だけでは「読んだ時点で分かる」にならない**。**codex レビュー＝高 1・中 2・問題なし 2。実測で 2 件を確かめ 1 件を直した**〔**高 #5 が最も重要**＝旧文面は「`APP4233$テーブル` から選んでください。」で終わっており**`FROM` を差し替えるだけでよいと読める**が、実測すると `SELECT $id, 数値_0 FROM APP4233$テーブル` は**落ちずに `$id` が空になる**——**防ごうとした「静かに空」を別の列で繰り返す**。**B140 で踏んだ「従うと壊れる助言」と同じ形**だった。文面に**親のレコード ID は `_pid`、親項目は `_p.<フィールドコード>`** を足し、**書き換えた形が動くことを実測**（`SELECT _pid, _p.数値, 数値_0 FROM APP4233$テーブル WHERE _pid = 1` → `_pid=1 / _p.数値=454002 / 数値_0=5`）。**codex は「壊れる」と言い実際は「静かに空になる」だった＝壊れ方の予測は外したが、指摘そのものは正しく最も価値があった**。中 #1 は**半分だけ正しい**＝`ORDER BY` は警告が出る（実測）、`WHERE` だけは漏れるが **kintone の `GAIA_IQ07` で止まるため静かには間違わない**。中 #4（BYO クライアントが `inSubtable` を省くと再発）は**環境が無く未確認**で契約の明記を残件に。問題なし 2 件＝JOIN で同じ appId が親と子に現れても誤警告なし／`DESCRIBE_COLUMNS` の 7 列を仮定する経路は無い〕。**既存テスト 5 件を更新**（7 列を固定していた B130・B137）。 | 正しさ | [B145](internal/ksql_b145_describe_subtable_field_issue.md) |
| **v3.55.0** | **B144 サブテーブルの `SELECT` で親項目の条件を押し下げる（2026-08-06・minor）**。**利用者の指摘（`SELECT COUNT(*) FROM APPn$テーブル` が全件取得になるのに記載が無い）から調べて、文書の穴の裏に実装の食い違いが出た**。**`EXPLAIN` は `fetch: EXACT` / `kintone query: $id in (1,2)` と表示しながら、実行は親を全件取得していた**〔実測・APP4233（親 14 / 明細 24）で `_p.$id IN (1,2)` を `maxRecords=3` で実行すると `FetchAllLimitError`。親アプリ側の同じ条件は成功する〕。**`EXPLAIN` の存在意義が「実行前に取得量を知る」ことなので、そこが外れると使えない**うえ、**文書は「分類に迷いがある場合はより多く取得する側を表示する」と宣言しており本件はその逆**（実際より少なく表示）。**原因＝`const parentQuery = isMainTable ? "" : "";` で三項演算子の両辺が空文字**。親テーブル経路は query を組んでいるのにサブテーブル経路だけ未実装で、**`git log -L` で追うと初版から同じ形**（壊れたのではなく最初から placeholder）。**計画を作る側と実行する側が別実装で、後者だけ取り残されていた**。**案 A（実行側を計画に合わせる）を採用**＝`resolvedWhereCapabilities` が `EXACT_PUSHDOWN` のときだけ `whereToKintone(stmt.where)` を親クエリへ渡す。**exact なら述語は親項目だけで構成されており親を絞っても行は落ちない**（明細項目・`_pid`/`_rid`/`_idx`・混在はいずれも EXACT にならない）。**結果は不変で、変わるのは取得量と `maxRecords` の当たり方**。**codex レビュー＝高 2 件（同一原因）は再現せず、問題なし 3 件**〔指摘は「`_p.` 解決が `inSubtable` を除外しておらず `WHERE _p.<明細項目> != 5` で行が落ちる」だったが、**実機 3 形（`!=` / `=` / テキスト項目）すべて `WHERE_RESIDUAL` で全件取得**。ただし**安全性が「解決表に明細項目が無い」ことへの暗黙依存**である点は正しいので**回帰テストで固定**した。問題なしの 3 件＝`resolvedWhereCapabilities` は必ず設定済みではないが CTE 経路では空文字フォールバックが安全／`isMainTable` と `allowOriginalWherePushdown` のガードは妥当／`pushDownCond` と `additionalPushQuery` はサブテーブルで非空になる経路が無い——**いずれも根拠行つきで、こちらが確認しきれていなかった点を潰せた**〕。**codex はコードだけを読んで推論し実行はしていない**（高重大度が再現しない一方、静的にしか分からない 3 点で価値が出た）。**文書＝サブテーブルの `COUNT(*)` は全件取得**（`totalCount` は親レコードの件数で、**サブテーブル行を数える手段が API に無い**。行はレコードを取得してから展開するため件数を知るだけでも親の取得が要る）を §19 冒頭と `fetch:` の `COUNT_ONLY` の行に明記。**押し下げが効く場合に取得量が減るだけで、この事実自体は変わらない**。**B141 の散文の穴も 1 件修正**（スカラーサブクエリの説明 3 箇所が `MIN`/`MAX` の変更に追随していなかった。表ではないため機械照合の対象外）。 | 正しさ・性能 | [B144](internal/ksql_b144_subtable_pushdown_issue.md) |
| **v3.54.4** | **v3.54.3 のテスト失敗を修正／B141 同じ値を書く場所を 5→2 へ（2026-08-06・patch）**。**リリース手順に穴があった**＝v3.25.0 の移行注記の削除は**リリース直前の指示**で、その後 `docs:check` とパリティテスト 2 本だけを回して PR・タグ・Release を実行した。**タグ v3.54.3 では `npm test` が失敗する**〔①§6 から `server prefilter` の記述が消えた（削除したブロックにあったため）→**現行仕様として書き直し**②テストが**v3.25.0 の告知そのもの**（`minor` / `破壊的` の文言）を固定していた→**削除の判断に合わせて外した**。拒否される代表例（`作成者 = 'taro'` 等）は現行仕様として固定を継続〕。**配布物の文書内容は破綻していないが、リリースの前提を満たしていなかった。****B141 は依頼元の提案で方向が変わった**＝「**照合の網を強くするより、書ける場所を減らすほうが効く**」。**構造から導出する照合は「N 個の複製をすべて見る」ための道具で、N が増え続ける前提の対策でもある**。**数えたら N = 5**（うち機械照合は 2）だったので **2 まで減らし、残る 2 を両方とも照合下に入れた**〔§8「0 件時の挙動」＝**正本**（標準 SQL 列と刻印を統合）／§8 のもう 1 つの表＝**参照へ置換**／§8 集計関数の表の「値が無ければ空文字」＝**削除**／§6 スカラーサブクエリの散文＝**値を持たない参照へ**（6 件目として直したもの）／§5 除数ガードの表＝**値は残す**（ガードの選び方を示す表のため）が**列見出しを `値が無いとき` へ揃えて機械照合の対象に入れた**〕。**決定的な確認＝v3.54.1 で古くなった §5 の表そのものを壊して、いま落ちることを実測**。**パリティテストから「表は 2 つ以上」という個数の固定も外した**（数を書くと N を減らしたときに落ちる＝v3.54.1 の照合が構造ではなく現状を写していたのと同じ失敗になる）。**依頼元の分析ラン 2 本から 2 点**＝①**`EXPLAIN` は `warnings` を返さない**（計画の行には出るが `warnings` は常に空。**素の状態で回した別セッションが「詰まった点」の 1 番目に挙げた**）→文書化し **B143** として起票②**`LAG` の端の空文字は前月比以外でも踏む**＝**踏みやすいのは「率」ではなく「〜あたり」の指標**（在庫日数・単価・1 件あたり・回転率。**分母が「動きの無い対象」で空になる**）→§5 と R16 に**症状の側から引ける言葉**として明示。**「正しく書いてあるが、探す語が違う」形は機械照合では見つからず、別の入口から来た人が踏んで初めて分かる。****エンジンの変更なし。** | 正しさ | [B141](internal/ksql_b141_doc_sql_unverified_issue.md) / [B143](internal/ksql_b143_explain_warnings_issue.md) |
| **v3.54.3** | **B141 5 回目＝機械照合が「もう 1 つの表」を素通りさせた（2026-08-06・patch）**。**依頼元が `ksql_docs` 経由で読んで指摘**。§8 には**空集合の戻り値を書いた表が 2 つ**あり、v3.54.1 で入れたパリティテストは**マーカー 1 つ分の表しか見ていなかった**ため、「### 0 件時の挙動」の表が `SUM`/`AVG`/`MAX`/`MIN` → `0` のまま残っていた。**5 回目の取り残しで、しかも同じ節の中**。**機械化そのものが、防ごうとした穴と同じ形をしていた**＝「1 か所を直して、同じことを書いた別の場所が残る」。**直した形＝「値が無いとき」/「0 件時の値」という列見出しを持つ表を文書全体から自動で見つけて全部照合する**（表を新しく足しても、その列名を使う限り自動で検査対象になる）。関数名はバッククォート断片の先頭識別子から拾うので `` `COUNT(*)` `` も `COUNT` として拾える。**強化したテストが直す前の文書で落ちることを確認してから直した**。**学び＝「機械照合を入れた」は「その照合が網羅している」を意味しない。網の張り方が対象を列挙する形（マーカー・許可リスト）だと、同じ取り残しがメタレベルで再発する。網は構造から導出する**。**あわせて v3.25.0 の移行注記を削除**（29 版前の告知でリファレンスに置く段階を過ぎている。**§6 の注記に混じっていた現行仕様＝ユーザー系・複数選択系は `in`/`not in`・型不一致は `WHERE_OPERATOR_INVALID_FOR_FIELD_TYPE` で取得前に拒否・`TODAY()`/`NOW()`/`LOGINUSER()` は押し下げられる形だけ——は版数の枠を外して現在の規則として残した**）。**エンジンの変更なし。** | 正しさ | [B141](internal/ksql_b141_doc_sql_unverified_issue.md) |
| **v3.54.2** | **B141 `fetch` は形で変わることを追記／挙動を写した表に実測した版を刻む（2026-08-06・patch）**。**どちらも依頼元の返信から**。**①依頼元が当方の書いた鉄則 9 の表の不整合を見つけた**＝`fetch` の値を 4 行並べた表で、**最終行だけ `SELECT COUNT(*)` で測っていた**〔`担当者 IN ('rex0220')` が `COUNT_ONLY`・他 3 行は `GROUP BY` クエリ〕。**`SELECT COUNT(*)` は押し下げが効いていても `COUNT_ONLY` になる**（走査しないため）ので、**同じ列に並ぶと「`IN` は `=` と別物の何か」に見える**。**4 行とも同じ `GROUP BY` の形で測り直して確認**＝`入出庫区分 = '出庫'`→PREFILTERED／`= '廃棄'`→ALL／`担当者 = 'rex0220'`→ALL／`担当者 IN ('rex0220')`→**PREFILTERED**（旧記載 COUNT_ONLY）。押し下げの有無という論点では 4 行とも正しく結論も変わらないが、**表としては誤読を招く**。**分類表そのものは定義表なので混在の問題は無い**が、**「同じ `WHERE` でも形で変わる」がどこにも書かれていなかった**ので実測例とともに追記した。**②依頼元の提案＝挙動を写した表に「実測した版」を刻む**。**依頼元は自分の文書で既にこれをやっており、今回そのまま効いた**＝v3.54.0 の連絡を受けたとき依頼元の表には `v3.53.0` と刻まれており、**「この表は v3.53.0 時点の観測＝挙動が変わった今は再確認が要る」と機械的に判定できた**。**下流複製に効く**＝複製先が自分で staleness を検出でき（`grep` で洗える）、**こちらが複製先を把握する必要がない**（連絡の網羅性に依存しない）。**費用はほぼゼロ**。**v3.54.1 の 4 回目も、刻印があれば拾えた**（除数ガードの表に `v3.53.0` と刻まれていれば刻印と実装の差で気づけた）。**パリティテストのマーカーは刻印を含めない形へ直した**（刻印を更新しただけで落ちないように。実際に一度落ちて気づいた）。**README の版数を一括置換したとき、過去の見出し 3 か所まで書き換えてしまった**のを検算で発見し戻した——**B141 と同じ「一括操作が履歴を壊す」形**。**エンジンの変更なし。** | 正しさ | [B141](internal/ksql_b141_doc_sql_unverified_issue.md) |
| **v3.54.1** | **B141 除数ガードの表が同じ版の挙動変更に追随していなかった／再発防止を機械化（2026-08-06・patch）**。**案 E（新規・変更分だけ実行必須）を決めた直後に 4 回目が起きた**＝v3.54.0 で `MIN` / `MAX` の空集合を `0` から空文字へ変えたとき、**同じ言語リファレンスの中に古い値を書いた表が残った**〔除数ガードの表＝`MIN`/`MAX` を「集計を通した値」に含め `0` としていた／集計関数の節＝空文字。**同一文書内で矛盾**〕。**しかもその表は 1 つ前のリリースで自分が書いたもの**で、**依頼元が読んで指摘してくれるまで気づいていない**。**案 E では止まらなかった**＝案 E は「追加・変更した例を実行する」だが、**壊れたのは変更していない別の節**で実行対象に入らない。**学び＝「実行してから載せる」は書いた瞬間の正しさしか守らない。挙動を変えたときに古くなる文書は実行では見つからない。機械照合が要るのは例の SQL ではなく、挙動を写した表のほうだった**。**そこで機械化**＝空集合の戻り値の表を解析し、各関数を空入力で実行して照合するテストを追加（対応する集計関数がすべて表に載っていることも確認するので、関数を足したら落ちる）。**表を機械が読める形へ揃えた**＝「統計集約」を `VAR_POP`/`VAR_SAMP`/`STDDEV_POP`/`STDDEV_SAMP` へ展開し、**書かれていなかった `GROUP_CONCAT` を追加**（文書としても正確になった）。**表を一時的に `0` へ改変してテストが落ちることを確認済み**。**残る穴＝機械照合できるのは「値の表」だけで、散文で書かれた挙動の説明と下流文書への複製は依然として守れない**。**エンジンの変更なし。** | 正しさ | [B141](internal/ksql_b141_doc_sql_unverified_issue.md) |
| **v3.54.0** | **B142 値が無いときの `MIN` / `MAX` が `0` から空文字へ／B141 除数のガードが要る形を絞り込む（2026-08-06・minor・挙動が変わる）**。**依頼元の返信を裏取りしていて出た**＝「集計を通した値は空にならない」という依頼元の線引きを `SUM` 以外でも確かめる過程で、**`MIN`/`MAX` が 0 行のとき長さ 1 の文字列 `"0"` を返す**ことに当たった。**当初の見立ては 2 回外した**＝①「テキスト列だけ既定値が漏れている」→**実測で日付・時刻・選択肢・レコード番号・計算フィールドすべて `"0"`**（型は無関係）②「経路で割れている」→**割れていたのは値の個数**。空セルは `MIN`/`MAX` では**値として保持される**（`5e14418` の canonical empty band。`ORDER BY` で空が先頭に来ることと一致し、`MIN` の `""` はその 1 行目そのもの）ため、**値が 1 個なら `""`・0 個なら `"0"`** になっていた。**同じ規則が正しく働いた結果で、副作用ではなかった**。**それでも `0` を直した理由**＝`MODE`/`MEDIAN`/統計集約は**以前から空集合で空文字**を返しており、**比較系で `0` を返すのは `MIN`/`MAX` だけ**だった。実装のコメントも「全値空グループと同じ 0」と根拠を書いていたが、**全値空グループの `MIN`/`MAX` は実測でもテストでも `""`**（`SUM`/`AVG` については正しい説明）。標準 SQL も 0 行の `MIN`/`MAX` は型に依らず `NULL`。**`SUM`/`AVG`/`COUNT` は変えていない**ので、B141 で出したばかりの「集計を通した値は該当が無いと `0`」という案内は維持できる。**費用の見積りを外した**＝「固定する既存テストは無い」と書いたが**2 本あった**（`空入力` という語と `mn`/`mx` という別名で grep が拾えていない。**「無い」の確認は grep ではなく実行で取るべきだった**）。**2 本目は DML の挙動変更**＝`UPDATE ... SET x = (SELECT MAX(...))` が 0 件のとき**書き込む値が `0` から空になる**。該当が無いのに「最大値は 0」と書くのは事実に反するための変更だが、**対象が必須フィールドならこれまで通っていた `UPDATE` が失敗する**（移行案内を CHANGELOG と README に記載）。既に `0` が書き込まれたレコードは遡らない。**B141 は依頼元の指摘で見立てを変えた**＝案 B（すべての例を実行可能に書き換える）を降ろし**案 E（新規・変更分だけ実行必須）**へ。`APP100` のような架空アプリは**読みやすさのために選ばれており、実在アプリへ置き換えると読み手の環境で再現しない例になる**（費用が大きいだけでなく文書として悪くなる）。**射程が下流へ伸びることも判明**＝依頼元の `06-ksql-tips.md` に同じ ○ が写っていた（踏んではいないが、**踏まなかったのは運**）。**実機で 5 形を確認**（0 行スカラー集計の全型／全行非一致の `CASE`／`LEFT JOIN` 不一致が従来どおり空／値があるときの回帰／空判定が効くこと）。 | 正しさ | [B142](internal/ksql_b142_empty_group_text_aggregate_issue.md) / [B141](internal/ksql_b141_doc_sql_unverified_issue.md) |
| **v3.53.0** | **B140-C2 「従っても消せない」警告に限界を併記／B141 文書の SQL の取り残しを 2 件修正（2026-08-06・minor）**。**v3.52.0 の案 C は不十分だった**＝「壊れる助言」は直したが「**満たせない助言**」が残っていた。CTE 経路の新しい案内「その表の中で一意になる列（元の集約のキーなど）を `ORDER BY` に含めてください」は、**月次集約 → `LAG` という依頼元の主用途では従いようがない**〔`年月` は既に `ORDER BY` にあり、それが `GROUP BY` キーそのもの。CTE の中にそれ以上「一意だと証明できる列」は存在しない〕。**指示のままでは「やれと言われたことが既にやってある」状態**になるため、限界を併記して**指示から説明に変えた**（「集約結果の列は一意でも証明できないため、すでに一意な場合もこの警告が出ます」）。**案 A が入るまで警告は消えないという事実を、警告自身が伝えるようにした。****B141 を起票**＝**説明文を直しても例が残る形が 3 回目**（B136 の `DESCRIBE` 列表・R16 の注記・本件）。**本件が最も悪い形**＝言語リファレンスの `NULLIF` の注記は「空文字が NULL 相当のため `Number('') = 0` となり結局ゼロ除算で `NaN` になる」と**原因を正しく説明したうえで、その 3 行下の ○ が、まさにその理由で `NaN` を出していた**（`CASE WHEN 金額 = 0 THEN '' ELSE 1000 / 金額 END`＝**空文字は算術では 0 になるが比較では 0 と等しくない**ため空セルの行が `ELSE` へ落ちる）。**実測で確定**＝`LEFT JOIN` 不一致の空セルで旧ガードは `NaN`・新ガード（`= '' OR = 0`）は空、本物の 0（`仕入価格 - 仕入価格`）は新旧とも捕まえ、正常値は不変。**発見の経緯＝依頼元が R16 の `LAG` 先頭行で同じ罠を報告し、「他に同じ形が無いか」を探して当たった**（依頼元は R16 しか見ていない）。**R17 が空の数値セルを作るレシピなので、今回足した R17 の読者がまっすぐこの ○ へ来る**構造だった。**R16 の注記も 2 点直した**＝CTE の話なのに `レコード番号` を名指ししていた／「年月は 1 行に定まります」と書きながら実行すると警告が出て**文書と実行が矛盾して見えた**。**機械で防げない理由**＝B136 は列名という照合可能な対象だったので同期テストで塞げたが、**例の SQL が期待どおりの値を返すかは実行しないと分からない**。`docs:check` は SQL を素通しする。**案 B（実行可能な例を固定スタブへ）が本命だが、例の多くが `APP100` のような架空アプリで、実行可能にする書き換えが費用の大半**。**エンジンの変更は警告文のみ。警告を出す条件と値は不変。** | 正しさ | [B141](internal/ksql_b141_doc_sql_unverified_issue.md) / [B140](internal/ksql_b140_cte_groupby_total_order_issue.md) |
| **v3.52.0** | **B140-C 実行できないタイブレーク助言を出さない／レシピ R17「行の無いもの」を 0 として並べる（2026-08-06・minor）**。**B140 は「放置した場合の問題」を実測したことで判断が変わった例**＝当初は「警告疲れ」（正しさに影響しない・急がない）と見ていたが、**警告が指示する対処が CTE 経路では実行できない**ことが分かった〔「ORDER BY にレコード番号などのタイブレークキーを足してください」に従うと `unknown field code(s): レコード番号 (月次)` で落ちる。CTE には `レコード番号` が無い〕。**「読み飛ばされる」より悪く、従うと壊れる助言**だったため、見送り（案 D）を選べなくなった。**経路に応じて助言を変える案 C を先に出した**。**信号は `stmt.from.cteName` ではなく `context`**＝実体化の過程で `cteName` がクリアされることが実測で判明（最初 `cteName` で実装して効かず気づいた）。JOIN は `DIRECT` のままなので従来の案内が残る。**B127（既定 RANGE）と B128（VALUE）の両方に効く**（警告生成が共通）。**実装中に VALUE 用と RANGE 用で元の助言文が違っていたのを一度統合してしまい、既存テストが落ちて気づいた**——直接経路では両方の文言をそのまま維持している。**案 A（`GROUP BY` キーを全順序の根拠にする）はレビューで中核が崩れて別途**〔①**出力列名の一致はグループキーとの同一性を証明しない**＝現行は同名の物理列があれば alias より先に物理列で解決するため `SELECT UPPER(k) AS k ... GROUP BY k` では出力が非単射になり**偽陰性**②CTE キャッシュに定義 AST が残らない③**複合 `GROUP BY` は列単位フラグで表せず関係レベルの候補キーと伝播規則が要る**〕。**R17 は B134 の当面の手当て**＝**`GROUP BY` はデータのある行しか作らない**ので 0 件のものは行ごと消え、**エラーも警告も出ないため「0 だった」と「そもそも無かった」の区別が付かない**。マスタ起点の `LEFT JOIN` で 0 を出す形を示し、**条件を `WHERE` に書くと `LEFT JOIN` の後に効いて元の問題へ戻る**ことと、**日付には起点になるマスタが無いので同じ手が使えない**ことを明記した（掲載 SQL は 6 行 vs 8 行の両方を実機で確認）。**B134 では判断を 2 つ改めた**＝①**「B53 との関係整理が先」を撤回**（B53 は 18〜29 人日で実需待ちの棚上げ中。日付系列のために払う額ではなく、待つことは実質「何もしない」）②**「無から行を作るのは kSQL の性格が変わる」という案 A への反対理由も弱い**（`FROM` なしの `SELECT ... UNION ALL` が現に動き、レシピ R7 が同じ形を使っている）。**案 C（カレンダーアプリ）は推奨として出さない**＝用意した範囲を外れると同じ静かな失敗が再発する。 | 正しさ | [B140 spec R1](internal/ksql_b140_cte_groupby_total_order_spec.md) / [review](internal/ksql_b140_codex_review_1.md) / [B134](internal/ksql_b134_series_generation_issue.md) |
| **v3.51.0** | **B128 Phase 2a `LAG` / `LEAD`／B138 文書の構造検査（2026-08-06・minor）**。**依頼元が実測でスコープを組み替えさせた**＝当初の依頼③は「移動平均が先」だったが、**日次集約＋`ROWS` では 7 日移動平均にならない**ことを実データで示した〔取引のあった日が稼働期間の半分以下で、**7 行が相当する暦日が製品ごとに 15〜29 日と 2 倍の開き**。同じ `PARTITION BY` の 1 本で窓幅が製品ごとに変わり、製品を横に並べる表に使えない〕。**依頼元の言葉＝「依頼③で移動平均を先に挙げたのは当方の見立ての粗さで、実際に効くのは前期比の方でした」**。**`LAG` 先行の根拠は実測で裏取り済み**＝履歴 13 か月すべてに行があり歯抜けが無いので、月次なら [B134](internal/ksql_b134_series_generation_issue.md) の 0 埋め問題を踏まない。**2 回のレビュー（旧 16 件・新 9 件）で潰した罠**＝①**`isRankingWindow` は「AGGREGATE でなければ RANKING」の二者択一**で `VALUE` を足すと順位系と誤判定される→positive discriminator＋3 分岐＋未知 kind は fail-closed②**引数フィールドが required-field 収集から漏れる**（walker が集計系だけ `arg` を走査）＝**B125 の集計引数漏れと同じ静かな誤り**③**型メタが一律 number になる**（「AGGREGATE でなければ number」の二者択一が複数）→6 箇所を同期。**とくに source metadata のロードゲートは集計窓の MIN/MAX だけが対象**で、直さないと direct APP の `LAG(選択肢列)` が崩れる④**完全入力の理由は `WINDOW_ORDER`**（`AGGREGATE_WINDOW` ではない）⑤**B129 の nested 検出は `LAG`/`LEAD` を自動では拾わない**（scanner が aggregate token map だけを見ており `ROUND(LAG(x) OVER (...), 1)` が診断へ行かなかった）⑥**第 3 引数 `default` は取らない**（通常行が数値・先頭行が文字列で列全体の型が一貫しない）。**実機（APP4228/4229）で受入を全件確認**＝3 段構成の前月比 13 行（2025-09 = 52.0 / 2026-08 = −87.8 を独立に検算）／引数にしか現れないフィールドで値が空にならない／direct APP の `LAG(選択肢列)` が次段で定義順／`LAG(x,0)` は自分自身・`LAG(x,999)` は空文字／B129 の 3 形すべてで診断本文／`complete input reason: WINDOW_ORDER`。**副産物として [B140](internal/ksql_b140_cte_groupby_total_order_issue.md) を起票**＝**主用途（月次集約 CTE → `LAG`）で毎回 全順序警告が出るが `年月` は `GROUP BY` キーなので構造上一意＝偽陽性**（判定器自体は正しく、direct APP＋レコード番号では実測で抑止された）。**B138** は文書の構造検査を `npm test` へ入れたもので、**リンク切れ 184 件を 0 にした**（177 件は深さ違いで機械的・6 件は名前の取り違え・1 件は `[object Object](R34)` という地の文がリンク解釈されていた）。**検査側の偽陽性も塞いだ**＝コードフェンス内の正規表現をリンクと誤検出していた。**同日に台帳を 2 回壊し、2 件目はこの検査を書いて初めて見つかった**。 | 機能・正しさ | [B128 spec R2](internal/ksql_b128_lag_lead_spec.md) / [review2](internal/ksql_b128_codex_review_2.md) / [impl](internal/ksql_b128_codex_impl_report.md) |
| **v3.50.0** | **B137 列数の違う `UNION` をエラーに／B136 の再発防止／B139 CHANGELOG のリリースゲート（2026-08-06・minor）**。**3 件とも「文書・生成物・期待値の二重管理をゲートで止める」系列**（B132 / B135 / B138 と同じ）。**B137 の起点は B130 の実装時にこちらが受入条件の書き方を誤ったこと**＝「既存挙動が変わらないことを確認する」つもりで「UNION の列数不一致が検出される」と書き、**codex がそのとおりエンジンへ列数検査を追加した**。B130 の範囲外で既存クエリを壊すため**差し戻して起票**した。**方向は案 A（エラー）で確定**〔根拠 3 つ＝①**主要 4 実装がすべてエラー**（MySQL 1222 / PostgreSQL / SQLite / SQL Server。標準 SQL が union-compatible を要求）②**オーナー判断「まだユーザーがいないので問題ない」**③**内部依存も無い（測定済み）**＝差し戻し前の実測で 225 suites 全通〕。**【実装時に判明した最重要の事実】言語リファレンスは以前から「両辺の列数が一致しない場合はエラー」と書いていた**＝**文書が正しく、実装がその契約を守っていなかった**（B136 と同じ乖離で**向きが逆**）。したがって挙動の追加ではなく**文書どおりに直したもの**。**R1 の誤り 3 件**〔①**「静的検出は不可能」は偽**（**オーナー指摘**＝`DESCRIBE` の列数は版内で固定、`SELECT *` はアプリ定義から分かる／レビュー H2＝両枝が wildcard を含まなければ AST で判定できる）→ **「できない」ではなく「しない」設計判断**へ書き換え（形ごとにエラーの時機と優先順位が分岐し observable contract が割れるため。静的 fast-fail は別課題）②**「C まで到達しない」は誤り**（レビュー H1＝`Promise.all` で左の入れ子全体と右を同時に開始するので**全枝が実行される**）③**「padding 依存の既存テストは無い」は現時点では偽**（レビュー M4＝**B130 でこちらが現状挙動を固定するテストを書いた**）〕。**`EXPLAIN` は従来どおり計画を返す**（行を取らず列が実体化されないため）。**実機（APP4229）で 8 項目を確認**＝両方向のエラー／**列数が合えば従来どおり 16 行**／**`EXPLAIN` は計画を返す**／3 段連鎖の内側／CTE 内／`DESCRIBE` 7 列 vs 3 列／`FROM` なしの同列数。**テストは「不一致でも全枝が実行される」ことを `startedApps` で固定**。**B136 の再発防止**＝列名を `SHOW_APPS_COLUMNS` / `DESCRIBE_COLUMNS` へ定数化し、**言語リファレンス §14 の列表と一致することをテストで固定**（**飾りのテストでないことを確認済み**＝文書側を 1 つ変えると落ち、戻すと通る）。**B139**＝`version:check:release` で**先頭の版数見出しが `package.json` と一致すること**を要求。**v3.49.0 と v3.50.0 の実装でリリース済みの節へ追記する取り違えを 2 回続けて踏んだ**ため（原因は `version-sync-guard` の「CHANGELOG は対象外」という明示的な除外。開発中は正しいのでリリースモードにだけ入れた）。 | 正しさ | [B137 spec R2](internal/ksql_b137_union_column_count_spec.md) / [review](internal/ksql_b137_codex_review_1.md) / [impl](internal/ksql_b137_codex_impl_report.md) |
| **v3.49.0** | **B130 `DESCRIBE` に「値の由来」4 列／B129 ウィンドウ診断の改善とレシピ R15／B136 の一部 文書の例の訂正（2026-08-06・minor）**。**B130 は依頼元の実需が実装前に消えた珍しい例**＝依頼⑤は「軽くて手が伸びるから構造的に塞ぎたい」が動機だったが、こちらの確認に対し依頼元が**自ら「優先度を下げてください。当方の実需はほぼ消えています」と回答**（`sql/`・`scripts/` に `DESCRIBE` 0 件、カタログ作成以降 `ksql_describe_app` を呼んだ記録も無い。`/catalog` が明示的に禁止）。**依頼文の根拠「1 ビットあれば metadata の要否を判断できる」も「当方の誤り」と認められた**〔依頼文が例に挙げた `仕入先` は**ルックアップのコピー先**で 3 フラグがすべて空になり、素の文字列と区別が付かない〕。**残った根拠は一般利用者の面**＝「当方の鉄則は当方のリポジトリでしか効かない。`CLAUDE.md` を持たない環境のエージェントは MCP のツール説明だけを見て `ksql_describe_app` を選ぶ」で、**重み付けはこちらへ委ねられた**。**依頼元の提案で枠が「値の由来」に変わり 4 列になった**〔`書込不可` では読み取り専用プロジェクトに意味が無いが「値がどこから来たか」は読み方を変える。**決定打＝`GROUP BY 仕入先` はマスタの現在値ではなく入力時点の値で割れ、エラーも警告も出ない**〕。**停止規則も数から意味へ**＝入る＝値の**読み方**を変えるもの／入らない＝値を**書く**ための制約。**codex レビューで R1 の中核が 3 つ覆った**〔①`lookup` は権限不足時に `null` を返すので**値の truthiness で判定すると実在するルックアップを取りこぼす**②`KintoneFieldInfo` も `ReadonlyFieldInfo` も公開型で必須追加は npm の BYO client を壊す。**projector が最後に cast するため型不一致が compile error にならず runtime で `undefined` が入る**③「`SELECT *` の列増は破壊的ではない」は誤りで、UNION の位置対応と `INSERT ... SELECT` の列数一致という**列数依存経路が `validCodes` 以外にある**〕。**実装時に受入条件の誤りが 1 件露出**＝§4.2-9 に「UNION の列数不一致が検出される」と書いたのは「既存挙動が変わらないことを確認する」の意味だったが、**codex はそのとおりエンジンへ列数検査を追加した**。B130 の範囲外で既存クエリを壊すため**差し戻し、[B137](internal/ksql_b137_union_column_count_issue.md) として起票**（現象自体は本物の欠陥）。**B129 は依頼元の文案をほぼそのまま採用**（一般形＋○×の例。位置・トークンは残す）し、**レシピ R15 は依頼元の R18 を土台にした**〔「診断文を読むのは止まった人だけだが、レシピは書く前に読まれる」〕。**掲載 SQL は 3 本とも実機で実行して確認**（牛乳 34.7%／累積 34.7→…→100／ABC は A A A B B C C C）。**B136 は B130 の調査中に発覚**＝言語リファレンスの `SHOW APPS` / `DESCRIBE` の例が実装と違う列名で**そのまま実行するとエラー**になっていた（この文書は MCP リソースの原本＝**エージェントが壊れた例を写す**）。**依頼元の「エージェントは文章より例を写す」という指摘がそのまま跳ね返った形**。例の訂正は本版で実施し、**再発防止（言語リファレンスの SQL 例を検証する仕組みが無い）は B136 に残る**。 | 機能・正しさ | [B130 spec R3](internal/ksql_b130_describe_flags_spec.md) / [review](internal/ksql_b130_codex_review_1.md) / [impl](internal/ksql_b130_codex_impl_report.md) |
| **v3.48.0** | **B133 保存クエリの複文対応＋実行時の変数注入／B135 `mcp:smoke` の期待値ずれ（2026-08-05・minor）**。**起点はオーナーの一言「保存クエリは単文制約とあるが、制約を解除してはどうか？」**（B129 の検討中、CTE の代わりに一時テーブルが使えることを実測で示した流れで出た）。**制約は安全のためではなかった**＝`requireSingleStatement` のコメントに**「対応時にこのガードを外す」**と明記され、安全側は `assertSavedQuerySafety` と `readOnly` による `query()`/`mutate()` 振り分けが担っていた。**R1 の最大の誤りはレビューで覆った**＝R1 は受入に「実行時に `@d90` を上書き」と書きながら**それを実現する変更を書いていなかった**。`variables`（`DECLARE` への外部注入）は `ksql_query`/`ksql_mutate` にはあるが **`ksql_run_saved_query` には無く**、しかも**注入対象は `DECLARE` 専用で依頼元の実需の形は `SET`** だった。R1 のままでは「3 文を保存できる」だけで**基準日はハードコードされたまま**になる。**したがって `variables` の追加が本体**で、複文許可と一体で出した。**【B4 のクローズ判断の前提が一部崩れていた】** B4（保存クエリのパラメータ化）は「中核価値の外部注入は `@var` が既に提供済み」としてクローズしたが、**その `@var` は保存クエリ経路には無かった**。本件は B4 の再実装ではなく**既存機構をもう 1 ツールへ配線するだけ**。**そのほかレビューで覆った点**＝許可条件は `containsDml` ではなく **`canRunWithQueryTool`（=`isReadOnlyBatch`）**（`containsDml` は「DML 構文か」ではなく `writesKintone()` で `VALIDATE ONLY` 付き DML は false）／返却は「最後の結果セット」ではなく**バッチエンベロープ**／**`ksql_list_queries` は SQL を返さない**（R1 の記述が誤り）／**カタログは手編集できるので防御は save と run の両方**。**単文の挙動が完全に保たれる根拠**＝`isReadOnlyType` が DML 以外の全文型を含むため、単文では `canRunWithQueryTool === !isDml` が恒等的に成り立つ（`dmlGuard.ts:36-61`）。**実機検算は常駐 MCP を使わずローカルビルドを stdio 起動して行った**（常駐は旧版のまま沈黙して旧挙動を返すため）＝`variables` 無し 1147 → `d90='2026-07-20'` で **99**（トマト缶）へ変わり、**日付リテラル直書きの `ksql_query` と完全一致**（99/103/549/50/188/282）＝「変わった」だけでなく**正しく変わった**ことまで確認。**B135 は B133 の実装中に発覚した副産物**＝v3.45.0 でレシピ R14 を足した際に `mcp-smoke.mjs` の期待値が `r1..r13` のまま残り、**`mcp:smoke` が 3 版にわたり失敗していた**。**`mcp:verify` にしか入っておらず `npm test` にも `prepack` にも無い**ため素通りした（出荷物には影響なし）。**再発防止は未着手**＝生成側と smoke 側の二重管理が残っている。 | 機能 | [B133 spec R2](internal/ksql_b133_saved_query_batch_spec.md) / [review](internal/ksql_b133_codex_review_1.md) / [impl](internal/ksql_b133_codex_impl_report.md) |
| **v3.47.0** | **B126 選択系の `=` / `!=` を押し下げ正規化／B127 ウィンドウ既定フレームの警告／B132 docs キーの番号（2026-08-05・minor）**。**起点は `ksql-analytics` からの依頼**（B123〜B125 と同じく「AI に実データを分析させ、最初に書いた形が通らなかった／黙って違う結果になった箇所」を拾ったもの）。**①②は B119〜B122 と同じ「利用者から検出できない」系列**＝あちらは「値が黙って誤る」、B126 は「**コストが黙って跳ね上がる**」、B127 は「**意味の違う値がエラー無しで出る**」。**B126 は方針を 2 回変えた**〔当初は「警告を出す」（案 B）で仕様 R1→R2 まで書いたが、**オーナーの「表記ができるのなら押し下げが可能では？」で正規化（案 A）へ転換**。**そのとおりで、正規化にすると R2 で悩んでいた論点（JOIN で「絞れます」と断定してよいか・警告の重複排除・converter から execute への運搬）がまとめて消えた**＝正規化後の AST を「利用者が最初から `IN` と書いた場合」と同一にすれば下流に分岐を足さないため〕。**唯一の障害は実在しない選択肢**＝**`= '存在しない値'` は 0 行で正常終了するが `IN ('存在しない値')` は `GAIA_IQ10`**（実測・`ksql_language_reference.md:447` に文書化された既存挙動）。**無条件の正規化は動いていたクエリを壊す**ので `optionOrder` で実在を確認できたときだけ正規化する。**レビューで R1 の中核前提が 2 回覆った**〔1 回目＝検出規則が広すぎ `USER_SELECT` 等の**複数値型**まで拾う（`IN` が集合 overlap で意味が変わる）／B127 の抑止が誤りで、**ウィンドウは JOIN 後の行を評価するため 1:N JOIN やサブテーブル展開で同じ `$id` が複数回現れる**。2 回目＝**`!=` を Phase 2 に送った根拠が誤り**（`NOT IN` は未選択行を含み `!=` と同値。実測 95=95）／正規化は `classifyWhereCapability` の中では行えない（下流が別途 `stmt.where` を読む）／判定は WHERE 全体でなく**各 leaf 単位**／**`EXPLAIN` の非変更性を受入に置いたのは誤り**で、計画が変わるのが効果そのもの〕。**実装時に codex が R4 の内部矛盾も検出**（§1 のスコープ表に「非対象: `!=`」が R3 のまま残っていた）。**B132 は v3.45.0 で入れた自分の退行**で、旧キーはエイリアスで解決を維持。 | 正しさ・性能 | [B126/B127 spec R4](internal/ksql_b126_b127_warnings_phase1_spec.md) / [review1](internal/ksql_b126_b127_codex_review_1.md) / [review2](internal/ksql_b126_b127_codex_review_2.md) |
| **v3.46.0** | **B124 集計算術式に `GROUP BY` キーの列と `@変数`（2026-08-05・minor）**。**起点は v3.45.0 と同じ「AI に kSQL を使わせた記録」**で、分析セッションのエージェントが在庫金額（現在庫 × 仕入価格）を素直に書いて `ParseError` を踏んだもの。**実需の測り方を一度誤った**＝起票時は「回避策 `SUM(a * 単価)` が厳密に等価だから実需なし」と判断したが、**正しい軸は「計算できるか」ではなく「最初に書かれる形が通るか」**だった（オーナー指摘で優先度を上げた）。**壁は 1 往復の損では終わらない**＝同じセッションの v2 で窓関数の壁に当たったとき、エージェントは回避策を探さず **SQL を捨てて 40 行のスクリプトへ出た**（出た先は validate も EXPLAIN も効かない）。**そしてその「厳密に等価」自体が偽だった**＝binary64 の丸め位置が違い、**非数値の列では外側 `NaN` / 内側 `0`**（実測）。**「回避策があるから軽い」という判断の土台が二重に崩れた**のがこの課題の核心。**codex の仕様レビューで高 4・中 4・低 2 を全件反映**し、うち 2 件は R1 の中核前提を覆した〔①`ROLLUP`/`CUBE`/`GROUPING SETS` では**小計行で除外キーが `""` に上書きされ**（実測）、元行を読む SELECT 側と出力行を読む `HAVING` 側で値が食い違う → **拒否**②**非集計始まりの式は既存の入口に到達しない**（`単価 * SUM(a)`）→ **集計関数始まりに限定**〕。**スコープは意図的に狭くした**＝`GROUP BY` キーの同一性は**表記一致**（既存の名前解決は metadata-backed でパース時には確定できない）。**緩和は純加法・逆は破壊的変更**という B125 と同じ原則。**`AGG_VARIABLE` の専用タグは作らず既存 `VariableRef` を再利用**（既存の変数解決器・未解決変数 backstop・静的参照解析は `type === "VARIABLE"` だけを見ている）。**合成キー名の維持が効いた**＝変数解決器の `numericArithmeticOperand` を `false\|"ARITH"\|"AGG_ARITH"` へ広げ、`AGG_ARITH` 文脈でだけ `raw` に `@name` を残す（変わると `HAVING`/`ORDER BY` の解決が壊れる＝alias 消失バグと同じ経路）。**実機で既知期待値と一致**（在庫金額 8 製品・合計 482,710）。**仕様のサンプル `SET @税率 = 1.1;` が無効だった**（変数名は英字か `_` 始まり）ことを実機確認で発見——B125 の数値例と同じ「**サンプルは実行しないと腐る**」失敗。 | 機能 | [B124 spec R2](internal/ksql_b124_aggregate_arithmetic_phase1_spec.md) / [review](internal/ksql_b124_codex_review_1.md) |
| **v3.45.0** | **B125 集計のウィンドウ関数 Phase 1／B123 `EXPLAIN` の `GROUP BY` 判定漏れ（2026-08-05・minor）**。**起点は依頼ではなく、AI に kSQL を使わせた記録の裏取り**＝`ksql-analytics` の在庫分析セッションで AI が遭遇した 4 件の判断誤りのうち 3 件がエンジン側に原因があり、B123〜B125 として起票した。**B125 の実需の測り方を最初は誤った**＝「計算できるか」で測って「回避策があるから実需なし」と結論しかけたが、正しくは「**最初に書かれる形が通るか**」。実際 v2 分析では窓関数の壁に当たった AI が **SQL を捨てて 40 行のローカルスクリプトへ逃げており**、壁は 1 往復の損では終わらず SQL の外へ出る動機になる（出た先は validate も EXPLAIN も効かない）。**フレーム既定は標準どおり `RANGE` をオーナー判断で採用**〔同順の行が同じ値になる点は踏みやすいが、非標準の既定にすると他 RDB で検証した SQL が黙ってずれる。緩和は 3 点セット＝`ROWS` 明示を Phase 1 に含める・`EXPLAIN` に実効フレームを表示・言語リファレンスに比較表〕。**仕様は 2 回間違えた**〔①**`SELECT DISTINCT` 併用不可と書いたが v1 で実装済みの機能で、そのまま実装すれば既存機能を削っていた**（codex の仕様レビューが検出）②**§3.2 の数値例を R1=130・R2 初版=80 と 2 回誤った**（正しくは 110＝同順グループ末尾）。前提残高を変えたときに片方の列だけ更新し忘れたもので、**アルゴリズムと数値例を別々に書くと数値例だけが腐る**。実装時に codex が再検出〕。**codex の仕様レビューは高 6・中 5・低 1 を返し全件反映**。うち 3 件（集計引数の物理フィールドが取得されず全行スキップ／`WINDOW_COL` の型メタが一律 number で `MIN`/`MAX` が壊れる／`ORDER BY` 無しで完全入力を要求せず truncate が部分集計になる）は**そのまま実装すれば静かに誤る**もので、仕様段階で潰せた。**外部検算が強かった**＝APP4228 の 8 製品の最小在庫が `ksql-analytics` の独立実装（40 行スクリプト）と**完全一致**（26/34/37/74/21/29/61/42・件数合計 1,000）。牛乳の min=74/max=1004 はコードポイント順なら逆になる値で、型メタ修正の実証を兼ねた。**B123 は「止まるべきでない所で止まる」型**で誤った計画は返していないが、**「JOIN や大量取得は EXPLAIN まで通す」という運用ルールが集計クエリに対して最初から機能していなかった**（分析クエリはほぼ全部 `GROUP BY` を含む）。真因は要否判定に通常の `GROUP BY` が入っておらず、B65 の `ROLLUP`/`GROUPING SETS` だけ入っていたこと。**`ksql_docs` の `window-functions` セクション独立化も同梱**＝§10.1 は独立節なのにセクション一覧に無く `order-by` に畳まれており、**AI が「窓関数は無い」と誤判断した実例**が出典セッションにあった。 | 機能・正しさ | [B125 spec R2](internal/ksql_b125_aggregate_window_phase1_spec.md) / [review](internal/ksql_b125_codex_review_1.md) / [B123](internal/ksql_b123_explain_groupby_metadata_issue.md) |
| **v3.44.0** | **B119〜B122 集計まわりで静かに間違う 4 件を修正（2026-08-05・minor＋移行案内）**。**起点は依頼ではなく、Claude Code + kSQL MCP の分析プロジェクト（`ksql-analytics`）で実データに問いを投げていて、返る数字が合わないことに気づいたもの**。**B119**＝集計の引数に文字列関数を書くと `0`／空（`COUNT(DISTINCT UPPER(会社名))` が `0`・`MAX(UPPER(x))` が `0`）。原因は `evalAggregate` が `STRING_FUNC` を算術式と同経路で数値評価し `NaN` で全行を捨てていたこと。**§8 は関数呼び出しを引数に使えると読め、数値評価の注記は `MODE(COALESCE(...))` の 1 行だけだった**。**B120**＝`CASE` の中の集計が集計として扱われず、**GROUP BY 無しで 1 行のはずが 20 行**、GROUP BY 有りは `unknown field code(s)` と誤診。**当初「スカラー式に集計を書けない」と起票したが実測で誤りと判明し切り分けを訂正**（`UPPER(MIN(x))`・`GREATEST(SUM(x),1)` は元から動く＝制約は `CASE` 固有）。**B121**＝`HAVING` の直接参照が文字列比較（`COUNT(*) > 10` が 0 行のはずが 5 行、`AVG(売上) > 9` が 8 行のはずが 0 行）で**過大にも過小にも振れる**。**別名参照は元から数値比較で正しく、そちらが参照実装になった**。**B122**＝`HAVING` に集計を式でくるむと無言で 0 行（`SUM(売上) - 0 > 9`・`ROUND(AVG(売上), 0) > 9`）。直接参照は素の集計と `SUM(CASE ... END)` しか評価していなかった。**レビューで 3 件差し戻した**（B120 の修正が①CASE 条件内の集計を文字列比較 ②無名 CASE 列のキーが固定 `case` で衝突し先の列の値が消える ③`MIN`/`MAX` が GROUP BY 無しだけ文字列比較、という新たな「静かに間違う」を持ち込んでいた）。**B121・B122 は差し戻し 0 回**。**なぜ長く見つからなかったか＝桁数が揃っていれば文字列比較でも正答する**——§9 の例（`HAVING COUNT(*) >= 5`／`HAVING SUM(金額) > 1000000`）はどちらも偶然一致し、既存テストも閾値がほぼ 1 桁か等値、**唯一 桁違いの境界を踏んでいたテスト（`HAVING total >= 10`）が別名参照＝正しい方の経路**だった。**教訓＝この方言では境界値テストを「桁を変えて両方向で」置くこと。等値比較は文字列/数値の取り違えを検出できない**（受入条件④をゼロ除算ガードの `= 0` で書いたため 1 回目の実装が素通りした）。**横展開調査**＝`ORDER BY` 直接参照（ParseError）・CTE／一時テーブル経由・スカラーサブクエリ比較・`JOIN`・`IN (SELECT 集計)` はいずれも問題なしを実測。**公開型**＝`CaseResult` に `AggregateRef \| AggArithExpr` を追加（網羅的 `switch` は要追従）・`FieldRef.aggregateRef?` は任意。 | 正しさ | [B119](internal/ksql_b119_aggregate_string_function_arg_issue.md) / [B120](internal/ksql_b120_aggregate_position_diagnostics_issue.md) / [B121](internal/ksql_b121_having_numeric_comparison_issue.md) / [B122](internal/ksql_b122_having_aggregate_expression_issue.md) |
| **v3.43.0** | **B118 関数の引数の数を検証し、未知の関数名を名指しする（2026-08-04・minor＋移行案内）**。**起点は Pro の反省文**＝「構文エラーを機能の非対応と読み替えていた」を読み、**原因を実測して発見**した（依頼ではない）。**②引数の数を黙認**＝`DATE_ADD(列, 1)` が単位なしで `DAY` 扱いで通り、`ROUND(列, 1, 2)` は余分な引数を無視、`FORMAT(列)` は既定パターンで黙って整形。**`ksql_validate` も通っていた**。**B117 と同じ「静かに間違う」形**で、B117 を「その形は残さない」理由で直した直後に同種を残すのは一貫しないと判断。**①未知の関数名で無関係なメッセージ**＝`DATE_SUB(...)` が「文の区切りには ; が必要です」、`DATE_ADD(d, INTERVAL 1 MONTH)` が「「)」が必要です」としか出ず、**これが Pro の誤記の直接原因**（実際に B117 の発見を 1 往復ぶん遅らせた）。**原因は設計の欠落ではなく適用漏れ**＝`assertArity` は既にあり **16 箇所で使用／関数の case 分岐は 65**、`?? "DAY"` のような既定値が書き間違いを正当化していた。**修正**＝言語リファレンスの構文列を正本に引数の数を一元化（`src/core/functionArity.ts`）し、**評価時と静的検証の両方**で検出。未知の関数名は名指しし `DATE_SUB` では負数指定を案内、既知関数の引数構文エラーは期待する書式を提示。**codex の停止 1 回＝`FORMAT(売上)` を正常系として固定していた既存テスト**——リファレンスに 1 引数形の記載は無く、実装の `?? "0"` が黙認していたもので、**テストの目的は列名の優先**（由来 `39182d8`）だったため決定の変更ではないと判定し、`FORMAT(売上, '#,##0')` への書き換えのみ許可（期待値は不変）。**レビューで 1 件差し戻し**＝既知関数の案内メッセージで**位置情報が二重**に付いていた（内側 ParseError が既に接尾辞を持っていた）。**この課題自体がメッセージの質を直すもの**なので出す前に修正した。**教訓＝「既定値による黙認」は書き間違いを正当化し、テストの中にまで入り込む**。 | 正しさ | [B118](internal/ksql_b118_function_call_diagnostics_issue.md) |
| **v3.42.0** | **B117 `DATE_ADD` の月・年加算を月末へ丸める（2026-08-04・minor＋移行案内）／文書 4 件**。**Pro の報告 K-112 が起点**＝`DATE_ADD('2026-01-31', 1, 'MONTH')` が `2026-03-03` を返し、`DATE_ADD('2026-03-31', -1, 'MONTH')` も**同じ日**になるため**往復しても戻らず順序も壊れていた**（こちらの実測で 5 例すべて一致）。**原因＝`applyDateAdd` が JavaScript の `Date` の正規化をそのまま漏らしていた**（存在しない「2 月 31 日」→ 3 月 3 日）＝設計ではなく副産物。**エラーにならず、もっともらしい日付が返るため気づく手段が無かった**。**破壊的変更だが丸めた**〔①いまの出力は擁護できる意味論ではない②主要な SQL エンジンはすべて丸める③**静かに間違う形**で締め日が月末の業務が踏む——同種を fail-closed で潰してきた（B97/B98/B102）のでここだけ残すのは一貫しない④壊れるのは「今日ちがう月の日付を返している」ケースだけ〕。**丸めの有無を切り替えるオプションは設けない**（設定で SQL の意味が変わる形・B107 と同じ判断）。**丸め後に往復が戻らないこと**（`1/31 → 2/28 → 1/28`・MySQL も同じ）は**バグではなく仕様**としてテストと文書に残した——後から再報告されないため。**この往復の直前に、Pro のレシピ集の「`DATE_ADD` は非対応」という記載を訂正していた**（原因は MySQL 式の `INTERVAL` 構文で試したこと）。**訂正した直後に、そのおかげで本欠陥が見つかった**という経緯。**文書 4 件（B116）も同梱**＝Pro のレシピ集から取り込んだ**押し下がる形への書き換え表**（全行を実測してから採用。JOIN の非厳密比較は主表だけ押し下がるため Pro より限定して記述）／日時境界の**半開区間**／`CURRENT_DATE()` の TZ 注意／未知の `fetch` 値を軽い側へ寄せない。 | 正しさ | [B117](internal/ksql_b117_date_add_month_end_issue.md) |
| **v3.41.0** | **B114 `fetch` の `NONE` を `COUNT_ONLY` へ改める（2026-08-04・minor）**。**オーナーの指摘「`COUNT(*)` は、取得 1 件では？」が起点**。実測すると `{"query":"limit 1","fields":["$id"],"totalCount":true}` の単発 GET で **1 件が転送され `metrics.fetchedRows` も 1** だった＝v3.40.0 の定義「レコードを取得しません」は**1 件ぶん不正確**で、**同じ API で返している `fetchedRows: 1` と矛盾**していた。「取得しない」ではなく**「走査しない」**が正しい。**機械的な全置換をしなかったのが要点**＝`none` には性質の違う 2 つが入っており、`COUNT(*)` の totalCount 経路だけを `COUNT_ONLY` とし、**取得ソースが 1 つも無い文（一時テーブル参照のみ・本当にゼロ）は `NONE` のまま据え置いた**。値は 5 つ・順序は `NONE` < `COUNT_ONLY` < `EXACT` < `PREFILTERED` < `ALL`。**publish の直後に直した理由**＝公開型の値集合は出したら変えられず、**唯一の消費者である Pro にはまだ連絡していなかった**ため実被害ゼロで直せる最後の機会だった（`role` の `temp` をリリース前に直したのと同じ判断）。**移行案内は付けない**＝v3.40.0 の利用者がいないため（オーナー判断で、書いた移行案内の節を削除した）。README の v3.40.0 の節に残っていた「4 つ」の記述も 5 値へ直した（同じ文書に古い定義が残ると次の誤読の元になる）。**教訓＝表示の名前は実測と突き合わせる**。「走査しない」を「取得しない」と書いた 1 語のずれが、自分たちの `metrics` と矛盾する契約を生んでいた。 | 正しさ（表示） | [B114](internal/ksql_b114_explain_fetch_scope_issue.md) |
| **v3.40.0** | **B114 `EXPLAIN` が取得範囲を名乗る（2026-08-04・minor・第 1 段＋第 2 段）**。**出典は Pro の依頼 K-110 だが、依頼の形をなぞらず EXPLAIN の設計として検討した**（オーナー指示）。**問題＝`mode: FULL_SCAN` は内部の評価戦略名なのに読み手にはコストの言明に見え、ペインは大半が GROUP BY / 集計を含むため「押し下げが効いていても FULL_SCAN」が正常系として頻出**していた（Pro 経由で非エンジニアの kintone アプリ管理者が直接見るため実害）。**原因はこちらの命名。** **構造的欠陥 4 点**〔①最も目立つ行が読み手の問いに答えていない②**テキストが正で構造が無い**（99 ラベルがフラット・下流は正規表現で読むしかなく Pro は断念）③確定と見込みが混ざる④ソース単位と全体の事実が同じ平面〕。**④は実測が決め手**＝1 クエリの UNION で片方の枝は走査なし・もう片方は全件で、**スカラー 1 値に畳むと必ず嘘になる**。**第 1 段**＝ソースごとの `fetch:` と文ごとの最悪値の要約。**判定材料は出力位置に既に揃っており新しい解析を足していない**のが安く付いた理由。**第 2 段**＝`ExplainResult.plan?` を純加法で追加し取得計画の行を構造から描画（**人間向けテキストは 1 文字も変えない**を最重要制約とし、`b76` の配布面一致テストと `console.e2e` の計画行数が期待値変更なしで通ることを検知手段にした）。**第 2 段のレビューで仕様の穴**＝`role` を定義しておらず `temp` が 2 つの意味を持ち、片方が**存在しない APP 0 を名乗っていた**。公開型は出したら値集合を変えられないためリリース前に是正。**§24 に「出力の読み方」を新設**（従来は呼び出し方しか無く 99 ラベルが説明なしに並んでいた）。**契約の範囲も明示**＝安定した読みどころ 4 つ以外は実装の詳細で予告なく増減する（`console.e2e` が行数を契約にして壊れた教訓を外部へ繰り返させないため）。**【リリース作業の事故】既存テスト 2 件が落ち「B114 が壊した」と誤認しかけた**＝実際は開発者のシェルの `KSQL_USERNAME` / `KSQL_PASSWORD` が原因で、B114 を退避した HEAD でも落ちた（**B115 として起票**）。切り分けの stash 操作で、セッション開始前から未コミットだった grouping snapshot の変更を失った。 | 機能（診断） | [B114](internal/ksql_b114_explain_fetch_scope_issue.md) |
| **v3.39.0** | **B111 相対日付変数 `DECLARE @x RELATIVE_DATE`／B112 `EXPLAIN` の app 行復元（2026-08-02・minor）**。**B111 は Pro 相談 K-6（変数コントロールバー）が起点**＝閲覧者がプリセットを選ぶと期間が注入される画面で、変数の値が常に文字列のため `受注予定日 = "THIS_MONTH()"` という無意味な条件になっていた。**測って前提が 1 つ覆った**＝依頼文の「現行でも `TODAY()` は既定値に書ける、の拡張」は成り立たず、`DECLARE @p = THIS_MONTH()` は ParseError、通る `TODAY()` は**位置で意味が変わる別物**（DECLARE 右辺＝クライアント評価のリテラル／WHERE 右辺＝サーバー押し下げ）＝**拡張の出発点ではなく既存の罠**だった。**実害（日時フィールドの終端取りこぼし）は engine 変更なしで解決できた**（実測 `作成日時 <= '2026-07-14'` が 18/20 件・RFC3339 なら 20 件）ため、**リリースをブロックしないと伝えたうえで**実装した。**設計の要点＝「値が code になる」形を採らなかったこと**〔値束縛の保証＝値は決して code にならない／**DML で fail-open**（`DELETE ... WHERE 日付 = @period` が単日→年間に変わりうる・B102 と同じ判断）／グローバルフラグは B107 で却下した「設定で意味が変わる」形〕→ **SQL 側で宣言**する純加法の型注釈にし、注釈なし `DECLARE` の挙動は完全に不変。**実装が安く付いた理由＝変数解決が planning より前**なので、`KINTONE_FUNC` ノードへ差し替えるだけで**既存 B67/B72/B75 の押し下げ判定と fail-closed がそのまま効いた**（新しい判定を足していない）。トークン解釈は既存パーサを再利用（引数つき関数があるため完全一致では足りない）。配置は WHERE 比較右辺と BETWEEN 境界のみ・違反とホワイトリスト外と DML/VALIDATE は **API 0 回で拒否**（呼び出し数を数える client で実測）。**B112 は B111 の実測中に発見**＝`EXPLAIN` の `app:` / `JOIN:` 行で**別名のある形だけ括弧内に内部仮想 ID が残り**（3 面すべて・ライブラリ含む）、物理アプリでは profile が二重（`APP4149@dev@dev`）になっていた。**B108 / B109 と同じ契約の残りで、範囲の取り落としは 2 度目**＝仕様に「経路」は列挙したが**出力の形**を列挙していなかったのが共通原因。修正は 2 段 split を 1 パスの関数形置換へ（自分の出力を再処理しない）。**束縛が無い面（プラグイン等）は括弧が残るが、そこには論理アプリが存在せず露出は起こり得ないため許容**（§4.1bis）。**codex の停止 2 回＝1 度目は既存テストの機械的更新、2 度目は「`b76` は固定期待値ではなく面どうしの一致テスト」というこちらの指示の誤りの指摘**（正しい指摘で、面差の扱いを仕様へ追記して解決）。 | 機能 / 正しさ | [B111](internal/ksql_b111_relative_date_variable_issue.md) / [B112](internal/ksql_b112_explain_app_line_restore_issue.md) |
| **v3.38.0** | **B110 SELECT 別名の表示表記を `QueryColumn.displayName` で保持（2026-08-02・minor）**。**Pro の相談 K-96 が起点**＝別名の ASCII 英字が結果列名で小文字化され、ダッシュボードの表の見出し・グラフの凡例が「書いたとおりに出ない」（実利用者からの報告あり・顧客ランク A〜D のクロス集計で見出しが a〜d になった）。**原因＝`parseAliasName()` が parse 時に `toLowerCase()` して表記を破棄**（バッククォートでも退避できず・全角英字にも効く）。**文書未記載の暗黙挙動だった**＝「仕様どおり・記載はここ」と答えられる状態ではなく、言語リファレンス §1 へ明文化も同梱した。**案の分岐**＝案 A（行キーごと表記保持）は小文字キーで結果を読む既存利用者への破壊的変更のため不採用、**Pro 自身が挙げていた非破壊案（案 B）をそのまま採用**（オーナー確認＝「返る列名は小文字のまま・表記はメタに添えるだけ」）。**実装**＝`parseAliasName()` が正規形＋表記を返し、AST の SELECT 列へ `aliasDisplay` を**非列挙プロパティ**で保持（既存 AST snapshot を変えないため。clone で落ちても `displayName = name` へ安全側に退化）、B69 の `MaterializedColumnMeta` に相乗りして `toPublicColumn` で公開。表記の規則＝明示別名=書かれたとおり／別名なし=`name` と同一／UNION=第 1 枝／temp passthrough=引き継ぎ／明示再選択=その位置。照合（重複検査・ORDER BY / HAVING・UNION 列合わせ）は不変。**codex の停止 1 回**＝既存テスト 3 件（`queryResult.test.ts` の完全一致期待値 2 件・`b65GroupByConsumerAllowlist` の行番号 1 件）の機械的更新の確認で、**承認済みの純加法への追従**と判定して範囲を限定して許可（決定の変更ではない）。**独立検証**＝ビルド済み dist-engine の公開 API で 7 ケース実測（クロス集計形・全角英字・バッククォート・キーワード別名・0 行・UNION・temp passthrough）。公開 .d.ts 差分は `displayName?` の 1 行のみ。**リリース作業**＝README が 150 行ガードに迫ったため v3.31.0・v3.33.0 の節を CHANGELOG へ委譲。**Pro は同日に取り込み・実機確認・出荷まで完了**（移行作業ゼロ）。 | 機能（表示） | [B110](internal/ksql_b110_alias_display_name_issue.md) |
| **v3.37.1** | **B109 engine ライブラリの `EXPLAIN` 計画本文の論理名併記（2026-08-01・patch）**。**Pro の報告 K-88 が起点**＝v3.37.0 の `logicalApps` を組み込み設定画面に実行計画ボタンを付けた実機確認で、`explainQuery` の計画本文が内部仮想 ID（`APP900000000 AS APP900000000 (900000000)`）のみと判明（`runBatch` の EXPLAIN 文も同じ）。**原因＝B108 の仕様の範囲落ち**＝縫い目を CLI 2＋MCP 1 の 3 箇所に絞り、engine ライブラリ面を挙げていなかった（仕様を書いた Claude の見落とし。codex は仕様どおり。Pro への返信でも正直に伝えた）。**修正**＝`restoreSqlDiagnosticValue` を B107 のスキャナと同じ形で core（`src/core/sqlDiagnostics.ts`）へ移して node は re-export（ゼロ依存・bundle guard 維持）、`logicalAppDisplay: "physical"` で **`@profile` 無しの `LAPP_名前 -> APPnnnn`** をライブラリ 2 経路（`explainQuery`・`runBatch` の EXPLAIN 文由来の結果）へ配線。B108 §6.2 の制約を引き継ぎ**文の型で判別・データ行に掛けない**（`SELECT 'APP900000000' AS x` の残存をテストで固定）。**codex の停止 0 回**。**実機確認は Pro の呼び方そのまま**（`explainQuery(pane.sql, { client, maxRecords, logicalApps })`）で併記とデータ残存・CLI / MCP 表示の不変を確認。 | 正しさ（表示） | [B109](internal/ksql_b109_library_explain_mapped_id_issue.md) |
| **v3.37.0** | **B107 論理アプリ名の日本語対応＋engine ライブラリの `logicalApps`／B108 EXPLAIN 文の内部 ID 露出修正（2026-08-01）**。**B107 は Pro の依頼 K-87 が起点**＝`RunQueryOptions` / `runBatch` への受け口。**測って前提が覆った**＝依頼文の例 `LAPP_案件管理` は**論理名が ASCII 限定のため現行構文で PARSE_ERROR**（Pro のフォールバック（テキスト置換）は日本語を扱えるため「受け口を開けるだけでは欲しいものにならない」）。**「ASCII で足りるか」を質問 → Pro は日本語名を希望し破壊的変更を受け入れ**〔理由＝**論理名は「読み手全員が見る名前」**でバッチ変数と役割が違う〕。**日本語拡張の地盤は良かった**＝lexer とスキャナの日本語 4 範囲が既に一致・サブテーブル部は日本語走査済み・半角 `$` と `@` は識別子文字でなく境界が自然。**設計＝単一ソース化**（規則が 4 重定義だった: スキャナ・config・MCP zod・リファレンス → `src/core/logicalApps.ts` 1 箇所へ。スキャナ本体も fs 非依存の core へ移し node は re-export）／**両側 NFC**（NFD 濁点 U+3099 が識別子範囲内で、正規化しないと「見た目同じなのに未定義」・実測）／**大小は `toUpperCase()` のまま＝ASCII＋全角英字のみ**／64 UTF-16 ユニット／**quirk＝全角 `＄``＠`・中黒・長音は名前の文字**。**破壊的変更 1 件**＝`LAPP_`＋日本語の識別子を予約（fail-closed・バッククォート退避・**「定義済みのときだけ解決」は同じ SQL の意味が設定で変わるため禁止**）。**codex の停止 1 回＝`config.test.ts` の `注文`（旧 ASCII 限定の決定記録）**＝**「既存テストの書き換え＝決定を覆す合図」のルールが、29 回目にして初めて本物の合図として働いた**（それまでの 28 回はすべて Claude の仕様の矛盾が原因）。**B108 は B107 の実機検証中に発見した v1.13.x 以来の既存ギャップ**＝「文として書いた EXPLAIN」の 4 経路（CLI 単文・バッチ／MCP `ksql_query` 単文・バッチ）が内部仮想 ID `APP900000000` を露出（専用経路 `--dry-run`・`ksql_explain` は正しかった）。**設計注意 2 点が要**＝①EXPLAIN の実行結果は `result.type` が SELECT で返るため**文の型で判別**②restore は全体を文字列置換で歩くため**データ行へ掛けず計画出力に限定**（データのリテラルが消えないことをテストで固定）。**実機確認**＝engine 3 面の日本語・NFD 解決／未定義名・`@profile` は API 0 回／CLI 日本語 config／EXPLAIN 4 経路の併記とデータ残存。**mcpb 検証は前リリースで踏みかけた「古い展開物を見る」を避け、再展開してから確認**。**【リリース作業の事故】この履歴行の追記自体で JS `String.replace` の `$` パターンを踏んだ**＝行に含まれる「バッククォート＋`$`」の並びが置換文字列の特殊パターン（マッチ前方の挿入）として解釈され、**行の途中にファイル冒頭が差し込まれた状態でコミットされた**。修復し、以後の教訓＝**置換文字列に `$` を含み得る編集は `replace(from, () => to)` の関数形か、配列 splice を使う**。 | 機能 / 正しさ | [B107](internal/ksql_b107_lapp_spec.md) / [B108](internal/ksql_b108_inline_explain_mapped_id_issue.md) |
| **v3.36.0** | **B105 `UNION` の枝と定数列の `COUNT(*)` を単発 GET に／B103 行末の LF 固定／文書 3 件（2026-07-31）**。**B105 はオーナーの実クエリ（12 アプリの件数一覧）が起点**＝同じ `COUNT(*)` が単体では成功し `UNION` の枝では既定 `maxRecords` でエラー。**原因は B94 の単発 GET がトップレベル単一 SELECT 限定**だったこと〔`markCountTotalCountRoot` が `case "SELECT"` のみ・`EXPLAIN` の `allowTotalCountPlan` もトップレベルのみ＝**両者一致の設計で、`UNION` は B94 仕様のどこにも書かれていない言及漏れ**〕。FULL_SCAN に落ちると B97 の完全入力要求が効く。**線引き＝`UNION` の枝は終端**（出力がそのまま結果集合へ入り、`EXACT_PUSHDOWN` なら 1 行で値は `totalCount` そのもの）なので枝だけ広げ、**CTE・一時テーブル・サブクエリ・DML source は B94 の判断を維持**。**【R1 の切り分けが不完全】発端のクエリはラベル列が `columns.length !== 1` で弾かれていた**＝`UNION` 枝であることだけを見て実際のクエリ形を条件に当てていなかった。**R2 で定数列（`LITERAL_COL` のみ）の併用を追加**＝リテラルはレコードに依存せず、`COUNT(*)` が唯一の集計なら出力 1 行はリテラル値と `totalCount` だけで構成できる（`ARITH`・`STRFUNC`・`CASE`・`FIELD`・文脈関数は従来どおり＝迷ったら従来経路へ）。**列名は既存 `project()` を再利用して命名規則を書き下さず、正しさはフォールバック経路との `columns`/`rows`/`rowCount` 完全一致で担保＝従来経路が参照実装**。実機＝12 アプリ 10,675 件がエラー → 約 5 秒・GET 約 32 回 → 12 回。**codex 停止 0 回（R1・R2 とも）だが、R2 仕様追記時に §3「変えないもの」との矛盾を作りかけ、出す前に自分で見つけた**（codex が止めてきた形と同じ）。**B103**＝`KORDER BY` 文書修正中に発覚。`.gitattributes` が無く `core.autocrlf=true`（Git for Windows の system 既定）で、**index は全 LF なのに作業ツリーが 515 CRLF / 80 混在に割れ**、B84 の生成照合テストが CRLF で落ちる。**案 A（`* text=auto eol=lf`）＋案 B（doc を読む 3 テストで正規化）の両方**。`text=auto` は NUL バイト入り仕様書 3 件の保護（**後に NUL 自体をエスケープ表記へ置換**し binary 判定は真のバイナリ 4 件のみに）。**`--renormalize` が codex 実装中のファイルまで stage する事故 → `git reset` で復旧**。**文書 3 件**＝SELECT 基本構文に `KORDER BY`（オーナー指摘。照合検査は B104 として測り誤検知 7/真の検出 0 で「何もしない」クローズ）・kintone クエリ関数の性能（同一アプリ・同一上限で成功/上限エラー/取得前拒否の 3 対比を実測で掲載）・`PRIMARY_ORGANIZATION()` の `SELECT` 側注意（Pro の `5 = 5` 実機再現を受け根拠を「公式の記述＋実機確認」へ更新。**言語リファレンスは 2 箇所とも DML 拒否しか書いておらず、CHANGELOG 等には書いたのに SQL を書きながら読む文書に無かった**）。**検証の失敗**＝mcpb を古い展開物（3.35.0）で確かめかけ再展開で発見（**B101 で Pro が踏んだ「検証したつもりが古いものを見る」を自分でも踏みかけた**）。desktop.js は minify されるため**識別子でなく文字列リテラルで混入確認**。**B106**（テスト監査）も同日クローズ＝5,052 件の約半分は `test.each` の行・dead/重複 0・整理しない。 | 性能 / 正しさ / 改善 | [B105](internal/ksql_b105_union_count_totalcount_spec.md) / [B103](internal/ksql_b103_line_ending_test_fragility_issue.md) / [B106](internal/ksql_b106_test_suite_audit_issue.md) |
| **v3.35.0** | **B102 `PRIMARY_ORGANIZATION()` のサポート（2026-07-29）**。**オーナー指示が起点**。用途＝ダッシュボードの自組織抽出（`LOGINUSER()` と同じ位置づけ）。**[B67] が Phase2 として意識的に見送っていた関数**。kintone 公式＝**組織選択のみ・`in`/`not in` のみ・優先組織未設定では条件が無視され全件**（**kintone 自身が fail-open し、無視されたことは応答から分からない**。`LOGINUSER()` は逆に 0 件へ閉じる）。**初版は fail-open を重く見すぎた**＝「トークン認証では常に」と想定したが、**測ると 4 プロファイル全てパスワード認証**で、起きるのは「実在するが優先組織未設定」のときだけ。**実装は `LOGINUSER` の写し**（型×演算子の能力表は既に `ORGANIZATION_SELECT` × `in`/`not in` を持ち、**足りないのは関数だけ**）＋**DML だけ fail-closed**（条件が消えると全件 UPDATE/DELETE。用途が SELECT に閉じるので失うものが無い）。**SELECT は素通し＝kintone 自身の挙動で、KLIKE の前例と同じ筋**。ローカル解決は空文字＝閉じる側。**fail-open はこちらで再現できず**（優先組織を持たないユーザーを用意できない）、**文書には「公式の記述による」と明記し「実測」とは書かなかった** → **後日（07-31）Pro が実機で再現**（優先組織なしで `5 = 5`）し根拠を更新。**経路を全部たどってから仕様を書き、codex 停止 0 回・語数予算も予想と完全一致**。検証アプリ APP4227。**リリース後**＝Pro の質問から **`kintone.user.getOrganizations()`**（一般利用者で `organization.primary` を引ける。**ただしプラグイン設定画面では使えない**）を発見。**案 C'（プラグイン面限定の fail-closed）は「面で同一」原則と衝突するため保留**とし、**Pro が閲覧側警告（関数使用ペインのみ・1 画面 1 回・判定不能時は何も出さない）で実装**したためエンジン側は当面不要。 | 機能 | [B102](internal/ksql_b102_primary_organization_issue.md) |
| **v3.34.1** | **B101 MCP の `instructions` に版数を載せる（2026-07-29）**。**起点＝Pro の検証事故**〔常駐 MCP が 3.33.0 を保持したまま v3.34.0 の検証に答え「B98 が再現しない」と誤報告。**`npm install` はディスクを置き換えるが常駐プロセスは差し替わらず、旧版は旧版として正しく動くので失敗が沈黙する**〕。**案は 3 転**＝R1「`ksql_docs` へ版数」→ **R2 で取り下げ**〔MCP 標準 `_meta['io.modelcontextprotocol/serverInfo']`（spec PR #3002）を 2026-era codec が全 result に自動で押しており **B99 の移行で既に満たされていた**。**「版数を返す手段が無い」を自分の tool/resource だけ見て結論づけた＝B99 R2 と同じ「正しく調べたが正しいものを調べていない」**〕→ **R3 で `_meta` は実クライアント 2 つ（Pro・Claude Code）で読めない**（ツール結果の中身しか届かない）と判明し **案 A'＝`instructions` の 1 行目**へ。**AI の文脈にそのまま入り・常駐プロセス自身が返す（＝プロセスの版）・v1/v2 両 era に載る・1 語**。codex 停止 1 回＝「語数予算の exact 値のみ」と書き**段落数の exact 固定（5→6）と衝突**（**検知装置は「変えるな」ではなく「変えるなら意識せよ」**）。**リリース後に限界の記述が誤りと判明**＝**`instructions` が文脈に入るのは接続時ではなく会話の開始時**〔Pro の切り分け「**同じ瞬間に 2 つの会話が違う本文を見る。サーバーもプロセスも 1 つ**」・こちらの進行中セッションでも再現〕→ **「繋ぎ直す」ではなく「新しい会話を始める」**へ 4 箇所（CHANGELOG・README・GitHub Release 本文・B101 §7）を訂正。**失敗の向きは安全側**（古い会話では古い版が見えるだけ）＝案 B（陳腐化の自己検出）を急がない根拠。**Pro のカナリアクエリ（プロセスの版を測る唯一の方法だが実データと版ごとの作り直しが要る）が不要になった**。 | 改善 | [B101](internal/ksql_b101_mcp_stale_process_version_issue.md) |
| **v3.34.0** | **B98 外部結合の保持されない側の打ち切りを fail-closed 化／B99 MCP SDK v2（プロトコル 2026-07-28）への移行（2026-07-29）**。**B98＝B97 §5 で意識的に対象外とした残件**。実機で再現データを作り（APP4225/4226）、`maxRecords=20`/truncate で**真の値 `b01` の行が空になり、本当に相手がいない行とバイト単位で同一**と確認＝結果から取りこぼしと該当なしを区別できない。**保持されない側が打ち切られたときだけ止める**（保持側は行が減るだけなので警告付き・`INNER` 不変）。判定は alias でなく **object 同一性**（alias は `null` があり得る）。**B99＝初版評価の中心的な結論が誤り**〔「2026-07-28 対応の SDK が存在しない」→ **package が分割されていた**（`@modelcontextprotocol/server` 2.0.0）。**WebFetch の要約を実データで否定したが要約のほうが正しかった**＝「正しく調べた」と「正しいものを調べた」は別〕。**Node 20 はオーナー決定**。dual-era spike 5 点成立 → `serveStdio(factory)`・`ProtocolError`・**すべて devDependencies**（**公開 package の runtime 依存ゼロと `engines` 未宣言を守る＝engine 利用者に不要な要件を課さない**）・**stdio 256 MiB**（宣言済み上限からの導出値をテストで固定＝`IMPORT_MAX_BYTES` を上げた人が気づける）。**codex 停止 5 回すべて正しく、すべて Claude の仕様が原因**（B95 から通算 27 回）。**「面が同一」を報告で受け取らず、v1/v2 両方を build して `tools/list` を diff** → **`$schema` 方言（draft-07 → 2020-12）だけが変わる外向きの変化を発見**し CHANGELOG に明記。リリース作業では **desktop.js が stale**（B98 ガード未混入・full build で解消）と **README 150 行ガード超過**を検出＝ガードが機能した。 | 正しさ / 機能 | [B98](internal/ksql_b98_join_truncated_right_null_spec.md) / [B99](internal/ksql_b99_mcp_v2_migration_spec.md) |
| **v3.33.0** | **B97 打ち切られた入力の集計を fail-closed 化／B96 `getRecords()` の応答契約（2026-07-29）**。**どちらも Pro の v3.32.0 報告が起点**。**B97 は挙動の変更**で、**取得上限に達したとき集計・`GROUP BY`・`DISTINCT`・`UNION` が `truncate` でもエラー**になる。**根拠＝現在成功して見えるクエリは実際には誤った結果を返している**〔真の件数 3 のクエリが **`0`** を返す実測。該当が 4 件目以降にあり先頭 3 件だけを数えたため〕。**`0` は「小さすぎる値」より危ない**＝`COUNT` 5 / `AVG` 3.4 は疑う余地があるが、**`0` は「該当なし」という完結した答えに読め、ダッシュボードで平常の値**。**B95 で「平均が特に危うい」としたのは桁が変わらないから**だったが、**`0` はそもそも異常に見えない**。**【最大の収穫】codex に事実調査だけを依頼して起票時の前提が覆った**＝**新しいモードを足す必要が無かった**。**`CompleteInputReason` と、理由が 1 つでもあれば `truncate` を `error` へ差し替える配管が既に完成**しており、**ローカル `ORDER BY`・window・統計集計・小計総計は従来から同じ仕組みで守られていた。足りないのは対象だけ**だった。**しかも [B56](internal/ksql_b56_statistical_aggregates_spec.md) が「既存集計の完全入力化を別課題とし、新旧非対称は意図的」と明記**しており、**設計が本件を見越していた**。**「調査は codex・判断は Claude」の分担が効いた初回**（オーナー提案）。**判定を新規に書かないという最大の要件が、調査の結果そもそも不要になった**。**原理＝行を畳む処理**＝1 行の出力が複数の入力行についての主張になっているとき、部分入力から作った出力は誤り。**素の明細はその行 1 件の事実なので対象外**（要件の本体）。**`UNION`（`ALL` なし）も対象**＝枝をまたいで畳むので `SELECT DISTINCT` と同じ主張。**含めないと「`DISTINCT` はエラーだが `UNION` は通る」という説明できない差**が残る。**`JOIN` は対象外**＝行を畳んでいない。**LEFT JOIN で右が打ち切られると `NULL` が入る別の問題**は残件として記録。**【codex が 4 回停止・すべて Claude の仕様が原因】**〔①例外を 3 テストに限定して書いた（`b65CompleteInput` の存在を知らなかった）②**「複数理由は既存の形に合わせる」と書いた結果、ROLLUP + `COUNT(*)` の主語が「小計・総計」→「クエリ」へ劣化**した③影響範囲を見誤った（集計 CTE 2 件＋EXPLAIN の選択子 4 件）④「巻き添え」と予測した 1 件が同じ性質だった〕。**②が最も重要**＝codex は「期待値を更新するのが自然」と判断を添えてきたが、**それは product の判断なので覆し、主語の規則のほうを直した**（理由を**集計系と並び系**に分け、集計系は最も具体的なものを選ぶ。**既存の文言は 1 つも変わっていない**）。**B97 は文言を良くこそすれ悪くしてはいけない**。**④の後に進め方を変えた**＝**1 件ずつ止めさせるのをやめ、「打ち切られた入力の集計を成功として固定していたテスト」に限ってまとめて更新する許可**を出した。**私が「これで最後のはず」と言うたびに外れた**ため。**書き換えた既存テストは 7 件 / 6 ファイル**〔誤った挙動を成功として固定 4／理由一覧への純加法 2／選択子が緩すぎた 4（1 ファイル）〕。**選択子は除外ではなく見たい行を正確に指す形にした**（除外だと次に行が増えたときに同じ問題を起こす）。**B96 の核心＝非対称**〔**`totalCount` の欠落はエンジンが自分で塞げる**が、**`searchAborted` の欠落は塞げない**。検出経路が client の応答だけで `false` と欠落を区別できない〕。**BYO で躓いたのは 3 回目だが種類が違う**＝B85・B93 は自作クライアントの実装漏れ、**本件は公式 factory をそのまま使っているのにラッパーが応答を痩せさせた**。**ラッパーを挟む実装は珍しくない**（キャッシュ・計測・リトライ）ので自作クライアントを書く人だけの問題ではない。**Pro の効果報告が想定を超えた**＝**APP730（実件数 618,525）が 1 リクエスト・676ms**で返り、**「大規模アプリでは総件数を出せない」という記述をレシピ集から削除**した（B94）。npm test 195 suites/4,972 green・snapshot 22 不変・**公開型は不変**（`onLimitReached` は `"error" | "truncate"` のまま）。 | 正しさ / 改善 | [B97](internal/ksql_b97_incomplete_aggregate_failclosed_spec.md) / [B96](internal/ksql_b96_getrecords_response_contract_spec.md) |
| **v3.32.0** | **B94 `SELECT COUNT(*)` の単発取得／B95 打ち切りの構造化／B93 `getFields` 契約エラー（2026-07-29）**。**3 件とも Pro の報告が起点**で、**B94 と B95 は同じ症状の両側**〔実件数 10,228 のアプリで `COUNT(*)` が 10000 を返し KPI に誤った数字が出た〕。**B94 でその形の症状自体が消え**（`totalCount` はレコードを取らないので `maxRecords` を適用しない）、**B95 で残る経路を利用者が判別できる**。**B94 の設計判断＝押し下げ完全性の判定を新規に書かず既存の `PredicateCapability`（`EXACT_PUSHDOWN`）を再利用した**。誤ると B78/B79/B86 と同じ silent wrong result になり、**件数は検算されにくい**（利用者は返ってきた数を信じる）。**判定は実行時に確定する**点も守った＝計画時の見込みで決めると実行時に候補が外れた場合に誤った件数を返す。**副次的に不能が解消**＝MCP の既定 `maxRecords` は 500 なので、**500 件を超えるアプリの件数は従来 MCP から取得できなかった**（`FetchAllLimitError`）。**B94 の失敗＝打ち切りの扱いを R1→R2→R3 と二転**し、**R2 の誤りは codex が B72 の fail-closed テストを警告期待へ書き換えたことで発覚**した。→ **「既存テストを書き換える必要が出たら止めて報告」というルールを新設**（codex 自身が判定できる形）。**B95 でそのルールが早くも過剰と判明**＝`toEqual` の網羅検証へ純加法のフィールドを足す**機械的な追記まで止めた**。→ **「期待している挙動が変わる書き換え」だけを対象に絞り、機械的な追記は可（報告に列挙）**とした。**1 リリースで作って次のリリースで絞った。ルールは一度では正しい形にならない。****codex は緩すぎるときも厳しすぎるときも検出した。****B95 のもう 1 つの誤り＝「エンジンが返す型だから利用者の実装は壊れない」**と書いたが、**公開型は利用者が構築できる**（`publicApi.test.ts` が利用者コードとして `QueryResult` を構築していた）。→ **任意プロパティにした**。**B95 の実装レビューで 1 件差し戻し**＝初版は **client の同一性で `WeakMap`** を引き、見つからなければ黙って return する形で、**再ラップのたびに引き継ぎを手で書く必要**があった。**忘れると打ち切りが起きても `false` が返る**＝**本課題が無くそうとしている黙って誤った値そのもの**であり、**警告文言だけは出ていた従来より悪い**（新 API が「打ち切られていない」と積極的に断言する）。**呼び忘れはすでに 1 件あった**。→ **同ファイルの `SEARCH_ABORT_FAIL_CLOSED` と同じく列挙可能な symbol にし、`{...client}` が自動で運ぶ形**にして**失敗様式ごと消した**。**B93 の教訓＝`InternalError:` は到達しないはずの不変条件が破れたときに使う接頭辞であり、client 由来の入力に使うのは誤用**。**Pro が回帰として報告し双方が調査に時間を使った**。`getFields` の契約を「渡す」「渡さない」の対で文書化した。npm test 194 suites/4,951 green・snapshot 22 不変・**公開面は `totalCount?`／`limitReached?`／`limitReachedApps?` の純加法のみ**。 | 性能 / 正しさ / 改善 | [B94](internal/ksql_b94_count_star_totalcount_spec.md) / [B95](internal/ksql_b95_truncation_visibility_spec.md) / [B93](internal/ksql_b93_getfields_contract_error_issue.md) |
| **v3.31.1** | **B92 EXPLAIN が変数の算術を拒否する回帰の修正（2026-07-29）**。**v3.31.0 の当日に、デプロイ済み MCP の実機確認で発覚**〔`SET @total = (SELECT SUM(...)); SELECT (顧客No * 100) / @total` の EXPLAIN が `variable @total is not numeric and cannot be used in arithmetic.` で必ず失敗。**v3.30.0 まで動いていた `ROUND(算術式, ...)` 形も巻き添え**〕。**B89 と B90 が噛み合って Pro の用途を正面から壊していた**＝Pro の設定画面は explainQuery で構文チェックし（B89 で複文対応したばかり）、そこへ流すのが構成比のバッチ（B90 で書けるようにしたばかり）。**両方の目玉機能が組み合わせると必ずエラー**になる状態を一度公開した。**原因＝EXPLAIN は変数を評価しないため名前を文字列プレースホルダーとして保持**しており、B90 の非数値チェックが**それを文字列変数と誤認**した。**【レビューの穴】B90 §4 で「buildBatchExplainPlans は解決を計画前に呼ぶ」ことは確認していたが、渡される値が実値ではなくプレースホルダーである点まで見ていなかった＝受入条件に EXPLAIN 経路が1件も無かった。****codex が 15 回止めた領域とは別で、最後まで誰も見ていなかった箇所**。**リリース後の実機確認を手順に入れている意味が出た**（していなければ Pro が最初に踏んでいた）。**修正＝`VarValue` に placeholder の目印を足し、算術位置では `{type:"NUMBER", value:0, raw:"@name"}` で置換**。**算術以外の位置では型を変えない**（EXPLAIN の計画は実行時の挙動を映すべきで、型を数値へ変えると押し下げの判定が実際と違って表示される）。**プレースホルダーを作るのは buildBatchExplainPlans だけ**なので**実行経路は不変**＝B90 の fail-closed は維持。**受入2は R2 で内部契約へ変更**＝**当該計画は算術式自体を表示しない**（mode/temp/note の3行のみ）ことを codex が指摘し、表示では固定できないと判明。表示契約の新設は patch の範囲を超えるため見送り。**受入4の固定が良い**＝算術外のプレースホルダー計画を**文字列リテラルを直接書いた計画との完全一致**で固定し、型を変えていないことを内部実装ではなく出力の一致で示した。**独立検証＝修正前 6 件 fail → 修正後 115 件 pass**。npm test 191 suites/4,917 green・snapshot 22 不変・公開面不変。**あわせて release/ を整理**＝README が **911 行・v1.12.0 まで遡っていた**ため直近3版だけ残して **82 行**へ、プラグイン zip **15 本 4.0MB** を最新版のみへ。**削除前に全 15 版が GitHub Releases に添付済みであることと、`git show <tag>:release/...` で取り出せることを実測**（二重の保管を確認してから消した）。**再発防止を手順書でなく機械化**＝version-sync-guard のリリースモードへ **README 150 行上限**と**旧版 zip の残存**を追加し、**両方向で壊して落ちることを確認**。**デプロイ済み MCP で実機確認**＝EXPLAIN が3文とも計画を返し（`0` の漏れなし）、実行時の非数値変数は停止し、直接算術は正しい構成比を返す。 | 正しさ | [B92](internal/ksql_b92_explain_variable_placeholder_issue.md) |
| **v3.31.0** | **B89 explainQuery のバッチ対応／B90 変数の直接算術／B87 アプリ定義キャッシュ／B88 0 行 SELECT * の列復元（2026-07-29）**。**⚠ 当日に B92 の回帰が判明し v3.31.1 で修正。単独では使わないこと。****B89＝Pro の依頼①（3依頼で唯一 代替手段なし）**〔設定画面が explainQuery で構文チェックしているのに、B68 の runBatch を採用した瞬間から検証が効かなくなっていた〕。**Pro の「配線するだけ」は不正確**＝論点はプランナではなく**受理集合**で、複文対応だけ足してもバッチ内の CREATE TEMP TABLE や SET で弾かれる。→ **受理集合を runBatch と揃えた**。**実装中に B68 の穴を2つ塞いだ**〔①**静的検証エラーに文の情報が載っていなかった**（withStatementDiagnostic は executeBatch が文別結果を返した後の実行時失敗にしか届かず、Pro が最も必要とする入力中の構文エラーには載らない。**runBatch も同じ**だったので両経路へ）②**runBatch が `EXPLAIN UPDATE` を受理していた**（read-only ガードが EXPLAIN を展開せず外側の型で判定。`isReadOnlyType("EXPLAIN")`=true・`isDmlType("EXPLAIN")`=false のため素通り）〕。**後者は純加法でない**（受理範囲が1形狭まる）が、read-only ライブラリで DML の計画を出すこと自体が意図外・EXPLAIN は書き込まないので誤った結果を得ていた利用者がいない・**explainQuery の既存テスト3件が意図された姿を示していた**の3点で許容。**B90＝Pro の依頼②**〔構成比は「SQL では出せない」と Pro が文書化していた列で、ROUND() で包む説明が「文法上の都合です」になっていた〕。**式文法が2系統**あり関数引数側だけが変数に対応していた。**変数は実行ディスパッチより前に解決される**ため押し下げ変換器には解決済みリテラルしか届かず、押し下げの安全性に触れずに済んだ。**依頼を超えて silent wrong result を1つ潰した**＝**非数値変数が黙って NaN を返していた**（既存の ROUND 経路も同じ）。B78/B79/B86 と同じ基準で両経路を fail-closed に。**B87＝アプリ定義キャッシュに無効化が無く、項目を追加しても再起動まで反映されなかった**〔**同一プロセスで `SELECT *` には見えるのに名指しすると unknown field code(s)** という矛盾。古くなるのは定義だけでなく選択肢・プロセス管理・数値精度・**v3.30.0 で公開したばかりの制約検証**も〕。**起票時の推奨（案 A リビジョン照合）は撤回**＝`KintoneClient` にリビジョン取得の口が無く、追加すれば **Pro の自前クライアントが壊れる**。しかもリビジョン取得自体が 1 API でコストが同じ。→ **実行単位スコープ**へ。**増分は 1 実行×1 アプリあたり 1 回だけ**で、**1 実行内の重複排除は不変**（2 アプリ JOIN=2 回・4 文バッチ=2 回）。**Pro 側は SharedRecordCache のメモ化で吸収**されると回答あり（実測でエンジン 3 回に対し実 GET 2 回）＝**TTL は追加しない**ことで合意。**B88＝0 行の SELECT * が列を失い、データがある日は動き 0 件の日だけ落ちていた**。**起点は1箇所**で 0 行の `SELECT * FROM APP` が getFields を一度も呼んでいなかった（**できないのではなく、していない**）。**列順の懸念は実 kintone 5 アプリで解消**＝**後から追加した項目は末尾に付かない**（APP423 で 6・8 番目に挿入）**のに、その予測不能な位置まで getFields と `SELECT *` が一致**。**STATUS は型では決まらず status.json の enable に依存**することも実測。**サブテーブル仮想テーブルも対象**（`_pid,_rid,_idx` ＋子）。**v3.30.0 で「残る限界」とした JOIN 入力エラーも解消**。**codex が実装前に 14 回停止し、すべて正しかった**〔B89 だけで 5 回。既存契約の未確認・自分の文書内の矛盾・**手集計の誤り**（B84 で「手書きは腐る」と自分で書いておきながら、消費側の数を grep の手集計で仕様に書いた）〕。**実装前だったため手戻りはゼロ**。npm test 191 suites/4,908 green・CLI e2e 26・snapshot 22 不変・公開面不変。SemVer=**minor（破壊的変更2件だが、いずれも誤った結果か意図外の機能）**。 | 機能 | [B89](internal/ksql_b89_library_explain_batch_spec.md) / [B90](internal/ksql_b90_variable_arithmetic_spec.md) / [B87](internal/ksql_b87_metadata_cache_spec.md) / [B88](internal/ksql_b88_empty_wildcard_schema_restore_spec.md) |
| **v3.30.0** | **B86 実体化ソースの不存在列を fail-closed 化（破壊的）／B85 VALIDATE の検証範囲開示／B84 押し下げ可否の公開／B83 診断列の2形明記（2026-07-28）**。**B86＝オーナー検証で発覚**〔`WITH A AS (SHOW APPS) SELECT * FROM A WHERE アプリ名 LIKE 顧客`（引用符なし）が **977 件＝全件**を返し、エラーも警告も無かった〕。**裸の識別子は列参照**（v1.14.0 の右辺フィールド比較・仕様どおり。実在列なら正常）だが、**存在しない列が黙って空文字へ解決**されるため **`LIKE` は空パターンで全件一致**（`=` は 0 件）。**調査で想定より深刻と判明＝書き込みまで届いていた**〔`INSERT INTO APPn (f) SELECT 不存在列 FROM #t` が **ok=true で空文字レコードを実際に POST**。モックで POST 呼び出しを観測して確認。**B79（誤った値を読む）より深刻**＝読みは再実行できるが**書き込みは戻せない**〕。**もう1件＝混在 JOIN では物理アプリ側の検証まで迂回**していた（穴は当初把握より広い）。**修正＝`executeQueryWithCte()` の実行ディスパッチ前で source-aware に検証**（実体化済み columns が使え、サブクエリ・UNION 枝・物理 GET・DML mutation より前に拒否）。**`=` も対象**（0 件は正しい空集合ではなく不存在列を空文字評価した誤結果）。**`defs.length===0` の抜け道は持ち込まない**（materialized columns が非空なら mock metadata に関係なく検証＝「検証があるつもりで無い」状態を作らない）。**残る限界＝`rows=[] && columns=[]`** は x と missing を区別できず、R1 は JOIN なし 0 行読み出しに限定して既存挙動を維持し JOIN 利用は取得前 error（完全解消は B2 の schema 復元拡張が別途必要）。**仕様の静的監査「既存テスト影響 0 件」は外れ**＝空 temp の `SELECT *, extra` を**成功として固定していたテスト**が1件あり、**テストが誤った挙動を正しいものとして固定**していた（実行して初めて判明）。**B85＝Pro の確認依頼③**〔同じ `VALIDATE` 文が MCP 10 行/errorCount 12 に対し library 0 行/0〕。**Pro の推測（契約上の限界）は外れ**＝**パイプラインは制約を素通ししており**、真因は**型が宣言していないこと**（TypeScript の余剰プロパティ拒否で渡そうとしても止められる）。**`createReadonlyKintoneClient` は実行時には制約を返していた**ため同関数の利用者は既に検証できていた。**案 B は「警告」から「開示」へ設計変更**〔**制約が1つも無いアプリが実在**（検証環境の1アプリは 21 項目すべて制約なし）＝警告は誤警告になり**無視される警告は無いのと同じ**。さらに**ライブラリは「アプリに制約が無い」と「クライアントが落としている」を原理的に区別できない**〕→ **`validateStats.constraintMetadata { present, absent }`** で**渡されたメタデータの事実だけ**を返す。**B84＝押し下げ可否を実装から生成して公開**〔TypeScript AST で分類器2ファイルを走査し 25 型 × 8 演算子を照合。**表を1セル変えても分類器に型を足しても落ちる**ことを破壊テストで確認。**軸を先に明示**（JOIN・field vs literal の単一条件のみ）し、**`$id` は表に載せず別扱い**（表では RECORD_NUMBER が押し下がらないと出るため、書かないと逆の結論を読ませる）。**2値**で公開〕。**B83＝診断列を 9 列 / SUMMARY 5 列の2形明記**（`UPSERT_SELECT` のカタログ例も追加し B68 parity の追跡穴が閉じた）。全 npm test 188 suites/4,853 green・snapshot 22 不変・4 smoke green・**release/ の3成果物は中身で版数検証**。SemVer=**minor（破壊的変更だが誤った結果を返していたもの・v3.25.0/v3.27.0 と同じ判断基準）**。**tag v3.30.0→GitHub Release（アセット3点）→npm publish 済み（latest 3.30.0）＝リリース完全完了**。**デプロイ済み MCP で実機確認**＝発覚した SQL が `ArgumentError: unknown field code(s): 顧客 (A)` で拒否され（**発生源を名指し**）、引用符つきは 44 件で正常。 | 正しさ | [B86](internal/ksql_b86_materialized_unknown_column_issue.md) / [B85](internal/ksql_b85_library_validate_constraints_issue.md) / [B84](internal/ksql_b84_pushdown_visibility_spec.md) / [B83](internal/ksql_b83_mcp_validate_columns_docs_issue.md) |
| **v3.29.0** | **B68 engine ライブラリの read-only 拡張／B81 語数予算／B82 未リリース表記検出（2026-07-28）**。**本質は機能追加ではなく「面をまたいだ契約の不一致」**〔**生成 AI が MCP で SQL を作り、ダッシュボードが engine library で実行する**ため、許可構文の差は**AI が検証して通した SQL が本番で落ちる**事故になる。B76 の「能力表が2つ」と同型だが、**押し下げの差は性能差にとどまるのに対しこちらは動くはずのものが動かない**〕。**①判定を `isReadOnlyStatement()` へ一本化**（ライブラリ独自 allowlist を廃止＝二重管理の解消）**②単文 `VALIDATE` 解禁**〔戻り値が `SELECT` と**同じ `SelectResult`** ＋任意 `validateStats` と判明したため**新関数も判別共用体も不要**＝純加法。当初 `explainQuery` の前例から `runValidate()` を推したが**戻り値の型を確認する前の判断**だったため却下〕**③`runBatch()` 新設**（文単位の read-only 強制・`results[]` は `QueryResult` 再利用）**④一時テーブル上限を公開契約化**（既定 10,000 行・**同時**16 表・truncate 指定でも error・**利用者アプリのプロセス内**実体化）＋`variables`（`DECLARE` のみ・`SET` 注入は拒否）**⑤失敗時は throw**〔レビューで**失敗バッチが `ok:false` のまま部分結果を返す**ことを実測し **B79 の「部分結果が黙ってアプリロジックへ流れ込むほうが危険」と衝突**すると判明→オーナー決定で案 A。`ok` を削除し（常に true にしかならず**決して発火しない `if (r.ok)`** を書かせるため）`statementIndex`/`statementType` だけをエラーに載せる〕**⑥parity をテストで固定**〔型レベル網羅・例外3件（`IMPORT`/`APPLY`/DML `VALIDATE ONLY`＝**いずれも書き込みを前提とした文またはその周辺**）・**どちらの面が受理するかまで固定**〕。**codex が実装前に3回停止し、いずれも Claude 側の誤りだった**〔16 表は**同時数**／parity 例外と非スコープ指定の矛盾／受入条件の訂正漏れ〕＝**そのまま実装されていれば公開契約に誤りが入っていた**。**parity は破壊テストで空回りしていないことを確認**〔初版は「いずれかの面」だったため `runQuery` からのみ外す破壊が通ってしまい、**Pro が使う `runQuery` から機能が消えても検出できなかった**→面の固定を追補〕。**B81**＝語数予算を散文とカタログ列挙で分けて計上〔**上限に近づけているのは instructions で最も効いている部分**（捏造を防ぐカタログ）なのに、抑えたいのは散文の冗長さだった。**調査の結果 550 にも新設した閾値にも外部根拠は無い**（MCP 仕様・SDK ともサイズ規定なし／実コストは毎セッション約 1,000 トークンで無視できる）→**guard の位置づけを「予算」から「変化の検知」へ**改め、exact 固定を本体とした〕。**B82**＝リリース時に公開文書の `Unreleased`/`未リリース`/`次回リリース` を検出し `prepack` を止める（B70 §7 の残作業）。**本リリースで初めて実運用で効いた**。全 npm test 186 suites/4,813 green・snapshot 22 不変・docs/pack/declaration/bundle-guard/mcp:verify green。**release/ の3成果物は中身の manifest と版数文字列で検証**（v3.5.0 の copy 漏れ事故の対策）。SemVer=**minor（純加法）**。**tag v3.29.0→GitHub Release（アセット3点）→npm publish 済み（latest 3.29.0）＝リリース完全完了**。**実機確認は Pro（engine library 利用）側が本来の受入**。 | 機能 | [B68 計画](internal/ksql_b68_engine_library_readonly_impl_plan.md) / [B81](internal/ksql_b81_mcp_instructions_word_budget_issue.md) / [B82](internal/ksql_b82_release_stale_marker_guard_issue.md) |
| **v3.28.0** | **B76 Phase B JOIN の server-only 関数・第5許可形（2026-07-27）**。alias 付き物理 APP だけを入力にする `INNER JOIN` で**相対日付12関数と `TODAY()`/`NOW()`/`LOGINUSER()` を kintone server へ exact に押し下げ**、関数の client 評価を 0 にした。Pro 検証報告の NG ケース①を解消。**設計の核心＝不可能性証明**〔`superset`＋residual 除去は偽陽性・`superset`＋residual 維持は「関数の client 評価禁止」に反する・**両立する第三案はない**→**関数 leaf は exact のみ**〕。**第5-W**＝`WHERE` 全体が単一 alias の whole-WHERE exact（同一 alias の `OR`/`NOT`・whole-exact な KLIKE 共存可・residual `null`）／**第5-L**＝AND スパイン上の exact 関数 leaf を alias ごとに採用（複数 alias 分散可・関数なし残余だけ client 評価）。**Phase A §5.5 の `KLIKE ∨ superset` ハザードは residual 再評価が前提**だったため、**residual `null` の第5-W では同じ形が安全**という区別を実装とテストで固定〔`klikeValidation.ts` に defer 経路を追加したが、関数なしの KLIKE-in-OR は従来どおり静的拒否・whole exact 不成立なら拒否＋fetch 0 を実測〕。**Step 4 で EXPLAIN 不整合を解消**＝Step 2〜3 は実行のみ解禁で「実行 OK / EXPLAIN 拒否」が残っていたが、`statement.type !== "EXPLAIN"` を外すだけでなく**EXPLAIN が runtime とは別に JOIN plan を再生成していた箇所を同一 plan 参照へ統合**〔条件だけ外すと表示と実挙動が別計算になり**より悪い不整合**になる〕。許可 11 形・拒否 5 形で**両方向の不一致 0**。混在文は関数集合ごとに**独立集計**（従来の一行選択 helper では `THIS_MONTH()`＋`LOGINUSER()` を表せなかった）。**B79 契約は非回帰**＝plan あり/なしの `INNER JOIN` で `searchAborted` 結果が一致＝**Phase A §16 で撤回した非対称を再導入していない**。**引き続き不可**＝`LEFT`/`RIGHT JOIN`・cross-alias `OR`・関数を含む cross-table 述語・whole-WHERE exact でない KLIKE-containing `OR`・サブテーブル/入れ子 SELECT/派生表/CTE/一時テーブルを JOIN 入力にする形・`GROUP_SELECT`（kintone 公式に関数なし）。**純加法**だが一部の KLIKE 混在形は**拒否理由が KLIKE 固有から関数側の requires-exact へ変わる**。全 npm test 184 suites/4,779 green・snapshot 22 不変・配布 smoke 全 green・版同期 8 箇所 green。**実機 PASS**＝ブラウザ プラグイン（オーナー実施）＋ローカル v3.28.0 CLI で実データ〔APP4147×APP4148・**JOIN ＋ `LAST_YEAR()` が 18 件＝単一表 18 件・JOIN ＋リテラル日付 18 件と一致**・同じクエリが v3.27.0 では requires-exact で拒否＝before/after 対比〕。SemVer=minor（純加法）。**feat PR #298→merge→tag v3.28.0→GitHub Release（アセット3点）→npm publish 済み（latest 3.28.0）＝リリース完全完了**〔npm トークン期限切れで publish が一度 401 になりオーナーが再ログインして実施〕。**デプロイ済み MCP v3.28.0 でも実機確認**＝同一クエリが v3.27.0 の requires-exact 拒否から **18 件**へ変わり、EXPLAIN が `pushdown applied: 対応日付 = LAST_YEAR()` / `relation: exact` / `client residual: LENGTH(a.会社名) > 1` / `relative date client evaluations: 0` を表示、fetch する `fields` に residual 用の `会社名` が含まれることも確認 | 機能 | [Phase B spec](internal/ksql_b76_join_pushdown_phase_b_spec.md) / [eval](internal/ksql_b76_join_predicate_pushdown_extension_evaluation.md) / [実機証跡](internal/evidence/b76_phase_b_browser_smoke_steps.md) |
| **v3.27.0** | **B79 外部結合の検索打ち切りを fail-closed／B80 engine ライブラリの reason 保持（2026-07-27）**。テーマ＝**失敗時の挙動を正直にする**（どちらも**利用者に誤った情報を返していたのを止める**）。**B79＝破壊的変更**＝プラグイン/CLI/MCP で `LEFT`/`RIGHT JOIN` を含むクエリの検索が 10 万件で打ち切られたら `SearchAbortedError`。**従来は「行が減る」のではなく「値が間違う」**〔右表だけ打ち切りで結合相手が欠けると LEFT は `rows=[{目標金額:""}]`（正しくは 300）＝null 拡張されて「該当なし」という嘘の値。INNER は `rows=[]` で行の欠落＝返る行は正しい〕。**行が減るのは警告で分かるが値が間違うのはその行自体が正しく見えるため気づけない**（集計で合計・平均が静かにずれる）。**現在成功して見えるクエリは実際には誤った値を返しているのでエラー化しても正しい結果を失わない**（B78 と同じ判断基準）。**判定は「AST 上の外部結合の有無」という意味的な条件に直接紐づけ**、CTE 本体・一時テーブル source・`UNION` 枝・入れ子 SELECT まで再帰走査＝**B76 Phase A で `"joinPlan" in pushdownPlan` という plan の存在で判定し押し下げ皆無のクエリまでエラーにした失敗を回避**。**影響なし**＝プラグイン/CLI/MCP の `INNER JOIN`・単一表は従来どおり警告＋部分結果／**engine ライブラリは元から全クエリ形で `SEARCH_ABORTED` の hard error**（プログラム API では部分結果が黙ってアプリロジックへ流れ込むほうが危険なため意図的に厳格＝面ごとの差）。**案 B（警告文の是正）は不実施**〔案 A で誤値経路が消えるため残る警告経路は行の欠落のみとなり現行文言が正確になる〕。**B80＝非破壊**＝`statementGuard.ts` が `parseSqlStatement()` の例外を `PARSE_ERROR` 以外は汎用 parse error へ置き換えていたため engine ライブラリだけ**構文は正しいのに「SQL statement could not be parsed」と偽っていた**。`KlikeValidationError` を **class identity（`instanceof`）で allowlist**〔`name` を `"ArgumentError"` に上書きしているため name 一致だと将来の別クラスを誤って通す〕し、**`code = PARSE_ERROR` と `cause` の identity を維持したまま具体的な reason を返す**＝利用者コードは壊れない。**影響は KLIKE のみ**〔`src/core/sql.ts` の後段検証は `validateKlikeStatement()` だけで他は parser が先に拒否〕で **11 条件**を回帰固定。**B76 §17 の 4面 parity 緩和も同一 merge で撤回**。全 npm test 183 suites/4,735 green・snapshot 22 不変・配布 smoke 全 green。**実機 PASS**＝ブラウザ プラグイン（オーナー実施・`LEFT JOIN` が正常に結果を返す＝過剰拒否なし）＋デプロイ済み MCP v3.27.0（KLIKE を含む OR が具体的な reason で拒否）。**B70 のガードが 11 件＋`current release heading` の欠落を検出**（前回と違う形の漏れも捕捉）。SemVer=minor。**feat PR #295→merge→tag v3.27.0→GitHub Release（アセット3点）→npm publish 済み（latest 3.27.0）＝リリース完全完了** | 正しさ | [B79 issue](internal/ksql_b79_outer_join_search_abort_issue.md) / [B79 計画](internal/ksql_b79_outer_join_search_abort_plan.md) / [B80 spec](internal/ksql_b80_engine_library_reason_spec.md) |
| **v3.26.0** | **B76 Phase A JOIN 述語の APP 別 prefilter（2026-07-27）**。**性能改善であり挙動変更なし**＝押し下げ後も元 `WHERE` を client で再評価するため**結果は不変**で、records API から取得する候補件数だけが減る。**問題の再定義**＝「日付が押し下げられない」ではなく**押し下げ機構が単一表と分断されていた**〔単一表は WHERE 全体を `whereToKintone()` で直列化／JOIN は AND スパインから安全なリーフだけ抽出という別機構。**穴は DATE と TEXT**〕。**方針は A-1 → A-2 → A-2'（ハイブリッド）と二度見直し**＝codex レビューで Claude の主張3点が却下〔①単一表で押し下がる事実は「kintone が受理する」ことしか示さず**サーバー評価 ≡ JS 評価ではない**・同値性が要るのは**residual 除去時だけ**②`whereCapability`（REST 受理性）と `wherePredicatePushdown`（JOIN プレフィルタの安全性）は**異なる契約**で統合は危険③A-1 は段階案として価値がある〕。**中核成果物＝型×演算子×exact/superset/unsafe 対応表**〔`superset` は元 WHERE を再評価するため採用可・**完全同値性が要るのは residual 除去または server-only node の消費時だけ**〕。**Step 1**plan 型・ownership〔`OWNED`/`AMBIGUOUS`/`UNKNOWN`・非修飾はちょうど1 source のときだけ〕・E/S/U classifier・tree 合成→**Step 2** INNER JOIN 単一 alias の AND leaf に DATE/TEXT の `=` を superset で追加〔serializer 前の ownership 再検査で **alias を黙って捨てて別 APP へ送るのを fail-loud で防止**〕→**Step 3** 同一 alias `OR`/`GROUP`・AND 因子分離・既存押し下げの plan 統合→**Step 4** EXPLAIN plan renderer・limits・負例→**Step 5** parity・docs。**実装前に仕様の欠陥を2件検出**＝①**§5.4 と KLIKE residual 契約の衝突**〔`KLIKE ∨ superset` を OR ごと押し下げると `appliedKlikes` が無条件 `true` を返すため**superset の偽陽性行が residual で除去できず誤結果**。KLIKE を含む OR を非採用へ訂正・現行挙動への後退なし〕②**SearchAborted fail-closed の非対称**〔再検証で**危険なのは INNER ではなく LEFT/RIGHT**（誤った値を返す）と判明し実装が危険度と逆向きだったため撤回〕。**副産物として B79・B80 を起票**（いずれも B76 由来ではない既存欠陥）。**B70（リリース時の版数同期ガード）も同梱**＝`scripts/version-sync-guard.mjs` を新設し `prepack` と `npm test` に配線、`build.mjs` が `prod/manifest.json` を自動同期、`engine-docs-examples-smoke` のハードコードを廃止。**7 ケースの破壊テストで全箇所の検出を確認**。**初の実戦投入**＝`package.json` のみ更新した時点で **11 件を検出**し、`prod/manifest.json` は `build.mjs` が自動同期＝v3.25.0 で漏らした2箇所が予防と検出の二層で塞がれた。全 npm test 182 suites/4,701 green・snapshot 22 不変・配布 smoke 全 green。**実機 PASS**＝ブラウザ プラグイン（オーナー実施）＋デプロイ済み MCP v3.26.0〔EXPLAIN が `pushdown applied: 都道府県 = "長野県" and $id <= 3000` / `relation: superset` / `residual: original WHERE` を表示・**単一表 1684 件と JOIN 経由 1684 件が一致**＝結果不変を実データで確認〕。SemVer=minor（純粋な性能改善）。**feat PR #292→merge→tag v3.26.0→GitHub Release（アセット3点）→npm publish 済み（latest 3.26.0）＝リリース完全完了** | 性能 | [B76 Phase A spec](internal/ksql_b76_join_pushdown_phase_a_spec.md) / [計画](internal/ksql_b76_join_predicate_pushdown_impl_plan.md) / [eval](internal/ksql_b76_join_predicate_pushdown_extension_evaluation.md) |
| **v3.25.0** | **B75 相対日付を CTE 本体・一時テーブルでも／B77 kintone クエリ関数の fail-closed 統一／B78 ログインユーザー絞り込み（2026-07-27）**。**破壊的変更を含む minor**（`^3` に自動更新で届くため移行案内を CHANGELOG・言語リファレンス・`release/README.txt` の3箇所に具体例＋回避策付きで記載）。**B75**＝実体化 CTE 本体／`WITH` 最終 SELECT／`CREATE TEMP TABLE ... AS SELECT`・`AS WITH`／単一 CTE インライン展開で、その SELECT の `WHERE` 全体が exact なら相対日付を使える。**原因は `collectWith()` の無条件 FORBIDDEN だけで runtime は既に完成**（実験で確認）＝変更は guard に集約。Step 3 で **Steps 1-2 が `inheritedForbidden` を無効化していた回帰も修正**。**B77**＝`TODAY()`/`NOW()`/`LOGINUSER()` を相対日付 12 関数と**同じ allow-form を共有**させ、関数 occurrence が client residual に残る形だけ拒否＝**押し下げできる形はむしろ増えた**（`WHERE 日付 = TODAY() AND LENGTH(件名) > 1` は従来 全件取得＋client 評価→Phase2 A で押し下げ）。`isLegacyKintoneFunction()` が型・演算子を見ず `日付 = NOW()`・`$id >= TODAY()` を exact と誤分類しえた既存バグも解消。**B78**＝**`作成者 in (LOGINUSER())` が書けるように**（押し下げは kintone 公式そのままの形。`CREATOR`/`MODIFIER`/`USER_SELECT` × `in`/`not in` の singleton のみ・`グループ選択` は公式表で関数「なし」のため拒否）＋`CREATOR`/`MODIFIER`/`CHECK_BOX`/`MULTI_SELECT` の `in`/`not in` 以外を取得前に拒否（従来は **silent 0 rows**）。**仕様は codex 起草 R1 を Claude が公式ドキュメントで裏取りして承認**（型×演算子×関数表が完全一致・`日付` に `NOW()` が無い点まで）。**既存 docs の誤り3件を是正**＝「`UNION` の枝は fail-closed」「DML の対象選択と `INSERT`/`UPSERT…SELECT` source は fail-closed」「`KORDER BY` は fail-closed」はいずれも誤りで、実際は whole-WHERE exact なら可。MCP function catalog の「LOGINUSER resolves to an empty string in Node/MCP」も実態（全実行面で空文字・WHERE では fail-closed）へ是正。**B74（言語リファレンス §5 の記述是正）が直したのは「一覧の漏れ」で「一覧の嘘」は残っていた**という構図。全 npm test 177 suites/4,607 green・snapshot 22 不変・`mcp:verify`／`engine:docs-smoke` green。**実機 PASS**＝ブラウザ プラグイン（オーナー実施）＋デプロイ済み MCP v3.25.0〔`作成者 in (LOGINUSER())` が押し下げ・`作成者 = '...'` が `WHERE_OPERATOR_INVALID_FOR_FIELD_TYPE`・`TODAY()` の Phase2A が `YESTERDAY()` と同一 EXPLAIN〕。**リリース準備で `prod/manifest.json` の bump 漏れにより zip 名 v3.25.0／中身 manifest 3.24.0 の成果物を配布する寸前だった**（オーナー指摘で発覚・再ビルド。再発防止は B70）。**feat PR #288→merge→tag v3.25.0→GitHub Release（アセット3点）→npm publish 済み（latest 3.25.0）＝リリース完全完了** | 機能・正しさ | [B77/B78 spec R1](internal/ksql_b77_b78_kintone_function_fail_closed_spec.md) / [B75 計画](internal/ksql_b75_relative_date_cte_temp_impl_plan.md) / [言語 §5](ksql_language_reference.md) |
| **v3.24.0** | **B72 相対日付を集計クエリでも使えるように（第3許可形 `FULL_SCAN_EXACT`）（2026-07-26）**。`WHERE` 全体が押し下げ可能なら **`GROUP BY`/`DISTINCT`/集計/window/通常 `ORDER BY` を含む文でも相対日付を使える**（例 `WHERE 日付 = THIS_MONTH() GROUP BY 区分`・従来は `WHERE_RELATIVE_DATE_REQUIRES_EXACT_PUSHDOWN` で拒否）。**解消した逆転**＝押し下げ不能述語を AND で足すと通るのに純 exact だと拒否される非対称（許可形が2つ〔v3.20.0 SIMPLE＋whole exact／v3.21.0 prefilter＋残余〕しかなく「whole exact＋FULL_SCAN」がどちらにも当てはまらなかった）。本来もっとも安全な形（whole WHERE を server へ・client 残余評価なし・相対日付 client 評価 0）。fully-exact な `OR` も対象。**重要な設計点**＝①通常 `ORDER BY` は static SIMPLE でも local 処理になるため第3許可形の条件に **canonical ORDER BY の存在**を含める②`executeSimpleSelect` に client filter が無いためその形は **guard 許可だけで成立**（runtime 配線不要）③`allowOriginalWherePushdown = exact && prefilterPlan === undefined` で既存の内部エラー防御と排他し whole WHERE を一度だけ serialize。**fail-closed 維持**＝KORDER（native/Cursor）・DML 対象選択・INSERT/UPSERT…SELECT source・JOIN・VALIDATE・subtable・temp/実体化 CTE/派生表・OR/NOT で非 exact。**取得上限はリテラル日付と同一挙動**（`onLimit=truncate` なら truncate・既定 error ならエラー）で相対日付ゆえの追加制約なし／検索打ち切り（10 万件）は利用者が選んだ設定でないため fail-closed 維持。**フロー**＝eval→仕様 R1（codex 起草→Claude 実コード裏取り）→**4 Step**（①pure builder〔未配線〕②**guard 緩和と runtime を同一 merge**〔guard 先行＝backstop 漏れ／residual 抑止先行＝一般 exact FULL_SCAN の client filter 消失、を仕様が明示禁止〕③EXPLAIN・B71 合成④4面 parity・docs）。**実機 smoke が過剰制約を炙り出した**＝`RELATIVE_DATE_FULL_SCAN_EXACT` complete-input reason により相対日付だけ truncate 不可という非対称（Claude の Step 2 指示の過剰指定）→オーナー決定 A で撤回し成果物を再ビルド。B71（v3.23.0）の GROUP BY 解決計画・強制 fetch 列は不変。全 npm test 167 suites/4,440＋CLI 26 green・snapshot 22 不変・配布 smoke 4種 passed。**実機 PASS**＝プラグイン（ユーザー実施）＋デプロイ済み MCP v3.24.0 で `FROM_TODAY(-3650,DAYS)+GROUP BY` が3グループ〔山梨503/岐阜813/長野1684〕・KORDER は拒否維持。SemVer=minor（純加法・既存の成功クエリの結果は不変）。**feat PR #277→merge→tag v3.24.0→GitHub Release（アセット3点）・npm publish 済み（latest 3.24.0）＝リリース完全完了** 。**B74（言語リファレンス §5 の相対日付 fail-closed 記述の是正・docs のみ）も本リリースに同梱**＝fail-closed 一覧に `GROUP BY`/`DISTINCT` 等の記載漏れがあった件を是正し `ksql_docs` へ反映済み。ただし**同じ一覧に別の誤りが3件残っていた**ことが後日判明（`UNION` の枝／DML の対象選択・`INSERT`…`SELECT` source／`KORDER BY`）。前2件は B75 Step 4、`KORDER` は B77/B78 Step 5 で是正 | 機能 | [B72 spec R1](internal/ksql_b72_relative_date_fullscan_exact_spec.md) / [eval](internal/ksql_b72_relative_date_fullscan_exact_evaluation.md) / [言語 §5](ksql_language_reference.md) |
| **v3.23.0** | **B71 `GROUP BY` エイリアスの誤集計修正（silent wrong results）（2026-07-26）**。`GROUP BY` に SELECT のエイリアスを指定すると**エラーにならないまま全行が1グループへ潰れる**バグを修正（例 `SELECT DATE_FORMAT(作成日時,%Y-%m) AS 年月, COUNT(*) … GROUP BY 年月` が件数=全件を返していた）。B51 と同種の silent wrong results で BI 用途で最も危険なクラス。**⚠ 結果が変わる**（従来値は誤り）。**解決順＝同名フィールド優先→SELECT エイリアス**（標準 SQL/MySQL と同じ。`ORDER BY` がエイリアス優先なのは出力行への操作だからで意図的な非対称）。フィールドは物理列・`$id`/`レコード番号` 等システム列・subtable の `_pid`/`_rid`/`_idx`/`_p.<親>`・CTE/temp 実体化列を含み、**判定は schema ベースで fetch の偶然に依存しない**。**shadow ケースも修正**＝エイリアスが実フィールド名と衝突すると実フィールドが取得対象から外れて誤集計になっていた（PHYSICAL 確定列は必ず fetch）。取得前拒否＝集計エイリアス/集計内包 CASE・文字列関数・連結/集計合成名/`GROUPING()`・window/GROUP BY が解決する重複エイリアス/JOIN 非修飾曖昧/unknown。CTE 本体・UNION 両枝・scalar subquery・DML source・`CREATE TEMP TABLE AS SELECT` の5経路に適用。B65 ROLLUP/GROUPING SETS は物理フィールドのみ・B59 ORDER BY/HAVING/DISTINCT は不変。**フロー**＝Pro 報告→調査（プローブで再現・根因特定）→仕様 R1（Claude）→codex レビュー「実装不可」→R2（Claude）→codex 再レビュー「まだ実装不可・P1 5件」→**役割交代し R3 を codex 起草→Claude レビュー**→**4 Step 実装**（①基盤・未配線 ②schema-aware planning＋PHYSICAL fetch＝誤結果ゼロの中間状態 ③エイリアス解決 ④非回帰・docs）。**Claude レビューで計5件捕捉**（UNKNOWN の独自 throw が B51 検証を横取り／B65 drift guard 未更新／`isSystemLikeFieldCode` 統合で検証が緩む／ドット入りエイリアスで取得前拒否の契約違反／スコープ外の UI 変更）。**教訓＝プローブのモックが実 kintone の fields projection を再現しないとバグを隠す**（R1 の中核前提が artifact だった）。全 npm test 165 suites/4,382＋CLI 26 green・snapshot 22 不変。**実機 PASS**＝デプロイ済み MCP v3.23.0 で `GROUP BY 県`（alias）が3グループ〔山梨503/岐阜813/長野1684＝合計3000〕を返し実フィールド指定と完全一致。SemVer=minor（B59 v3.13.0 の前例）。**feat PR #274→merge→tag v3.23.0→GitHub Release（アセット3点）・npm publish 済み（latest 3.23.0）＝リリース完全完了** | 正しさ | [B71 spec R3](internal/ksql_b71_groupby_alias_phase1_spec.md) / [issue](internal/ksql_b71_groupby_alias_wrong_result_issue.md) / [言語 §8](ksql_language_reference.md) |
| **v3.22.0** | **B69 engine ライブラリ `QueryColumn` 列メタ公開（2026-07-25）**。B66 engine ライブラリ `runQuery()` が返す `QueryColumn` に列メタを後方互換で追加公開＝`fieldType?`（元 kintone 型 or 導出擬似型 `KSQL_NUMBER`/`KSQL_STRING`/`KSQL_UNKNOWN`）・`sortKind?`（`number`/`string`）・`sourceApp?`（参照元 appId）。**`sourceApp` は CTE/一時テーブルを含まない単一物理アプリ文の直接フィールド参照列（`$id` 等システム列含む）のみ**＝CASE/MIN/MAX/MODE/集計/算術の式列・JOIN 曖昧列・CTE 経由列（inline/materialize とも）は付けない（provenance opaque）。追加は全て primitive で内部型 re-export 0（型隔離維持・declaration-smoke 5 value/20 type 不変）。**kSQL の SQL 方言・パーサ・実行意味論・結果は不変**（列メタはライブラリ結果の付随情報・CLI/MCP/プラグイン・言語リファレンス不変・capture は既定 off の opt-in で runQuery のみ true・内部メタ `semantics.source`・temp append 互換も不変）。ksql-dashboard-pro 側 Claude セッションのドラフト引き継ぎを review-driven 化＝**仕様 R2（Claude 起草→自己検証＋実エンジンプローブ〔真のブロッカー特定＝`execute()` 結果コピーで列メタ WeakMap が外れ全列 bare〕→codex クロスレビュー〔§10 見落とし P1 4件＝式・集計/CTE への source 漏れ・専用フラグ要・UNION 文言・guard 名〕→CTE provenance＝opaque オーナー決定）→ codex 実装→Claude 実コードレビュー**。P1-1 は返却オブジェクトへ meta 引き継ぎ・publicSourceApp 方式（forLibraryCapture フラグ・直接 FIELD/WILDCARD 列のみ）で source 漏れ/CTE/実体化回帰を一挙解決。全 npm test 161 suites/4,250＋CLI 26 green・snapshot 22 不変・build 全面・engine guard 群（docs-smoke/declaration-smoke/bundle-guard）green。SemVer=minor（純加法・既定 off）。**feat PR #269→merge→tag v3.22.0→GitHub Release（アセット3点）・npm publish 済み（latest 3.22.0・npm view 確認）＝リリース完全完了**。副次で engine:docs-smoke の version pin を 3.22.0 へ同期（毎リリース陳腐化する設計 smell）。Pro 側 vendoring は Pro スコープ | 機能 | [B69 spec R2](internal/ksql_b69_query_column_metadata_phase1_spec.md) / [eval](internal/ksql_b69_query_column_metadata_evaluation.md) / [利用ガイド](ksql_engine_library.md) |
| **v3.21.0** | **B67 Phase2 A 相対日付 prefilter＋残余 client 評価（SUPERSET_PREFILTER）（2026-07-25）**。相対日付 exact leaf が「相対日付を含まない残余」（例 `LENGTH(都道府県) > 1`・通常 `LIKE`）と `AND` で結ばれた**単一物理アプリ SELECT** で、相対日付 leaf だけを kintone REST クエリの prefilter として exact pushdown し取得後は残余だけを client 評価（例 `WHERE 更新日時 >= YESTERDAY() AND LENGTH(都道府県) > 1` → server prefilter `更新日時 >= YESTERDAY()` ／ client residual `LENGTH(都道府県) > 1`）。v3.20.0 では文全体 fail-closed だったケースを共存させる。**相対日付の client 評価は 0 回**（planner allowlist＋evalWhere backstop の二段保証）。`BETWEEN` 各境界・複数 leaf・`KLIKE`/安全リーフ併用も同規則で prefilter・KLIKE identity 不変。`ksql_explain` は Phase2 計画で `where capability: SUPERSET_PREFILTER`/`server prefilter:`/`client residual:`/`relative date client evaluations: 0`/`kintone query:` を表示（純 exact は従来 `EXACT_PUSHDOWN`/`client evaluation: forbidden` を byte 不変）。**fail-closed 維持**＝OR/NOT 内相対日付・`KORDER BY`（native/Cursor）・UPDATE/DELETE 対象選択・**INSERT/UPSERT…SELECT の DML source**・JOIN 残余・VALIDATE・サブテーブル・一時/実体化 CTE・派生表。**owner 決定 A**＝DML source SELECT は spec スコープ尊重で fail-closed（`WalkCandidate.allowPhase2Prefilter`）だが **pure-exact DML source は第1許可形で非回帰**。フロー＝spec R1→Claude レビュー→R2→実装計画（codex 起草→Claude 裏取り）→**5 Step 段階マージ**（Step1 decomposition／Step2 guard 第2許可形〔実行未開通・原子性テスト〕／Step3 fetch/residual 配線＝実行開通／Step4 EXPLAIN parity・評価0証明／Step5 受入・docs・benchmark・DML source 拒否・各 codex 実装→Claude レビュー→commit）。全 npm test 159 suites/4,242＋CLI 26 green・snapshot 22 不変・build 全面・mcp:smoke ok・受入 ID 9.1-1〜9.3-4（35 tests）・benchmark exit 0。**実機 PASS**＝Firefox/Chrome プラグイン（ユーザー実施）。SemVer=minor（純加法・既存 SQL/CLI/MCP/plugin 挙動不変）。**feat PR #262→merge→tag v3.21.0→GitHub Release（アセット3点）・npm publish 済み（latest 3.21.0・npm view 確認）＝リリース完全完了**。**Phase2 B**（KORDER・DML・JOIN・VALIDATE・OR/NOT の相対日付・client 評価）は対象外＝実需待ち | 機能 | [Phase2 A spec R2](internal/ksql_b67_phase2_superset_prefilter_spec.md) / [impl plan](internal/ksql_b67_phase2_impl_plan.md) / evidence: [acceptance](internal/evidence/b67_phase2_acceptance.md)・[browser](internal/evidence/b67_phase2_browser_smoke.md) |
| **v3.20.0** | **B67 kintone REST クエリ関数（相対日付）の押し下げ Phase1（2026-07-24）**。kintone クエリ関数のうち**相対日付12関数**（`YESTERDAY`/`TOMORROW`/`FROM_TODAY(n, DAYS\|WEEKS\|MONTHS\|YEARS)`/`THIS_WEEK`/`LAST_WEEK`/`NEXT_WEEK`/`THIS_MONTH`/`LAST_MONTH`/`NEXT_MONTH`/`THIS_YEAR`/`LAST_YEAR`/`NEXT_YEAR`）を `WHERE` で使えるように（例 `WHERE 作成日時 < FROM_TODAY(5, DAYS)`）。**方針＝押し下げネイティブ（server-only）**＝関数を kintone REST クエリへそのまま出力し、リクエスト時刻・TZ・週境界・月末は kintone サーバが評価（kSQL は日付へ解決しない）。対象は日付系フィールド（`DATE`/`DATETIME`/作成日時/更新日時）×比較6演算子の `WHERE` 右辺と `BETWEEN` 境界のみ。`IN`/`NOT IN`・非日付・関数左辺は不可。**押し下げできない場合はレコード取得前に fail-closed**（client 評価へフォールバックしない）＝FULL_SCAN/JOIN 残余/集約/window/DISTINCT/通常 ORDER BY/一時テーブル/実体化 CTE/VALIDATE/サブテーブル DML/UPDATE FROM/APPLY/REORDER で関数付き比較が残ると `WHERE_RELATIVE_DATE_REQUIRES_EXACT_PUSHDOWN`。**二段保証**＝planner allowlist（`relativeDatePushdownGuard`）＋evalWhere runtime backstop（`resolveKintoneFuncValue` で相対日付名を明示 throw・legacy 3関数だけ client 解決）。EXPLAIN はサーバ評価と押し下げ後クエリを表示・`KORDER BY`（native/Cursor）も同一関数表現・既存 `TODAY()`/`NOW()`/`LOGINUSER()` は byte 不変。Node engine/CLI/MCP/プラグインで同一の受理判定と REST クエリ。**フロー**＝評価→仕様 R1→Claude レビュー〔§5.3 backstop 指摘〕→R2→実装計画→**8 Step 実装（各 codex 実装→Claude レビュー→commit）**・エンジン本体は WHERE 経路のみ加法。全 `npm test` 155 suites/4,146＋CLI subprocess 26 green・snapshot 22 不変。**実機 PASS**＝実 dev kintone〔APP730@dev〕CLI 8ケース（FROM_TODAY/YESTERDAY のサーバ相対評価が実データ整合・KORDER native・BETWEEN 展開・非exact/非日付 reason 拒否・dry-run EXPLAIN API none・TODAY() 非回帰）＋Firefox/Chrome プラグイン smoke＋**デプロイ済み MCP（v3.20.0）で FROM_TODAY+KORDER が EXACT_PUSHDOWN/client evaluation forbidden・`YESTERDAY() AND LENGTH()>1` が fail-closed** を確認。SemVer=minor（純加法・既存 SQL/CLI/MCP/plugin 挙動不変）。**feat PR #259→merge→tag v3.20.0→GitHub Release（アセット3点）・npm publish 済み（latest 3.20.0・npm view 確認）＝リリース完全完了**。**Phase2 最優先候補＝SUPERSET_PREFILTER**〔相対日付 exact leaf と押し下げ不能述語（LENGTH/LIKE 等）の AND は Phase1 では文全体 fail-closed → 相対日付 leaf を prefilter 押し下げ＋残余のみ client 評価で共存・spec R1 起草済み・v3.21.0 候補〕 | 機能 | [B67 spec R2](internal/ksql_b67_rest_query_functions_phase1_spec.md) / [Phase2 A spec R1](internal/ksql_b67_phase2_superset_prefilter_spec.md) / [impl plan](internal/ksql_b67_impl_plan.md) / evidence: [acceptance](internal/evidence/b67_relative_date_acceptance.md)・[browser](internal/evidence/b67_relative_date_browser_smoke.md) |
| **v3.19.0** | **B66 read-only kSQL エンジン・ライブラリ公開 Phase1（2026-07-24）**。kSQL の read-only エンジンを他の kintone プラグイン／カスタマイズから利用可能に。**公開面**＝npm subpath `@rex0220/kintone-sql-tools/engine`（ESM/CJS＋`.d.ts`）と UMD `window.ksql` version registry。API は `version`/`createReadonlyKintoneClient()`/`runQuery()`/`explainQuery()`/`KsqlEngineError`＋専用 DTO のみ（5 value/20 type・**内部型は一切 re-export しない型隔離**・結果セルは全て文字列）。**安全境界**＝①parse 後の再帰 allowlist（WITH 全 CTE・UNION 左右まで検査・未知 type は default deny）②write 3メソッドを持たない client 射影（Proxy write-trap＝`in`/ownKeys で不在・bypass 時も生 TypeError でなく clean `READ_ONLY_VIOLATION`）の**二重強制**。検索打ち切りは常時 `SEARCH_ABORTED` hard error（部分結果なし・既存 plugin の warning 挙動は不変）。Cursor は per-query close・lease/lifecycle は instance 内。**複数プラグインの別バージョン同居**＝registry が別版非上書き・同版重複 warn・非 registry 衝突 fail-closed、global 副作用 0（listener 増加0・`kintone.api` identity 不変）、Cursor lease は非協調のため合計 `cursorMaxActive`≤5 運用を明記。**設計判断 A**＝Step 1 の import グラフ監査で `execute()` が DML/APPLY/IMPORT を同梱すると判明→安全性は二重強制で担保されるため dead code 同梱を許容し §4.4 を forbidden 群 0 維持へ緩和（read router のクリーン抽出は fast-follow）。**フロー**＝評価→仕様 R1→Claude レビュー→R2→実装計画→**9 Step 段階マージ**（各 Step: codex 実装→Claude レビュー→commit・gate＝npm test 全 green/build 5面/snapshot 不変/**エンジン本体無改変**）。全 3,184 green・bundle baseline ESM 119,684 B gzip（forbidden 0）・pack 済み ESM/CJS/UMD で docs 例実行確認・**Firefox/Chrome 実機 PASS**（engine fixture〔primary・2版共存〕＋plugin 非回帰）。SemVer=minor（純加法・既存 SQL/CLI/MCP/plugin 挙動不変）。**feat PR #254→merge→tag v3.19.0→GitHub Release（アセット3点）・npm publish 済み（latest 3.19.0・npm view 確認）＝リリース完全完了**。Phase2＝`runMutation()`（DML）・partial-on-abort opt-in・read router 抽出 | 機能 | [B66 spec](internal/ksql_b66_engine_library_phase1_spec.md) / [利用ガイド](ksql_engine_library.md) / [impl plan](internal/ksql_b66_impl_plan.md) / evidence: [acceptance](internal/evidence/b66_engine_acceptance.md)・[browser](internal/evidence/b66_engine_browser_smoke.md)・[import graph](internal/evidence/b66_engine_import_graph.md) |
| **v3.18.0** | **B65 小計・総計 Phase2＝CUBE / HAVING 内 GROUPING() / SELECT DISTINCT 併用（2026-07-23）**。Phase1（v3.17.0）で対象外にしたコア4件をリリース。**#1 field-only CUBE**＝`GROUP BY CUBE(a,b)` を全 `2^n` grouping set へ糖衣展開（各軸小計＋総計）・engine 新設なしで Phase1 の縦結合/空文字上書き/sidecar を共用・`2^n` を配列生成前に倍加検査し上限（既定 64 set）超の列数（7 列以上）は取得前 `GROUPING_SET_LIMIT_EXCEEDED`。**#2 HAVING 内 GROUPING()**＝`HAVING GROUPING(会社名)=1`（総計・小計）/`=0`（明細）を集計条件と AND・membership 判定・WHERE/JOIN/集計引数/window は拒否維持。**#5 SELECT DISTINCT 併用**＝Phase1 の一括拒否を解除・`project()` と `applyDistinct()` の列投影を単一 evaluator（`evaluateSelectColumnValue`）へ統合し SELECT 出力列全体（列位置＋値）で dedupe・集計は materialize 値のみで再集計しない・`GROUPING()` 投影で明細0/総計1 別行/非投影で表示値一致行を集約・`GROUP BY DISTINCT` は拒否維持。**#8 static validate 一致**＝`SELECT DISTINCT ... ROLLUP` を純 AST 段階でも受理し v3.17.0 の「validate ok/実行拒否」を解消（PR #246 で先行 main 済み）。フロー＝codex Phase2 評価→**各機能を独立に codex 実装→Claude レビュー＋独立 test/build＋branch dist-cli 実データプローブ→コミット**。全 3,000+25 green・snapshot 21 不変。**実機 PASS**＝デプロイ済み MCP で CUBE+HAVING GROUPING+DISTINCT 統合（明細除外の小計・総計15行・3軸とも合計20整合）・ksql_docs §8 に CUBE/HAVING/DISTINCT 反映。SemVer=minor（既存 SQL 不変・純加法的）。**feat PR #247→release PR #248→tag v3.18.0→GitHub Release（アセット3点）・npm publish 済み（latest 3.18.0・npm view 確認）＋release/ の mcpb/mcp.js 更新漏れを PR #249 で 3.18.0 へ同期＝リリース完全完了**。残＝第二波 #6 CTE/temp 実体化列 grouping item・別仕様 #3 式 item/#7 window 併用・#4 GROUPING_ID（B65-P2 §1 に継続） | 機能 | [B65 Phase2 eval](internal/ksql_b65_phase2_evaluation.md) / [言語 §8/§9](ksql_language_reference.md) / [recipes R13](ksql_batch_recipes.md) |
| **v3.17.1** | **B65 の解説・レシピを docs へ反映（docs のみ・2026-07-23）**。バッチレシピ集に **R13「明細＋小計・総計を1クエリで作る（ROLLUP / GROUPING）」** 追加（看板 ROLLUP クエリ・実データ総計値・複数列/GROUPING SETS の階層・Phase1 制約）。言語リファレンス §8/§10 の B65 例を `APP4149`→汎用 `APP100` に統一（実在アプリに無い「地域」参照の解消）。**リリース検証で発見・修正した埋め込みバグ**＝`docsResourceBuilder.cjs` の recipe 見出しパターンが R1-R12 固定（`count:12`・`R([1-9]|1[0-2])`）で R13 が ksql_docs に embedding されず `recipes/r13` 未生成→**ksql_docs を SDK Client で駆動する独立プローブで捕捉**→`count:13`＋`R([1-9]|1\d)` へ修正・波及する section-key 固定7箇所（builder＋テスト5＋mcp-smoke）も `r1..r13` 統一。SQL 挙動不変（SemVer=patch）。全 npm test green・build/mcp:smoke/pack-smoke/mcpb-verify 成功・ビルド済み ksql_docs で R13（1810字）と §8 の APP100 を実機プローブ確認。**PR #245→tag v3.17.1→GitHub Release（アセット3点）・npm publish 済み（latest 3.17.1・npm view 確認）＝リリース完全完了** | 機能（docs） | [recipes R13](ksql_batch_recipes.md) / [言語 §8](ksql_language_reference.md) |
| **v3.17.0** | **B65 小計・総計（ROLLUP / GROUPING SETS / GROUPING）Phase1（2026-07-23）**。`GROUP BY ROLLUP(a[,b])` / `GROUP BY GROUPING SETS ((...),())` と `GROUPING(field)` で、**明細＋小計＋総計を1クエリ・1結果セット**で出せる。`GROUPING()` で総計行を判別（`CASE … '合計'`）・`ORDER BY GROUPING()` で末尾・B64 条件付き集計と併用。**設計**＝kintone にサーバ ROLLUP なし→クライアント FULL_SCAN で複数 grouping set を縦結合・除外列は空文字上書き（super-aggregate 行に先頭行値を残さない）・grouping membership は非列挙 symbol sidecar・GROUPING() は集計文脈専用 ref を SELECT/CASE 条件/direct ORDER BY へ供給（number メタで B59 4層）・完全入力必須（専用 `GROUPING_SETS` reason・truncate 不可）・3独立 guard 有界 fail-closed（set64/item16/生成行50k・Node benchmark で確定）。**field-only・重複 set 保持・後方互換 AST（optional `grouping?`＋`normalizeGroupingSpec`）**。対象外（Phase2）＝CUBE/入れ子/混在/HAVING GROUPING/GROUPING_ID/式 item/SELECT DISTINCT/KORDER/window。フロー＝評価→codex 評価→R1→codex レビュー→R2→実装計画→**6 Step 段階実装（各 codex 実装→Claude レビュー＋独立検証）**。Claude が Step 4 で consumer allowlist テストの外部 rg 依存バグを発見・pure-Node 化。全 2,947+25 green・**実機 PASS**（デプロイ済み MCP で看板 ROLLUP 実行・数値整合・CUBE/DISTINCT 拒否・ksql_docs §8 反映＋**プラグイン実機 PASS＝ブラウザ×実 kintone で2階層 ROLLUP(商談フェーズ,会社名)＋複数 GROUPING() の CASE 分岐〔フェーズ小計/総計/空セル未設定判別〕・多段 ORDER BY GROUPING を数値整合で確認**）。SemVer=minor。**feat PR #243→release PR #244→tag v3.17.0→GitHub Release（アセット3点）・npm publish 済み（latest 3.17.0・npm view 確認）＝リリース完全完了**。残＝軽微 UX（`SELECT DISTINCT+ROLLUP` 拒否を validate へも＝B65 Phase2 候補） | 機能 | [B65 spec R2](internal/ksql_b65_rollup_grouping_sets_spec.md) / [impl plan](internal/ksql_b65_impl_plan.md) / [benchmark](internal/evidence/b65_guard_benchmark.md) / [言語 §8](ksql_language_reference.md) |
| **v3.16.1** | **B64 を言語リファレンスへ反映（docs のみ・2026-07-23）**。MCP `ksql_docs` が返す言語リファレンス §4（CASE WHEN に集計引数での使用可・ELSE 省略の文脈別扱い・スカラー限定）／§8（新節「集計関数の引数に式を指定」＝12集計対応・条件付き集計/条件付き件数/横持ちピボット例・比較拒否＋CASE 誘導・ネスト集約/MODE(DISTINCT) 禁止・空値/型推論規約・MIN/MAX 空セル表記更新・MODE カテゴリ化を COALESCE→CASE へ）／§9（HAVING の CASE 例）に反映。**SQL/パーサ/エンジンの挙動は v3.16.0 から不変（SemVer=patch）**。codex 記事レビュー（続編 `qiita_ksql_処理手順2.md`・4/5）から派生。ビルド済み＋デプロイ済み `ksql_docs §8` が新内容を返すことを両面実機確認・新例3種を validate 受理確認・docs テスト11 green。**PR #242→tag v3.16.1→GitHub Release（アセット3点）・npm publish 済み（latest 3.16.1・npm view 確認）＝リリース完全完了** | 機能（docs） | [B64 spec R2](internal/ksql_b64_aggregate_case_expression_spec.md) / [言語 §8](ksql_language_reference.md) |
| **v3.16.0** | **B64 集計関数引数のスカラー値式対応（2026-07-23）**。集計関数の引数を算術式限定から `AggregateArgExpr = ArithNode \| ScalarValueExpr` へ拡張し、全12集計（SUM/COUNT/AVG/MIN/MAX/GROUP_CONCAT/統計集約/MODE）で **CASE 式・`\|\|` 連結・裸の `@var`** を受理。条件付き集計 `SUM(CASE WHEN 区分='受注' THEN 売上 END)` を直接記述可能に（従来は CTE 回避が必要）。**比較式（`SUM(売上>0)`）は kSQL に boolean 型がないため恒久拒否**し CASE 誘導の専用エラー。旧 `ArithNode` 先行の二段パースで既存 AST/結果/snapshot 不変。集計入力専用 nullable 評価で CASE の非一致（ELSE 省略）は除外・一致空セル/明示`ELSE ''`は MIN/MAX の canonical empty band に保持（三値区別）。HAVING は共通 AST serializer `aggregateSyntheticName`（`core/aggregateExpression.ts` 新設）で SELECT 列キーと byte 一致統合（空白/大小非依存）。ネスト集約三重防御。フロー＝codex 起草 R1→Claude レビュー（コード全数裏取り）→オーナー判断（比較恒久拒否・CASE+`\|\|`+@var 一括）→codex R2→codex 実装→Claude コードレビュー＋独立検証。B64 テスト28件追加・全2,858+30 green・**実機 PASS（B64 ビルドの CLI `--dry-run`＋MCP `ksql_validate` 両面）**。SemVer=minor。**PR #240（実装）→release PR #241→tag v3.16.0→GitHub Release 公開（アセット3点）・npm publish 済み（latest 3.16.0・npm view 確認）＝リリース完全完了**。デプロイ済み MCP（VSCode 再読込後）実機 PASS＝`ksql_query` で Qiita 実クエリを1クエリ集計・記事 #7+#8 統合表と一致。Qiita 記事 `qiita_ksql_処理手順.md` に陳腐化追記済み（main 6c1b9ba・本文は歴史記録として保持し冒頭に追記）＝**B64 完全クローズ** | 機能 | [B64 issue](internal/ksql_b64_aggregate_case_expression_issue.md) / [spec R2](internal/ksql_b64_aggregate_case_expression_spec.md) |
| **v3.15.0** | **B62 AI 可視性の注記強化（2026-07-22）**。B61 の AI 行動検証（計 30 シナリオ）で観測した 6 件のまとめ対応。**#3 CHECK 参照値の混同**（唯一の意味論 FAIL・validate 不可視）を最優先に、instructions 共通注記へ「UPDATE CHECK sees pre-update values; repeat the SET expression」＋変数名誘導（529 語 exact・5 段落維持）・言語 §16 相互参照（CHECK 評価行は文種別＝UPSERT は update 分岐でも入力ソース行等）・§25 に @変数の配置詳細表（**使える A01-A13/使えない R01-R05＝パーサ特性化テストと ID 一対一**・SET サブクエリ内先行変数の例外・変数名 regex/64 字/小文字正規化・回避レシピ）・`ksql_mutate` description 二重化＋部分文字列 guard。codex レビュー P1×10 で **R1 の変数配置表自体の不正確を検出**（B60 R1 と同じ轍＝レビュー機構の再実証）。**効果実証＝摩擦シナリオ同一プロンプト再実行で意味論 FAIL 1→0（SET 式再掲＋誤りパターンを反例として説明）・自己修正 計 5→1**（[効果実証](internal/evidence/b62_visibility_notes_smoke.md)）。SQL 挙動不変・SemVer=minor。全 2,858 green。**PR #226（実装）→release PR #229→tag v3.15.0→GitHub Release 公開（アセット3点）・npm publish 済み（latest 3.15.0・npm view 確認）＝リリース完全完了** | 機能 | [B62 issue](internal/ksql_b62_ai_visibility_notes_issue.md) / [効果実証](internal/evidence/b62_visibility_notes_smoke.md) |
| **v3.14.0** | **B60 MCP Statement syntax catalog（2026-07-22）**。Claude Desktop が INSERT の `ON ERROR` 構文を発明した実測（B55 の構文版＝機能名/意味論はあるが文法骨格が無い）への対策。**instructions へ Statement templates 段落**（CHECKS/CONTROL 共通記法＋18 family 骨格＋句順・併用規則＋completeness 宣言・502語 exact・5段落）・`ksql_query`/`ksql_mutate` description へ構文テンプレート・行動規範「初出文型は ksql_docs で確認・発明禁止」。**機械 guard**＝`statementSyntaxCatalog.ts`（`satisfies Record<StatementSyntaxId,Entry>` で family 全数をコンパイル時保証・全 example を parse＋expectedTypes＋batch は analyzeBatch＋負例5種＝「カタログに載る構文は必ず通る」）・mcpb-verify に launcher instructions 検証新設（stale MCPB 検出）・言語 §24 の実 drift 修正（EXPLAIN 一覧へ VALIDATE/IMPORT）。仕様 R1→codex レビュー（P1×8・**R1 自身の発明構文2件検出**＝機構の必要性の生きた証明）→R2。全2,840 green。**リリース前 AI 行動検証 両面 PASS**＝Claude Code headless×2（新ビルド明示指定・1回目で発見したカタログ括弧表記の摩擦を修正→2回目初回正解）＋**Claude Desktop（発端環境・ユーザー実施）＝`ON ERROR SKIP INTO #err REJECT LIMIT 100` を一発正配置・ksql_docs 先読み・Tier 0/ランタイム境界の正確な説明**（[evidence](internal/evidence/b60_syntax_hints_smoke.md)）。SemVer=minor（MCP のみ・SQL 挙動不変）。**PR #213（実装）→release PR #214→tag v3.14.0→GitHub Release 公開（アセット3点）・npm publish 済み（latest 3.14.0・npm view 確認）＝リリース完全完了** | 機能 | [B60 issue](internal/ksql_b60_mcp_syntax_hints_issue.md) / [spec R2](internal/ksql_b60_mcp_syntax_hints_spec.md) / [evidence](internal/evidence/b60_syntax_hints_smoke.md) |
| **v3.13.0** | **B56 統計集約＋B57 日付集計軸＋B58 MODE＋B59 ORDER BY alias 修正（2026-07-22）**。**B56**=`STDDEV_POP`/`STDDEV_SAMP`/`VAR_POP`/`VAR_SAMP`/`MEDIAN`（Welford 先頭値オフセット・完全入力必須＝`completeInputReasons` 分離で truncate は error 強制・未定義統計量=空文字・非数値/非有限は ArgumentError・統計5関数のみ数値単位 DISTINCT・型メタ5経路・`*`/OVER/無印 STDDEV は ParseError。規約は主要4 RDB 前例調査で確定＝spec §10）／**B57**=`DAYOFWEEK`(1=日曜)/`QUARTER`/`WEEK`（**ISO-8601 固定**・MySQL WEEK() 非互換明記）＋`DATE_FORMAT` `%w`/`%a`(kSQL 定義日本語曜日)/`%v`/`%G`（週次ラベル `%G-%v` 推奨・新3関数は妥当日付検証＝不正は空文字・新4指定子は不正日付時のみ個別空置換・`applyDateFormat` 単一パス化で既存9指定子は出力不変・数値意味型3集合）／**B58**=`MODE` 最頻値（文字列頻度・**タイは canonical 最小＋raw コードポイント二次比較の total order**・空集合=空文字・DISTINCT/`*` ParseError・STATISTICAL_AGGREGATE 共用・型メタは MIN/MAX 方式の source 継承＋SET @var は source meta 数値判定）／**B59**=ORDER BY alias 黙殺（silent wrong order）修正（B57 実機で発見。**alias 完全一致優先で値解決・semantics・取得列解析の三層統一**・SELECT 列から事前コンパイルする alias evaluator を FULL_SCAN/SIMPLE のみに供給・OVER 内非解決・`$id`/ドット alias の REST top-N/KORDER planner guard・未知キーは既存 ORDER_KEY_UNRESOLVED fail-closed 維持・**従来 no-op だった alias ORDER BY の結果順が変わる**）。予約語9語追加・カタログ aggregate 12/scalar 46・instructions 277語 exact。全フローで仕様 R1→codex レビュー→R2→codex 実装（**git 操作は Claude 側＝sandbox .git 拒否**）→Claude コードレビュー→npm test 独立実行→CLI 実機照合。全2,834 green・**CLI 実機 PASS（evidence 4点＝B56 統計6値外部照合一致/B57 ISO週境界/B58 実データタイ決定性/B59 before-after 全✅）**。SemVer=minor。**PR #205/#206/#207/#209→release PR #210 マージ→tag v3.13.0→GitHub Release 公開（アセット3点）・npm publish 済み（latest 3.13.0・npm view 確認）＝リリース完全完了** | 機能・正しさ・性能 | [B56 spec](internal/ksql_b56_statistical_aggregates_spec.md) / [B57 spec](internal/ksql_b57_date_axis_functions_spec.md) / [B58 spec](internal/ksql_b58_mode_aggregate_spec.md) / [B59 fix spec](internal/ksql_b59_orderby_alias_fix_spec.md) / evidence: [B56](internal/evidence/b56_statistical_aggregates_smoke.md)・[B57](internal/evidence/b57_date_axis_smoke.md)・[B58](internal/evidence/b58_mode_aggregate_smoke.md)・[B59](internal/evidence/b59_orderby_alias_smoke.md) |
| **v3.12.0** | **B55 MCP read-only ドキュメントツール `ksql_docs`＋全量関数カタログ instructions（2026-07-21）**。Claude Desktop のデバイスブリッジ（`remote-devices` プロキシ）が **tools のみ中継し resources/prompts capability を通さない**実測（B50 の言語リファレンス resource に到達不能→AI が `ksql_validate` 総当たりで不完全な関数洗い出し）を受け、tool 経由のドキュメント導線を追加。**`ksql_docs`**=引数なし→統合インデックス（全 40 キー）／`section`→B50 resource と同一キー語彙（`ksql://` URI 形受理・trim のみ・曖昧マッチなし）で 1 章の markdown。成功 text-only・未知キーは既存 envelope バイト互換 ArgumentError（有効キー案内）・schema 層 `-32602` の二層契約・固定 map lookup のみ（kintone API/資格情報/bundle runtime fs 不触・config 不在でも応答）・annotations readOnlyHint。**instructions 4 段落再構成＋全量関数カタログ**（scalar 43/aggregate 6/window 3/contextual 3=TODAY・NOW・LOGINUSER/alias 5=SUBSTR・CONVERT・CEILING・TRUNC・POW/syntax・実測 257 語・guard 240-280）＋「validate 試行錯誤での関数推定禁止」。パーサ受理集合を frozen 定数 export（挙動不変）し catalog⇔parser⇔fixture の三者双方向 drift guard で固定。配布 3 面 exact 検証強化（mcpb-verify exact list 化＋実呼び出し・mcp-smoke envelope バイト一致＋io-guard tool 適用・pack-smoke 全 40 キー standalone）。言語 §5 に SUBSTR＋alias 対応注記。全 2,729 テスト green。**実機 PASS**=発端の resources 非対応経路でプローブ抑止＋全量関数回答・STDDEV 不存在即答（[evidence](internal/evidence/b55_claude_desktop_smoke.md)）。SemVer=minor（plugin/CLI の SQL 挙動不変）。**PR #198→tag v3.12.0→GitHub Release 公開（アセット3点）・npm publish 済み（latest 3.12.0・npm view 確認）＝リリース完全完了** | 機能 | [B55 issue](internal/ksql_b55_mcp_docs_tool_fallback_issue.md) / [spec R2.1](internal/ksql_b55_mcp_docs_tool_spec.md) / [impl plan](internal/ksql_b55_impl_plan.md) / [evidence](internal/evidence/b55_claude_desktop_smoke.md) |
| **v3.11.0** | **B51 複数 CTE の CTE 間 JOIN 誤結果 修正＋B52 単一 CTE 列別名インライン化 unknown field 修正（2026-07-21）**。**B51**（silent wrong results・優先度高）=CTE/一時テーブル参照が明示 alias なしだと `alias:null`→最終 JOIN の tables Map が null キー衝突で片側上書き＋修飾キー欠落で空文字直積（左列空・行重複・LEFT 未一致欠落）。修正=`effectiveTableAlias=alias??cteName` を識別/JOIN/WHERE/SELECT/ORDER/GROUP/メタ解決に一貫適用（明示 alias 優先・SELECT * 出力は非修飾維持）＋alias 衝突/cache miss を fail-closed＋applyJoin が空文字値と構造的キー欠落を区別してエラー化。**B52**（error）=単一 CTE の改名列別名/式を `canInlineSingleCte` がインライン化し `unknown field`→別名/式ありCTEをインライン不可にして実体化（B51 で正しく解決）・SELECT */同名列はインライン＋pushdown 維持。全2,718 green。実機（APP730）=CTE 間 INNER/LEFT 正常化・別名 CTE 解決・式 CTE 実体化・SELECT * 非回帰。SemVer=minor。**PR #188→tag v3.11.0→GitHub Release 公開・npm publish 済み（`@rex0220/kintone-sql-tools` latest 3.11.0）＝リリース完了** | 正しさ | [B51 issue](internal/ksql_b51_cte_to_cte_join_wrong_result_issue.md) / [B52 issue](internal/ksql_b52_cte_column_alias_inline_issue.md) |
| **v3.10.0** | **B7 プラグイン検索打ち切り検出＋B47 APPLY 複数親 UPDATE 親 WHERE の LIKE/KLIKE＋B5 通常親 UPDATE/DELETE の KLIKE（2026-07-21）**。**B7**=プラグイン `getRecords` のみ raw fetch 化（`kintone.api.urlForGet` 委譲＋4KB 超は POST override・自前直列化なし）し `X-Cybozu-Warning` で打ち切り検出→DML failClosed が全 surface で成立（B47/B5 KLIKE の前提）＋SELECT 警告。**B47**=APPLY 複数親 UPDATE の親 WHERE に LIKE＋KLIKE 解禁（`buildApplyParentSelectionPlan` 共有 primitive＝node identity 保持・safe prefilter→元 WHERE を evalWhere 残余評価→target だけ prepare・candidate≠target・maxRecords/onLimit:error/no stopAfter・target のみ dmlMaxRows・unapplied KLIKE(OR/NOT) は records API 前拒否）。**B5**=通常（APPLY なし）親 UPDATE/DELETE の WHERE で KLIKE 解禁（whereToKintone が KLIKE→native like・exact pushdown で残余不要→OR/NOT KLIKE も許容・安全境界は既存 EXACT_PUSHDOWN ゲート）。LIKE は通常 DML で拒否維持・サブテーブル KLIKE/INSERT/UPSERT/REORDER/独立 VALIDATE 拒否維持。全2,696 テスト green。**実機**＝B7:APP730 で打ち切り警告/POST override（414 なし）、B47:APP4223 で LIKE candidate 223→target 10・KLIKE target 21・実 mutation は target のみ書込・unapplied 拒否、B5:APP730 で `KLIKE 'ケン'`>10万件→SearchAbortedError（KLIKE DML 打ち切り fail-closed 確定）・実 KLIKE UPDATE 対象のみ書込。SemVer=minor。**PR #182→tag v3.10.0→GitHub Release 公開・npm publish 済み（`@rex0220/kintone-sql-tools` latest 3.10.0）＝リリース完了** | 機能・安全性 | [B7 spec](internal/ksql_search_abort_warning_issue.md) / [B47 R2 spec](internal/ksql_b47_apply_parent_where_like_spec.md) / [B5 spec](internal/ksql_b5_klike_parent_dml_spec.md) / [実装計画](internal/ksql_b7_b47_impl_plan.md) / [証跡 B7](internal/evidence/b7_plugin_search_abort_smoke.md)・[B47](internal/evidence/b47_apply_parent_like_klike_smoke.md)・[B5](internal/evidence/b5_klike_parent_dml_smoke.md) |
| **v3.9.0** | **B43 DML 事前検証の complete post-image＋B49 MCP 読み取り専用メタデータ API＋B50 MCP discoverability（2026-07-21）**。**B43**=`UPDATE`/`UPSERT`（update 分岐）の `VALIDATE ONLY`/`ON ERROR SKIP` が更新対象レコードの post-image（レコード全体・サブテーブル子行含む）を検証し、SET 対象外の既存違反による false pass を解消（B44 の `validatePostImage` 再利用・10メタ列統一・`ON ERROR SKIP` で違反親を true isolation・通常実行は非変更）。CLI 実機 T1〜T3 pass。**B49**=MCP 新ツール `ksql_app_metadata`（8 resource allowlist=app/fields/layout/settings/status/views/reports/customize・生 JSON を structuredContent.data へ・二重 read-only 強制・records/ACL/apps 除外・2 MiB 上限・core interface/SQL/plugin 不変の Node-only reader）。**B50**=MCP discoverability（server `instructions`＝能力索引＋方言注意＋行動導線・言語リファレンス/レシピを MCP resource で公開（build embed・章別 template・fail-closed key）・tool description 用途起点改善・MCPB manifest 是正）。全2,613 テスト green・build:mcp/smoke/pack-smoke/mcpb-verify 全 ok（pack-smoke で docs なし embed standalone 検証）。SemVer=minor。**PR #179→tag v3.9.0→GitHub Release 公開・npm publish 済み（`@rex0220/kintone-sql-tools` latest 3.9.0）＝リリース完了**。**MCP 実機確認＝Claude・codex 両クライアントで pass**（B50 instructions/resources/fail-closed/`ksql_show_apps` 誘導・B49 単一アプリ GET `/k/v1/app.json`/制約 maxLength:64/allowlist 外 `-32602` 拒否）。codex headless では MCP tool call が承認ゲートで `user cancelled`→**`--dangerously-bypass-approvals-and-sandbox` で解消**（`-a`/`approval_policy=never` は shell 用で無効） | 安全性・正しさ・機能 | [B43 spec](internal/ksql_b43_dml_prevalidation_subtable_spec.md) / [B49 R2 spec](internal/ksql_b49_mcp_readonly_metadata_api_spec.md) / [B50 spec](internal/ksql_b50_mcp_discoverability_spec.md) / [言語 §17.1/17.2](ksql_language_reference.md) |
| **v3.8.0** | **B44 APPLY ブロック＋B48 プラグイン親/子ガード兼用＋B45 サブテーブル SELECT のシステム列 WHERE（2026-07-21）**。**B44**=`UPDATE/INSERT/UPSERT … APPLY <テーブル> (PATCH/APPEND/REMOVE…)` でテーブル外項目とテーブル内行を1文=1 PUT で同時更新（テーブル内既存違反による修復不能を解消）。スナップショット意味論・post-image 検証（新エンジン）・行アドレッシング（`ALL ROWS`/`WHERE`/`_idx`/`_rid`）・`EXPECT ROWS`・多値 ADD/REMOVE・複数親（100件/チャンク・非トランザクション・部分成功あり）・`INSERT/UPSERT APPLY`。revision 必須＋二重ガード（dmlMaxRows/dmlMaxSubtableRows 新設・既定500）。MCP は全 APPLY mutation を実行前 fail-closed（VALIDATE ONLY/EXPLAIN 許可）。CLI/プラグイン実機で全機能 pass。**B48**=プラグインの複数親 APPLY 100 親超を「最大取得件数」設定兼用で解消（`dmlMaxRows=max(100,maxRecords)`/`dmlMaxSubtableRows=max(500,maxRecords)`・floor で後方互換・新設定 UI なし）。実機で親200/子600 pass。**B45**=サブテーブル SELECT の WHERE/ORDER BY でシステム列 `_pid`/`_rid`/`_idx` を解決可能に（旧 `WHERE_FIELD_UNRESOLVED`＝言語 §19 例が動かない乖離を修正）。型契約=`_pid`/`_idx` 数値・`_rid` 文字列・全比較/BETWEEN/IN/IS NULL/LIKE 対応・非押し下げ・`_p._pid` は無効維持。CLI 実機 全10項目 pass。全2,474 テスト green。SemVer=minor。PR #178→tag→GitHub Release（アセット3点）・**npm publish 済み（latest 3.8.0・2026-07-21）** | 機能・安全性・正しさ | [B44 spec](internal/ksql_apply_block_spec.md) / [B44 実装計画](internal/ksql_apply_block_impl_plan.md) / [B45 plan](internal/ksql_b45_subtable_system_column_where_plan.md) / [言語 §19.1](ksql_language_reference.md) |
| **v3.7.0** | **B42 VALIDATE のサブテーブル子フィールド監査＋B46 選択肢空値 false positive 修正（2026-07-20）**。B42=既定対象へ子フィールド追加（監査の抜け修正）・scoped target `(テーブル)`/`(テーブル(子,…))`・詳細固定9列（8列+`$err_count`・同一メッセージ集約・`$err_subrow`/`$err_subrow_id` は全該当行リスト・message へ `（2行: 1,2）` 装飾）・生成時集約 `SUMMARY` 5列・`validateStats{errorRecords,errorCount}`（プラグインヘッダー/VALIDATE INTO 統計行/CLI/MCP へ露出）・WHERE/CHECK 子参照の取得前拒否・B29 精度・EXPLAIN 拡張。B46=空（未選択）の選択系を ERR_CHOICE_INVALID と誤判定する v3.5.0 以来の false positive を修正（kintone パリティ実機裏付け=空 DROP_DOWN 受理・RADIO 空指定は黙って無視）。実機=B43 で 0 行だった監査が実 CB_VA01 と完全一致・`$err_subrow_id`≡`_rid` 突合。2,115 テスト green。PR #175→tag→GitHub Release（アセット3点）。**npm publish 済み（latest 3.7.0・2026-07-20）** | 安全性・機能・正しさ | [B42 spec](internal/ksql_validate_subtable_audit_spec.md) / [言語 §17.4](ksql_language_reference.md) |
| **v3.6.1** | **B39 IMPORT の面 UX 改善・修正＋ドキュメント整備（2026-07-19）**。プラグイン=ファイル選択 UI をヘッダー上部へ移動／既定ソース名を拡張子除去＋識別子化（`plugin_import_10.csv`→`plugin_import_10`）／サブテーブル全置換の確認ダイアログをサマリ表示（親全列挙の画面はみ出し解消）／ファイル選択で JSON も選択可（accept に `.json`）。CLI/MCP/plugin=ファイル未選択の IMPORT エラーを面別の案内に（gate エラーかつ当該面ソース未供給時のみ差し替え）／DML 成功メッセージの「隔離 0 件（undefined）」除去。**fix(mcp)=`ksql_query` の IMPORT `VALIDATE ONLY` が capability gate に弾かれる不具合を修正**（再パース時に import フラグ未伝播）。ドキュメント=レシピ R11/R12 追加・言語リファレンス §18.1 新設＋codex レビュー反映（maxRecords 面/経路別・破壊的全置換の CSV/JSON 分離・`ROW ID SOURCE` 位置・`VALIDATE ONLY [INTO #err]`・`EXPLAIN`=`ksql_explain`）・バックログ整理。2,068+25 テスト green。PR #172→tag→GitHub Release（アセット3点）。**npm publish 済（latest 3.6.1）＝v3.6.0 はスキップのため v3.6.1 が公開版**。SemVer=patch | 機能・安全性・正しさ | [recipes R11/R12](ksql_batch_recipes.md) / [言語 §18.1](ksql_language_reference.md) |
| **v3.6.0** | **B39 IMPORT（ファイル → アプリ取込の自己完結文・2026-07-19）**。`IMPORT [UPDATE] INTO app (fields|subtable(children)) FROM CSV|JSON <source> [ENCODING/NO HEADER/SELECT 射影/BY NAME/IGNORE UNKNOWN COLUMNS] [MATCH RECORD NUMBER SOURCE] [ON DUPLICATE] [REPLACE SUBTABLES] [CHECK] [VALIDATE ONLY|ON ERROR SKIP INTO #err [REJECT LIMIT]]`。ソースは**面が名前供給**（CLI `--import-csv/--import-json <name=path>`・MCP inline `importSources`・plugin file picker）でパス非埋込・off-by-default（source 供給時のみ有効）・10 MiB/source。CSV=RFC4180（UTF-8/SJIS・BOM・セル内改行）/位置・SELECT 射影（`CAST`/関数/`\|\|`/`@var`）・源内キー重複拒否。JSON=厳密10進 decoder（元字句保持・safe-int のみ数値・精度対象は string 必須）・全階層 duplicate key 拒否。cli-kintone 互換（`BY NAME`＝ヘッダ=フィールドコード・既知非書込/未知列は監査付き無視・複数値 LF 分割）・レコード番号純 UPDATE（`MATCH RECORD NUMBER SOURCE`・INSERT 0）・サブテーブル（JSON=ID なし全置換/CSV `*`=行 ID 保持 UPDATE 専用・`REPLACE SUBTABLES`・4層エラー位置・親単位隔離）。破壊的全置換は confirm で削除件数明示・内訳表示不可の面（MCP 等）は fail-closed。USER/ORG/GROUP 選択の `… SELECT` payload 修正も同梱。**添付（FILE）は対象外**。Phase1-5 を gate 付きで段階 main マージ・実機（標準/ゲスト・CLI/MCP/plugin・1000件・cli-kintone v1.21.0 round-trip）検証・2,093 テスト green。npm publish はスキップ（v3.6.1 で公開予定）。SemVer=minor。面 UX 改善・修正・ドキュメント整備は v3.6.1（下記）。 | 機能・安全性 | [eval](internal/ksql_import_vs_cli_kintone_evaluation.md) / [v2 spec](internal/ksql_import_v2_spec.md) / [互換](internal/ksql_import_cli_kintone_compat_spec.md) / [recipes R11/R12](ksql_batch_recipes.md) / [言語 §18.1](ksql_language_reference.md) |
| **v3.5.0** | **既存レコード制約チェック＋バッチ変数の参照拡張（2026-07-19）**。**B41**=`VALIDATE <app> [(fields)] [WHERE] [CHECK …] [INTO #err]` の read-only 監査文（既存レコードを組み込み制約＋B37 CHECK で検査し `$id/$err_field/$err_code/$err_message/$err_value` の5列で返す・組み込み検証は生値で USER/組織/グループ/複数選択の空も必須違反検出・WHERE は KLIKE/サブクエリ/修飾参照を静的拒否・書込み API 0・単文 INTO 拒否・EXPLAIN 専用 plan で違反件数なし）／**B3**=`SET @l=['A','B']`＋カッコ無し `IN @l`/`NOT IN @l`（literal IN 展開・空配列は親aware固定点簡約で `in ()` 非送信・更新系恒真は実行前拒否）／**B10-B**=SELECT 定数列 `@x AS alias`（型保持）。仕様 R1→R4（B41）/R2（統合）の codex レビューを通し実装・npm test 1,955＋25 green・dev 実機 smoke（両フェーズ）pass。SemVer=minor | 安全性・機能 | [B41 spec](internal/ksql_existing_record_validation_spec.md) / [参照拡張 spec](internal/ksql_batch_variable_reference_extension_spec.md) / [計画](internal/ksql_v3_5_0_implementation_plan.md) |
| **v3.4.0** | **DML カスタムチェック＋文字列連結の 2 件（2026-07-18）**。**B37**=`CHECK WHEN <条件> THEN <メッセージ>` で INSERT/UPSERT/UPDATE に行レベル業務ルールを付与し該当行を `#err`(`ERR_CHECK`)＋カスタムメッセージで隔離（`ON ERROR SKIP`=隔離/`VALIDATE ONLY`=報告/素 DML=停止）。CHECK ブロック=グループ（ブロック内先勝ち・ブロック間独立）。参照=読み取り行（SELECT 出力行[先頭N列書込み・末尾CHECK専用]/挿入値/UPDATE 更新前値/UPDATE FROM は `APP<n>.列`=旧値・`<src>.列`=新値の修飾解決）。組み込み検証と独立・評価器例外は fail-closed・サブテーブル非対応・新ソフトキーワード（予約語増なし）／**B38**=文字列連結 `\|\|`（`CONCAT` 同義・NULL/空は空文字・左結合・加減算と同優先順位）＋関数引数の `@var` 受理（`CONCAT('x=',@v)`）＋再利用 `ScalarValueExpr`（既存 `ArithNode`・`FORMAT(SUM())` は非破壊）。`\|\|` は WHERE 比較オペランド未対応（`CONCAT` を使う）。全 1,943 テスト green・ブラウザ実機（B37/B38）パス。SemVer=minor | 機能・**正しさ** | [B37 spec](internal/ksql_custom_check_spec.md) / [B38 spec](internal/ksql_concat_operator_spec.md) |
| **v3.3.0** | **厳密10進比較・数値精度整合・正規表現の 4 件バンドル（2026-07-18）**。**B9**=最大30桁の厳密10進比較（16 有効桁超の NUMBER/CALC を binary64 で丸めず区別＝WHERE=・MIN/MAX・ORDER BY が正しくなる・数値リテラルは元字句保持で指数表記も受理。**実機検証中に発見した押し下げバグ＝指数リテラル `1e3` の生字句が kintone クエリへ漏れ CB_VA01 も修正**＝`numberLiteralText` を平文10進へ正規化）／**B29**=書き込み先アプリの `numberPrecision` から整数部の桁超過を書き込み前に検出（`ERR_NUMBER_INTEGER_DIGITS`・`ON ERROR SKIP` で行隔離）。**小数は kSQL で検証せず kintone の自動丸めに委ねる**（REST/CSV/編集画面と一貫＝案B。R1 の小数拒否・`quantizeDecimal` は撤回）／**B20**=正規表現関数 `REGEXP_LIKE`/`REGEXP_REPLACE`/`REGEXP_SUBSTR`（ECMAScript・`i`/`m`/`s`・`u` 常時有効・ユーザー責任・予約語3語）／**B36**=`REGEXP_REPLACE` 第5引数 `occurrence`（省略/0=全置換・1=先頭・N=N番目・後方参照は手書き展開）。全 1,912 テスト green・ブラウザ実機（B9/B20/B29/B36）パス。SemVer=minor（16桁超の比較のみ結果が正しく変わる） | **正しさ**・機能 | [migration](ksql_v3_3_migration_guide.md) / [B9 evidence](internal/evidence/b9_exact_decimal_semver_probe.md) / [B29 evidence](internal/evidence/b29_number_precision_smoke.md) / [B36 spec](internal/ksql_regexp_replace_occurrence_spec.md) |
| **v3.2.0** | **正しさ・関数の 7 件バンドル（2026-07-18）**。**B34**=DML 書き込み先フィールド検査（不存在/サブテーブル子/書込不可を文単位 ArgumentError・検査はソース SELECT/confirm/POST・PUT より前・VALIDATE ONLY の inSubtable 素通し解消）／**B22**=LEFT/RIGHT/SUBSTRING/LPAD/RPAD のサロゲートペア分割解消（コードユニット予算内の最大安全部分列・性質テスト 18 入力×全境界）／**B21**=UPDATE SET の文字列関数直接受理（行評価経路へ統合・LPAD 先頭ゼロ保持・ERR_LENGTH_MAX 捕捉・UPDATE FROM/サブテーブルは明示拒否）／**B23**=LENGTH_CHAR（コードポイント計数・LENGTH−LENGTH_CHAR=ペア数）／**B24**=TRANSLATE（コードポイント整列 1 対 1 写像・長さ不一致はエラー・レシピ R8 同梱）／**B28**=VALUES の符号付き数値リテラル受理（-- は行コメントの罠を文書化）／**B35**=message なしエラーの fallback 表示。全件 codex 実装→Claude レビュー→実機（evidence 6 点）。1,761 テスト green。SemVer=minor | **正しさ**・機能 | [台帳各行の issue/spec] / [evidence](internal/evidence/) |
| **v3.1.0** | **B33 `KORDER BY` 大規模窓の Cursor API 対応（2026-07-18）**。単発GETに収まらない窓（`LIMIT>500`/`OFFSET>10000`）を `KORDER_CURSOR` で kintone 固有順のまま実行（条件=走査件数 `OFFSET+LIMIT<=maxRecords`・超過は理由コード付き planning error・フォールバックなし）。必須 `openCursor` 契約・host 単位 lease（既定2最大5・新設定 `cursorMaxActive` 全面公開）・Create/Get 再試行なし・既解放=実測 `404`+`GAIA_CN01` のみ（plugin は status 非公開のため code 単独許可）・quarantine 10分+30秒・必要窓到達で早期 DELETE・plugin 離脱 best-effort cleanup・EXPLAIN `KORDER_CURSOR`/metrics・MCP `ksql_explain.maxRecords` 追加。**実機=raw Cursor API と 7/7 全件照合一致（618,525件・21ページ・500件境界同値群）＋Chromium/Firefox plugin smoke**（evidence 3点）。純加法 minor・挙動変更なし | 機能・性能 | [spec](internal/ksql_cursor_api_fetch_spec.md) / [plan](internal/ksql_korder_cursor_implementation_plan.md) / [migration](ksql_v3_1_migration_guide.md) |
| **v3.0.0** | **B26＋B27＋B30＋B31＋B32（型付き比較と ORDER BY 計画の major・2026-07-17）**。B26=4面共通の型付き canonical 比較（文字列・型不明はコードポイント順。`localeCompare`・Unicode 正規化・ペア単位の自動数値化を廃止。typed number は`空セル < -Infinity < 有限数 < +Infinity < "NaN" < その他非数値`の固定バンド・`#err` の検証失敗値をエラーにしない）／B27=schema-aware ORDER planner（REST top-N 初期 allowlist は `$id` のみ・query 全体の窓が同値な場合だけ押し下げ・STATUS は `states.*.index` 保持で定義順）／B30=部分候補の誤 top-N を fail-closed 化（`onLimit=truncate` でも local ORDER は完全入力必須）／B31=`KORDER BY`（kintone REST 固有順を明示する別構文・`$id`＋受理15型 allowlist・条件外は planning error でフォールバックしない）／B32=WHERE 型×演算子能力表（EXPLAIN ok→実行 `GAIA_IQ03` を解消。SELECT は FULL_SCAN 残余・DML は実行前拒否）。EXPLAIN は schema-aware 化・`KORDER` 予約語。SemVer=**major** | **正しさ**・機能・性能 | [migration](ksql_v3_migration_guide.md) / [semantics](internal/ksql_string_semantics.md) / [order draft](internal/ksql_local_order_by_draft.md) / [impl plan](internal/ksql_v3_order_by_implementation_plan.md) |
| **v2.17.0** | **B19 スカラー関数バンドル**。第1部＝`TRUNCATE`/`TRUNC`・`LEFT`/`RIGHT`・`INSTR`・`GREATEST`/`LEAST`・`LPAD`/`RPAD`・`LAST_DAY`（`RIGHT` は代替不能＝`SUBSTRING` は引数に算術式を書けず負数開始は全文を返す）。`GREATEST`/`LEAST` は `compareScalarValues` を畳み込まない（畳み込みは全順序でなく `2<10<1a<2` の循環）→空文字を先に確定・集合単位でモード判定・数値同値は元文字列で二次比較。第2部＝**既存欠陥の修正**: `DATE_ADD` の構文が文書と実装で食い違い（`INTERVAL` はトークン非存在＝**書いたら必ず ParseError**）・**不正な単位が黙って DAY 加算**（→実行時 `ArgumentError`）・`SUBSTRING` の負数開始が全文を返すことを文書化。8 語の新規予約語 | 機能・**正しさ** | [spec](internal/ksql_scalar_function_bundle_spec.md) |
| **v2.16.0** | **B17 順位系ウィンドウ関数**。`ROW_NUMBER()`/`RANK()`/`DENSE_RANK()` ＋ `OVER ([PARTITION BY …] [ORDER BY …]) AS alias`。**「各グループ最新1件をその行の全列とともに取得」を初めて可能に**（`MAX()` の結果を元行へ結合し直すには複合等値が要るが JOIN は単一等値のみ・派生テーブルも無いため従来は表現不可能）。CTE で1文。評価は HAVING 後・DISTINCT 前（SQL 標準）でウィンドウ列は FULL_SCAN 強制（CTE 誤インライン化＝`WHERE rn=1` の押し込みを防ぐ正しさの前提）。ウィンドウ内 `ORDER BY` はトップレベルと比較器・ソートメタを共有。`AS alias` 必須・`GROUP BY`/集計との同一 SELECT 併用は v1 非対応・予約語 | 機能 | [spec](internal/ksql_window_function_spec.md) |
| **v2.15.0** | **B14** 一時テーブル/CTE 列の型メタ伝播（temp 経由でもテキスト・日時の `MIN`/`MAX` が効く。`#err` の列は DML 対象アプリの定義から宣言・`$id` は RECORD_NUMBER 相当。推論は素通し/集約/算術/リテラルのみで「迷ったら載せない」）／**B16** 文字列集約 `GROUP_CONCAT([DISTINCT] 引数 [SEPARATOR '区切り'])`（暗黙の切り捨てなし・空値スキップ・`DISTINCT` 初出順・予約語・`GROUP_CONCAT(*)` は ParseError・**B14 とは独立**）／**B18** 事前検証がミリ秒付き ISO 日時（`NOW()` の形式）を誤って拒否し `ON ERROR SKIP` が正常行を誤隔離していた不具合を修正／バッチレシピ集に **R6「不良データを隔離して残りを流す」**新設・R2 に `VALIDATE ONLY` 追記・**実行できなかった B12 の例を修正** | 機能・正しさ・安全性 | [B14](internal/ksql_temp_column_type_meta_spec.md) / [B16](internal/ksql_group_concat_spec.md) / [B18](internal/ksql_validate_datetime_millisecond_issue.md) / [recipes](ksql_batch_recipes.md) |
| **v2.14.1** | B15 `IN`/`NOT IN` の負数リテラル受理。`= -1` / `BETWEEN` は受理するのに `IN (-1)` が `ParseError` になる非対称を解消（`parseInValues` が単項 `-`/`+` を受理）。`IN (+1)`≡`IN (1)`・`IN ('-1')` は文字列のまま・押し下げは `in (-1,1,"-1")`。受理範囲の拡大のみ（patch） | 正しさ | [issue](internal/ksql_in_list_negative_number_issue.md) |
| **v2.14.0** | B13 文字列・日時 `MIN`/`MAX`。実アプリのテキスト・選択・日時（正規化済み DATE/TIME/DATETIME・作成/更新日時）と文字列 CALC を UTF-16 辞書順で集約。NUMBER・数値 CALC は従来の数値比較を維持（回帰なし）。専用 `AggregateSortKindResolver`（sortKind 一次＋fieldType 補完）で型解決し `getFieldsCached` 共有。JOIN 修飾列・一意な非修飾列に対応。同名競合・非対応複合型・temp/CTE は従来の数値経路（フェーズ2で別途） | 正しさ | [spec](internal/ksql_string_min_max_aggregate_spec.md) |
| **v2.13.0** | B12 バンドル。B12-A `VALIDATE ONLY`（Tier 0 事前検証・書き込みゼロ・複数エラー収集・`INTO #err`）／B11.1 `UPDATE … FROM` 業務キー結合（`target.field = source.field` 単一等値・数値/文字列キー正規化・ターゲット重複全件更新・64 文字超は全文一致逆引き）／B12-B `ON ERROR SKIP INTO #err [REJECT LIMIT n]`（NG 行を隔離し合格行のみ書き込み・REJECT LIMIT 超過は書き込みゼロで診断返却） | 機能・安全性・正しさ | [validate](internal/ksql_validate_only_implementation_plan.md) / [update-from §12](internal/ksql_update_from_spec.md) / [on-error-skip](internal/ksql_on_error_skip_isolation_spec.md) |
| **v2.12.0** | B11 `UPDATE … FROM`（アプリ間・一時テーブル転記）。SET 値に他テーブル（`#temp`/実アプリ `APP<n>`）のフィールドを参照・`$id` 単一等値・複数マッチ/不正キー/列欠落/複合型/上限は PUT 前 fail-closed・50 件チャンク・MCP は `maxRecords` で読み取り | 機能 | [spec](internal/ksql_update_from_spec.md) / [roadmap](internal/ksql_batch_processing_roadmap.md) |
| **v2.11.0** | B1 CLI DML×`truncate` 暗黙部分書き込み防止（`error` 固定）／B2 空 `SELECT *` の列スキーマをパイプライン伝播（0 行 no-op 完走）／B8 `LIMIT>500` の安全な取得打ち切り（`ORDER BY` なし・KLIKE なし限定）＋`maxRecords` 意味論変更 | 安全性・正しさ・性能 | [B1](internal/ksql_cli_dml_on_limit_truncate_issue.md) / [B2](internal/ksql_empty_select_wildcard_pipeline_spec.md) / [B8](internal/ksql_limit_over_500_fetch_truncation_spec.md) |
| **v2.10.1** | SIMPLE SELECT の `LIMIT` > 500 が API エラーになる不具合を修正（案A ＝ `fetchAll` でページング） | 正しさ | [issue](internal/ksql_simple_select_limit_over_500_issue.md) |
| **v2.10.0** | 検索打ち切り（10 万件）検出＋`FROM` なし SELECT の実体化バグ修正。SELECT は警告 / DML・一時テーブル実体化は fail-closed | 正しさ・安全性 | [abort](internal/ksql_search_abort_warning_issue.md) / [fromless](internal/ksql_fromless_select_materialize_bug.md) |
| **v2.9.0** | KLIKE プレフィルタ押し下げ（v2）。FULL_SCAN でも KLIKE を安全 AND リーフとして kintone 押下・INNER JOIN 限定 | 性能 | [spec](internal/ksql_klike_pushdown_v2_spec.md) |
| **v2.8.0** | KLIKE / NOT_KLIKE（kintone ネイティブ `like` 素通し）。大規模キーワード検索の高速化 | 性能・機能 | [spec](internal/ksql_klike_native_search_spec.md) |
| **v2.7.0** | STATUS（ワークフロー状態）の IN 押し下げ（`status.json` 検証・型ベース判定） | 性能 | [spec](internal/ksql_status_in_pushdown_spec.md) |
| **v2.6.0** | 選択系 IN 押し下げ（4 型・optionOrder 実在検証）＋ `IN ('')` 空セル評価修正（案B） | 性能・正しさ | [pushdown](internal/ksql_selection_in_pushdown_spec.md) / [empty-in](internal/ksql_selection_empty_in_eval_issue.md) |
| **v2.5.0** | 型メタ付き `IN`/`NOT IN` 評価（複数値・ユーザーコード） | 正しさ | [spec](internal/ksql_fullscan_in_typed_eval_spec.md) |
| **v2.4.0** | バッチ変数 Phase 1c：`DECLARE @x = 既定値` 外部パラメータ注入（MCP/CLI） | 機能 | [spec](internal/ksql_batch_variables_phase1c_spec.md) |
| **v2.3.0** | バッチ変数 Phase 1b：`SET @x=(SELECT ...)` スカラーサブクエリ代入 | 機能 | [spec](internal/ksql_batch_variables_phase1b_spec.md) |
| **v2.2.0** | 述語押し下げの安全化と数値対応（案A ＝ `=`・strict `<`/`>`）＋空セル数値 −∞ 準拠 | 性能・正しさ | [numeric](internal/ksql_numeric_predicate_pushdown_spec.md) / [empty-num](internal/ksql_evalwhere_empty_cell_numeric_issue.md) |
| **v2.1.2** | 集計算術式（`SUM(a)-SUM(b) AS diff`）の alias 消失を修正 | 正しさ | [issue](internal/ksql_agg_arith_alias_dropped_issue.md) / [fix](internal/ksql_agg_arith_alias_dropped_fix_spec.md) |
| **v2.1.1** | 0 行 SELECT（明示列）の出力列欠落を修正。差分 0 件の空ソース INSERT/UPSERT が no-op 完走 | 正しさ | [issue](internal/ksql_empty_select_columns_issue.md) |
| **v2.1.0** | バッチ変数 Phase 1a：`SET @var`（スカラー式・時刻固定・DRY）＋ R4 の IN 要素対応（`WHERE k IN (@a, @b)`。チェックボックス等 `in` 必須フィールド向け） | 機能 | [spec](internal/ksql_batch_variables_phase1a_spec.md) |
| **v2.0.0** | 全 `LIKE` を JS 評価へ統一・kintone 押し下げ全廃（`isLike` 統一・親 DML fail-closed） | 正しさ・安全性 | [spec](internal/ksql_like_js_default_optin_pushdown_spec.md) |
| **v1.14.0** | WHERE 右辺フィールド比較 / LIKE モード不一致の 2 バグ修正 | 正しさ | [issue](internal/ksql_where_rhs_field_and_like_mode_divergence_issue.md) / [fix](internal/ksql_where_rhs_field_and_like_fix_spec.md) |
| **v1.13.0–.2** | 論理アプリ参照 `LAPP_<NAME>`（CLI/MCP・プラグイン非対応）。.1 CLI エラー復元・.2 単文 dry-run 露出修正 | 機能 | [spec](internal/ksql_logical_app_id_mapping_spec.md) / [plan](internal/ksql_logical_app_id_mapping_impl_plan.md) |
| **v1.12.1** | `extractAppIds` がコメント/文字列内 `APPxxxx` を誤ってトークン要求する不具合を修正 | 正しさ | [issue](internal/ksql_extract_app_ids_comment_string_issue.md) |
| **v1.12.0** | `GROUP BY` なし集計は 0 件でも 1 行返却（SQL 標準準拠） | 正しさ | [plan](internal/ksql_ungrouped_aggregate_empty_result_implementation_plan.md) / [spec](internal/ksql_ungrouped_aggregate_empty_result_spec.md) |
| **v1.11.0** | 一時テーブル実体化行数上限 `tempTableMaxRows` を可変化（env / profile / プラグイン UI） | 機能 | [spec](internal/ksql_temp_table_max_rows_option_spec.md) / [plan](internal/ksql_temp_table_max_rows_option_implementation_plan.md) |
| **v1.10.0** | バッチ拡張 第1弾（`ASSERT` / CLI バッチ JSON 出力 / `requestGate` 設定公開） | 機能 | [spec](internal/ksql_batch_enhancement_phase1_spec.md) / [proposals](internal/ksql_batch_enhancement_proposals.md) |
| ～v1.9.0 | 初期リリース群（順次バッチ＋一時テーブル v1.4.0 / プラグイン DML バッチ v1.9.0 / MCP INSERT・UPSERT … SELECT / `dmlMaxRows` ソース読み取り v1.8.0 ほか） | 機能 | [temp-table](internal/ksql_batch_temp_table_spec.md) / [plugin-dml](internal/ksql_plugin_dml_batch_spec.md) / [dml-read-limit](internal/ksql_mcp_dml_source_read_limit_issue.md) |

---

## 関連文書

- 進行中の課題・保留: [`ksql_issue_tracker.md`](ksql_issue_tracker.md)
- リリース履歴の一次情報: [`CHANGELOG.md`](../CHANGELOG.md)
- リリース手順: auto-memory `release-procedure.md`


---

## リリース単位の決定記録

出荷済みの版がどういう単位でまとめられたか、その判断理由。
**台帳（進行中の課題）から移設したもの**で、済んだ決定をここに集約する。

### v3.26.0 → v3.27.0 → v3.28.0 の分割（2026-07-27 オーナー決定）

**着手順序・リリース単位（2026-07-27 オーナー決定）＝① B76 Phase A リリース → ② B79 ＋ B80 を1リリース → ③ B76 Phase B**。**②をまとめる理由**＝どちらも「**失敗時の挙動を正直にする**」課題で機能追加ではない〔B79＝外部結合＋打ち切りで**誤った値**を返し警告文も「欠落」としか言わない／B80＝検証エラーを「SQL statement could not be parsed」と偽る〕＝**どちらも利用者に嘘をついているのを止める変更**で CHANGELOG も一つのメッセージで書ける。各 1〜2 人日と小さく、**リリース準備1回あたりのコスト**（版数同期・ビルド・成果物配置・4点同期・実機確認）を考えると別々に出すのは非効率。**②は破壊的変更を含む**ため v3.25.0 と同様に移行案内を3箇所（CHANGELOG・言語リファレンス・`release/README.txt`）へ。**③を分ける理由**＝**機能追加**（第5許可形）で **5〜8 人日**と規模が違い、**Phase A のリリースと実地検証が前提**。Phase A は取得挙動を大きく変えるため、使われた結果を見てから積み上げる（B67 が Phase1→Phase2 A→B72→B75 と段階を踏んで正解だった前例と同じ）。3つ全部を1リリースにすると 7〜12 人日の長いサイクルになり **silent wrong result である B79 の修正が数週間遅れる**ため避ける。**依存関係**＝B79 は B76 Phase B の前提**ではない**（Phase B も INNER JOIN 限定で範囲が重ならない）が、仕様 §0.3.6-10 の「SearchAborted 時の母集合完全性」は Phase B が相対日付リーフを residual から除去する以上の論点なので、**B79 を先に片付ければ「決着済み」として扱える**。**実需確認待ちの B54/B63/B68、実需待ちで棚上げの B53（18〜29 人日）、継続運用の B61 は据え置き**。**③着手前に Pro の返答を見て B76 Phase B と B73 の優先度を再評価する**。
