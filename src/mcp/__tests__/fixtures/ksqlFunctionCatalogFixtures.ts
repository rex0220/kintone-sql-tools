export const KSQL_FUNCTION_SQL_FIXTURES: Readonly<Record<string, string>> = Object.freeze({
  UPPER: "SELECT UPPER(名前) AS x FROM APP1",
  LOWER: "SELECT LOWER(名前) AS x FROM APP1",
  TRIM: "SELECT TRIM(名前) AS x FROM APP1",
  LTRIM: "SELECT LTRIM(名前) AS x FROM APP1",
  RTRIM: "SELECT RTRIM(名前) AS x FROM APP1",
  LENGTH: "SELECT LENGTH(名前) AS x FROM APP1",
  LENGTH_CHAR: "SELECT LENGTH_CHAR(名前) AS x FROM APP1",
  SUBSTRING: "SELECT SUBSTRING(名前, 1, 2) AS x FROM APP1",
  SUBSTR: "SELECT SUBSTR(名前, 1, 2) AS x FROM APP1",
  LEFT: "SELECT LEFT(名前, 2) AS x FROM APP1",
  RIGHT: "SELECT RIGHT(名前, 2) AS x FROM APP1",
  INSTR: "SELECT INSTR(名前, 'a') AS x FROM APP1",
  CONCAT: "SELECT CONCAT(名前, 'x') AS x FROM APP1",
  REPLACE: "SELECT REPLACE(名前, 'a', 'b') AS x FROM APP1",
  REGEXP_LIKE: "SELECT REGEXP_LIKE(名前, '^a') AS x FROM APP1",
  REGEXP_REPLACE: "SELECT REGEXP_REPLACE(名前, 'a', 'b') AS x FROM APP1",
  REGEXP_SUBSTR: "SELECT REGEXP_SUBSTR(名前, 'a') AS x FROM APP1",
  TRANSLATE: "SELECT TRANSLATE(名前, 'a', 'b') AS x FROM APP1",
  COALESCE: "SELECT COALESCE(名前, 'x') AS x FROM APP1",
  ISNULL: "SELECT ISNULL(名前, 'x') AS x FROM APP1",
  NULLIF: "SELECT NULLIF(名前, 'x') AS x FROM APP1",
  GREATEST: "SELECT GREATEST(数値, 1) AS x FROM APP1",
  LEAST: "SELECT LEAST(数値, 1) AS x FROM APP1",
  LPAD: "SELECT LPAD(名前, 3, '0') AS x FROM APP1",
  RPAD: "SELECT RPAD(名前, 3, '0') AS x FROM APP1",
  ROUND: "SELECT ROUND(数値, 2) AS x FROM APP1",
  FLOOR: "SELECT FLOOR(数値) AS x FROM APP1",
  CEIL: "SELECT CEIL(数値) AS x FROM APP1",
  CEILING: "SELECT CEILING(数値) AS x FROM APP1",
  TRUNCATE: "SELECT TRUNCATE(数値, 2) AS x FROM APP1",
  TRUNC: "SELECT TRUNC(数値, 2) AS x FROM APP1",
  ABS: "SELECT ABS(数値) AS x FROM APP1",
  MOD: "SELECT MOD(数値, 2) AS x FROM APP1",
  POWER: "SELECT POWER(数値, 2) AS x FROM APP1",
  POW: "SELECT POW(数値, 2) AS x FROM APP1",
  SQRT: "SELECT SQRT(数値) AS x FROM APP1",
  FORMAT: "SELECT FORMAT(数値, 2) AS x FROM APP1",
  CAST: "SELECT CAST(数値 AS TEXT) AS x FROM APP1",
  CONVERT: "SELECT CONVERT(数値, TEXT) AS x FROM APP1",
  YEAR: "SELECT YEAR(日付) AS x FROM APP1",
  MONTH: "SELECT MONTH(日付) AS x FROM APP1",
  DAY: "SELECT DAY(日付) AS x FROM APP1",
  DATE_FORMAT: "SELECT DATE_FORMAT(日付, '%Y-%m') AS x FROM APP1",
  DATEDIFF: "SELECT DATEDIFF(日付, '2026-01-01') AS x FROM APP1",
  DATE_ADD: "SELECT DATE_ADD(日付, 1, 'DAY') AS x FROM APP1",
  LAST_DAY: "SELECT LAST_DAY(日付) AS x FROM APP1",
  CURRENT_DATE: "SELECT CURRENT_DATE() AS x FROM APP1",
  CURRENT_TIMESTAMP: "SELECT CURRENT_TIMESTAMP() AS x FROM APP1",
  COUNT: "SELECT COUNT(*) AS x FROM APP1",
  SUM: "SELECT SUM(数値) AS x FROM APP1 GROUP BY 種別",
  AVG: "SELECT AVG(数値) AS x FROM APP1 GROUP BY 種別",
  MIN: "SELECT MIN(数値) AS x FROM APP1 GROUP BY 種別",
  MAX: "SELECT MAX(数値) AS x FROM APP1 GROUP BY 種別",
  GROUP_CONCAT: "SELECT GROUP_CONCAT(名前) AS x FROM APP1 GROUP BY 種別",
  STDDEV_POP: "SELECT STDDEV_POP(数値) AS x FROM APP1 GROUP BY 種別",
  STDDEV_SAMP: "SELECT STDDEV_SAMP(数値) AS x FROM APP1 GROUP BY 種別",
  VAR_POP: "SELECT VAR_POP(数値) AS x FROM APP1 GROUP BY 種別",
  VAR_SAMP: "SELECT VAR_SAMP(数値) AS x FROM APP1 GROUP BY 種別",
  MEDIAN: "SELECT MEDIAN(数値) AS x FROM APP1 GROUP BY 種別",
  ROW_NUMBER: "SELECT ROW_NUMBER() OVER () AS rn FROM APP1",
  RANK: "SELECT RANK() OVER (ORDER BY 数値) AS rn FROM APP1",
  DENSE_RANK: "SELECT DENSE_RANK() OVER (ORDER BY 数値) AS rn FROM APP1",
  TODAY: "SELECT * FROM APP1 WHERE 日付 = TODAY()",
  NOW: "SELECT * FROM APP1 WHERE 更新日時 < NOW()",
  LOGINUSER: "SELECT * FROM APP1 WHERE 作成者 = LOGINUSER()",
  IF: "SELECT IF(数値 = 1, 'a', 'b') AS x FROM APP1",
});
